#!/bin/bash
# WSL VM 유휴 종료 방지 데몬.
#
# [증상] Claude 세션이 "작업 폴더가 더 이상 존재하지 않는다"는 오류를 간헐적으로
#   낸다. 채팅을 보관했다 해제하면 풀린다.
#
# [원인] 이 세션의 작업 디렉터리가 \\wsl.localhost\Ubuntu\ (UNC 경로)다. WSL VM 은
#   안에 프로세스가 없으면 유휴 타임아웃(기본 약 60초) 뒤 스스로 꺼지는데, 그 순간
#   UNC 경로가 통째로 사라져 세션의 cwd 가 무효가 된다. 보관/해제가 고치는 이유는
#   그 과정에서 WSL 접근이 일어나 VM 이 다시 뜨기 때문이다. 계산이 돌 때는 그
#   프로세스들이 VM 을 붙잡아 줘서 안 났고, 전부 끝난 순간마다 났던 것이다.
#
# [이 해법은 틀렸다 — 2026-08-12 확인]
#   이 데몬이 떠 있으면 VM 이 유휴로 판정되지 않는다고 적었었다. 아니었다.
#   08-10 17:25 에 이 데몬이 돌고 있는 채로, .wslconfig 에 vmIdleTimeout=-1 이
#   들어 있는 채로 VM 이 꺼졌다.
#
#   08-12 08:20~08:23 에 세 번 재현했다. 윈도우 쪽에 붙어 있는 wsl.exe 가 없으면
#   `wsl.exe -e` 호출이 끝나는 순간 setsid 로 떼어 놓은 프로세스까지 전부 사라진다.
#   08:23 에 윈도우에서 vm_hold.sh 를 붙잡아 두자 같은 방식으로 띄운 감시견이
#   그대로 살아남았다. VM 을 붙잡는 것은 **안쪽 프로세스가 아니라 바깥쪽 클라이언트**다.
#
#   실제 해법은 C:\Users\mangw\.claude_work\wsl_hold.ps1 (예약 작업 ClaudeWslHold,
#   5분 간격) 이 vm_hold.sh 를 wsl.exe 로 붙잡아 두는 것이다.
#   이 스크립트는 그 아래의 여벌로만 남긴다.
while true; do sleep 300; done
