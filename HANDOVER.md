# 인수 문서 — 새 세션에서 여기부터 읽으세요

이 프로젝트를 처음 보는 세션이 **이 파일 하나로 현황을 파악**하도록 쓴 문서입니다.
작성 시점: 2026-08-05

| 목적 | 문서 |
|---|---|
| **현황·판단 근거** (이 파일) | `HANDOVER.md` |
| 환경 설치·함정 7가지 | `00_Migration/MIGRATION.md` |
| ZIF-7 분석 전문 | `20_ParentScan/README.md` |
| 데스크탑 이전 결과·재현 검증 | `00_Migration/PART5_DRAFT.md` (Notion Part 5 대기) |
| **밀도맵·ParaView 워크플로** | `10_DensityMap/README.md` |
| 상세 판단 기록 | `git log` (메시지에 근거 서술) |

---

## 1. 무엇을 하는 프로젝트인가

**배가스(flue gas) CO₂ 포집용 MTV-ZIF 스크리닝.** ZIF-8의 유기 링커를 다른 이미다졸레이트로 부분 치환해(MTV = MultiVariate) CO₂ 흡착을 개선하고, RASPA로 계산 평가합니다.

- 조건: **CO₂ 분압 0.15 bar, 298 K** (배가스 조건)
- 목표: **Q_st 30~40 kJ/mol** — 작업용량 모형 두 개로 독립 검증됨(`15_Target/optimal_qst.py`)
- 지표: Q_st(등량 흡착열), CO₂/N₂ 선택도, 0.15 bar 흡착량

## 2. 현재 성적

2026-08-05 데스크탑에서 전하부터 전부 다시 계산한 값입니다(5000 사이클, 1σ 병기).

| | Q_st | 선택도 | 로딩 |
|---|---|---|---|
| 순수 ZIF-8 | 14.04 ± 0.09 | 7.24 ± 0.16 | 0.2723 ± 0.0050 |
| SO₃H 50% | 25.13 ± 4.15 | 28.44 ± 10.88 | 0.7406 ± 0.0635 |
| Cl50 + SO₃H50 | 22.73 ± 2.27 | 23.49 ± 3.64 | 0.6259 ± 0.0688 |
| **목표** | **30~40** | — | — |

**목표 미달입니다.** 그래서 방향을 바꿨습니다(아래 5절).

### ⚠️ 오차를 보면 말할 수 있는 것이 줄어듭니다

이전 판에는 위 표가 점추정값만 적혀 있었고 **"최고: SO₃H 50%", "최고 Q_st: Cl50+SO₃H50"** 라는 순위가 붙어 있었습니다. **그 순위는 성립하지 않습니다.**

| 주장 | 판정 |
|---|---|
| −SO₃H 계열이 순수 ZIF-8보다 낫다 | **유의함** — Q_st 차이가 오차의 여러 배 |
| SO₃H 50% 가 Cl50+SO₃H50 보다 Q_st 가 높다 | **유의하지 않음** — 25.13 ± 4.15 vs 22.73 ± 2.27, 구간이 크게 겹침 |
| SO₃H 50% 가 로딩 최고다 | **유의하지 않음** — 0.7406 ± 0.0635 vs 0.6259 ± 0.0688 |
| SO₃H 50% 선택도가 28.44 다 | **자릿수만 신뢰** — ± 10.88 (상대오차 38%) |

원인은 물리적입니다. 강한 흡착점이 있으면 삽입 수용률이 떨어져 수렴이 느려집니다. 순수 ZIF-8의 로딩 상대오차가 1.8%인데 −SO₃H 계열은 8.6~11.0%입니다.

**−SO₃H 조성끼리 순위를 매기려면 사이클을 늘려야 합니다.** 이건 데스크탑 이전 때문에 생긴 문제가 아니라, 원래 있었는데 오차를 병기하지 않아 보이지 않았던 것입니다.

> 노트북 값(Q_st 23.88 / 24.12, 로딩 0.8321 / 0.7077)과의 차이는 전부 1.5σ 미만이라 **재현 실패가 아닙니다.** DDEC6 전하도 원소별 평균 차이 최대 0.008 e 로 재현됩니다.

---

## 3. 판단의 사슬 — 왜 지금 여기 있는가

순서대로 읽으면 왜 현재 방향에 도달했는지 이해됩니다.

1. **정전기가 계산에 빠져 있었다.** 원래 파이프라인이 `UseChargesFromCIFFile no` — 프로젝트 핵심 가설(EWG의 국소 쌍극자와 CO₂ 사극자 상호작용)을 검증 불가능한 상태였습니다. 전하를 넣자 선택도가 3.5배로 벌어졌습니다.

2. **EQeq는 니트로기를 못 다룬다.** 공명에 의한 전하 분리를 표현하지 못해 니트로 N이 +0.025(정상 +0.6)로 나왔습니다. PACMAN(DDEC6)으로 교체했습니다. 다만 Q_st는 안 올랐습니다 — 니트로는 N(+)과 O(−)를 한 작용기에 갖고 있어 전체적으로 중성이고, 강한 전기장이 아주 가까이서만 셉니다.

3. **게이트 오프닝은 배제.** 열린 상을 만드는 데 셀당 **281 kJ/mol**이 드는데 0.15 bar 흡착 이득은 14~29 kJ/mol입니다. 좋은 소식이기도 합니다 — 유연 골격 MD가 불필요하고 정적 강체 스크리닝이 정당화됩니다.

4. **OMS(열린 금속 자리)는 세기가 아니라 밀도 문제.** 셀당 OMS 1개면 기공 평균에 7%만 실려서, 목표 도달에 국소 결합 215 kJ/mol이 필요합니다(불가능). 현실적 45 kJ/mol 기준으로는 **링커 결손 약 10%** 가 필요합니다. 골격은 **16.7% 결손까지 버티고 OMS도 전량 보존**되며, 뜻밖에 결손 2개 이상에서 **창구가 영구히 열립니다**(PLD 3.25→3.80, AV 0→710 Å³). 이 결과는 유효합니다.

5. **⚠️ 구조 생성 버그가 치환 구조 결과를 전부 무효화했다.** `plan_substitution`이 고리 좌표를 주기 경계 보정 없이 Kabsch 정합에 넣었습니다. ZIF-8 단위셀의 **고리 24개 중 12개가 셀 경계를 걸쳐** 있어(좌표 스팬 16.32 Å, 정상 2.2 Å), 그 고리들은 치환기가 링커가 아니라 **공동 한가운데에 버려졌습니다.** 수정 완료(커밋 `7ce1d35`).
   - 오래 안 걸린 이유: 검사가 **전역** 최소 원자간 거리만 봤고, 메틸 C–H(0.929 Å)가 항상 최솟값을 차지해 1.37 Å 충돌도 4.7 Å 고아 원자도 가려졌습니다
   - **결과: −Cl 결론이 뒤집혔습니다.** Cl 50% 선택도 19.83 → **6.99**(순수 ZIF-8보다 나쁨). 떠 있던 Cl이 CO₂가 원래 몰리는 자리에서 사방 접근 가능한 강한 흡착점 노릇을 했던 것입니다
   - **새 구조를 만들면 반드시 `python audit_orphans.py` 로 검증하세요**

6. **Q_st 천장은 기하학적 한계다.** 밀도맵을 해부하니 CO₂의 밀도가중 평균 벽거리가 조성과 무관하게 **3.32~3.48 Å**로 고정입니다. ZIF-8 공동이 11.4 Å이라 **CO₂가 한쪽 벽만 느낍니다.** 분산력은 $r^{-6}$이라 반대편 기여가 1% 미만입니다.
   - 이 "밀도맵 해부"는 **전하 on/off 두 번 GCMC 를 돌려 차분**한 것입니다. LJ 항이 전하와 무관해 소거되므로 정전기 효과만 남습니다. ParaView 로 볼 산출물이 `10_DensityMap/diff_vtk/` 에 있습니다 — **근거·판독법·구현 함정 넷은 `10_DensityMap/README.md` 에 있습니다.** 전하 끈 계산을 중복으로 오해해 지우지 마세요

7. **기공 축소로는 해결 안 된다(실측 반증).** 치환율을 올려 LCD를 11.0→10.4로 줄였더니 Q_st가 **14.04→12.74로 단조 감소**했습니다. 이유: **sod 위상에서 C2 치환기는 6원환 창구를 향하지 케이지 중심이 아닙니다.** 치환하면 창구(PLD)만 막히고 공동(LCD)은 안 줄어듭니다.

8. **−SO₃H가 최고 작용기.** SO₃H 포함 3종 평균 Q_st 22.86 vs 미포함 4종 13.46. **기하가 아니라 화학적 이득**입니다. 로딩도 이 프로젝트 역대 최고치입니다(0.7406 ± 0.0635).
   - 다만 **−SO₃H 조성끼리의 순위는 오차 안에서 구별되지 않습니다**(2절 참조). "SO₃H 계열 > 나머지"는 유의하고, "SO₃H 계열 내부의 1·2등"은 유의하지 않습니다

9. **수분 경쟁 통과 — 2026-08-05 기준 20/20 완료.** RH 90%에서 술폰산 조성이 물을 1.85 mol/kg(순수 ZIF-8의 59배) 먹지만 **CO₂ 흡착이 줄지 않았습니다.** 5개 조성 유지율은 **93.7~121.2%** 이고 편차가 전부 1.5σ 미만이라, 증가도 감소도 유의하지 않습니다 — **"변하지 않음"** 이 정확한 서술입니다.
   - 마지막에 추가된 `Cl50+SO₃H25` 가 유일하게 100% 아래(93.7% ± 7.0)인데 **0.9σ 라 감소로 볼 수 없습니다**
   - 해석: 물은 수소결합으로 술폰산기에 묶이고, CO₂는 수소결합을 못 하니 나머지 벽을 씁니다. **11.4 Å 공동이 여기서는 장점**으로 작용했습니다
   - **물 흡수량은 −SO₃H 밀도를 따릅니다**(RH 90%): 순수 ZIF-8 0.031 / Cl100 0.017 / SO₃H25 0.481 / Cl50+SO₃H50 1.262 / SO₃H50 1.854 mol/kg. Cl 100%가 순수 ZIF-8보다도 물을 덜 먹는 것이 눈에 띕니다

10. **핵심 긴장 — 두 전략이 같은 공간을 다툰다.** 해석적 공동 모형(`20_ParentScan/cavity_model.py`)으로 최적 공동이 **4.0~4.5 Å**이고 거기서는 **작용기 없이 Q_st 33~35**가 나옵니다. 그런데 4.3 Å 공동에 CO₂(3.3 Å)를 넣으면 한쪽에 0.5 Å만 남아 **−SO₃H(2.8 Å)를 붙일 수 없습니다.**
    - **분산력**은 좁은 공동을 원하고 **정전기**는 작용기 자리를 원합니다 → 같은 부피 경쟁
    - ZIF-8 고밀도 치환 실패와 작은 공동 모체의 치환 불가가 **같은 원인**입니다

---

## 4. 유효 / 무효 구분 (중요)

### 유효

| 결과 | 파일 |
|---|---|
| 결손 안정성 (16.7%까지 OMS 보존, 창구 개방) | `16_DefectStability/stability_results.json` |
| 기공 축소 12종 GCMC (−SO₃H 최고) | `18_PoreNarrowing/narrow_results.json` |
| 위험도 판정 12/16 통과 | `18_PoreNarrowing/risk_results.json` |
| **수분 경쟁 20/20 (완료)** | `19_WaterCompetition/water_results.json` |
| 공동 크기 모형 | `20_ParentScan/README.md` |
| 초기 nIm 스크리닝, ZIF-69 | 구조 감사 통과 |
| 목표치 30~40 검증 | `15_Target/` |

### 무효 — 주기경계 버그 영향

**2026-08-05 갱신: 구조는 전부 다시 만들었습니다. 아직 안 된 것은 그 구조로 돌리는 계산입니다.**

| 대상 | 구조 | 계산 |
|---|---|---|
| `07_Bracketed_MTV/` 14종 | **재생성 완료, 감사 통과** | 미실행 (`run_widom_batch.py`) |
| `10_DensityMap/density_analysis.json` | (07에 의존) | 미실행. **−Cl enrichment E=1.66\~1.98 은 여전히 인용 금지** |
| `14_Strategies/StratB_pushpull` | **재생성 완료, 감사 통과** | 미실행. **전하 부여 스크립트가 없음**(아래 주) |
| `17_NestEffect/amIm050_nIm050` | **재생성 완료, 감사 통과** | 전하 재계산 완료, RASPA 미실행 |
| `17_NestEffect/mIm075_saIm025` | **여전히 결함** — 아래 주 | 계산 금지 |

재생성 절차: 해당 `build_*.py` 재실행 → `audit_orphans.py` 통과 확인 → PACMAN 전하 → RASPA.
구조 감사는 현재 **68개 중 67개 통과**입니다.

> **`mIm075_saIm025` 는 주기경계 버그가 아닙니다.** 재생성해도 그대로입니다. 술폰산의
> −O**H** 수소가 이웃 링커의 Zn 배위 이미다졸레이트 **N** 을 1.569 Å 에서 찌릅니다.
> 네 자리 전부 정확히 같은 거리라 빌더가 **S−O−H 이면각을 고정**해 둔 결과입니다.
> 정상 N···H 수소결합은 1.8~2.0 Å 이고, 이 거리는 양성자 이동(−SO₃⁻ / N−H⁺)이
> 일어날 만한 영역입니다. **−SO₃H 가 최고 작용기이므로 이면각 탐색을 넣든
> 술폰산염으로 모델링하든 결정이 필요합니다.** 현재는 `charged/_clash_do_not_compute/`
> 로 격리해 RASPA 배치에서 제외했습니다. `18_PoreNarrowing` 의 −SO₃H 구조들은
> 이 문제가 없어 주력 결과는 영향받지 않습니다.

> **`14_Strategies` 와 `17_NestEffect` 의 전하 부여는 스크립트가 없습니다.**
> `pmcharge` 를 호출하는 코드는 `13_PACMAN`(대상 07계열)과
> `18_PoreNarrowing/charge_and_run.py`(대상 18계열) 둘뿐입니다. 17번 전하는 이관 때
> 아카이브·git 양쪽에서 빠져 노트북에서 따로 받아왔습니다(지금은 저장소에 포함).
> 14번 StratB 는 구조가 바뀌었으므로 전하를 새로 만들어야 하고, 그러려면
> `charge_and_run.py` 의 `make_charges()` 를 참고해 충전 단계를 작성해야 합니다.

> 단, 6절의 "Q_st 천장은 기하학적 한계"라는 논지는 **정상 위치 골격 원자와의 거리**에 근거하므로 유지될 가능성이 높습니다. 재계산으로 확인이 필요합니다.

---

## 5. 다음 작업

> ## ⚠️ 2026-08-06 — 우선순위가 바뀌었습니다. 아래 5절보다 이 항목을 먼저 보세요.
>
> **ZIF-69 계열이 목표권에 진입했고, ZIF-7 의 근거는 실측으로 무너졌습니다.**
>
> | | Q_st | 선택도 | 로딩 |
> |---|---|---|---|
> | ZIF-8 + SO₃H 50% (종전 최고) | 25.13 ± 4.15 | 28.44 ± 10.88 | 0.7406 ± 0.0635 |
> | **ZIF-69 + SO₃H 100%** | **31.07 ± 0.72** | **102.22 ± 9.92** | **2.1556 ± 0.0352** |
>
> 목표 30~40 의 하단을 1σ 밖에서 넘긴 **첫 사례**입니다. 무치환 ZIF-69 만으로도
> Q_st 21.08 ± 0.08 로, ZIF-8 이 SO₃H 50% 를 넣어야 도달하던 수준입니다.
> 상세는 `21_ZIF69_MTV/zif69_results.json`.
>
> **왜 gme 인가** — Part 4 는 분산력(좁은 공동)과 정전기(작용기 자리)가 같은 부피를
> 다툰다고 결론지었습니다. ZIF-69 는 아릴 치환으로 LCD 8.76 → 7.13 Å 로 줄이면서
> PLD 는 4.24 Å 로 열어 둡니다(CO₂ 3.3 Å 여유). ZIF-8 이 못 하던 일입니다.
>
> **ZIF-7 은 우선순위에서 내려갑니다.** 실측 LCD 가 문헌 4.30 이 아니라 **5.587** 이라
> 공동 모형 예측이 34.04 → **25.37** 로 떨어집니다. 이미 달성한 31.07 보다 낮고,
> 작용기를 붙일 수 없어 더 올릴 방법도 없습니다. 게다가 PLD 2.404 Å 로 CO₂ 크기
> 프로브 기준 접근가능부피가 **정확히 0** 입니다(정적 구조에서 CO₂ 진입 불가).
>
> **AV 를 CO₂ 용량의 대리지표로 쓰지 마세요.** SO₃H 계열에서 AV 가 989.8 → 228.7 Å³
> (−77%) 떨어지는 동안 로딩은 3.2배 올랐습니다. Zeo++ AV 는 N₂ 크기 프로브(1.82 Å)
> 기준이라 CO₂ 가 들어가는 곳을 못 봅니다.
>
> **검증이 남아 있습니다** — 수분 경쟁과 구조 안정성 둘 다 미완입니다(7절 참조).
> 그 전까지 31.07 은 **미검증 수치**로 다루세요.

### ~~우선순위 1~~ (구) — ZIF-7 검증

공동 모형이 ZIF-7(공동 4.3 Å)에서 **작용기 없이 Q_st 34** 를 예측합니다. ZIF-8과 같은 sod 위상이고, 파이프라인은 이미 벤즈이미다졸레이트 이환식 고리를 다룹니다(`05_MTV_Ligand_Library`의 `RING_SMARTS_BICYCLIC`, `find_bicyclic_sites`).

1. ZIF-7 CIF 확보(CCDC/COD) → `audit_orphans.py` → Zeo++로 LCD/PLD 실측, 문헌값 4.3/3.0 확인
2. 닫힌상/열린상 괄호 계산 — 창구 3.0 Å이 정적 구조 위양성인지 판정
3. PACMAN 전하 → Widom/GCMC로 예측 34 검증
4. **로딩과 작업용량 확인** — Q_st가 올라도 용량이 무너지면 무의미
5. 통과하면 수분 경쟁(`19_WaterCompetition`과 동일 절차)

### 우선순위 2

- ~~수분 경쟁 남은 8개~~ → **2026-08-05 완료 (20/20)**
- 4절의 재계산 목록 처리 — 구조는 복구됐고 계산이 남았습니다:
  `07_Bracketed_MTV/run_widom_batch.py` → `13_PACMAN/run_pacman_pipeline.py`
  → `10_DensityMap/run_density_map.py` + `analyze_density.py` → `17_NestEffect/run_raspa_nest.py`.
  `14_Strategies/StratB` 는 전하 부여 단계를 먼저 작성해야 합니다(4절 주 참조)

### 우선순위 3

- ZIF-90, ZIF-68 문헌값 검증 (예측상 ZIF-8과 큰 차이 없어 우선순위 낮음)
- −SO₃H 가수분해 안정성 — 고전 계산 불가, 문헌/DFT/실험 필요

---

## 6. 작업 방식 (이 프로젝트에서 확립된 것)

- **구조를 만들면 `audit_orphans.py` 를 먼저 돌립니다.** 고아 0 / 충돌 0 / 분리 0 이 아니면 계산하지 않습니다
- **판정 기준은 계산 전에 적어 둡니다.** 결과를 보고 기준을 정하면 자기합리화가 됩니다(수분 경쟁에서 그렇게 했습니다)
- **새 계산은 답을 아는 경우로 먼저 검증합니다.** 순수 ZIF-8의 소수성, ZIF-8의 LCD 11.39 등
- **오차를 함께 봅니다.** 수분 경쟁의 "증가"는 1.0~1.5σ라 유의하지 않다고 명시했습니다
- 환경 함정 7가지는 `00_Migration/MIGRATION.md` 3절에 있습니다. **특히 바이너리가 conda 환경마다 흩어져 있어 절대경로로 잡아야 합니다** — 과거에 `network` 호출 실패가 `except: pass` 에 삼켜져 LCD/PLD가 전부 0으로 기록된 적이 있습니다

## 6-1. 지금 바로 돌려야 할 것 (2026-08-06 미완)

**환경 문제로 세션에서 완주하지 못했습니다.** 백그라운드 `wsl.exe` 래퍼가 2~4분 뒤
종료하면서 WSL 안의 프로세스 트리를 끌고 갑니다(`setsid nohup` 도 소용없음).
03:32 에는 systemd 가 재시작해 `/tmp` 가 비워지고 실행 중이던 LAMMPS 가 SIGTERM 을
받았습니다. **사용자 터미널에서 직접 돌리면 이 문제가 없습니다.**

**남은 작업에 필요한 프로그램은 전부 설치돼 있습니다** (2026-08-06 04:29 실측).

| 환경 | 도구 | 상태 |
|---|---|---|
| `czeromof` | RASPA 2.0.41 / Zeo++ / ase 3.29.0 / rdkit 2026.03.4 | 정상 |
| `coremof_tools` | PACMAN(torch 2.7.0+cu126, **CUDA**) / mofchecker / numpy 1.26.4 | 정상 |
| `lammps_mof` | LAMMPS 22Jul2025-u4 / lammps-interface | 정상 |

> **`which` 만으로 점검하면 틀립니다.** conda 환경 안에는 `UFF_MOF` 도 `water.def` 도
> 없습니다. 둘 다 `$RASPA_DIR=~/RASPA/simulations/share/raspa/` 에 있고, `.bashrc`
> 135행이 그 변수를 잡아 줍니다. **`RASPA_DIR` 을 안 내보내고 스크립트를 돌리면
> 힘장을 못 찾습니다** — 백그라운드 실행 래퍼에는 반드시 `export` 를 넣으세요.

### ① 수분 경쟁 — 이 노선의 존폐 조건

```bash
cd ~/mof_project/21_ZIF69_MTV
export RASPA_DIR=$HOME/RASPA/simulations
export PATH=$HOME/miniconda3/envs/czeromof/bin:$PATH
python run_water.py 2>&1 | tee ~/.claude_work/water69.log
```

20작업(5구조 × RH 0/25/50/90%), 15000 사이클 이원 GCMC. 완료분은 자동으로 건너뜁니다.

**왜 결정적인가** — ZIF-8 이 90% RH 를 통과한 해석은 "11.4 Å 공동이 넓어 물과 CO₂ 가
자리를 나눠 썼다"였습니다(Part 3). ZIF-69 는 7.2 Å 이라 그 여유가 없습니다. 물 흡수량은
−SO₃H 밀도에 거의 선형이었으므로(ZIF-8: 25% → 0.481, 50% → 1.854 mol/kg) 100% 술폰화
gme 는 3~4 mol/kg 로 외삽됩니다. **Part 3 의 해석이 옳다면 여기서는 실패해야 합니다.**
어느 쪽이든 해석이 검증됩니다.

판정 기준(계산 전 확정, 스크립트에 박아 둠): RH90 유지율 80% 이상 유효 /
50~80% 조건부(전처리 건조 비용) / 50% 미만 종료. 유의성은 1.5σ.

### ② 구조 안정성

```bash
cd ~/mof_project/21_ZIF69_MTV
PATH=$HOME/miniconda3/envs/lammps_mof/bin:$PATH python risk_screen.py
```

두 함정은 이미 스크립트에 처리돼 있습니다 — `group` 상한 32 초과(98개 생성)와
축퇴 이면각(`Invalid atom ID in Dihedrals`). 후자의 필터는 단위시험을 마쳤습니다
(saIm100 에서 이면각 15,288 → 15,072, 각·개선각은 불변, 잔여 축퇴 0).

### ③ 아릴 계열 9종 — 전하는 끝, GCMC 만 남음

**전하 부여(PACMAN DDEC6)는 04:34 에 9/9 완료했습니다.** 순전하는 전부 5e-5 미만.
`charged/{nbIm,brbIm,mbIm}*_DDEC6.cif`, 목록은 `aryl_charged.json`.

전하와 GCMC 를 한 스크립트에서 잇지 않고 `charge_aryl.py` / `run_aryl_gcmc.py` 로
쪼갠 이유가 있습니다. **PACMAN 은 GPU(torch CUDA)를 쓰고 RASPA 는 CPU 코어를
통째로 씁니다.** 수분 경쟁이 물리 코어 8개를 전부 잡고 있어도 PACMAN 은
놀고 있는 GPU 로 돌 수 있으므로, 기다리는 동안 직렬 의존을 미리 끊어 둘 수 있습니다.

```bash
cd ~/mof_project/21_ZIF69_MTV
export RASPA_DIR=$HOME/RASPA/simulations
export PATH=$HOME/miniconda3/envs/czeromof/bin:$PATH
python run_aryl_gcmc.py 2>&1 | tee ~/.claude_work/aryl_gcmc.log
```

27작업(9구조 × Widom CO₂ / Widom N₂ / GCMC), 15000 사이클. 완료분은 건너뜁니다.
사이클을 줄이지 마세요 — `zif69_results.json` 과 나란히 놓으려면 같은 잣대여야 합니다.

**−CH₃ 는 25% 한 점뿐입니다.** 50/75/100% 는 원자 겹침 6/12/27 개로 탈락했습니다.
대조군의 조성 의존성은 말할 수 없고, 25% 끼리만 비교해야 합니다.

### ④ 밀도맵 — Q_st 31.07 이 어디서 왔는지

```bash
cd ~/mof_project/21_ZIF69_MTV
export RASPA_DIR=$HOME/RASPA/simulations
export PATH=$HOME/miniconda3/envs/czeromof/bin:$PATH
python run_density_map.py
python ../10_DensityMap/export_diff_vtk.py density
```

조성 5개 × 전하 ON/OFF = 10작업. 치환율을 올리면 공동 축소(분산력↑)와 술폰산
도입(정전기↑)이 **동시에** 일어나 로딩만으로는 원인을 못 가릅니다. 전하 ON/OFF
차분에서 LJ 항이 소거되므로 이것만이 정전기 기여를 따로 볼 수 있는 수단입니다.
**전하 끈 계산을 중복으로 오해해 지우지 마세요.**

ZIF-8 계열과 달리 **닫힌상/열린상 축은 없습니다** — 그건 ZIF-8 게이트 오프닝
(1.47 GPa 고압상)에서 온 축이고, gme 는 강체 골격이라 상이 하나입니다.

## 7. 미완 사항

**Notion 커넥터가 여전히 끊겨 있어 Part 4·5를 올리지 못했습니다.** 데스크탑에서
Notion 을 설치한 뒤에도 이미 실행 중이던 세션에서는 커넥터가 잡히지 않았습니다
(설치 목록·레지스트리 검색 모두 비어 있음). **세션을 새로 시작한 뒤 다시 확인하세요.**

대기 중인 문서 두 개는 그대로 붙여넣으면 됩니다.

| | 내용 | 파일 |
|---|---|---|
| **Part 4** | 모체 공동 크기 — 최적 4.0~4.5 Å, ZIF-7 예측 34 | `20_ParentScan/README.md` |
| **Part 5** | 데스크탑 이전·재현 검증, 오차 병기로 뒤집힌 순위 | `00_Migration/PART5_DRAFT.md` |

Notion에 올라간 것: Part 0-A/B/C(기초 물리화학), Part 1(밀도맵 해부), Part 2(기공 축소 반증), Part 3(수분 경쟁), 팀 문헌조사 반영.

> **Part 1~3 도 수정이 필요합니다.** 성적표의 "최고 SO₃H 50%" 류 순위 표현이
> 오차를 반영하면 성립하지 않습니다(2절). Notion 복구 시 Part 5 업로드와 함께
> 기존 Part 의 해당 문장도 고쳐야 합니다.
