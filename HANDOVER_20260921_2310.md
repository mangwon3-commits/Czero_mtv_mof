# 인계 — 2026-09-21 23:10 ~ 09-23 10:00 (사용자 부재)

> 사용자 지시: *"사용량 한도가 거의 찼으니 9/23 오전 10시까지 로컬 작업을 두라."*
> **이 파일 하나로 부팅됩니다.** 대화 맥락은 없습니다(CLAUDE.md §8) — 여기와 `SESSION_LOG.md` 가 정본.

## 0. 한 눈

    ✅ 끝난 것   §AS Q_st(n) **72/72**(자료만 뽑음, 판정 미완) · §AU **확정** · T-BR-1 **철회 판정**
                 §AV 등록·배정·기동 · 발표 차트 3장 확정 · postman 자율 배달 3기기
    🔄 도는 것   데스크탑 T-C10 이어받기(관문→WC 15) · 랩탑 §AV 204종 · laptop2 §AV 308종
    ⏳ 밀린 것   **§AS Q_st(n) 판정 하나** (자료 준비 끝: `21_ZIF69_MTV/qn_table.json`)
    🙅 사람 필요  **없습니다.** 09-23 10:00 까지 자동으로 돕니다.

## 1. 데스크탑 — **6시간 놀았습니다. 고쳐서 다시 띄웠습니다**

T-C10 사슬이 **09-21 17:09:28 에 ⑤관문에서 rc=2 로 멈췄습니다.** 원인은 계산이 아니라 **인터프리터**입니다 —
사슬이 `czeromof` 로 관문을 띄웠는데 `risk_screen_v3_sub.py` 는 `lammps_interface`·`lmp_serial` 이 필요하고
그건 **`lammps_mof`** 환경에 있습니다. 관문의 사전검사가 *"아무것도 안 돌리고 중단"* 한 것은 **옳은 동작**입니다
(§0 — 실패가 결과처럼 보이지 않게). 그래서 17:09 → 23:07 동안 데스크탑이 비었습니다.

    23:07:47  `.claude_work_tc10_resume.sh` 기동 (PID 3581570)
              고친 것은 **관문 인터프리터 하나뿐** — 태그·워커·결과 경로·나머지 단계 전부 원 사슬 그대로
              ⑤ Zeo++ 관문(9.5 GB/건, `simulate 0` 3분 확인 뒤) → ⑥ 습윤 WC 15작업
    로그      `21_ZIF69_MTV/tc10_chain.log` · 출력 `.claude_work_tc10_resume.out`
    결과      `21_ZIF69_MTV/v3w_humid_wc/humid_working_capacity_w2_saIm0583e_ext_desk.json`
    등록      `21_ZIF69_MTV/TC10_REGISTRATION_20260920.md` — 판정은 §3·§4 그대로

> **교훈(적어 둡니다)**: 사슬이 환경을 셋 쓰는데(`czeromof`·`coremof_tools`·**`lammps_mof`**) 앞의 둘만
> 변수로 잡혀 있었습니다. 다음에 사슬을 쓸 때 **단계마다 필요한 환경을 먼저 적으십시오.**

## 2. 두 랩탑 — §AV (CoRE 풀 524종을 우리 자로)

    등록   `21_ZIF69_MTV/COREPOP_REGISTRATION_20260921.md`  (읽을 것: §5 예측 · §6 사용 제한 · §9 진행)
    랩탑   204종 · 워커 8  · 완주 전망 09-22 **08시경**
    laptop2 308종 · 워커 12 · 단독이면 09-22 15:3x ~ 09-23 06:4x (지수 CI 전파)
    합침   `merge_core_pop.py` -> `core_pop_merged.json` (현재 27/524)

### ★ 09-22 08시경 자동 분기 — 재배분
랩탑이 자기 204종을 끝내면 **랩탑 세션이** `make_handoff.py` 를 돌려 laptop2 꼬리를 받습니다(§9-1·9-3).
그러면 둘 다 **09-22 14시경** 완주. 관문 셋이 걸려 있어 조건이 안 맞으면 **스스로 멈춥니다**(§9-4).
**사람이 안 해도 됩니다.** 랩탑 세션이 없으면 재배분이 안 일어날 뿐이고, 그래도 09-23 06:4x 에는 끝납니다.

## 3. 밀린 판정 하나 — §AS Q_st(n)

**09-21 17:0x 에 72/72 완주.** 자료를 표로 뽑아 두었습니다 — 판정은 **안 했습니다**.

    자료   `21_ZIF69_MTV/qn_table.json` (6조성 × 3온도 × 4압력, 72/72 값 있음)
    뽑개   `21_ZIF69_MTV/extract_qn.py`
    등록   `21_ZIF69_MTV/QSTN_REGISTRATION_20260920.md` §3 — **결과 전 고정된 판정 기준**
           판정 1 |Q_st(n_w) − Widom Q_st(보정)| ≤ 5 kJ/mol → Widom 이 작업 적재를 대표
           판정 2 ΔQ_st ≥ +8 협동 항 있음 / ≤ +3 없음 / 사이 부분
           판정 3 n_w 가 공통 범위 밖이면 "판정 불가" 로 적고 §-보완을 새로 등록
    방법   온도별 등온을 `ln p` 대 `n` 으로 **선형 보간(외삽 금지)** → 공통 적재 격자에서
           `Q_st(n) = −R·d ln p/d(1/T)`. 오차는 ± 를 보간에 전파. n_w = 298 K·0.15 bar 의 n.

자료는 T·P 양쪽으로 단조이고 결측이 없습니다(눈으로 확인). **판정만 남았습니다.**

## 4. 손대지 말 것

    · `postman.sh` 는 세 기기가 **저장소 밖 사본**에서 돌고 `origin/master` 에서 스스로 받아 갑니다.
      수동으로 먹이지 마십시오. 상태는 로그의 `판=<md5>` 한 줄로 봅니다.
    · **러너가 도는 기기에서 수동 `git pull`/`merge`/`checkout` 금지**(CLAUDE.md §6, 09-21 신설).
      꼭 받아야 하면 러너가 안 읽는 파일만 `git checkout <ref> -- <경로>`.
    · 발표 차트 `04_Analysis/figs/fig7·fig8·fig9` 는 **확정**입니다. T-BR-1 철회로 CoRE 성능 축을 안 씁니다.

## 5. 09-23 10:00 에 사용자가 볼 것

    1) §AV 완주 여부와 `core_pop_merged.json` (목표 524/524)
    2) §AS Q_st(n) 판정 — 안 되어 있으면 그것부터
    3) T-C10 습윤 WC 결과와 그 판정
    4) `SESSION_LOG.md` 09-21 항목들 (규약이 여섯 개 늘었습니다: §0 결함 6건째 · §5 셋 · §6 둘 · §8 둘)
