# 60시간 무인 계산 설계 (랩탑 10시간 세션 이후)

**전제:** 08-21 장표 제출이 끝난 뒤, 사람이 거의 못 붙는 60시간을 쓴다.
48H_COMPUTE_PLAN.md 가 08-20 까지의 계약이라면 이 문서는 **그 다음**이다.

## 0. 60시간짜리 무인 운전에서 진짜 위험은 자원이 아니다

11일에 근본 결함 4건, 전부 **실패가 결과처럼 보이는 것**이었다. 무인 시간이
길수록 그 위험만 커진다. 60시간이면 잘못된 규약으로 **60시간어치**를 쌓을 수
있다. 그래서 이 설계의 중심은 처리량이 아니라 **관문**이다.

08-16 밤에 실제로 겪은 것 셋을 그대로 반영한다.

- 파이썬은 편집해도 돌던 프로세스에 반영되지 않는다. **재기동할 때 터진다.**
  그래서 60시간 파이프라인은 시작 전에 모든 러너를 **import 시험**한다.
- pool.map 은 작업을 미리 잘라 배분한다. 이어받기로 느린 것만 남으면 워커가
  논다. 모든 배치는 **imap_unordered(chunksize=1)** 또는 동등한 동적 배분.
- 이어받기는 출력의 존재만 본다. **설정을 바꾸면 실행 폴더를 지운다.**

## 1. 단계 -- 앞이 막히면 뒤로 가지 않는다

```
S0  사전비행     러너 import 시험 + 디스크 + 코어 + 힘장 해시      (5분)
S1  잔여 수확    습윤 WC v3 / 수분 v3 / 밀도맵 / 안정성 결과 확정   (0~2h)
S2  증거표       v3 전체를 한 표로. 1.5시그마 규칙 적용             (30분, 계산 아님)
S3  재생 에너지  받아들인 WC 로 공정 수지 재계산                     (30분, 계산 아님)
S4  조성 격자    62.5 / 87.5% 구조 생성 -> PACMAN -> GCMC           (12~16h)
S5  아릴 v3      아릴 12종을 v3 규약으로 재계산                      (10~14h)
S6  DAC 예비     400 ppm 조건 1~2종만                                (8~12h)
S7  포장         결과 노트 + 커밋 + 노션 대기열                      (30분)
```

**S4 가 이 60시간의 주 산출물이다.** v2 에서 TSA 회수율이 치환율에 대해 단조가
아니었다(87.1 -> 83.5 -> 77.4%, 75%에서 꺾임). 흡착열 하나로 설명되는 모델이면
나올 수 없는 모양인데, 지금 격자가 25% 간격이라 **꺾이는 지점을 짚을 수 없다.**
12.5% 로 채우는 것이 남은 계산 중 과학적으로 가장 값지다.

S5·S6 는 시간이 남을 때만. **S6 는 사람 승인 없이 결론을 내지 않는다** --
DAC_PLAN.md 는 아직 설계 문서다.

## 2. 관문 (통과 못 하면 정지하고 이유를 남긴다)

| 단계 | 관문 |
|---|---|
| S0 | 모든 v3 러너가 import 되는가. 힘장 C_co2 가 29.933/2.745 인가 |
| S1 | 결과 JSON 의 행 수가 기대치와 같은가. 순전하 5e-5 이내인가 |
| S2 | v1 값이 섞이지 않았는가. 세대 표기가 모든 행에 있는가 |
| S4 | 새 구조가 충돌 검사·고아 원자 검사를 통과했는가 |
| S4 | **연기 시험 1종의 Q_st 가 10~60 kJ/mol 인가** |
| 전 구간 | C: 여유 2 GB 이상. 미만이면 새 작업을 띄우지 않고 보고 |

## 3. 자원 배분

물리 8코어. RASPA 는 코어가 병목이고 한 건 471 MB.

- 건조 GCMC: 워커 7 (수분 백업이 1코어를 쓸 수 있게 하나 남김)
- **Zeo++ 는 RASPA 와 절대 동시 금지.** S1 의 안정성이 남아 있으면 워커 4 단독
- 구조 생성·PACMAN 은 GPU/짧은 CPU 라 RASPA 와 겹쳐도 된다

## 4. 무인 운전이 하면 안 되는 것

- `wsl --shutdown`, VHDX 압축, Fast Startup 변경
- `--allow-unsafe` 로 sparse 전환 (데이터 손상 경고가 붙은 플래그다)
- density_v2/ density_v3/ 의 VTK 삭제
- 결과를 보고 판정 기준을 고치는 것
- 1.5시그마 미만 차이에 순위를 매기는 것
- v1 숫자를 후보 결과로 인용하는 것

## 5. 사람이 돌아왔을 때 볼 것

```bash
cat ~/.claude_work/auto60.state      # 지금 어느 단계
tail -40 ~/.claude_work/auto60.log   # 무슨 일이 있었나
cat ~/.claude_work/notion_queue.md   # 노션에 올릴 것
bash bgstate.sh                      # 실제 상태
```

정지했으면 `auto60.state` 첫 줄이 **중단됨**으로 시작하고 이유가 붙어 있다.
그 이유를 읽기 전에는 재기동하지 않는다 -- 같은 관문에 다시 걸린다.

## 6. 구현 메모

overnight.sh 가 이미 이 형태다(단계 + 관문 + 상태파일 + 노션 대기열).
60시간판은 그것을 확장하되 **S4 의 구조 생성 단계가 새로 붙는다.** 그 단계만
기존 배치와 성격이 달라(계산이 아니라 생성) 검사 항목도 다르다 --
충돌·고아 원자·조성 개수의 정확성이다. 08-14 결함이 정확히 그 자리에서 났다.
