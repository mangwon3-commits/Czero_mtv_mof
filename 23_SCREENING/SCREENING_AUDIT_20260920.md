# CoRE MOF 스크리닝 정리·감사 + 우리 재료 평가 제안 (2026-09-20 18:28, 데스크탑 세션)

사용자가 올린 `MOF_Screening.ipynb`(83 셀, 25 MB, 마크다운 0개) 를 읽고 정리한 것입니다.
정리본은 `screen_core.py`, 자료 안내는 `data/MANIFEST.md`.

> ## ⚠ 2026-09-20 20:12 — 이 감사문은 **한 곳이 뒤집혔습니다**
> 사용자가 `D:\CoRE-MOF-Tools-main.zip`(업스트림 저장소 통째)을 알려 주어 열어 본 결과,
> 아래 §1 이 "교집합 0개" 라고 적은 것의 **원인이 다릅니다.**
> **1순위 관문 `all_nodes == 'stable'` 은 전 레코드에서 거짓입니다** — 그 필드에는 위상 기호(pcu·sql·dia…)가 들어 있고
> `'stable'` 이라는 값이 CR 17,202건 중 **0건**입니다. 관문이 엄해서 0개가 아니라 **관문이 성립하지 않아서** 0개였습니다.
> 관문을 고쳐 다시 걸면 교집합은 **6,604종**(위상 미상 통과) 또는 **4,587종**(미상 탈락)입니다.
> 그리고 원본이 그 0 을 보고 관문을 버렸으므로, **최종 115종은 안정성 검사를 한 번도 안 거쳤습니다**
> (115 중 unstable 6 · mismatch 3 · 위상 미상 28 — 확실히 통과는 78개).
> 이것을 **D6** 로 아래 §2 표에 넣었습니다. 전체 개봉 기록·검증 경로: **`ZIP_FINDINGS_20260920.md`**
>
> §3(우리 자료가 PLD 관문을 반증한다)은 그대로 유효합니다. 두 지적은 서로 독립입니다 —
> §3 은 관문의 **적용 범위**, D6 은 관문의 **성립 여부**에 관한 것입니다.
> 또한 `except:` 개수는 "스무 곳 넘게" 가 아니라 **97곳**입니다(`except Exception` 11곳 별도).

---

## 1. 원본이 하는 일 (복원한 파이프라인)

    입력   CR_meta_data_SI.json — CoRE MOF 메타데이터 **2,737종**(SI 부분집합)
           필드: Zeopp(PLD·LCD·VF·PV·dimension) · CrystalNets(all_nodes) · water(water_classification)
                 · metal(metal_type·has_OMS) · id(mofid-v1) · 그리고 **열 인덱스 9** = GEMC(Widom [K_H(CO2), K_H(N2)])
    관문   ① CrystalNets all_nodes == 'stable'  ② PLD >= 3.3 Å  ③ water_classification == 'weak'
    결과   **교집합 0개** → 관문을 창으로 바꿈
    창     'Magic Window / N2-Sieving'  PLD 3.3~3.6 & weak            → 63종
           'Bottleneck / High-Flux'     PLD 3.7~4.2 & LCD>=5.5 & weak → 52종
    지표   CO2 친화도 = Widom[0](K_H) · 선택도 = Widom[0]/Widom[1]
    이후   위양성 하드 필터(dimension==3 & PLD>=3.4 & VF>=0.2) → 점수화(0.6×선택도 + 0.4×용량, 상위 5 % 클리핑)
           → 내습성 분류(금속 Zr/Ce/Ti/Hf 또는 위상 fcu/scu/sod/rob/csq/ftw 이면 'Robust', OMS 있으면 'Core')
    산출   CSV·XLSX 20여 개, PDF·PNG 40여 개 (Frontier 도면 반복 재작성)

**잘 된 것**: 관문이 0개를 냈을 때 **조건별로 따로 세어 병목을 찾은 것**(셀 3·5)은 이 저장소가 여러 번 한
"나쁜 결과를 보면 검사기부터 의심한다" 와 같은 동작입니다. 누락(unknown) 개수를 따로 센 것도 좋습니다.

---

## 2. 결함 일곱 — 전부 **이 저장소가 이미 데인 유형**입니다

| # | 무엇 | 원본 위치 | 우리 전례 |
|---|---|---|---|
| **D8** | **CO₂ 라고 적힌 것이 물** — 공정 성능 층 전체(`q_ads_0.15bar`·`Working_Capacity`·`Recovery`·`DAC/FlueGas_Score`·Tier 랭킹)가 `water.GEMC`(**TIP4P 물**, `Molecules/Supercell`)를 CO₂ 등온선으로 읽습니다. 단위 미환산 + 자료 최고점의 **3.5배 바깥**(0.0432 bar → 0.15 bar)으로 외삽. 결과 7~12 mmol/g 은 0.15 bar CO₂ 로 측정된 적 없는 값 — 우리 MUF-16 GCMC 가 실측과 배율 1.035 로 맞는 눈금에서 보면 명백. **재현 완료(10/10 소수 셋째 자리 일치).** 상세 `D8_GEMC_IS_WATER_20260921.md` | 셀 27→28·30·31·32·35·36·71 | §0 "**실패가 결과처럼 보이는 것**" — 그리고 09-11 Q_st RT 부호처럼 **상수가 아닌 계통 오차가 전 층을 통과** |
| **D6** | **항상 거짓인 관문** — 1순위 `node_stability == 'stable'` 이 전 레코드에서 거짓. 그 필드는 **위상 기호**(pcu·sql·dia·unstable…)를 담고 `'stable'` 은 CR 17,202건 중 **0건**. "생존자 0명" 의 진짜 원인이고, 그 0 을 보고 관문을 버렸으므로 최종 115종은 **안정성 검사를 안 거침**(unstable 6·mismatch 3 포함) | 셀 2·8 | §0 "**나쁜 결과를 보면 검사기부터 의심하고**" · **검사기 오탐 3건**(멀쩡한 구조를 결함으로 찍음) |
| **D1** | **위치 인덱스** — `iloc[:, 9]` 는 실제로 **`water` 열**(이름표는 `GEMC_data`), `widom[0]/widom[1]` 은 이름이 아니라 순서를 믿음. 배포본 어디에도 **기체 이름이 없습니다**(zip 전체에서 "Widom" 2회, 둘 다 단위 문자열). **값 자체는 맞았습니다** — 순서 [CO₂,N₂] 를 크기로 확인했습니다. 다만 신판에서 이 구조가 평평해져 같은 코드가 곧바로 깨집니다 | 셀 16·17·19·69·81 | **08-14 부착 원자 고정 인덱스 4** → v1 치환 결과 전량 폐기 |
| **D2** | **맨 `except:`** 이 **97곳**(`except Exception` 11곳 별도) — 실패가 `NaN`·`0`·`'unknown'` 으로 조용히 바뀜 | 셀 17·44~81 전반 | CLAUDE.md §0 "**실패가 결과처럼 보이는 것**" — 근본 결함 5건 전부가 이 유형 |
| **D3** | **자료를 본 뒤 문턱이 움직임** — 3중 관문 0개 → PLD 창 신설 → 다시 PLD>=3.4·VF>=0.2 추가 | 셀 2→8→16→69 | §2 "**판정 기준은 계산을 돌리기 전에 등록**합니다. 결과를 보고 기준을 고치지 않습니다" |
| **D4** | **오차가 없음** — Widom K_H 점값으로 순위·점수 | 전체 | 08-28 "**오차를 병기하자 순위가 사라졌다**" · §2 의 1.5배 규칙 |
| **D5** | **임의 가중치·클리핑** — 0.6/0.4, 상위 5 % 컷. 근거가 "정상 물질 점수가 바닥에 깔리는 현상 방지" | 셀 69 `robust_scoring` | 08-20 "1위" 가 자 를 고쳐 사라진 것과 같은 자리 |

부수: 같은 셀이 여러 번 복제돼 있습니다(60≡62 · 76≡77 · 37≡40). 마크다운 셀이 **0개**라 무엇을 왜 했는지가 코드 주석에만 있습니다.

---

## 3. 🔴 가장 중요한 것 — **1순위 관문(PLD ≥ 3.3)이 우리 자료로 이미 반증됐습니다**

`21_ZIF69_MTV/TNF_RESULTS_20260910.md` ㉣ (09-20 확정):

    MUF-16(Co)   Zeo++ PLD **3.143 Å** (< 3.3) · CO2 탐침 AV **0.0**
                 그런데 건조 CO2 **1.117 mol/kg** — 우리 모체(0.5839)의 **1.9배**, 외부 실측 1.08 mmol/g 과 배율 1.035 로 일치

즉 **이 스크리닝을 그대로 돌리면 MUF-16 은 1번 관문에서 탈락합니다.** 실측으로 좋은 CO2 흡착제인데 말입니다.
우리 판정문은 그래서 문턱을 옮기지 않고 **적용 범위를 좁혔습니다** — "PLD >= 3.3 · AV >= 20 관문은 **ZIF-69 gme 위상 안에서만** 유효".

**이것이 이 노트북에 가져갈 수 있는 가장 값진 한 줄입니다.** 기하 관문은 **위상 밖에서 위음성(false negative)을 냅니다.**
원본이 공들인 것은 위**양**성 제거(dimension·VF 필터)인데, 정작 비싼 쪽은 **버려진 것들** 입니다 — 세어 볼 수도 없습니다.

---

## 4. 정리본 `screen_core.py` 가 바꾼 것

    D1 → `dig('Zeopp.PLD')` 처럼 **이름으로** 판다. 못 찾으면 `Missing` 예외. Widom 이 리스트면
         `'by-position(UNVERIFIED)'` 딱지를 붙여 돌려주고, 그 행이 있으면 **선택도 인용을 막는 경고**를 찍는다.
    D2 → 실패를 `Counter` 로 세어 보고서에 찍는다. 조용한 0/NaN 없음.
    D3 → 문턱을 `GATES` · `WINDOWS_POST_HOC` 두 상수로 분리. 후자는 이름 자체가 "자료를 본 뒤" 임을 말한다.
    D4 → `rank()` 는 오차 열이 없으면 **순위를 거부**하고 점값 정렬만 돌려준다. 있으면 1.5배 규칙으로 **덩어리**를 만든다.
    D5 → 점수화를 기본 경로에서 뺐다. 원자료 축(K_H, 선택도)만 쓴다.

---

## 5. 제안 — 우리 재료를 이 스크리닝으로 평가하기

### 5-0. 이미 돌려 봤습니다 (새 계산 0, `results_v3.json` 31조성)

    PLD >= 3.3 (1순위 관문)                    **31/31 통과**   PLD 최소 3.635 · 중앙 4.921 · 최대 6.292
    PLD >= 3.4 (위양성 필터)                    31/31 통과
    사후 창 'N2 sieving'  PLD 3.3~3.6           **0개**
    사후 창 'high flux'   PLD 3.7~4.2 & LCD>=5.5  **2개** (mslm050 · saIm075)
    선택도(오차 병기, 1.5배 규칙)               **10덩어리** — 1군 saIm100 150.9±13.7 · 2군 saIm075 94.5±4.7
                                                · 3군 saIm050 65.2±2.1 · 4군 mslm050 58.1±1.7 & mslm075 57.5±2.1

**읽기**: 우리 조성은 관문은 전부 통과하지만 **두 창에는 거의 안 들어갑니다**. 창이 우리보다 좁기 때문입니다.
그 창들은 설계 목표가 아니라 **그 데이터셋이 모여 있던 자리에 대한 기술(description)** 이므로(D3), 우리가 밖에 있는 것은 탈락이 아닙니다.
거꾸로 **우리는 이 스크리닝의 분류 체계에서 보이지 않습니다** — 쓰려면 창을 우리 위상에 맞게 다시 등록해야 합니다.

### 5-1. 무엇을 할 수 있고 무엇은 못 하나 (정직도 순)

| 갈래 | 내용 | 정직한가 |
|---|---|---|
| **(C) 관문 빌려 쓰기** | CoRE 가 가진 축 중 **우리가 안 만든 것**(water_classification · CrystalNets 안정성 · dimension)을 우리 CIF 에 **같은 도구로 직접 돌린다** | ✅ 계열 문제 없음 — 우리가 계산하므로 |
| **(B) 다리 놓기** | CoRE 후보 N종을 **우리 프로토콜**(UFF_MOF · García-Sánchez CO2 · DDEC6 · 5,000+15,000)로 다시 계산해 두 계열의 변환을 잰다 | ✅ 우리가 T-NF-0/1 에서 이미 쓴 방법의 확장 |
| **(A) 좌표만 얹기** | CoRE 조경 위에 우리 점을 얹되 **순위 금지·정성만** | ⚠ 힘장·전하·프로토콜이 다른 **다른 계열**. 같은 그림에 그리면 읽는 사람이 비교합니다 |

**권고 순서는 (C) → (B) → (A)** 입니다. (A) 를 먼저 하면 §2 의 "버전을 넘나들며 인용하지 마세요" 를 그림으로 어기게 됩니다.

### 5-2. (C) 를 먼저 하자는 이유 — 계산 0~적음, 얻는 것 큼
우리는 32조성의 **물 K_H**(`v3w_water_kh/water_kh_ALLw_hkhome_seedfixed.json`)를 이미 가지고 있습니다.
CoRE 의 `water_classification` 이 어떤 양으로 정의됐는지만 확인하면, **우리 조성에 같은 라벨을 붙일 수 있습니다**
— 그러면 "우리 재료가 CoRE 기준으로 소수성인가" 에 처음으로 답이 생깁니다. 지금은 그 축에서 우리 위치가 **없습니다.**

### 5-3. 등록해야 할 것 (계산 전에)
이 제안을 실제로 돌리려면 **문턱을 먼저 등록**해야 합니다 — D3 를 우리가 반복하지 않기 위해서입니다.
    · (C) water_classification 의 정의와 우리 K_H 를 잇는 환산식, 그리고 '소수성' 컷
    · (B) 다리용 CoRE 후보 선정 기준(몇 종·어떤 근거로)과 **허용 편차**(우리 대 CoRE 배율이 몇 배 안이면 통과인가)
    · (A) 를 할 경우 그림에 반드시 들어갈 단서: 계열이 둘이라는 것, 순위가 아니라는 것

---

## 6. 자료 — 깃허브에 올릴 것인가

**권고: 작은 것은 저장소에, 큰 것은 받는 스크립트 + 체크섬.** `data/MANIFEST.md` 에 적었습니다.
이 저장소의 정본 규율(§7 "지우면 안 되는 것", §8 "공유되는 것은 저장소의 파일뿐")상 **입력 자료가 저장소 밖에 있으면 재현이 끊깁니다** —
노트북이 참조하는 CSV 20여 개가 지금 정확히 그 상태입니다(파일이 없어 셀 대부분이 못 돕니다).

> **2026-09-20 20:12 갱신 — 라이선스 확정.** zip 안의 `LICENSE` 와 `setup.py` 가 **CC-BY-4.0** 입니다.
> 출처를 밝히면 재배포가 허용되므로 "확인 전에는 올리지 마십시오" 는 해제합니다. 결론은 바뀌지 않습니다 —
> **zip 통째(599 MB)와 models/(1.31 GB)는 올리지 말고, 관문·순위에 쓰는 필드만 뽑은 슬라이스(수 MB)를 올리십시오.**
> `MOF_Project_Master_Data.pkl`(0.7 MB, 최종 115종)은 **올릴 값이 있습니다** — 우리 재현과 대조할 정본이기 때문입니다.
> 상세: `data/MANIFEST.md §0`

---

## 7. 이 폴더에 있는 것

    screen_core.py                        정리본(83셀 → 한 모듈). `python3 screen_core.py` 로 우리 자료만으로도 돕니다.
                                          09-20 20:1x: 신·구 스키마를 **둘 다** 읽고, D6 관문을 고쳤습니다.
                                          실측 확인:  --meta <CR.json> 로 17,202 레코드 완주, 교집합 6,604 재현.
    SCREENING_AUDIT_20260920.md           이 문서
    ZIP_FINDINGS_20260920.md              `D:\CoRE-MOF-Tools-main.zip` 개봉 기록 — D6 의 검증 경로, Widom 순서 시험,
                                          발산값·깨진 레코드, GEMC 가 물이라는 것, 우리에게 새로 생긴 것
    data/MANIFEST.md                      입력 자료를 어디에 어떻게 둘 것인가 (라이선스 확정 반영)
    MOF_Screening_original_stripped.ipynb 원본 노트북 — **출력만 제거**(25 MB → 0.2 MB). 코드는 한 글자도 안 고쳤습니다.
                                          원본(출력 포함)은 저장소에 안 넣었습니다: 25 MB 이고, 출력에 파일 경로·중간표가 섞여 있습니다.
