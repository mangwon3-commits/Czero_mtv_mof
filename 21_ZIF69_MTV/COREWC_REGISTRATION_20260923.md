# §AW 등록 — CoRE 72종의 **CO₂ 등온 2점**으로 작업 용량 축을 연다 (2026-09-23 **01:0x**, 데스크탑, **자료 0건**)

## 0. 왜 — D8 이 닫아 둔 마지막 축

`23_SCREENING/D8_GEMC_IS_WATER_20260921.md §5` 가 이렇게 끝났습니다:

> ⚠ 안 되는 것: **"작업 용량" 축에서의 비교. CoRE 쪽에 CO₂ 등온선이 아예 없기 때문**입니다 —
> 노트북이 만들어 낸 것이 아니라, **자료에 없는 것을 외삽**한 것이었습니다.
> CoRE 로 작업 용량을 비교하려면 **CO₂ 등온선을 우리가 계산해야** 합니다.

§AV 가 **K_H·선택도** 축을 우리 자로 열었습니다(514/524). 남은 것이 이 축 하나이고,
여는 방법은 **직접 계산**뿐입니다.

## 1. 고정값 — 하나도 안 바꿉니다 (CLAUDE.md §1)

    사이클   GCMC 초기화 **5,000** + 생산 **15,000**      골격 힘장 UFF_MOF
    CO₂     García-Sánchez 2009 (경로가 `TraPPE/CO2.def` 지만 **TraPPE 아님**)
    전하     `UseChargesFromCIFFile` — CoRE CIF 의 PACMAN DDEC6      Ewald 1e-6 · 컷오프 12 Å · 298 K
    슈퍼셀   숫자가 아니라 `unit_cells()` 규칙 (§AV 와 같은 예외)
    **압력**  **0.15 bar**(배가스 CO₂ 분압) · **0.01 bar**(진공 탈착) — 둘의 차가 VSA 작업 용량

> 0.15 bar 를 고른 이유: **우리 31조성의 `results_v3.json:loading_015bar` 와 같은 조건**이라
> 환산 없이 바로 견줍니다(§AV §6 (ㅁ) 와 같은 근거).

## 2. 대상 — **선정 규칙을 자료 전에 고정**

    풀     §AV 에서 **우리 자로 K_H 가 나온 514종**(`core_pop_merged.json`)
    선정   **우리 자 K_H(CO₂) 오름차순 등분위 72종** — 분포를 보존하는 표본입니다(치우쳐 뽑지 않음)
           범위 8.780e-06 ~ 6.627e-03 (모집단 4.660e-06 ~ 1.508e-02)
    배정   그 순서로 **엇갈리게** — 짝수 `laptop`(36) · 홀수 `laptop2`(36)
           한쪽이 죽어도 남은 몫만으로 분포가 섭니다(§AV §3 과 같은 이유)
    목록   `core_wc_pick.json`

## 3. ★ 실행 폴더를 압력별로 가릅니다 (이 등록의 기술적 핵심)

`run_aryl_gcmc.run_one` 의 실행 폴더 이름이 `{mode}_{gas}_{name}` 이라 **압력이 안 들어갑니다.**
두 압력을 같은 뿌리에서 돌리면 **이어받기가 0.01 bar 실행을 0.15 bar 완주분으로 회수합니다** —
CLAUDE.md §3 이 경고한 *"이어받기는 출력 파일의 존재만 본다"* 그대로입니다.
그래서 `core_wc_runs_p0.15/` · `core_wc_runs_p0.01/` 로 **뿌리를 가릅니다.**

## 4. 예측 — **자료 0건 시점에 고정**

우리 31조성의 0.15 bar 건조 CO₂: 최소 0.416 · 중앙 0.752 · 최대 **1.543** mmol/g
(`D8 §3`, MUF-16 실측 대비 배율 1.035 로 검증된 눈금).

    P1  CoRE 72종의 0.15 bar 적재 **중앙값 0.3 ~ 1.5 mmol/g**
    P2  saIm100(1.543)의 백분위 **70 ~ 95 %**
    P3  우리 중앙(0.752)의 백분위 **40 ~ 70 %**
    P4  VSA 작업 용량(0.15−0.01)이 0.15 bar 적재의 **70 % 이상**(저압 탈착이 잘 되는 계)
    P5  **7~12 mmol/g 대의 구조는 0종**. 노트북이 낸 그 대역은 물 등온선이었으므로,
        CO₂ 로 다시 재면 **한 자릿수 아래**로 내려와야 합니다. → **D8 의 직접 검정**
    P6  실패는 대체하지 않고 자리를 비웁니다(n 이 줄어듦)

**반증**: P5 가 깨지면(0.15 bar CO₂ 적재가 7 mmol/g 넘는 구조가 나오면) D8 의 물리적 논거를 다시 봅니다.

## 5. 사용 제한 — 결과를 쓰기 전에 고정

    (ㄱ) 백분위는 **항상 표본의 정의와 함께**: "§AV 514종에서 K_H 등분위로 뽑은 **72종** 중".
         **"CoRE MOF 중 상위 몇 %" 로 줄이지 않습니다.**
    (ㄴ) 우리 값은 **그대로** 얹습니다 — 같은 러너·같은 사이클·같은 힘장. 환산 없음.
    (ㄷ) 72종은 §AV 514종의 **표본**입니다. 514종 전체의 작업 용량을 주장하지 않습니다.
    (ㄹ) 실패율 > 10 % 면 백분위 보류, 실패 집합의 성질을 먼저 적습니다.

## 6. 실행

    러너   `run_core_wc.py` (= `run_aryl_gcmc.run_one`, mode='gcmc', 압력 2회차)
    CIF    `core_pop_cifs/` (§AV 에서 이미 푼 524개)
    결과   `core_wc_results_<기기>.json` — **작업마다 갱신**
    견적   **laptop 10.1 h · laptop2 11.8 h** (둘 다 묶는 쪽은 `작업합/워커`)
           출처: 저장소 비용란 "ZIF 2×2×2 건조 CO₂ 1.5~3 h"(= N_super 5,520 에서 2 h)를
           **§AV 실측 지수 b=0.651**(T-BR-1 구조 12점, CI [0.500,0.801])로 옮기고,
           기기 경합은 §AV 실측(laptop 1.10x · laptop2 1.92x). **다른 계에서 옮긴 값입니다** —
           첫 파도 실측이 나오면 그것으로 갈아 주십시오(CLAUDE.md §5 규약 ㉴).

    기동(각 기기):
      cd ~/mof_project && git pull        # ← 러너가 안 돌 때만. 돌고 있으면 경로만 checkout (§6)
      cd 21_ZIF69_MTV
      COREWC_ASSIGN=<laptop|laptop2> COREWC_WORKERS=<8|12> \
        setsid nohup ~/miniconda3/envs/czeromof/bin/python -u run_core_wc.py \
        > ../.claude_work_corewc.out 2>&1 < /dev/null &

## 7. 안 바뀌는 것

우리 32조성 판정 · Q_st 목표대 · 1.5 규칙 순위 · §AV · D8 판정은 **이 시험으로 안 바뀝니다.**
바뀔 수 있는 것은 "우리 물질이 **작업 용량 축**에서 외부 모집단 어디에 있는가" 하나입니다.

## 8. 짝 — **설계로 남깁니다** (2026-09-23 01:2x 보강, laptop2 요청)

§AV 에서는 겹치는 짝이 **우연히**(제 오독으로) 생겼고 `NAtoms` **하위 2 %** 에 몰렸습니다.
§AW 는 **설계로** 남깁니다. 규칙과 한계는 `PAIRS_REGISTRATION_20260923.md` 에 **짝을 보기 전에** 박았습니다.

    선정   `N_super` 4분위 + `NAtoms` 4분위 → **8종**(중복 제거). **두 축으로 가릅니다** —
           §AV 의 구석이 `N_super` 는 중간대인데 `NAtoms` 는 하위 2 % 였습니다. 둘이 다릅니다.
           N_super 832~3,104 · NAtoms 104~388(백분위 15·22·26·35·39·64·89·99 %)
    배정   **두 기기 모두** 같은 8종을 돕니다(`pair: True`, `pair_axis` 로 어느 축에서 뽑혔는지 기록).
           laptop 36+8 = **44** · laptop2 36+8 = **44**
    비용   추가 laptop **+2.0 h** · laptop2 **+2.3 h**
    기록   결과 머리말의 `workers` 에 **양쪽 워커 수**가 남습니다 — 경합이 지수에 섞이는 것이
           후보 ③ 의 요지라 **안 적으면 나중에 그 항을 못 뺍니다**(laptop2).

## 9. 진행이 master 에 보이게 (같은 날 보강)

§AV 에서 작업 단위 진행이 `.claude_work_*.out` 에만 있었고 그 파일이 `RESULT_PATTERNS` **밖**이라,
**master 에서 보면 구조 하나에 20~40분씩 행이 안 늘어나는 구간**이 생겼습니다. 종합자가 그걸 "정지" 로
읽어 같은 일을 두 기기가 돌았습니다(`COREPOP_RESULT_20260923.md §1-1`).
→ `run_core_wc.py` 가 **결과 파일 머리말에 `progress`**(done/total/stage/last)를 씁니다.
결과 파일은 패턴 안이니 **master 에서 작업 단위로 보입니다.**
    [09-24 10:11] 완주(524/524, 실패 0) → 판정 `COREWC_VERDICT_20260924.md`(이의 창 09-24 18:1x): P1~P6 4/6 적중 · 짝 8종 적재 비 산포 최대 2.09 %.
