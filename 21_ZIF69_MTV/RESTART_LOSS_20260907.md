# ⚠️ **승인된 연장이 랩탑 단일 9건에는 못 걸립니다 — `Restart` 가 지워졌습니다** (09-07 12:0x)

**사용자 승인 `EXTEND_APPROVED_20260907.md` 를 집행하려다 막혔습니다.** 승인된 (가)는
*"재실행이 아니라 **사슬 끝에서 계속**"* 인데, **이어 붙일 자료가 없는 실행이 있습니다.**

## 1. 원인 — 설계된 삭제입니다

`run_water.py:263-268` 이 **완주한 작업마다** 지웁니다:

    for sub in ('VTK', 'Movies', 'Restart', 'CrashRestart'):
        shutil.rmtree(...)

주석의 이유는 옳습니다 — *"남겨 두면 다음 실행이 끝난 계산을 이어받으려 할 수 있다."*
**그런데 그 위험은 `CrashRestart` 에서 옵니다**(`ContinueAfterCrash` 가 그것을 읽습니다).
`Restart` 는 입력이 `RestartFile no` 인 한 **안 읽힙니다.** 둘을 같이 지운 것이 문제입니다.

## 2. 피해 범위 — 러너가 갈랐습니다

    **랩탑 단일 9건**(`water_runs_v3w/`, `run_water.py` 경로)   **Restart 이미 없음 -> 연장 불가**
      saIm025 · saIm050 · saIm0583 · saIm0583e1~e5 · saIm0625
    랩탑 진행 5건 (RH90 2 + 씨앗반복 3)                          완주 시 지워질 예정이었음
    **랩탑 사슬 2건**(`water_runs_v3w_chunk/`, chunked 러너)      **Restart 있음** -> 연장 가능
    **laptop2 7종**(chunked 러너)                                 **Restart 있음** -> 연장 가능
    데스크탑 RH90 3종 (`run_water.py` 경로)                       **같은 위험** — 확인 필요

**사슬 러너는 조각마다 `Restart` 를 남기므로 무사합니다.** 단일 러너만 잃습니다.

## 3. 조치

    ① **진행 중 5건의 Restart 를 떴습니다** — `restart_snapshots_20260907/` (228 MB, 읽기만 함)
       완주 전 상태라 마지막 배치가 아니지만 **이어 붙이기에는 유효한 배치**입니다
    ② `run_water.py` 를 고쳐 **`Restart` 는 남기고 `CrashRestart` 만 지웁니다.**
       이어받기 위험은 그대로 막히고, 앞으로의 실행은 연장 가능해집니다
    ③ **이미 잃은 9건은 못 되돌립니다.**

## 4. 승인된 설계와의 충돌 — **결정이 필요합니다**

`MORNING §2` 의 공통 조항이 *"**전 조성이 같은 조각 수**를 가져야 유지율(조성 간 비교)이
같은 엄격함에서 멈춘다"* 입니다. 그런데 지금:

    연장 가능   laptop2 7종 · 랩탑 사슬 2종(base·saIm050) · 랩탑 진행 5건(스냅숏)
    연장 불가   **랩탑 단일 9건**

**같은 조각 수를 줄 수가 없습니다.** 선택지:

    (ㄱ) 랩탑 9종을 **사슬로 다시 돌린다** — 승인된 (가)의 "재실행이 아니다" 를 깨고,
         8코어 2파도 ≈ 20 h. **연장분은 그 뒤에.**
    (ㄴ) **연장 가능한 것만** 연장하고 랩탑 9종은 **상한 인용**((나) 취급).
         조성 간 비교의 엄격함이 갈립니다 — §8 불균질을 새로 만듭니다
    (ㄷ) 랩탑 9종 중 **스냅숏이 있는 5건**만 살리고 나머지 4건은 (ㄴ). 반쪽

**저는 (ㄱ)을 권고하지 않습니다** — 20 h 를 쓰고도 연장을 그 뒤에 해야 하고, 승인문이
명시적으로 "재실행이 아니다" 라고 적은 것을 뒤집습니다. **(ㄴ)을 권고하되**, 그 경우
**"어느 조성이 연장됐고 어느 조성이 상한인지" 를 유지율 표에 열로 넣어야** 합니다.

> **이것은 제 실수의 결과이기도 합니다.** 단일 러너로 11종을 돌린 것은 09-06 등록
> (`WATER_FIX §4 ①`)대로였지만, **연장 가능성을 그때 아무도 안 봤습니다.**
> laptop2 가 사슬로 돌린 것이 결과적으로 옳았고, 그 이유는 **속도가 아니라 재개 가능성**
> 이었습니다. 사슬 러너의 존재 이유가 원래 그것이었습니다(`CHUNKED_PROTOCOL §6`).

---

## 5. ✅ **스냅숏 재개가 됩니다 — 실측** (12:4x)

종합자 ④의 경고("데스크탑 쪽은 2,969 B 껍데기였다")를 받아 직접 확인했습니다.

    **랩탑 스냅숏 5건은 껍데기가 아닙니다**
      saIm025(rep) 106,202 B · saIm050(rep) 134,296 · saIm0583(rep) 210,405
      saIm0667 278,960 · saIm075 205,581            (분자줄 258~685)
      대조: 사슬 chunk4 의 진짜 Restart 79,724 B / 192줄 — 같은 등급
      대조: 데스크탑 껍데기 **2,969 B**

    **재개 시험** — `saIm025`(rep) 스냅숏을 `RestartInitial/System_0/` 에 넣고
    `RestartFile yes` · 초기화 0 으로 띄웠더니 **사이클 0 에서**:

        Component 0 (CO2)   integer molecules **46**
        Component 1 (water) integer molecules **24**
        Number of Adsorbates **70**

    스냅숏 파일의 내용(3원자 분자 46 + 5원자 분자 24)과 **정확히 일치**합니다.
    **RASPA 가 배치를 그대로 실었습니다.**

### 5-1. ⚠️ 다만 **"완주분의 연장" 이 아니라 "중간 배치에서 갈라진 가지" 입니다**

스냅숏은 **실행 중**에 뜬 것이라 그 조성의 완주 배치와 다릅니다. 그러므로:

    랩탑 saIm0667 · saIm075 의 연장 사슬 = [스냅숏 배치] + N x 3,000
    유지율에 쓰는 15,000 단일 실행과는 **다른 가지**입니다

**물이 평탄해지는지 보는 목적에는 유효합니다** — 같은 앙상블의 유효한 배치에서
출발하기 때문입니다. 다만 판정문에 **"완주 배치의 연장" 이라고 적으면 틀립니다.**
**"중간 배치에서 갈라진 가지"** 로 적으십시오.

`_rep` 3건은 아직 도는 중이므로, **완주 시점에 고친 러너가 `Restart` 를 남기는지**
확인해서 남으면 그것을 쓰고(진짜 완주 배치), 아니면 스냅숏으로 갑니다.
⚠️ **그 드라이버도 옛 모듈을 물고 있으므로 남지 않을 공산이 큽니다.**
