# 무인 30시간 계획 — 2026-09-08 10:2x ~ 09-09 18:00 (Claude 한도 초과 대비)

**왜**: 주간 한도가 09-09 에 초기화되고 그 전에 세션이 끊길 수 있습니다. **이 문서는 Claude 없이도 계산이 이어지도록** 각 기기가
지금 걸어 둘 명령과, 사람이 붙었을 때 볼 것을 적습니다. 결정은 전부 이미 승인·등록된 것만 씁니다(`ASSIGN_20260907.md §G~§I`).
새 등록·새 가설은 없습니다. **한 줄 원칙: 세 기기가 같은 조각 수로 상한 4라운드까지 가고, 정지 라운드는 사후 분석으로 읽는다.**

## 0. 지금 도는 것 (10:2x)

    laptop2   7종 연장 라운드 1 (10:13, PID 415, `--rounds 1`)             → ~16시
    랩탑      사슬 2종 라운드 1 (10:18, PID 929943) + e6·e8 RH90→RH0 4작업    → 16~19시
    데스크탑  3종 라운드 1 (10:4x, chunk0 = 완주 배치)                       → ~16시

## 1. 무인 구간의 규칙 하나 — 풀 정지를 기기 하나가 판정할 수 없다

승인 설계(`EXTEND_APPROVED §2`)는 *"정지 = 전 조성(12종)이 만족할 때"* 인데, `extend_chain_v3w.py` 는 **자기 기기 목록만** 봅니다.
사람(종합자)이 없으면 라운드마다 세 기기의 판정을 모을 수 없습니다. 그래서 무인 구간에는:

    · 세 기기 모두 `--no-local-stop --rounds N` 으로 **같은 라운드 수**를 돕니다 (상한 4 는 도구가 그대로 걸어 줌)
    · "처음 정지를 충족한 라운드" 는 라운드마다 로그에 남으므로(`report()`), **사후에** 12종 표에서 풀 정지 라운드를 읽습니다
    · 비용: 정지가 일찍 왔다면 최대 3라운드(9,000 사이클)를 더 쓰는 것. 잃는 것은 코어 시간뿐, 자료는 전부 남습니다(상한 4 안)

이것은 새 결정이 아니라 승인 설계의 **무인 집행 방식**입니다(같은 조각 수 우선 — `ASSIGN §C·§G-1`). 종합자가 돌아오면 원 규칙으로 돌아갑니다.

## 2. 기기별 — 지금 걸어 둘 것 (라운드 1 이 끝나면 자동으로 이어지게)

세 기기 공통: 도는 드라이버를 **죽이지 말고**, 아래 "잇기" 스크립트를 `setsid nohup … &` 로 따로 띄웁니다. 스크립트는 라운드 1 드라이버가
끝나기를 기다렸다가(PID 감시) 라운드 2~4 를 겁니다. `pkill -f` 금지(CLAUDE.md §4).

### 데스크탑 (종합자가 10:23 에 걸어 둠 — `.claude_work_extend_desktop_r234.sh`)

    # ⚠ 아래 한 줄 예시는 PID 재사용에 취약(30 h 안에 번호가 재사용되면 조건이 계속 참 → 잇기가 영영 안 걸림). 실제 스크립트는 /proc/PID/cmdline 에 도구 이름이 있는지로 대기 — laptop2 chain_r234.sh · 데스크탑 v2 · 랩탑 931328 전부 그 형태.
    while [ -r /proc/<PID>/cmdline ] && tr "\0" " " < /proc/<PID>/cmdline | grep -q extend_chain_v3w; do sleep 300; done
    CHAIN_ROOT=water_runs_v3w_chain python extend_chain_v3w.py --rounds 3 --no-local-stop --names saIm0625 saIm0667 saIm075

### 랩탑 (Melchior 가 걸 것)

    while kill -0 929943 2>/dev/null; do sleep 300; done
    CHAIN_ROOT=water_runs_v3w_chunk python extend_chain_v3w.py --rounds 3 --no-local-stop --names base saIm050
    # e6·e8 는 별도 드라이버(928643)가 끝나면 `run_tmtv2w.py --stamp-only` 로 다시 찍고 태그 사본 확인(`run_water_v3w.py` 의 TARGETS 는 saIm 11종이라 sa50nb50e* 를 안 찍음 — 랩탑 지적)

### laptop2 (Balthasar 가 걸 것)

    while kill -0 415 2>/dev/null; do sleep 300; done
    python extend_chain_v3w.py --rounds 3 --no-local-stop        # 7종 기본 목록, 기본 뿌리

세 기기 다 도구는 **master `f558140` 병합 이후 판**(`80f902e` 판은 배너 NameError 로 모든 호출에서 즉시 죽음 — laptop2 발견) — **잇기를 걸기 전에 `--dry-run --no-local-stop` 을 한 번 친다. `--help`·grep 통과는 판 확인이 아니다.** 잇기 대기는 PID 번호가 아니라 `/proc/PID/cmdline` 에 도구 이름이 있는지로(번호 재사용 대비, laptop2 `chain_r234.sh`).(`--no-local-stop` 이 있는 판)이어야 합니다 — `git fetch` 뒤 `git show origin/master:21_ZIF69_MTV/extend_chain_v3w.py > 21_ZIF69_MTV/extend_chain_v3w.py`(pull 금지, COMMS ②). 도는 프로세스는 옛 판을 물고 있어도 됩니다(다음 호출부터).

## 3. 사람이 붙었을 때 볼 것 (순서대로, 5분)

    bash bgstate.sh                                  # 코어·디스크(C: 는 반드시 df /mnt/c)·로그 마지막 줄
    tail -3 21_ZIF69_MTV/extend_desktop.log          # 데스크탑 라운드 진행. "라운드 N 벽시계" 줄이 라운드 종료
    python 21_ZIF69_MTV/check_ff_per_run.py 21_ZIF69_MTV/water_runs_v3w_chain    # 새 조각의 힘장 관문(착수 직후에도 답함)
    df -h /mnt/c                                     # 5 GB 아래면 새 계산 금지 (auto68 의 disk_guard 와 같은 문턱)

죽어 있으면: 같은 명령을 다시 치면 됩니다 — `extend_chain_v3w.py` 는 완주한 조각을 보고 다음 조각부터 잇습니다. **단, 끊긴 조각 폴더
(Simulation finished 0건)는 지우고** 다시 치십시오(CLAUDE.md §3 — 이어받기는 파일 존재만 봅니다). Zeo++ 는 RASPA 가 돌 때 절대 안 띄움.

## 4. 09-09 18:00 까지 끝나 있어야 할 것 / 사람이 할 판정

    끝나 있을 것   12종 × 조각 5+4(상한) — 각 라운드의 Δ40·폭·남는 물 로그. e6·e8 RH90/RH0 (n=7 배치 단위 재기).
    사람이 할 것   ① 12종 표에서 풀 정지 라운드 읽기(처음 정지 충족 라운드 열 + 풀 규칙) → 그 라운드의 마지막 15,000 창으로 유지율 표
                   ② 표의 열: 유지율 · 습윤 절대량 · 남는 물 · 조각 수 · 라운드 0 지위(정지/계속) · 처음 정지 라운드 · 연장 종류(완주 배치/사슬)
                   ③ e6·e8 로 혼합 배치 단위 갱신(§H, LCD 조건부 병기) → `CAND_VERDICT §6` 의 자 갱신
                   ④ base 폭 1.8배 미결(§I) — 랩탑 UnitCells 회신 뒤 "2.2σ 미만" 재계산
    하지 않을 것   T-MTV-A·2·B′(9/20 뒤), 새 조성, MAGI. 문턱·자·판정 규칙 변경 없음.

## 5. 고꾸라짐 신호가 무인 구간에 나오면 (사람이 판단, 계산은 준비돼 있음)

`ASSIGN §G-4` 그대로. (ㄴ) 는 e6·e8 결과로 k 를 다시 재고, 필요하면 GCMC 2작업(e6·e8 는 이미 그 자체가 그 작업). (ㄱ)·(ㄷ) 는 추가 계산 0.
