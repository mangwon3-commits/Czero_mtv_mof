# RASPA 난수 시드가 고정돼 있지 않습니다 (2026-08-28)

> # 🔴 2절의 크기 추정이 실측으로 **반증**됐습니다
>
> 이 문서는 실행간 SD 를 `≈ 2.69 × ±` (블록 σ 의 7.5배)로 추정했습니다.
> Junseok 이 **같은 구조·같은 조건·다른 시드로 4회** 재실행해 직접 쟀습니다:
>
> ```
> 추정   실행간 SD ≈ 2.69 × ±  = 0.0363     (블록σ의 7.5배)
> 실측   R̂ = **0.008941**       = **0.66 × ±**  (블록σ의 **1.84배**)
> ```
>
> **방향이 반대입니다 — `±` 는 실행 잡음을 못 담는 것이 아니라 오히려
> 넉넉히 덮습니다.** 4·5절의 결론(판정은 서고 이름이 틀렸다)도 뒤집힙니다:
> **자리 산포가 실재하고 `1a38f39` 가 유지됩니다.** 8절에 정리했습니다.
>
> **1·3절(시드가 작업 단위로 다르다)은 그대로 유효합니다.** 틀린 것은
> 그 잡음의 **크기 추정**뿐입니다.

**발견 경로:** Junseok 이 보낸 `saIm0583` 앙상블 건조 로딩이 저장소의
`results_v3ens0583.json` 과 갈렸고(χ²=36.09, dof=5, **p<0.0001**), 구조 해시와
실행 조건이 전부 일치해 남은 설명이 실행 잡음뿐이었습니다.

## 1. 사실

**러너 어디에도 `RandomSeed` 가 없습니다.** `21_ZIF69_MTV/*.py` 전수 확인,
생성된 `simulation.input` 에도 0줄. RASPA 가 기동 시각으로 시드를 만듭니다.

**그리고 시드는 배치가 아니라 작업 단위입니다** — 같은 배치 안에서 넷을
재 보니 전부 **작업 폴더 생성 + 3초**였습니다:

```
rh15_e1   1787823853 = 08-27 18:44:13    폴더 18:44:10    +3초
rh05_e2   1787845513 = 08-28 00:45:13    폴더 00:45:10    +3초
rh10_e3   1787847722 = 08-28 01:22:02    폴더 01:21:59    +3초
rh15_e5   1787882839 = 08-28 11:07:19    폴더 11:07:16    +3초
```

> Junseok 은 *"v3ens 배치 전부 1787540797"* 로 보고했는데 **이 기기에서는
> 한 배치 안에서 다릅니다.** 이 차이가 결론의 방향을 바꿉니다(3절).

## 2. `±` 가 못 담는 것

RASPA 의 `±` 는 **한 궤적 안의 블록 평균 다섯 개**의 95% 신뢰구간입니다
(`ERROR_BARS.md`). **다른 시드로 다시 돌렸을 때의 흩어짐이 아닙니다.**

Junseok 의 표와 우리 자료 사이의 χ² 에서 크기가 나옵니다:

```
χ²/dof = 36.09/5 = 7.216      실행간 SD ≈ √7.216 × (보고된 ±) = **2.69 × ±**
```

`±` 를 진짜 σ 로 환산하면(÷2.776) **실행간 SD ≈ 7.5 × 블록 σ** 입니다.

## 3. 방향 — 작업 단위 시드라서 **부풀리는 쪽**입니다

```
시드가 배치 공통이면    실행 잡음이 실현들의 **공통 성분**  ->  차분에서 상쇄
                        -> 배치 SD 가 자리 산포를 제대로 잼
시드가 작업마다면       실행 잡음이 실현 간 **독립**       ->  배치 SD 를 부풀림
                        -> "배치 산포" 에 실행 잡음이 섞여 있음
```

**측정된 것은 후자입니다.**

## 4. 무엇이 서고 무엇이 틀렸나

### ✅ 판정은 그대로 섭니다

우리가 묻는 것은 *"새 실현을 **한 번** 돌리면 얼마나 다를까"* 이고,
**거기엔 실행 잡음이 포함되는 것이 맞습니다.** 분모가 옳은 양을 재고
있습니다. 습윤 WC·저압 물·유지율 판정 전부 영향 없습니다.

### ❌ 이름이 틀렸습니다

커밋 `1a38f39` **"Placement variance is real: 7.2x the statistical error on
dry loading"** — 그 7.2배 안에 실행 잡음이 들어 있습니다. **자리 배치만의
효과가 아닙니다.**

(덧붙여 그 `7.2배` 는 `±` 기준입니다. 진짜 σ 로는 **16.1배**입니다 —
`ERROR_BARS.md` 가 이미 잡아 둔 단위 문제와 같은 자리입니다.)

## 5. 분해 — `R` 이 측정되면 갈립니다

```
앙상블 건조 로딩   배치 SD **0.0781**   평균 1.1623   상대 6.7%
                   배치SD² = 자리² + R²
```

| 실행간 SD `R` | 자리 SD | 자리가 분산에서 |
|---|---|---|
| 0.035 (= 2.69×±) | 0.070 | **80%** |
| 0.050 | 0.060 | 59% |
| 0.078 | 0 | **0%** |

**`R` 이 0.078 을 넘지 않는 한 자리 산포는 실재합니다.** 크기만 줄어듭니다.

⚠️ **Junseok 이 재는 재실행은 구조 2개 × 1회 = dof 2 입니다.** `R` 의
95%CI 가 약 `[0.5R, 2.2R]` 로 벌어져, `R=0.035` 가 나와도 상한 0.077 이라
**위 표의 마지막 줄(자리 산포 0)을 못 배제합니다.** 결과를 "측정됐다" 가
아니라 **구간**으로 적어야 합니다. 같은 비용이면 **한 구조를 3~4회**가
두 구조를 1회씩보다 `R` 추정에 낫습니다.

## 6. 앞으로

**이미 돈 계산은 다시 안 돕니다.** 시드를 고정하면 기존 결과와 규약이
갈리고(`CLAUDE.md` §3), 판정도 안 바뀝니다(4절).

바꾸려면 **사용자 결정**이 필요하고, 바꾼다면 두 갈래입니다:

| | 뜻 |
|---|---|
| 시드 고정 | 재현 가능해지지만 **실행 잡음을 못 보게 됩니다.** 한 궤적에만 의존 |
| 시드 자유 + `R` 측정 | 지금 그대로 두고 `R` 을 따로 재서 분해에 씁니다 |

**후자를 권합니다.** 시드를 고정하면 `±` 가 여전히 실행 잡음을 못 담는데
그 사실이 더 안 보이게 됩니다.

## 7. 같은 날 같은 형태가 하나 더

Junseok 보고: `lammps-interface` 가 `PYTHONHASHSEED` 미고정이라
`angle_style` 순서가 프로세스마다 뒤집히고, 그것만으로 `base` LCD 가
**7.61978 ↔ 7.63144** 로 갈립니다. **어제 "LAMMPS 빌드 차이" 로 결론 낸
것(`d73c805`)의 정체가 이것입니다.**

**둘 다 "기계가 다르다" 로 잘못 귀속됐던 것**이 공통점입니다. 고정 안 된
시드는 기기 차이로 위장합니다.

---

# 8. 실측 (Junseok, 2026-08-28 19:xx) — 2·4·5절을 대체합니다

## 8-1. `R̂` 실측

`saIm0583e1`, 같은 조건, 다른 시드, `n=4`:

```
1.202428 (08-24)   1.190660   1.186060   1.181659
R̂ = **0.008941**   (dof 3)

R̂ / ±       = **0.66배**    <- ± 가 실행 잡음을 **과대**평가
R̂ / 블록σ   = **1.84배**    <- 블록 오차막대가 실행 잡음과 같은 자릿수
```

## 8-2. 사전 등록한 F 검정 — **자리 산포 실재**

랩탑이 결과 전에 등록한 기준(`R̂ < 0.02587`)을 그대로 적용합니다:

```
F = s_batch²/R̂² = 0.0781²/0.008941² = **76.30**    기각역 F(0.95,4,3) = 9.117
p = **0.0024**

자리 SD = sqrt(0.0781² − 0.008941²) = **0.0776**   자리가 분산의 **98.7%**
```

**`1a38f39` "Placement variance is real" 이 섭니다.** 실측이 등록 문턱의
**0.35배**로, 경계에서 멀리 떨어진 쪽입니다.

⚠️ **다만 `R̂` 의 95%CI 가 `[0.0051, 0.0333]` 이고 문턱 0.02587 이 그 안에
있습니다**(dof 3). **결과 전에 적어 둔 검정력 경고 그대로입니다** — 판정은
섰지만 `n=4` 한 번으로 확정된 것이 아닙니다.

## 8-5. `n=8` — 확정 (2026-08-29, Junseok `41fb1b7`)

```
1.202428(08-24)  1.190660  1.186060  1.181659  1.177334  1.181873  1.176957  1.181008
시드 8개 전부 다름

**R = 0.008424**  (dof 7)      n=4 의 0.008941 에서 거의 안 움직임
R / 통계 1σ = **1.18배**       시드 미고정이 만드는 추가 흩어짐은 통계오차의 18%
```

### 사전 등록 F 검정 — **실재, 그리고 안정**

```
              임계        R 문턱      실측 F     p
dof 3 (n=4)   9.117      0.02587     85.96    2.0e-03    문턱이 CI 안 -> 불안정
dof 7 (n=8)   **4.120**  **0.03848** 85.96    **5.0e-06** 문턱이 CI 밖 -> **안정**

R 95%CI [0.00557, 0.01714]      자리 SD **0.0776**   자리가 분산의 **98.8%**
```

> ⚠️ **등록 문턱 `0.02587` 은 `dof 3` 짜리입니다.** `n` 이 바뀌면 임계 F 가
> 바뀌어 문턱도 움직입니다. **문턱만 떼어 인용하지 마십시오.** 실측
> `0.008424` 는 두 문턱 모두 아래라 판정은 어느 쪽으로도 같습니다.

**결과 전에 적어 둔 검정력 경고가 `n=8` 로 해소됐습니다.**
`1a38f39` "Placement variance is real" 이 **확정**됩니다 — 8-4 의 단위 정정
(`±` 기준 7.2배 → 진짜σ 기준 16.0배)만 적용하면 됩니다.

## 8-6. 기기 간 차이 — **08-24 한 점이었습니다**

```
Junseok 08-24 1차   1.202428    오늘 7회 평균 대비  **+4.19 SD**
Junseok 오늘 7회     1.182222 ± 0.004821
**랩탑**            1.175005    오늘 7회 평균 대비  **−1.50 SD**
```

**바깥에 있는 것은 랩탑이 아니라 Junseok 자신의 08-24 입니다.**
랩탑 값은 Junseok 의 현재 분포 안에 들어갑니다.

`e1` 의 Junseok 값을 오늘 평균으로 바꾸면:

| `e1` 을 무엇으로 | 차 | z | χ²(5) | p |
|---|---|---|---|---|
| 08-24 1차 | +0.02740 | +2.30 | 11.38 | **0.044** |
| **오늘 평균** | +0.00722 | +0.61 | **6.45** | **0.265** |

**기기 차이가 사라집니다.**

> **08-24 를 못 믿겠다면 분모에서만 빼고 분자에 남길 수 없습니다.**
> `R` 만 1차 제외로 내리면 `χ²` 가 34.7 로 뛰는데, 그것은 같은 점을
> **한쪽 항에서만** 처리한 결과입니다. 둘 다에서 빼야 6.45 입니다.

⚠️ **`e5` 는 남습니다.** 차 +0.0250, `z = +2.10`, 단독 양측 `p = 0.036`.
합쳐서는 통과하지만 **그 한 점은 경계**입니다. `e5` 재실행 3회면 닫힙니다.
**지금 판정 중 여기 걸린 것은 없습니다.**

## 8-3. 남는 것 — 08-24 한 점이 `R̂` 를 만듭니다

```
1차(08-24)   1.202428
오늘 3회      평균 1.186126   SD **0.004501**
1차는 오늘 평균보다 +0.0163  =  오늘 SD 의 **3.6배**
```

**오늘 세 번끼리는 `0.0045` 로 훨씬 촘촘합니다.** 08-24 와 오늘 사이에
무언가 바뀌었을 수 있고, 지금 자료로는 못 가릅니다. `R = 0.0045` 로 재도
등록 문턱 아래라 **판정은 안 바뀝니다.**

## 8-4. 그래도 남는 교정 — "7.2배" 표기

`1a38f39` 의 **"7.2x the statistical error"** 는 여전히 고쳐야 합니다.
자리 SD 0.0776 기준이고, `±` 가 아니라 **진짜 σ 단위**로 적어야 합니다:

```
자리 SD 0.0776  /  블록 진짜σ 0.0048  =  **16.0배**
```

**이름은 맞았고 단위가 틀렸습니다.** `ERROR_BARS.md` 가 이미 잡아 둔 자리와
같습니다.
