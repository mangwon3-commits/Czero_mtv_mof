# 물 힘장 수정과 (라) 재계산 — 등록 (2026-09-06 10:5x, 종합자, 사용자 결정 "모두 권고대로")

**결정**: `MORNING_20260905.md §1` 의 **(라) 고치고 필요한 것만.** 포스터 문구는 **상대 주장**("습윤에서도 버틴다" 류)으로 제한.
**바뀌는 고정값**: CLAUDE.md §1 물 모델 행 — `UFF_MOF/force_field_mixing_rules.def` 에 `Hw none` / `Lw none` (항 수 55→57). 다른 §1 값은 불변.

## 1. 수정 — 기기마다 두 파일

    저장소 원본   00_Migration/raspa_share/raspa/forcefield/UFF_MOF/force_field_mixing_rules.def   (데스크탑 커밋 완료)
    실물          $RASPA_DIR/share/raspa/forcefield/UFF_MOF/force_field_mixing_rules.def          (기기마다 따로!)
                  데스크탑 ~/RASPA/simulations/… 수정 완료 · 랩탑 /home/skyjun/RASPA/simulations/… · laptop2 각자

    편집   6행 `55` → `57`.  62행 `Xe … 221.0 4.1` **다음**, 63행 `# general mixing rule for Lennard-Jones` **앞**에
             Hw             none
             Lw             none
    금지   파일 끝(`Lorentz-Berthelot` 뒤)에 붙이지 말 것 — RASPA 가 항으로 읽어 조용히 무시(랩탑 09-05 실증). 항 수를 안 올려도 같음.
    형태   `none`(배포본 Dubbeldam2007FlexibleIRMOF-16 42~43행과 동일). `Hw lennard-jones 0 0` 은 에너지는 같으나 쓰지 않음(텍스트 통일).
    검증   ① md5 == **8e8ec933f9013c7e932da04dc256efd3** (대조 (다) 의 ffroot 판과 글자 단위 동일; 저장소 원본 == 실물)
           ② `python ff_type_audit.py` → `OK Hw 정확 일치 · OK Lw 정확 일치`, 경고 0 (데스크탑 확인 완료)
           ③ 새 실행의 출력 머리말: `Hw - Hw [ZERO_POTENTIAL]`, `Ow - Hw [ZERO_POTENTIAL]`, `Ow - Ow 89.633 / 3.097` — 러너가 읽어 아니면 실패 처리
    검증된 축   `Hw none` 형태: 골격 안 Widom 축(D = 8.63) · **[11:1x 닫힘]** 랩탑 `saIm050` RH90 첫 실행 머리말에서 Ow-Hw·Ow-Lw·Hw-Hw·Hw-Lw·Lw-Lw 전부 ZERO_POTENTIAL,
                Ow-Ow 89.633/3.097 그대로 → `none` 형태가 **골격 안 GCMC 축에서도 확인**. (`Hw ε=0` 형태는 순수 물 상자 축, 에너지 동일.)
    ③ 의 함정   RASPA 는 쌍 이름을 오른쪽 정렬로 찍는다(`     Hw -      Hw [ZERO_POTENTIAL]`) — `'Hw - Hw'` 문자열 grep 은 **고친 판에서도 실패**. 정규식으로 쌍 이름을 읽을 것.
                **`22.14170` 을 grep 하지 말 것** — 골격 수소 `H_` 의 정당한 UFF 값이라 고친 판에도 1만 번 넘게 나온다. 결함은 그 수의 존재가 아니라 `Hw` 쌍이 그 값을 갖는 것.

## 2. 새 계열 이름 — 결함판을 절대 이어받지 않는다 (CLAUDE.md §3)

    러너의 cached 판정(run_water.py:181~184, main():283~)은 힘장을 안 본다 → **HERE 와 RUNS 둘 다 새 경로.**
    RH90 유지율   래퍼 `run_water_v3w.py`: rw.HERE = `v3w_water/` (water_results.json + water_results_<기기>.json), rw.RUNS = `water_runs_v3w/`,
                  rw.CHARGED·WATER_DEF 재지정, rw.RH_LIST = [0.90], rw.TARGETS = §3 목록. laptop2 는 `run_water_chunked.py` 를 같은 rw 로.
    T-B2 K_H      RUNS `tb2w_runs/`, OUT `v3w_water_kh/water_kh_{A,B,EXT}_<tag>.json`
    밀도 격자     OUT `density_water_v3w/`, RUNS `water_runs_density_v3w/` (선택, §3 ③)
    산출물 표기   **행마다** `forcefield`/`ff_check` 키 + 사이드카 (랩탑 판단, 11:1x 승인: `water_results.json` 은 맨 리스트라 최상단 키를 넣으면 판독기 열 곳이 깨짐;
                  행에 찍으면 행이 다른 파일로 복사돼도 표기가 따라감). 값: `UFF_MOF+HwLw_none_20260906`, `HwHw: ZERO_POTENTIAL`, `OwOw_eps: 89.633`.
    scope_report  PATS 에 `v3w` 줄을 v3 앞에 추가(안 하면 v3 로 잡힘)
    RH0 행        무관(물 분자 0) — 옛 값 재사용. 유지율 분모.

## 3. (라) 의 범위 — 등록

    ① RH90 유지율 분자 재계산 — **필수.** 대상 = RH90 행이 있는 v3 조성 전부 + 혼합 1:
       base · saIm025 · saIm050 · saIm0583 · saIm0583e1~e5 · saIm0625 · saIm0667 · saIm075 · saIm0875 · saIm0917 · saIm0958 · saIm100 · mslm050 · **sa50nb50**
       = **18작업** (RH90 만). 유지율 = CO₂(RH90, 새)/CO₂(RH0, 옛). 판정: 조성별 유지율을 옛 값과 나란히 적고, 방향("좋아진다")은 결과가 말한다.
       ⚠ 옛 값에 D 를 곱해 보정하지 않는다(TB2_VERDICT_C1 §6-2). 문턱 80/50 선과 1.5 규칙(자는 비교 종류대로)은 기존 등록 그대로.
    ② T-B2 물 K_H 재측정 — **필수(싸다).** 32종 + 확장 7종 + base 검증 = 약 40 Widom. ② Spearman 용, X4(물 K_H) 채움. 대조 4건 재실행 불필요.
       러너 `run_tb2_water_kh.py`/`run_tb2_ext.py` 의 고침 8건(`TB2_WATER_KH_FIXES_20260906.md`: ase 관문·PrintEvery·성분별 파싱·ALL 단일기기·힘장 출처·Hw-Hw 확인·5자리 확인·EXT 대조 분리) **전부 승인** — 새 계열이라 §3 함정 없음.
    ③ T-4·T-6 물 밀도 격자(base·nbIm025·saIm050, RH90) — **선택.** 포스터에 "물이 치환기에 안 앉는다" 를 쓸 때만. 단일 복셀 자리 주장은 어차피 금지(DENSITY_GRID_TWO_GENERATIONS).
       **[09-06 23:1x 사용자 지시 "띄워 당장 실행" → 착수]** 데스크탑, 프로세스 셋(2초 어긋내기), `run_density_water_v3w.py`, OUT `density_water_v3w/<조성>/`(JSON 충돌 방지, 병합은 종합자), RUNS `water_runs_density_v3w/`. RH90 3종과 함께 simulate 6건.
    범위 밖   저RH 등온선 101작업 · 습윤 WC 75 GCMC · RH0 50작업(무관). **"습윤에서도 버틴다" 의 근거는 ① 의 RH90 유지율이지 습윤 WC 앙상블(T-1)이 아니다** —
              포스터가 T-1 의 0.7306 을 쓰면 그것은 (라) 가 안 덮는다. 문구를 유지율로 고정한다.

## 4. 배정 — 예상 소요로 나눈다 (`COST_REFERENCE.md` §2b: 설폰산이 base 의 2배, 동시성 축 1.07)

    랩탑(Melchior)   ① 중 saIm 계열 11종(saIm025·050·0583+e1~e5·0625·0667·075) — 8워커, 건당 ≈17.5 h → 2파도 ≈ 35 h
                     + 힘장 수정·래퍼 작성·§1 검증 ③. 물 축 기기.
    laptop2(Balthasar) ① 중 base · saIm0875 · saIm0917 · saIm0958 · saIm100 · mslm050 · sa50nb50 = 7종 — 3~6병렬, chunked 러너 → ≈ 30~36 h
                     + 옛 물 JSON 71개에 `WARN_forcefield` 사이드카 표기(annotate 리스트형 변형, 측정값 불변).
    데스크탑          nbIm050e1~5 체인(`ASSIGN_NB050_20260906.md`) 뒤 ② T-B2 40 Widom (≈5~8 h). ③ 은 미배정.
    시작 조건        각 기기가 §1 검증 ①② 를 마치고 **첫 실행의 출력 머리말(③)** 을 확인한 뒤 나머지를 잇는다 — ③ 은 착수 수 분 안에 끝나므로 완주를 기다리지 않는다
                     (11:1x 문언 정정: "첫 완주" 로 적었던 것은 목적(힘장 적용 확인)보다 넓었다. 랩탑 지적. 나머지 7코어 × 17.5 h 유휴를 막음).

## 5. 옛 결과의 지위

    결함판(수소결합 없는 물)   v3_water*/water_results*.json 57파일의 RH>0 행 · v3_humid_wc*/·v4_humid_wc/ · v3_water_kh/*.json · density_water_v3/ · T-B1 현행
    표기                       `WARN_forcefield`(현재 4파일) → laptop2 가 사이드카로 71파일 확장. 측정값은 안 건드린다. 인용 시 "수소결합이 없는 물로 계산한 값" 병기.
    노션·포스터               RH90 유지율·습윤 WC 는 새 계열이 나올 때까지 인용 불가(11-F). 건조 축은 무관.

## 6. 이 등록이 못 하는 것

    유지율이 좋아질지 나빠질지는 결과가 말한다(MORNING §1-2-2 "순위가 바뀔 수 있다"). 습윤 WC 절대값은 이 범위 밖.
    D = 8.63 은 base n=1 — 조성별 보정 계수로 쓰지 않는다.

## 7. 결과 도착 (수정 힘장 v3w) — 분모는 `RH90_DENOMINATORS_20260906.md` §1·§5·§6

    조성      기기      완주        CO₂(RH90, 새)      물(RH90, 새)        분모(RH0, v3)   **유지율(새)**   옛 결함판   단서
    base      laptop2   17:2x 6.13h  0.5838 ± 0.0139    0.1845 ± 0.0581     0.5839          **100.0 %**      98.4 %      ⚠ 물 조각4 0.2652 = 조각0~3 대비 +7σ 이상치.
                                                                                                                              조각4 만이면 CO₂ 0.5658 → 96.9 %. 평형 부족인지 잡음인지는 다른 조성의 조각별 물을 본 뒤(Balthasar 유보).
                                                                                                                              물은 옛 0.4893 의 **0.38배** — D≈8.6(영피복)보다 온건, 유한 피복이라 예상 범위.
    (규칙)    러너의 "사슬 연속성" 검사는 CO₂ 만 본다 — **물 성분의 조각별 이상치도 함께 보고**한다(§9 대기 규칙과 무관한 보고 규약). 인용 시 단서 병기.
    (해석)    유지율이 오르고 물이 준 방향은 MORNING §1-2-2 의 추론과 같다. **조성 간 순위는 재계산 전량이 나온 뒤에만.**
    (T-4·T-6) 물이 0.38배면 밀도 격자 통계가 얇아진다 — §3 ③ 을 돌릴지는 사용자 결정(포스터에 그 주장을 쓸 때만).
    T-B2w     데스크탑  23:00 5h21m  **39/39 완주**(6워커 Widom, 8.2 분/작업 실효). `v3w_water_kh/water_kh_ALLw_hkhome.json`, 행마다 ff_check 재판독(fix_tb2w_ffcheck.py) 전부 ok.
              옛/새 K_H 비: **중앙값 8.3, 범위 1.7(saIm0875)~14.4(nbIm025)** — base 7.99(대조 (다) 8.63 과 같은 대역; base 새 7.13e-5 대 대조 6.25e-5 는 Widom 통계 폭).
              **비가 조성마다 1.7~14 로 다르다 → D 를 보정 계수로 못 쓴다는 등록(TB2_VERDICT_C1 §6-2)이 실측으로 확인됨.**
              T-B2 ①: 새 K_H 최소 1.8e-5(mbIm025) > 문턱 5e-6 → **전량 문턱 위, 판별력 없음 그대로**(정의역 D<41 안). ② Spearman 은 v3w 유지율이 다 나온 뒤.
    (이관 착오) 23:03 데스크탑에 saIm0625·0667·075 를 띄웠으나 **랩탑 큐는 파도가 아니라 연속 큐라 셋이 이미 거기서 돌고 있었다**(18:24·21:52·22:46 시작).
              laptop2 7종도 11:13 부터 전부 병렬. → 데스크탑의 셋은 죽이지 않고 남긴다. **[정정, Melchior]** `run_water.py` 는 씨앗을 안 적어 RASPA 기본값(시각)이 되므로
              두 판은 **같은 구조·같은 설정, 씨앗과 기기가 함께 다른 한 쌍** — "기기 간 재현성" 이라 부를 수 없다(씨앗 효과와 기기 효과를 못 가름).
              저장소 씨앗 재현성 3.0 %(TB2_CRITERION_NOTE §85)와 견주는 **상한**으로만 쓴다: 차이가 그 안이면 "씨앗+기기 합쳐도 3 % 안", 벗어나면 그때 질문이 생긴다.
              배치 산포에는 넣지 않는다. 정본은 랩탑 판. **러너 결함 기록**: 산출물에 씨앗이 없다 → 재계산 뒤 출력 머리말의 `Random number seed` 를 행에 기록하도록(ff_check 자리). 지금 실행은 안 건드림.

