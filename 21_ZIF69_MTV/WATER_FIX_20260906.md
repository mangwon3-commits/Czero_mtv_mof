# 물 힘장 수정과 (라) 재계산 — 등록 (2026-09-06 10:5x, 종합자, 사용자 결정 "모두 권고대로")

**결정**: `MORNING_20260905.md §1` 의 **(라) 고치고 필요한 것만.** 포스터 문구는 **상대 주장**("습윤에서도 버틴다" 류)으로 제한.
**바뀌는 고정값**: CLAUDE.md §1 물 모델 행 — `UFF_MOF/force_field_mixing_rules.def` 에 `Hw none` / `Lw none` (항 수 55→57). 다른 §1 값은 불변.

## 1. 수정 — 기기마다 두 파일

    저장소 원본   00_Migration/raspa_share/raspa/forcefield/UFF_MOF/force_field_mixing_rules.def   (데스크탑 커밋 완료)
    실물          $RASPA_DIR/share/raspa/forcefield/UFF_MOF/force_field_mixing_rules.def          (기기마다 따로!)
                  데스크탑 ~/RASPA/simulations/… 수정 완료 · 랩탑 /home/skyjun/RASPA/simulations/… · laptop2 각자

    편집   6행 `55` → `57`.  62행 `Xe … 221.0 4.1` **다음**, 63행 `# general mixing rule for Lennard-Jones` **앞**에
             Hw             none
             Lw             none
    금지   파일 끝(`Lorentz-Berthelot` 뒤)에 붙이지 말 것 — RASPA 가 항으로 읽어 조용히 무시(랩탑 09-05 실증). 항 수를 안 올려도 같음.
    형태   `none`(배포본 Dubbeldam2007FlexibleIRMOF-16 42~43행과 동일). `Hw lennard-jones 0 0` 은 에너지는 같으나 쓰지 않음(텍스트 통일).
    검증   ① md5 == **8e8ec933f9013c7e932da04dc256efd3** (대조 (다) 의 ffroot 판과 글자 단위 동일; 저장소 원본 == 실물)
           ② `python ff_type_audit.py` → `OK Hw 정확 일치 · OK Lw 정확 일치`, 경고 0 (데스크탑 확인 완료)
           ③ 새 실행의 출력 머리말: `Hw - Hw [ZERO_POTENTIAL]`, `Ow - Hw [ZERO_POTENTIAL]`, `Ow - Ow 89.633 / 3.097` — 러너가 읽어 아니면 실패 처리
    검증된 축   `Hw none` 형태: 골격 안 Widom 축(D = 8.63) · **[11:1x 닫힘]** 랩탑 `saIm050` RH90 첫 실행 머리말에서 Ow-Hw·Ow-Lw·Hw-Hw·Hw-Lw·Lw-Lw 전부 ZERO_POTENTIAL,
                Ow-Ow 89.633/3.097 그대로 → `none` 형태가 **골격 안 GCMC 축에서도 확인**. (`Hw ε=0` 형태는 순수 물 상자 축, 에너지 동일.)
    ③ 의 함정   RASPA 는 쌍 이름을 오른쪽 정렬로 찍는다(`     Hw -      Hw [ZERO_POTENTIAL]`) — `'Hw - Hw'` 문자열 grep 은 **고친 판에서도 실패**. 정규식으로 쌍 이름을 읽을 것.
                **`22.14170` 을 grep 하지 말 것** — 골격 수소 `H_` 의 정당한 UFF 값이라 고친 판에도 1만 번 넘게 나온다. 결함은 그 수의 존재가 아니라 `Hw` 쌍이 그 값을 갖는 것.

## 2. 새 계열 이름 — 결함판을 절대 이어받지 않는다 (CLAUDE.md §3)

    러너의 cached 판정(run_water.py:181~184, main():283~)은 힘장을 안 본다 → **HERE 와 RUNS 둘 다 새 경로.**
    RH90 유지율   래퍼 `run_water_v3w.py`: rw.HERE = `v3w_water/` (water_results.json + water_results_<기기>.json), rw.RUNS = `water_runs_v3w/`,
                  rw.CHARGED·WATER_DEF 재지정, rw.RH_LIST = [0.90], rw.TARGETS = §3 목록. laptop2 는 `run_water_chunked.py` 를 같은 rw 로.
    T-B2 K_H      RUNS `tb2w_runs/`, OUT `v3w_water_kh/water_kh_{A,B,EXT}_<tag>.json`
    밀도 격자     OUT `density_water_v3w/`, RUNS `water_runs_density_v3w/` (선택, §3 ③)
    산출물 표기   **행마다** `forcefield`/`ff_check` 키 + 사이드카 (랩탑 판단, 11:1x 승인: `water_results.json` 은 맨 리스트라 최상단 키를 넣으면 판독기 열 곳이 깨짐;
                  행에 찍으면 행이 다른 파일로 복사돼도 표기가 따라감). 값: `UFF_MOF+HwLw_none_20260906`, `HwHw: ZERO_POTENTIAL`, `OwOw_eps: 89.633`.
    scope_report  PATS 에 `v3w` 줄을 v3 앞에 추가(안 하면 v3 로 잡힘)
    RH0 행        무관(물 분자 0) — 옛 값 재사용. 유지율 분모.

## 3. (라) 의 범위 — 등록

    ① RH90 유지율 분자 재계산 — **필수.** 대상 = RH90 행이 있는 v3 조성 전부 + 혼합 1:
       base · saIm025 · saIm050 · saIm0583 · saIm0583e1~e5 · saIm0625 · saIm0667 · saIm075 · saIm0875 · saIm0917 · saIm0958 · saIm100 · mslm050 · **sa50nb50**
       = **18작업** (RH90 만). 유지율 = CO₂(RH90, 새)/CO₂(RH0, 옛). 판정: 조성별 유지율을 옛 값과 나란히 적고, 방향("좋아진다")은 결과가 말한다.
       ⚠ 옛 값에 D 를 곱해 보정하지 않는다(TB2_VERDICT_C1 §6-2). 문턱 80/50 선과 1.5 규칙(자는 비교 종류대로)은 기존 등록 그대로.
    ② T-B2 물 K_H 재측정 — **필수(싸다).** 32종 + 확장 7종 + base 검증 = 약 40 Widom. ② Spearman 용, X4(물 K_H) 채움. 대조 4건 재실행 불필요.
       러너 `run_tb2_water_kh.py`/`run_tb2_ext.py` 의 고침 8건(`TB2_WATER_KH_FIXES_20260906.md`: ase 관문·PrintEvery·성분별 파싱·ALL 단일기기·힘장 출처·Hw-Hw 확인·5자리 확인·EXT 대조 분리) **전부 승인** — 새 계열이라 §3 함정 없음.
    ③ T-4·T-6 물 밀도 격자(base·nbIm025·saIm050, RH90) — **선택.** 포스터에 "물이 치환기에 안 앉는다" 를 쓸 때만. 단일 복셀 자리 주장은 어차피 금지(DENSITY_GRID_TWO_GENERATIONS).
       **[09-06 23:1x 사용자 지시 "띄워 당장 실행" → 착수]** 데스크탑, 프로세스 셋(2초 어긋내기), `run_density_water_v3w.py`, OUT `density_water_v3w/<조성>/`(JSON 충돌 방지, 병합은 종합자), RUNS `water_runs_density_v3w/`. RH90 3종과 함께 simulate 6건.
       **완주 09-07 06:15**(착수 23:34 — 앞 "23:1x" 는 오기; base 3h07m · nbIm025 ≈4h · saIm050 6h41m). VTK `water_runs_density_v3w/rh90_*/VTK/System_0/COMDensityProfile_{water,CO2}.vtk.gz`(커밋).
       러너는 water_results.json 을 안 쓴다(run_water.main 미호출) — 로딩은 Output .data 에서 `density_water_v3w/loadings_from_output.json` 으로 회수. 이 셋은 **RH90 GCMC 이기도 하므로** 유지율 상한 점이 된다(base·saIm050 은 큐와 씨앗 다른 판, nbIm025 는 18종 밖의 새 점).
       **격자는 T-4′ 등록(Melchior 요청, `T4_PRIME_20260907.md`) 전에 열지 않는다** — 장 수준 비(ratio) 통계, 단일 복셀 금지.
       **T-4′ 판정(Melchior, `T4_PRIME_VERDICT_20260907.md`, 06:4x)**: 첨가 치환기 껍질의 물 평균 밀도 / 위약 치환 중앙값 — base(Cl 음성대조) 1.41 p 0.229 **대조 통과** ·
       nbIm025 0.43 p 0.906 · saIm050 0.94 p 0.567 → **"물이 치환기에 몰린다" 못 씀**(둘 다). 검정력: 같은 껍질에서 CO₂ 는 saIm050 0/200 p 0.005 로 잡힘 → 약한 시험이 아니라 **검정력 있는 음성**.
       **부차(서술)**: 옛 결함 물은 술폰산 껍질에 **유의하게 몰렸음**(1.58, p 0.020) → 수정 물은 0.94 로 사라짐 — **결함 힘장이 가짜 신호를 만들고 있었다**(수소결합 없는 물은 가장 극성인 자리에, 고친 물은 서로에게).
       미평형은 이 음성 쪽으로 안전(강한 자리부터 채워지면 초기 몰림이 과대여야 하는데 안 보임). 포스터 문구: "물은 치환기에 **몰리지 않는다**"(음성 대조 통과·CO₂ 검정력 병기). 못 쓰는 것: "피한다"·"평형에서"·자리 지목.
       **확장 덱 착수(06:5x, `T4_PRIME §7`)**: saIm100 · sa50nb50 · nbIm075 — 데스크탑 유휴 코어 3.
    범위 밖   저RH 등온선 101작업 · 습윤 WC 75 GCMC · RH0 50작업(무관). **"습윤에서도 버틴다" 의 근거는 ① 의 RH90 유지율이지 습윤 WC 앙상블(T-1)이 아니다** —
              포스터가 T-1 의 0.7306 을 쓰면 그것은 (라) 가 안 덮는다. 문구를 유지율로 고정한다.

## 4. 배정 — 예상 소요로 나눈다 (`COST_REFERENCE.md` §2b: 설폰산이 base 의 2배, 동시성 축 1.07)

    랩탑(Melchior)   ① 중 saIm 계열 11종(saIm025·050·0583+e1~e5·0625·0667·075) — 8워커, 건당 ≈17.5 h → 2파도 ≈ 35 h
                     + 힘장 수정·래퍼 작성·§1 검증 ③. 물 축 기기.
    laptop2(Balthasar) ① 중 base · saIm0875 · saIm0917 · saIm0958 · saIm100 · mslm050 · sa50nb50 = 7종 — 3~6병렬, chunked 러너 → ≈ 30~36 h
                     + 옛 물 JSON 71개에 `WARN_forcefield` 사이드카 표기(annotate 리스트형 변형, 측정값 불변).
    데스크탑          nbIm050e1~5 체인(`ASSIGN_NB050_20260906.md`) 뒤 ② T-B2 40 Widom (≈5~8 h). ③ 은 미배정.
    시작 조건        각 기기가 §1 검증 ①② 를 마치고 **첫 실행의 출력 머리말(③)** 을 확인한 뒤 나머지를 잇는다 — ③ 은 착수 수 분 안에 끝나므로 완주를 기다리지 않는다
                     (11:1x 문언 정정: "첫 완주" 로 적었던 것은 목적(힘장 적용 확인)보다 넓었다. 랩탑 지적. 나머지 7코어 × 17.5 h 유휴를 막음).

## 5. 옛 결과의 지위

    결함판(수소결합 없는 물)   v3_water*/water_results*.json 57파일의 RH>0 행 · v3_humid_wc*/·v4_humid_wc/ · v3_water_kh/*.json · density_water_v3/ · T-B1 현행
    표기                       `WARN_forcefield`(현재 4파일) → laptop2 가 사이드카로 71파일 확장. 측정값은 안 건드린다. 인용 시 "수소결합이 없는 물로 계산한 값" 병기.
    노션·포스터               RH90 유지율·습윤 WC 는 새 계열이 나올 때까지 인용 불가(11-F). 건조 축은 무관.

## 6. 이 등록이 못 하는 것

    유지율이 좋아질지 나빠질지는 결과가 말한다(MORNING §1-2-2 "순위가 바뀔 수 있다"). 습윤 WC 절대값은 이 범위 밖.
    D = 8.63 은 base n=1 — 조성별 보정 계수로 쓰지 않는다.

## 7. 결과 도착 (수정 힘장 v3w) — 분모는 `RH90_DENOMINATORS_20260906.md` §1·§5·§6

    조성      기기      완주        CO₂(RH90, 새)      물(RH90, 새)        분모(RH0, v3)   **유지율(새)**   옛 결함판   단서
    base      laptop2   17:2x 6.13h  0.5838 ± 0.0139    0.1845 ± 0.0581     0.5839          **100.0 %**      98.4 %      ⚠ 물 조각4 0.2652 = 조각0~3 대비 +7σ 이상치.
                                                                                                                              조각4 만이면 CO₂ 0.5658 → 96.9 %. 평형 부족인지 잡음인지는 다른 조성의 조각별 물을 본 뒤(Balthasar 유보).
                                                                                                                              물은 옛 0.4893 의 **0.38배** — D≈8.6(영피복)보다 온건, 유한 피복이라 예상 범위.
    (규칙)    러너의 "사슬 연속성" 검사는 CO₂ 만 본다 — **물 성분의 조각별 이상치도 함께 보고**한다(§9 대기 규칙과 무관한 보고 규약). 인용 시 단서 병기.
    (해석)    유지율이 오르고 물이 준 방향은 MORNING §1-2-2 의 추론과 같다. **조성 간 순위는 재계산 전량이 나온 뒤에만.**
    (T-4·T-6) 물이 0.38배면 밀도 격자 통계가 얇아진다 — §3 ③ 을 돌릴지는 사용자 결정(포스터에 그 주장을 쓸 때만).
    saIm025   랩탑      7.3h         0.9409 ± 0.0181    —                   0.9708          **0.969 ± 0.024**  0.805       +0.165
    saIm050   랩탑      10.7h        1.1318 ± 0.0343    —                   1.2297          **0.920 ± 0.033**  0.761       +0.160  ⭐ 조건부(50~80) → **유효(≥80)** 갈래 바뀜
    saIm0583e5 랩탑     11.6h        1.0074 ± 0.0181    —                   1.1716          **0.860 ± 0.020**  0.763       +0.097  ⭐ 같은 갈래 변화
    (해석)    세 건 다 +0.10~+0.17 — 수소결합이 생긴 물은 서로 붙어 골격 침입이 줄고 CO₂ 를 덜 밀어냄. **표본 3, 방향만.** 
    (비용)    완주 3건 7.3/10.7/11.6 h 인데 진행 5건이 13.5 h 경과 — 먼저 끝난 것이 빠른 것(검사 6 선택 효과). **등록값 17.5 h 는 반증 안 됨, 전부 끝난 뒤 COST_REFERENCE 갱신.**
    (격자 셋, 데스크탑, 씨앗 다른 판)  base CO₂ 0.5880±0.0092 물 0.203 → 유지율 **1.007**(laptop2 판 1.000) · saIm050 CO₂ 1.1309±0.0134 물 0.335 → **0.920**(랩탑 판 0.920)
              · nbIm025 CO₂ 0.7895±0.0054 물 0.086 (18종 밖의 새 점, 분모 미등록 → 유지율 미산출). 전부 "물 미평형 — 상한". **씨앗만 다른 판끼리 유지율이 ± 안에서 일치**(base 0.7 %, saIm050 0.0 %).
    랩탑 8/11 (07:1x)  saIm0583 0.873±0.025 · e1 **0.772**±0.027 · e2 0.877±0.023 · e3 0.839±0.010 · e4 0.849±0.021 (+ 앞의 025/050/e5) — 범위 0.772~0.969, 중앙 0.866, ≥80 % 7/8. 전부 "미평형(프로토콜) — 상한".
    T-B2w     데스크탑  23:00 5h21m  **39/39 완주**(6워커 Widom, 8.2 분/작업 실효). `v3w_water_kh/water_kh_ALLw_hkhome.json`, 행마다 ff_check 재판독(fix_tb2w_ffcheck.py) 전부 ok.
              옛/새 K_H 비: **중앙값 8.3, 범위 1.7(saIm0875)~14.4(nbIm025)** — base 7.99(대조 (다) 8.63 과 같은 대역; base 새 7.13e-5 대 대조 6.25e-5 는 Widom 통계 폭).
              **비가 조성마다 1.7~14 로 다르다 → D 를 보정 계수로 못 쓴다는 등록(TB2_VERDICT_C1 §6-2)이 실측으로 확인됨.**
              T-B2 ①: 새 K_H 최소 1.8e-5(mbIm025) > 문턱 5e-6 → **전량 문턱 위, 판별력 없음 그대로**(정의역 D<41 안). ② Spearman 은 v3w 유지율이 다 나온 뒤.
    (이관 착오) 23:03 데스크탑에 saIm0625·0667·075 를 띄웠으나 **랩탑 큐는 파도가 아니라 연속 큐라 셋이 이미 거기서 돌고 있었다**(18:24·21:52·22:46 시작).
              laptop2 7종도 11:13 부터 전부 병렬. → 데스크탑의 셋은 죽이지 않고 남긴다. **[정정, Melchior]** `run_water.py` 는 씨앗을 안 적어 RASPA 기본값(시각)이 되므로
              두 판은 **같은 구조·같은 설정, 씨앗과 기기가 함께 다른 한 쌍** — "기기 간 재현성" 이라 부를 수 없다(씨앗 효과와 기기 효과를 못 가름).
              저장소 씨앗 재현성 3.0 %(TB2_CRITERION_NOTE §85)와 견주는 **상한**으로만 쓴다: 차이가 그 안이면 "씨앗+기기 합쳐도 3 % 안", 벗어나면 그때 질문이 생긴다.
              배치 산포에는 넣지 않는다. 정본은 랩탑 판. **러너 결함 기록**: 산출물에 씨앗이 없다 → 재계산 뒤 출력 머리말의 `Random number seed` 를 행에 기록하도록(ff_check 자리). 지금 실행은 안 건드림.

## 8. ⚠ 물이 아직 차오르는 중 — **v3w RH90 유지율은 전부 상한** (laptop2, 09-07 00:40, `WATER_EQUILIBRATION_20260907.md`)

    자료   조각 사슬을 블록으로 이어 붙인 시계열(생산 5,001~20,000): base 25블록 · mslm050 20 · sa50nb50 15 · saIm0875 5 · saIm0917 5
    관찰   물 기울기 **5/5 양수**(+1.0~+6.6 %/블록), 앞절반→뒷절반 **+13.8~+21.9 %**; CO₂ 는 내리는 중. 부호검정 p = 0.031(블록 상관과 무관).
           base 의 "조각4 물 이상치" 는 이상치가 아니라 추세였다(5점으로는 안 보이고 25점에서 보임).
    유보   조각0 만 있는 둘은 약한 증거(과도상태). 무게는 base·mslm050·sa50nb50 3/3. 사전 등록 점검은 아니나 가설 검정이 아니라 측정 유효성 진단.
    **인용 규칙 (01:3x 정정, Melchior)**   "전부 상한" 은 너무 넓다 — **경계의 방향은 물 표류의 부호를 따른다**(오르면 상한, 내리면 하한). 랩탑 단일 실행 5블록:
                       saIm050 물 +10.0 %/블록 **t 4.84 유의(상한)** · saIm0583e5 +4.6 t 1.61 · saIm025 **−10.0** t −2.71 (둘 다 dof 3 임계 3.18 아래) · CO₂ 셋 다 0 과 구별 안 됨.
                       **[02:0x 결정 — 일괄 딱지]** 조성별 "유의" 를 실행 안 t 로 재면 블록 상관으로 부풀어 잡음이 딱지를 정한다(Balthasar). 조성들은 같은 물 모형·같은 프로토콜을
                       공유하므로 **실행 사이 검정이 "이 프로토콜에서 물은 아직 오른다" 를 말하면 전 조성에 걸린다.** 따라서 **v3w RH90 유지율 전체에 "물 미평형(프로토콜) — 상한"**
                       을 달고, 조성별로는 Δ40 값만 병기한다(saIm025 −10 %/5블록은 "표류 없음" 이 아니라 검정력 부재). 자는 하나: **Δ40** (`REG_DRIFT_POOLING_20260907.md`,
                       생산 15,000 의 뒤 40 % 물 평균 − 앞 40 % / 앞 40 %; 블록 수 무관; 실행이 독립 단위, 실행 사이 부호·순위 정확검정만). 두 러너의 블록당 % 차이(600 대 3,000 사이클)는 자 차이였다.
                       얼마나 과대인지는 이 자료로 못 정한다(평형 접근이 감속하므로 선형 외삽은 과대, 점근값 미식별). 조성 간 편향 방향이 같아 순위는 살 수 있으나 절대값은 아니다.
    기제    (Melchior) 수소결합 물은 (가) 협동적 채움(핵생성→성장, 유도기) (나) 방향 의존 삽입 수용률 저하 (다) 군집 삭제 저하 — 셋 다 **물 로딩이 클수록 심함**.
            (가)가 맞으면 접근은 아래에서 → 상한. saIm025 의 음 부호는 (가)로 설명 안 됨(초기화 과충전 해소 또는 잡음) — 그래서 부호 검정이 필요.
    비대칭  (`WATER_FF_ASYMMETRY_20260907.md`) Δ40 물: **결함판** base +5.4 · saIm050 +0.1 · nbIm025 −3.1 · nbIm075 +4.8 (0 근처) / **수정판** +13.8~+21.9 (5/5). 순위검정 p 0.0079 이나
            조성이 섞여 있어 힘장만 다른 짝은 base 하나(+5.4 → +20.4, n=1). **옛 값은 평형값, 새 값은 상한 → "물 0.38배" 중 일부는 "아직 안 찼다" 이고 힘장 수정의 진짜 크기는 0.38배보다 작다**(점근값 미식별).
            CO₂ 표류로는 두 힘장이 안 갈림(−0.2~−2.3 %). 연장은 **물 로딩이 큰 조성부터**(효과도 비용도 그 순서로 큼).
    ⚠ 갈래(07:2x)  랩탑 단일 실행 8건의 물 기울기(%/3,000): −10.0 +10.0 −2.8 −0.1 +3.1 +5.5 +3.1 +4.5 → 평균 +1.66, SE 2.14, **t 0.78, 부호 5/8 p 0.36** — 95 % 구간 [−3.4, +6.7] 이
              **laptop2 사슬의 +9.4~+10 을 배제**. 데스크탑 단일 실행(격자 셋) Δ40: **base −8.2 %** · saIm050 **+22.8 %** · nbIm025 +4.1 %.
              → base 는 사슬 +20.4 대 단일 −8.2(같은 조성·힘장, 프로토콜만 다름), saIm050 은 세 독립 실행(랩탑 단일 +10/블록·데스크탑 단일 +22.8·사슬)에서 전부 상승.
              **(가) 프로토콜 특유(재시작 사슬)인지 (나) 조성 의존(물 많은 술폰산만 오름)인지 아직 못 가른다** — 지금 자료는 (나) 쪽에 더 가깝고 base 는 물 11분자/셀이라 Δ40 잡음이 크다.
              일괄 "상한" 딱지는 유지(안전 방향)하되 **불균질 병기**. 연장 결정(MORNING §2)에 이 갈래를 붙인다: 진단 = 같은 기기에서 chunked 대 단일(base·saIm050) 직접 대조.
    좁혀짐(07:0x, laptop2 `CHAIN_ACQUITTED_20260907.md`)  **(가) 아님**: restart 파일의 분자 수를 조각마다 셌더니 모든 조각의 시작 = 앞 조각의 끝(base 14→15→7→16→13 …), 적응형 MC 보폭도 그대로 이월 —
              사슬은 경계에서 되돌리지 않는다. 초기화 5,000 은 두 프로토콜에 같이 걸리므로 차이를 못 만든다. **(나) 도 아님 — 물 분자 수다**: base 는 상자(8 uc) 안 물이 **~12개**뿐이라
              Δ40 +20.4 % = 2.4분자, 데스크탑 −8.2 % = −1.0분자, 차 3.4분자 ≈ 2.2σ 미만 — 모순이 아니라 잡음.
              **자(Melchior 지적)**: base 는 치환 자리가 없어 배치 자유도 0, 두 실행의 차이는 씨앗뿐 → **통계 비교, 단위(±)** = 2.2/2.776 = **0.79 단위(±) < 1.5, 구별 안 됨**. (배치 단위로 읽으면 2.2 > 1.5 로 뒤집히나 그 자는 여기 해당 없음.) Δ40/폭: base 1.29(꼴찌) · 술폰산·혼합 1.6~2.5.
              **물이 있는 곳에서는 두 프로토콜이 일치**(saIm050 +22.8 이 사슬 술폰산 띠 +13.8~+21.9 안). 사슬 증거는 base 를 빼면 4/4. → 상한 딱지 유지, **base 는 분자 수가 적어 해석 약함** 병기.
              랩탑 진단의 등록 예측(laptop2): saIm050 은 두 프로토콜 일치(+10~+25 겹침), base 는 프로토콜 무관하게 실행마다 갈림(부호조차). (가)가 참이면 반대.
              **판별력 단서(Melchior, 07:4x)**: 랩탑 단일 8건의 Δ40 SD = **16.7 %p** — 오간 값 전부 ±1.7 SD 안, 두 실행 차의 SD 23.6 → n=1 씩의 사슬 대 단일은 못 가르고 "일치" 는 약한 확인.
              그래서 "아직 오르는가" 는 실행을 쌓아서(+10 % 면 n ≥ 16) 못 답하고 **연장(누적)만이 답** — MORNING §2 권고 이유로 승격.
    안전 방향 상한으로 적으면 "참값 ≤ 적은 값" 이고 포스터 주장("버틴다" = 유지율 높다)은 과대 주장 쪽이 막힌다 — 틀려도 안전한 쪽(Melchior).
    미결(CO₂) 물은 두 러너가 일치(3,000 사이클 자로 base +9.4 대 saIm050 +10)하나 **CO₂ 표류는 아직 갈림**: 랩탑 t +1.13/−0.80/+0.17(자 무관, 0 과 구별 안 됨), laptop2 "하강" 은 %/블록 표기.
           → **[01:0x 닫힘, laptop2 `STOPRULE_AND_CO2_20260907.md`]** CO₂ Δ40 도 수정판 **5/5 음수**(p 0.031) — 랩탑 t 가 0 과 구별 안 된 건 5블록 t 의 검정력 부재.
           **교환비 −ΔCO₂/Δ물 = 0.07~0.34(평균 ≈0.2)** → 들어온 물 다섯 중 넷은 CO₂ 를 안 건드린다 = **상한은 생각보다 덜 느슨**(과도상태 측정이라 인용 금지, 방향만).
           미결: 결함판도 CO₂ 4/4 음수 — 빈 상자에서 CO₂ 가 먼저 과충전됐다가 내려오는 것일 수 있음(검증하려면 물을 채운 상태에서 시작한 실행 필요, 없음). 맞다면 교환비는 과대.
    미결    **생산 사이클 연장 여부 — 사용자 결정**(CLAUDE.md §1 고정값 5,000+15,000, 비용). 지금 도는 것은 그대로 완주. 권고는 MORNING_20260907.md.

## 12. §3 보완 — ④ 습윤 TSA 작업 용량 재계산 (09-10 17:0x 등록, 결과 전; MAGI-004 Balthasar R1 논제 1 수용)
    누락    §3 의 재계산 범위(①유지율 ②물 K_H ③밀도 격자)에 **습윤 WC 가 빠져 있었다** — DECISION_RULE_20260820 의 유일한 1위 자인데 09-06 이후 나흘째 인용 불가·재계산 미배정.
    등록    `run_humid_wc_v3w.py`(새 폴더 humid_wc_runs_v3w / v3w_humid_wc, 파일 관문 선행). 대상 1차 base·saIm025·saIm050·saIm0583(12작업: ads/tsa/vsa) → 2차 saIm0583e1~e5·saIm050e1~e5(앙상블).
    자      DECISION_RULE 그대로: 습윤 TSA WC 최대, 차이 < 1.5 단위(±)면 공동 1위; 앙상블은 배치 단위 병기. **문턱 불변.** 결과는 v3(결함판)와 나란히 순위 매기지 않는다.
    기기    데스크탑(비어 있음), 17:0x 착수. 예상 ads/vsa ~5 h · tsa ~2 h, 8워커 → 1차 ~10 h.
