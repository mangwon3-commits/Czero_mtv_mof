# 대조 (다) 판정 — **결함이 지배적. `D = 8.63`** (2026-09-05 16:4x, 데스크탑)

`TB2_CONTROLS_20260905.md` (다) 가 **측정 전에 등록**한 순서·기준으로 판정합니다.

## 1. 확인 조건 — 값보다 먼저 (통과)

    분자 `Hw none`  Hw-Hw **ZERO_POTENTIAL** · Ow-Hw **ZERO_POTENTIAL**  -> 적용됨
    분모 현행       Hw-Hw **22.1417** · Ow-Hw 44.54915                   -> 현행 유지됨
    성분 순전하     분자 **0.000000** · 분모 **0.000000**

**양쪽에 각자의 조건을 걸었습니다.** 한쪽만 봤으면 *"둘 다 수정본"* 이나
*"둘 다 현행"* 을 못 잡습니다 — **1차 실행이 정확히 후자였습니다**(아래 5절).

## 2. 결과

    K_H(현행)  **5.393680e-04 ± 1.352e-05**  (2.5%)
    K_H(수정)  **6.250060e-05 ± 9.483e-06**  (15.2%)

    **D = K_H(현행) / K_H(수정) = 8.630**,   σ_lnD = **15.4%**

## 3. 판정 — 등록 기준 (다)-2 그대로

| 등록 구간 | |
|---|---|
| 0.67 ≤ D ≤ 1.5 | 무시할 만 |
| **D ≥ 3 또는 ≤ 0.33** | **지배적** |
| 그 사이 | 유보 |

> **`D = 8.63` → 결함이 지배적입니다.** 로그 공간에서 `D=3` 경계까지 **6.87 단위**.
> **부호는 `D > 1`** — **현행 힘장이 물 `K_H` 를 약 8.6배 과대평가**합니다.
> `Hw` 에 없어야 할 LJ 가 붙어 **물–골격 인력이 더해졌기** 때문입니다.

**그러므로 `TB2_VERDICT_C1_20260905.md` §3 의 요구대로 T-B2 의 모든 물 `K_H` 인용에
"수소결합이 없는 물, 약 8.6배 과대" 를 병기합니다.**

## 4. ① 판정의 정의역 — **안전합니다**

`TB2_VERDICT_C1_20260905.md` §6 이 등록한 정의역은 **`D < 41`** 입니다.

    D = **8.630**,  `D=41` 까지 로그 공간 **10.13 단위**

> **① 판정(판별력 없음)은 유효합니다.** 수정판으로 환산해도 최소 조성 `mbIm025` 가
> 문턱의 **41/8.63 = 4.8배** 위에 남고, **문턱 아래로 내려가는 조성은 0개**입니다.

## 5. 1차 실행은 무효였고, **확인 조건이 그것을 잡았습니다**

    1차: `<rundir>/UFF_MOF/` 에 사본 -> **RASPA 가 안 봄** (짝표 22.1417 그대로)
         두 실행이 **같은 힘장** -> **D = 0.000580/0.000617 = 0.94**
         등록 창 0.67~1.5 **안** -> *"결함이 K_H 를 크게 안 움직인다"* 로 읽혔을 값

**`D ≈ 1` 은 두 실행이 같으면 당연합니다. 관문이 없었으면 동어반복을 결과로 적었을
것입니다.** `PREREG_CHECKLIST.md` **9-1** 이 이 사고에서 나왔습니다.

**고침**: `$RASPA_DIR/share/raspa/forcefield/UFF_MOF` 트리(공용 파일 불변).
**띄우기 전에 10사이클 연무시험**으로 짝표를 확인했습니다.
(랩탑 방식은 **`<실행폴더>/` 루트**에 정의 파일을 직접 두는 것 — 더 짧습니다.)

## 6. ⭐ 독립 경계 계산과의 대조 — **두 경로가 만납니다**

랩탑이 **RASPA 없이** 기하만으로(삽입 물의 `Hw`–골격 LJ 합, `⟨exp(−βΔU)⟩`) 낸 값:

    랩탑 경계 계산  `base` **8.65**   (`tb2_dbound.py`, 09-05 15:2x — **측정 전**)
    RASPA 실측      `base` **8.630**

    차이 **0.2%**

**완전히 다른 두 경로입니다** — 한쪽은 Ewald 를 포함한 RASPA Widom, 다른 쪽은
정전기를 뺀 기하 표본추출. **그런데 같은 답을 냅니다.**

> ⚠️ **0.2% 는 운이 섞였습니다.** 실측의 `σ_lnD` 가 **15.4%** 이므로 이만큼
> 가까울 이유가 없습니다. **말할 수 있는 것은 "두 경로가 15% 안에서 일치" 까지**입니다.
>
> 그리고 랩탑의 등록 대조 구간은 **9~15** 였고 실측 **8.63** 은 그 **하한 바로 아래**
> (로그 공간 **0.27 단위**)라 **구별되지 않습니다.** 등록문 (다)-8 의 읽기로는
> **"두 경로가 만남"** 입니다.

**그 대조가 말하는 것**: 랩탑 계산이 뺀 **정전기 가중이 LJ 가중과 크게 다르지 않습니다.**
그래서 그 경계 계산의 다른 결과(가중 민감도, 조성 간 안정성)도 **쓸 만합니다.**

## 7. 이 대조가 **하지 않는** 것

- **`base` n=1** 입니다. **`D` 를 32종에 곱하지 않습니다** — 골격 조성이 다르면
  `Hw`–골격 항도 다릅니다(`TB2_VERDICT_C1` §6-2).
- **`K_H` 는 무한희석**입니다. **RH90 유지율의 방향은 이 값으로 안 정해집니다** —
  거기서는 물–물(수소결합 억제)이 반대로 작동합니다(`MORNING_20260905.md` §1-2-2).
- **물이 옳아진 것이 아닙니다.** `D` 는 **결함의 크기**를 잰 것이고, 고칠지는
  **사용자 판단**입니다(CLAUDE.md §1).
