# 기기별 능력 경계 — 무엇을 줄 수 있고 무엇을 못 주는가 (2026-09-21 23:2x 개설)

> **왜 이 문서가 생겼나**: laptop2 가 **2026-09-18 05:2x** 에 *"laptop2 가 받을 수 있는 배정과 못 받는
> 배정의 경계 한 줄"* 을 청했고, **사흘간 아무도 안 썼습니다.** 그 사흘 뒤인 09-21 에 같은 뿌리의 결함이
> 데스크탑에서 6시간을 먹었습니다(T-C10 ⑤관문이 `czeromof` 로 떠서 rc=2). 경계가 문서에 없으면
> **배정하는 쪽이 매번 추측**하고, 추측은 "그 기기에 마침 있어서 안 걸린다" 로 몇 번 통과한 뒤 터집니다.
>
> 각 기기는 **자기 줄만** 고칩니다(COMMS 규약과 같은 이유). 추측으로 남의 줄을 쓰지 마십시오.

## 1. 한 줄 요약

| 기기 | 받는 것 | **못 받는 것** |
|---|---|---|
| **desktop** (HKHOME) | 전부 — RASPA · Zeo++ · LAMMPS 관문 · xtb · PACMAN · rdkit | (현재 알려진 제약 없음) |
| **laptop2** | **RASPA GCMC·Widom · xtb 이완 · PACMAN 전하** | **rdkit 빌드 · LAMMPS 위험도 관문** · **Zeo++ 대량** |
| **laptop** | **RASPA GCMC·Widom · Zeo++(소량) · rdkit 빌드 · LAMMPS 위험도 관문** | **xtb 이완**(환경 자체가 없음) · **Zeo++ 대량** |
| **Junseok** (원격지) | **RASPA GCMC·Widom · LAMMPS 위험도 관문 · Zeo++(동시 2건)** | **rdkit 빌드 · xtb 이완 · PACMAN 전하** · Zeo++ 대량 |

## 2. desktop (HKHOME) — 2026-09-21 23:2x 실측

    CPU   AMD Ryzen 7 5700X · **물리 8 / 논리 16** · RAM 19 GB
    환경  czeromof(**network** = Zeo++, **rdkit**) · coremof_tools · **lammps_mof**(lmp_serial) · spectra(xtb)
    비고  ⑤관문은 **lammps_mof** 파이썬으로 불러야 합니다 — `czeromof` 로 부르면 사전검사가 거부합니다.
          Zeo++ 는 v3 구조에서 **9.5 GB/건**, RASPA 와 **동시 금지**(CLAUDE.md §5).

## 3. laptop2 — 2026-09-21 23:2x, **그 기기가 보고한 값**

    CPU   Intel i7-9750H · **물리 6 / 논리 12** · RAM 15 GB
          ⚠ **처리량 견적에 논리 12 를 쓰지 마십시오. 실측 유효 6.26 개분입니다**(12워커에서 경합 1.92x).
    환경  czeromof · coremof_tools · spectra — **셋뿐**
    실적  RASPA GCMC·Widom **8회** · xtb 이완 1회 · PACMAN 전하 1회 완주

    ❌ **rdkit 빌드**        명세(`czeromof.yml:197`)에는 있고 **실물엔 없음**. 09-18 부터 그대로.
    ❌ **LAMMPS 위험도 관문** `lammps_mof` 환경 자체가 없음. 09-18 에 ⑤ 가 여기서 5시간 뒤 중단.
    ⚠ **Zeo++ 대량**        `czeromof/bin/network` 는 **있습니다**. 다만 15 GB 기기에서 9.5 GB/건이라
                            **동시 1건**이 한계이고(09-18 사슬 실측 "워커를 4 → 1 로 낮춥니다"),
                            §5 대로 RASPA 와 동시 실행도 안 됩니다. **대량은 주지 마십시오.**

> **laptop2 의 자기 정정(09-21)**: *"09-18 에 '⑤ 는 Zeo++ 라 RASPA 0 창이 필요하다' 고만 적어
> **Zeo++ 도 없는 것처럼 읽힐 여지**를 남겼습니다. 그 흐림이 사흘 갔습니다."*
> — 막힌 진짜 이유는 **LAMMPS 부재**였습니다. 결론(대량 금지)은 같고 **이유가 다릅니다.**

### 앞단이 붙은 배정을 줄 때
①빌드(rdkit) 또는 ⑤관문(LAMMPS)이 **하나라도 붙으면** 그 단계만 **다른 기기가 하고 결과를 푸시**하는
구조로 주십시오. §AK 가 그 형태였고 ②③④⑥ 은 laptop2 에서 전부 완주했습니다.
**사슬 스크립트를 통째로 주면 5시간 뒤 중단됩니다** — 09-18 에 실제로 그랬습니다.

## 4. laptop — 2026-09-23 01:0x 실측 (그 기기가 직접 잰 값)

    CPU   AMD Ryzen 9 5900HS · **물리 8 / 논리 16** · RAM 23 GB
    환경  czeromof · coremof_tools · **lammps_mof** · ghcli  — **`spectra` 환경이 없습니다**
    힘장  UFF_MOF `force_field_mixing_rules.def` md5 **8e8ec933**…  (파일 관문 통과)
    RASPA `simulate` 는 **czeromof 안**에 있습니다(`envs/czeromof/bin/simulate`, 도는 프로세스의
          `/proc/<pid>/exe` 로 확인). `~/RASPA/simulations/bin/` 에는 없으니 그 경로를 가정하지 마십시오.

    ✅ **RASPA GCMC·Widom**   §AU(습윤 WC 15작업 28h23m) · §AV(Widom 408작업 18h15m, 204/204 실패 0) 완주
    ✅ **rdkit 빌드**          `czeromof` 에서 실동작 확인(벤젠 6원자). **laptop2 에 없는 것입니다.**
    ✅ **LAMMPS 위험도 관문**   `lammps_mof` 에서 `lammps_interface`·`ase`·`numpy` import 되고
                              `lmp_serial` 은 **`~/.local/bin/lmp_serial`** 에 있습니다(환경 bin 이 아님 — PATH 로 잡힘).
                              **laptop2 에 없는 것입니다.** 데스크탑 §2 주의대로 `czeromof` 로 부르면 거부됩니다.
    ⚠ **Zeo++**               `czeromof/bin/network` **있고 응답합니다.** 다만 v3 구조 9.5 GB/건 · RAM 23 GB 라
                              **동시 2건이 상한**이고, §5 대로 RASPA 와 동시 실행 금지. **대량은 주지 마십시오.**
    ❌ **xtb 이완**            `spectra` 환경이 없고 어느 환경에도 `xtb` 바이너리가 없습니다.

    처리량(실측)  Widom CO₂/N₂ 408작업 · 8워커 · 작업합 145.2 h → 벽시계 **18.25 h**
                  비용 = f(N_super), **b = 0.733 ± 0.035**(구조 204개, 범위 9.7배 — §AV 실측)
                  최장 단일 작업 0.64 h → 묶는 쪽은 **작업합/워커**(CLAUDE.md §5 규약)
    ⚠ **경합 배수는 아직 모릅니다.** 같은 구조를 워커 수만 바꿔 돌린 짝이 없습니다.
      laptop2 처럼 "유효 몇 개분" 을 적으려면 그 짝이 먼저 필요합니다 — **추측으로 적지 않습니다.**

### 앞단이 붙은 배정을 줄 때
**①빌드(rdkit)·⑤관문(LAMMPS) 둘 다 여기서 됩니다** — laptop2 와 갈리는 지점입니다.
사슬을 통째로 줘도 그 두 단계에서 멈추지 않습니다. 다만 **단계마다 인터프리터가 다릅니다**:
⑤ 는 `lammps_mof`, 나머지는 `czeromof`. 09-22 에 데스크탑 T-C10 이 이것을 섞어 6시간 놀았습니다.
**xtb 이완이 붙으면** 그 단계만 다른 기기가 하고 결과를 푸시하는 구조로 주십시오.

## 5. 열려 있는 결정 — **사용자 몫**

    laptop2 의 `rdkit` · `lammps_mof` 를 **설치할 것인가.**
    설치하면 laptop2 가 앞단까지 받아 사슬을 통째로 돌릴 수 있습니다.
    **laptop2 는 설치하지 않습니다** — 환경 변경은 CLAUDE.md §9 의 "30분 규칙을 쓰지 않는 셋" 중 ①입니다.
    09-18 부터 열려 있습니다.

## 6. Junseok (원격지) — 2026-09-24 07:3x 실측 (그 기기가 직접 잰 값)

    CPU   AMD Ryzen 5 7500F · **물리 6 / 논리 12** · WSL 메모리 25 GiB (.wslconfig **26GB**, 09-24 사용자 지시 · 호스트 31.5 GB · Windows 몫 5.5 GB)
    환경  czeromof (py 3.10) · **lammps_mof** (py 3.11.15) — **`coremof_tools` · `spectra` 환경이 없습니다**
    힘장  UFF_MOF md5 **8e8ec933** (파일 관문 통과) — ⚠ **09-24 복귀 때 고쳤습니다.** 그 전은 73046fd7
          (09-06 수정 전 결함판). 기기가 09-06 에 쉬고 있었습니다. 지금은 저장소 정본 트리 498개 전부 일치.
    RASPA `simulate` 는 czeromof 안(`envs/czeromof/bin/simulate`). `~/RASPA/simulations/bin/` 에는 없습니다.

    ✅ **RASPA GCMC**          08-28~29 물 러너 RH0 재현성 반복 20회 완주, 실패 0
    ✅ **Widom**               09-25 MAGI-005 E-2 4작업 완주, 표지 4/4 · 실패 0 (그 전까지 미확인)
    ✅ **LAMMPS 위험도 관문**   `lammps_mof` 에서 `lammps_interface`·`ase`·`numpy` import 됨.
                              `lmp_serial` 은 **`envs/lammps_mof/bin/`** 에 있고 **PATH 에 없습니다**
                              (랩탑은 `~/.local/bin` 이라 PATH 로 잡힘 — **다릅니다**, 절대경로로 부르십시오).
                              08-27~29 안정성 관문 1단계 12종 · 2단계 완주.
       ⚠ `network`(Zeo++)는 czeromof 에만, `lammps_interface` 는 lammps_mof 에만 있어 **한 인터프리터로
         관문 전체를 못 돕니다**(데스크탑도 같은 분열, 08-29 서로 확인). 08-29 에는 제 래퍼에서만
         처리했고 공용 `risk_screen.py` 는 안 고쳤습니다.
    ⚠ **Zeo++**               `czeromof/bin/network` 있고 응답. v3 9.5 GB/건 · WSL 23 GiB → **동시 2건 상한.**
                              실측 반례: 08-27 `ZEO_GB_PER_JOB=4.0` 으로 4건을 띄워 **전역 OOM → WSL 다운**
                              (커널 로그 total-vm 9.47 GB/건). §5 대로 RASPA 와 동시 실행 금지.
    ❌ **rdkit 빌드**          czeromof · lammps_mof 둘 다 import 실패
    ❌ **xtb 이완**            `spectra` 환경 없음 · `xtb` 바이너리 없음
    ❌ **PACMAN 전하**         `PACMANCharge` import 실패(두 환경 모두). 저장소는 `coremof_tools/bin/python`
                              으로 부르는데 **그 환경이 이 기기에 없습니다.**

    처리량(실측)  물 러너 RH0 1작업(초기화 5,000 + 생산 15,000) · 같은 조성 saIm0583e1~e5 19회
                  시작 = 로그 머리 · 끝 = 결과 JSON mtime · 평균 동시 = 다른 작업과의 겹침 시간가중

                  동시성 1            e1 1회  98.2 분   (09-24 e1ff, §2 선행 확인)
                  평균 동시 2.9~4.0   e1 7회 105.7~107.1 분 · e4 3회 108.5~109.8 분
                  평균 동시 8.4~8.8   e2 3회 110.4~112.3 · e5 3회 122.6~126.0 · e3 3회 127.2~129.6 분

                  밀도맵(초기화 2,000 + 생산 5,000 · 90³ 격자) 10작업 · 10워커 동시 (09-24)
                    전하 OFF 14~18 분 · 전하 ON 26~38 분 · 벽시계 38.5 분 = 최장 단일 작업

                  nbIm025 RH0 물 러너 · 동시성 1 (09-24 §2)   4회 58.5~59.8 분
                  Q_st 저피복 GCMC (0.01·0.02 bar · 283/298/313 K) 18작업 · 워커 10 (09-24)
                    작업당 26.5~43.1 분 (중앙 29.5) · 벽시계 72 분 · 최장 = 283 K 0.02 bar
                  Widom (초기화 3,000 + 생산 15,000) · v3 base·nbIm100 2×2×2 · 4작업 · 워커 4 동시 (09-25 MAGI-005 E-2)
                    N₂ 24.1 / 26.7 · CO₂ 25.9 / 27.2 분 · 벽시계 28 분

    ⚠ **경합 배수 — 같은 구조(e1)의 짝만 적습니다.**
      동시성 1 (98.2 분, **1회**) 대 3~4 (105.7~107.1 분, 7회) -> 3~4 에서 **약 +8 %**.
      3 대 4 는 차이 없음(105.9 대 106.3 분). 다만 동시성 1 쪽은 **1회**이고 **날짜가 다릅니다**(09-24 대 08-28,
      그 사이 재부팅 · 힘장 09-06 판 — RH0 라 물리적으로 무관). **8 이상은 여전히 모릅니다** — 구조가 달라
      경합과 구조별 비용이 섞여 있고, 둘을 가를 설명을 붙이면 숫자가 맞아 보이지만(CLAUDE.md §0) 짝이 없습니다.
    ⚠ **정정 — 08-29 제 말이 틀렸습니다.** 그때 *"9병렬에도 경합 감속 0 (경과 = CPU 시간)"* 이라고
      함대에 알렸습니다. **CPU/경과 = 1.00 은 이 판정의 자가 아닙니다** — 물리 6코어에 9개면 3코어를
      두 스레드가 나눠 쓰는데, 나눠 쓰는 스레드도 **각자 100% 로 보입니다.** 소요 시간 자체를 봐야 했고,
      보면 8 이상에서 110~130 분으로 늘었습니다(위 단서대로 구조 차이와는 분리 안 됨).
      (물리 코어보다 적게 띄운 기기라면 그 자로도 틀리지 않습니다 — 공유가 없으니까요.)
    참고  등록부의 "SMT 이득 6→10 워커 처리량 +55%" 는 08-26 벤치마크 값이고 위 표와 **따로 잰 것**입니다.

### 앞단이 붙은 배정을 줄 때
**①빌드(rdkit) · 이완(xtb) · 전하(PACMAN) 는 여기서 안 됩니다** — **전하 CIF 가 이미 있는 구조**
(`charged_v3/` 등)만 받습니다. ⑤관문(LAMMPS)은 되지만 **단계마다 인터프리터가 다릅니다**
(⑤ `lammps_mof`, Zeo++·RASPA `czeromof`), `lmp_serial` 은 절대경로로.

### 운영 제약
- **Windows 업데이트 일시중지 — 09-24 복구됨**(만료 2026-10-19). 09-17~24 에 풀려 있었던 것을 사용자가 다시 걸었습니다.
  만료가 다가오면 **값으로** 확인하십시오(`PauseUpdatesExpiryTime`, COMMS.md 의 08-22 교훈).
- `ClaudeWslHold` 는 `Interactive only` 입니다 — 재부팅 뒤 **로그인 전에는 WSL 이 안 붙잡힙니다.**
  로그인되면 cron 이 postman 을 되살립니다(`mail_guard_junseok.sh`). 도는 계산은 되살리지 않습니다.
