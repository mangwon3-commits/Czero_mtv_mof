# 기기별 능력 경계 — 무엇을 줄 수 있고 무엇을 못 주는가 (2026-09-21 23:2x 개설)

> **왜 이 문서가 생겼나**: laptop2 가 **2026-09-18 05:2x** 에 *"laptop2 가 받을 수 있는 배정과 못 받는
> 배정의 경계 한 줄"* 을 청했고, **사흘간 아무도 안 썼습니다.** 그 사흘 뒤인 09-21 에 같은 뿌리의 결함이
> 데스크탑에서 6시간을 먹었습니다(T-C10 ⑤관문이 `czeromof` 로 떠서 rc=2). 경계가 문서에 없으면
> **배정하는 쪽이 매번 추측**하고, 추측은 "그 기기에 마침 있어서 안 걸린다" 로 몇 번 통과한 뒤 터집니다.
>
> 각 기기는 **자기 줄만** 고칩니다(COMMS 규약과 같은 이유). 추측으로 남의 줄을 쓰지 마십시오.

## 1. 한 줄 요약

| 기기 | 받는 것 | **못 받는 것** |
|---|---|---|
| **desktop** (HKHOME) | 전부 — RASPA · Zeo++ · LAMMPS 관문 · xtb · PACMAN · rdkit | (현재 알려진 제약 없음) |
| **laptop2** | **RASPA GCMC·Widom · xtb 이완 · PACMAN 전하** | **rdkit 빌드 · LAMMPS 위험도 관문** · **Zeo++ 대량** |
| **laptop** | (그 기기가 채울 것) | (그 기기가 채울 것) |

## 2. desktop (HKHOME) — 2026-09-21 23:2x 실측

    CPU   AMD Ryzen 7 5700X · **물리 8 / 논리 16** · RAM 19 GB
    환경  czeromof(**network** = Zeo++, **rdkit**) · coremof_tools · **lammps_mof**(lmp_serial) · spectra(xtb)
    비고  ⑤관문은 **lammps_mof** 파이썬으로 불러야 합니다 — `czeromof` 로 부르면 사전검사가 거부합니다.
          Zeo++ 는 v3 구조에서 **9.5 GB/건**, RASPA 와 **동시 금지**(CLAUDE.md §5).

## 3. laptop2 — 2026-09-21 23:2x, **그 기기가 보고한 값**

    CPU   Intel i7-9750H · **물리 6 / 논리 12** · RAM 15 GB
          ⚠ **처리량 견적에 논리 12 를 쓰지 마십시오. 실측 유효 6.26 개분입니다**(12워커에서 경합 1.92x).
    환경  czeromof · coremof_tools · spectra — **셋뿐**
    실적  RASPA GCMC·Widom **8회** · xtb 이완 1회 · PACMAN 전하 1회 완주

    ❌ **rdkit 빌드**        명세(`czeromof.yml:197`)에는 있고 **실물엔 없음**. 09-18 부터 그대로.
    ❌ **LAMMPS 위험도 관문** `lammps_mof` 환경 자체가 없음. 09-18 에 ⑤ 가 여기서 5시간 뒤 중단.
    ⚠ **Zeo++ 대량**        `czeromof/bin/network` 는 **있습니다**. 다만 15 GB 기기에서 9.5 GB/건이라
                            **동시 1건**이 한계이고(09-18 사슬 실측 "워커를 4 → 1 로 낮춥니다"),
                            §5 대로 RASPA 와 동시 실행도 안 됩니다. **대량은 주지 마십시오.**

> **laptop2 의 자기 정정(09-21)**: *"09-18 에 '⑤ 는 Zeo++ 라 RASPA 0 창이 필요하다' 고만 적어
> **Zeo++ 도 없는 것처럼 읽힐 여지**를 남겼습니다. 그 흐림이 사흘 갔습니다."*
> — 막힌 진짜 이유는 **LAMMPS 부재**였습니다. 결론(대량 금지)은 같고 **이유가 다릅니다.**

### 앞단이 붙은 배정을 줄 때
①빌드(rdkit) 또는 ⑤관문(LAMMPS)이 **하나라도 붙으면** 그 단계만 **다른 기기가 하고 결과를 푸시**하는
구조로 주십시오. §AK 가 그 형태였고 ②③④⑥ 은 laptop2 에서 전부 완주했습니다.
**사슬 스크립트를 통째로 주면 5시간 뒤 중단됩니다** — 09-18 에 실제로 그랬습니다.

## 4. laptop — **그 기기가 채울 자리**

추측으로 쓰지 않습니다. 랩탑 세션이 다음 보고 때 §2·§3 형식으로 한 줄 주면 여기에 올립니다.
지금 아는 것: 물리 8코어(`SESSION_LOG` 08-18), `~/.mof_postman/postman_laptop.sh` 로 postman 구동,
§AU(습윤 WC 15작업)·§AV(RASPA Widom) 완주 실적.

## 5. 열려 있는 결정 — **사용자 몫**

    laptop2 의 `rdkit` · `lammps_mof` 를 **설치할 것인가.**
    설치하면 laptop2 가 앞단까지 받아 사슬을 통째로 돌릴 수 있습니다.
    **laptop2 는 설치하지 않습니다** — 환경 변경은 CLAUDE.md §9 의 "30분 규칙을 쓰지 않는 셋" 중 ①입니다.
    09-18 부터 열려 있습니다.
