# T-C10 등록 — saIm0583 앙상블 보강 `e6~e10` (대조군 정밀화) (2026-09-20 17:40, 데스크탑 세션; **자료 0건** — 새 실현은 아직 하나도 안 만들었습니다; 사용자 지시 2026-09-20 "권고안 중 가장 의미 있어 보이는 거 계산 띄워")

## 1. 왜 이것을 골랐나
`DESIGN_STUDY_20260903.md` T-C10 에 등록돼 있고, **다른 문서 둘이 이 계산을 기다리며 문장을 보류**하고 있습니다 — 미착수 권고 중 **하류 의존이 가장 많은 항목**입니다.

    LCD_LOADING_CONFOUND.md L104   "배치 간 음의 상관을 기제로 읽는 문장은 **T-C10(e6~e10) 전까지 쓰지 않습니다.**"
    ASSIGN_20260904.md     L174   "배치 간 음의 상관(n=5, r=−0.76)은 **e6~e10(T-C10) 에서 재검** 대상이고 지금 인과로 읽지 않습니다"

saIm0583e 는 **"월등" 정의(`DESIGN_STUDY §2-3`)의 대조군**이라 그 산포가 문턱의 분모입니다. 또 09-20 현재 승자 지표의 모든 쌍 판정이 n=5 의 SD(자유도 4)에 걸려 있고,
WC §8 이 이미 **"`saIm050e` 쌍의 −1.31 은 실현 한 점에 걸려 있다"**(leave-one-out −1.08~−2.02)고 적어 두었습니다. **자 자체의 신뢰도**를 올리는 것이 지금 가장 값이 큽니다.

## 2. 계산
    구조   `build_ensemble_0583_ext.py`(`build_ensemble_0583.py` 파생 — **시드 6부터** 시작, 기존 e1~e5 는 시드 범위 밖이라 방문조차 안 함). 검사·기각·인덱스 병합 규칙은 원본 그대로.
    앞단   빌드 → 이완(xtb GFN-FF) → 판정(`judge_relax_v3.py`) → 전하(PACMAN DDEC6) → **관문(Zeo++)**
    본     `run_humid_wc_v3w.py` 15작업(5 × ads/tsa/vsa), 298 K·RH90 P_H2O 2,852.1 Pa → 373 K TSA / 298 K·0.05 bar VSA
           HWC_V3W_TARGETS=<채택 태그> · HWC_V3W_WORKERS=8 · HWC_V3W_RESULT=humid_working_capacity_w2_saIm0583e_ext_desk.json
    자     CLAUDE.md §1 고정값 전부(5,000+15,000 · UFF_MOF · García-Sánchez 2009 CO₂ · TIP5P-Ew 물 09-06 수정판 md5 8e8ec933 · DDEC6 · Ewald 1e-6 · 12 Å · 2×2×2)
    자원   ⚠ **Zeo++ 와 RASPA 동시 금지**(§5). 데스크탑은 (가) Q_st(n) 로 8슬롯이 차 있으므로 **관문과 본 계산은 simulate 0 을 기다립니다.** 앞단 빌드·이완·전하만 `nice -n 19` 로 먼저 돕니다(§5 "부수 작업").
    소요   DESIGN_STUDY 등록치 5 × 3 × 15 h. 앞단 별도(이완 ~5 h).

## 3. ⚠ 등록 문턱이 산수상 달성 불가입니다 — **결과 전에 적습니다**
DESIGN_STUDY T-C10 의 문턱은 **"CI 폭 ≤ 0.03"** 입니다. 지금 수로 확인하면:

    현재 n=5    SD 0.0566 · SE 0.0253 · 95 % CI 반폭 t(0.975,4)×SE = 0.0703 → **폭 0.1405**
    n=10 예상   SD 가 그대로면 SE 0.0179 · 반폭 t(0.975,9)×SE = 0.0405 → **폭 0.0810**
    폭 0.03 이 되려면  n=10 에서 SD ≤ **0.0210**(현재의 37 %)이어야 하고, SD 가 그대로라면 **n ≈ 57** 이 필요합니다.

즉 **이 시험을 설계대로 돌려도 등록 문턱은 거의 확실히 안 맞습니다.** 문턱이 자료 모양을 못 담은 경우이고, `SESSION_LOG` §8 이 같은 형태를 이미 세 번 적었습니다.
**문턱은 고치지 않습니다**(결과를 보고 기준을 바꾸는 것이 되므로). 대신 **"안 맞음" 으로 판정하고 왜 안 맞는지를 위 산수로 적습니다.** 아래 §4 가 정보를 나르는 예측입니다.

## 4. 예측 (자료 0건 시점 고정 — 틀리면 즉시 물린다)
    P1 문턱      CI 폭 ≤ 0.03 → **안 맞음**(n=10 에서 0.07~0.09 로 예상). 맞으면 SD 가 절반 아래로 떨어진 것이고 그 자체가 결과다.
    P2 평균      n=10 평균이 현재 n=5 평균의 95 % CI **0.8247 ± 0.0703 안**에 든다(0.754~0.895).
    P3 SD        n=10 표본 SD 가 **0.035~0.085**(현재 0.0566 주변, 자유도 4→9 로 카이제곱 폭이 좁아짐). 이 대역 **밖**이면 n=5 의 SD 가 대표값이 아니었다는 뜻.
    P4 덩어리    saIm0583e 대 나머지 넷(saIm050e·sa50nb50e·mslm050e·saIm025e)의 SD 형 판정이 **전부 그대로**(넷 다 못 가름). nbIm050e 대비는 **갈림(−) 유지**(현재 −2.49, WC §8 이 "어느 하나를 빼도 안 뒤집힌다" 고 적은 쌍).
    P5 상관      `LCD_LOADING_CONFOUND` 가 기다리는 **배치 간 LCD–로딩 상관**: n=5 의 r = −0.76 이 n=10 에서 **|r| < 0.6 으로 줄어든다**(우연이라는 쪽). |r| ≥ 0.6 이 유지되면 기제 후보로 승격 — 그때 비로소 그 문서의 보류가 풀린다.
    반증        P4 가 한 쌍이라도 뒤집히면 **승자 지표 덩어리 판정이 n 에 걸려 있었다**는 뜻이고, WC §7~§10 의 해당 쌍을 재표기한다(판정문 갱신, 순위는 여전히 안 매김).

## 5. 바꾸는 것 / 안 바꾸는 것
    안 바뀜  298 K·323 K 의 기존 판정(WC §7~§10 · T-RT-1/1b/1c/1d) — 이 시험은 **대조군의 n 을 늘릴 뿐**이고, 결과가 나오기 전에는 어느 문장도 안 건드린다.
    바뀔 수  (a) saIm0583e 의 평균 ± SD 표기가 n=10 판으로 갱신 (b) P4 가 깨지면 그 쌍의 재표기 (c) P5 가 유지되면 `LCD_LOADING_CONFOUND` 의 보류 해제.
    한계     이 시험은 **대조군 하나만** 보강합니다. WC §8 이 지목한 **`saIm050e` 의 SD 0.0960(한 점 의존)** 은 그대로 남습니다 — 그쪽 보강은 별도 등록이 필요하고, 이 등록은 그것을 약속하지 않습니다.

## 6. 절차
    사슬 `.claude_work_tc10_chain.sh`(데스크탑). 로그 `tc10_chain.log`.
      ① 빌드(nice 19, 지금) → ② 이완(nice 19, RELAX_WORKERS 2) → ③ 판정 5/5 pass 아니면 정지 → ④ 전하(nice 19)
      → ⑤ **simulate·network 0 을 3분 연속 확인한 뒤** 관문(Zeo++) → ⑥ 습윤 WC 15작업(워커 8)
    ⑤⑥ 이 (가) Q_st(n) 완주(전망 09-21 16:30 이후)를 기다립니다. 결과 JSON 은 postman 이 올립니다. 판정은 종합자(위 §3·§4), 검산 Melchior/Balthasar.

## 7. 진행 기록
    [2026-09-20 17:40] 등록(자료 0건). 빌더 파생 DRYRUN 통과("시드 6..20, 목표 5실현"). ASSIGN §AV.
    [09-23 08:20] 완주 09-22 15:07(관문 09-22 00:14 통과 5/5; 사슬 정지 09-21 17:09→23:07 은 인터프리터) → 판정 `TC10_VERDICT_20260923.md`(이의 창 09-23 16:2x): P1 CI 폭 0.115(안 맞음) · P2 안 · P3 SD 0.0803(안) · P4 유지 · P5 등록 양 부재(판정 불가). n=10 0.8455 ± 0.0803.
