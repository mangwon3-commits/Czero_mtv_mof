# 이의 수용 — §AV·§AW 의 "우리 대표 조성" 이 **관문 탈락 구조**였습니다 (2026-09-24 11:4x, 데스크탑)

**제기**: 사용자 — *"saIm100은 구조적 불안정성으로 디자인 불가능한 구조 아니었어?"*
**창**: `COREWC_VERDICT_20260924.md` 의 이의 창(09-24 19:1x) 안. **이의 수용합니다.**
**범위**: §AV(`COREPOP_RESULT_20260923.md`, 확정됨) · §AW/§AW-2(`COREWC_VERDICT_20260924.md`) ·
Notion 학회 초안. **계산은 하나도 안 바뀝니다 — 어느 값을 "우리" 로 세웠는가만 바뀝니다.**

## 1. 사실

    risk_results_v3.json   criteria: PLD ≥ 3.3 · LCD_drop ≤ 20.0 % · AV ≥ 20 · min_dist ≥ 0.7
      saIm100    LCD_drop **22.50 %**   pass **false**   (converged false, outer_loops 12)
      saIm075    LCD_drop  20.26 %      pass false
      mslm075    LCD_drop  21.67 %      pass false
      → 31조성 중 **28 통과 · 3 탈락**. 격자 판(risk_results_v3grid)도 saIm0625 20.87 ·
        saIm0667 20.65 · saIm0875 21.21 **전부 탈락**, 통과 상한은 **saIm0583 (15.71 %)**.

저장소는 이것을 **09-04 에 이미 확정**했습니다 — `ASSIGN_20260904.md:355`,
`PREREG_CHECKLIST.md:275`(failed 목록에 saIm100), `T_SREP_VERDICT_20260905.md §(가)`
*"saIm100 은 09-04 에 확정된 관문 탈락이다"*, `MULTILIGAND_20260905.md §6-1`
*"탈락은 확실합니다"*, 그리고 `MAGI/MAGI-004_sulfonic-strongest.md ㉣` 은 못 박아 뒀습니다 —

> **"고술폰산(≥ 62.5 %)은 관문(LCD −20 %)과 문헌(골격 붕괴)로 인용 불가 — saIm100 150.9 는 못 쓴다."**

그런데 §AV(09-23)와 §AW(09-24)가 **바로 그 150.9 를 헤드라인으로 세웠습니다.**

## 2. 왜 못 봤는가 — 값을 고르는 곳에 관문이 없습니다

`analyze_core_wc.py:55` 가 `saim100 = max(ourL)` 입니다. `ourL` 은 `results_v3.json` 의
`loading_015bar` 이고, **그 파일에는 관문 칸이 없습니다.** `status` 는 31종 전부 `ok` —
그건 **GCMC 가 완주했다**는 뜻이지 **구조가 쓸 수 있다**는 뜻이 아닙니다.
관문 판정은 `risk_results_v3.json` 에만 있습니다.

> **CLAUDE.md §0 의 그 무늬입니다 — 실패가 결과처럼 보입니다.**
> 탈락 구조가 결과 파일에서 `status: ok` 로, 그것도 **최대값**으로 앉아 있고
> 아무 표지가 없습니다. `max()` 를 쓰는 모든 자리가 이 구멍 위에 있습니다.
> 09-21 의 이어받기 결함과 같은 계열입니다: **값이 아니라 표지가 자입니다.**

등록 문서(§AV `COREPOP_REGISTRATION §5`, §AW `COREWC_REGISTRATION §4`, §AW-2 `COREWC2 §5`)
어디에도 "관문 통과만" 이라는 단서가 없습니다. **등록 단계에서 빠진 것이고, 판정이 그걸 물려받았습니다.**

## 3. 바뀌는 수 — 관문 통과만으로 다시 잰 백분위 (모집단 524종, 백분위 정의·자료 동일)

| 조성 | 관문 | 선택도 | K_H(CO₂) | 0.15 bar 적재 |
|---|---|---|---|---|
| `saIm100` (원 판정) | **탈락 22.5 %** | 150.91 → **94.8 %** | 4.314e-04 → **82.6 %** | 1.543 → **59.7 %** |
| `saIm050` (통과·선택도 최대) | 통과 13.75 % | 65.21 → **87.0 %** | 1.757e-04 → **64.9 %** | 1.239 → **53.4 %** |
| `mslm050` (통과·적재 최대) | 통과 6.68 % | 58.11 → **84.7 %** | 1.998e-04 → **67.7 %** | 1.243 → **53.4 %** |
| **`saIm0583` (설계 후보)** | 통과 15.71 % | 59.21 → **85.1 %** | 1.886e-04 → **66.6 %** | 1.325 → **55.0 %** |

    우리 중앙   31조성 0.752 → 28.6 %    대   관문 통과 28조성 0.730 → **27.1 %**
    풀 중앙 1.1647 대비   우리 최대(원) 0.75배  →  관문 통과 최대 **0.94배** · saIm0583 **0.88배**

**"선택도 상위 5 %" 는 죽습니다.** 관문을 통과한 우리 최고 조성은 선택도 **87.0 %(상위 13 %)**,
CO₂ 친화도 **67 %(상위 33 %)** 입니다. §AV §1 의 *"상위 5 %, 상위 17 %"* 문장을 **철회**합니다.

## 4. 안 바뀌는 것

- **§AW 의 방향은 그대로입니다** — 선택도 85~87 % > K_H 65~68 % > 적재 53~55 %.
  *"선택도로 이기고 용량으로 진다"* 는 관문 통과 조성에서 **오히려 더 또렷합니다**(적재가 더 내려감).
- **등록 예측 P2·P3 는 판정 그대로 둡니다**(59.7 % / 28.6 %, 둘 다 빗나감). 사전 등록된 양이고
  **결과를 보고 고치지 않습니다**(CLAUDE.md §2). 위 §3 은 **등록 밖(서술)** 재계산입니다.
- P1·P4·P5·P6·P8 은 **모집단 쪽 양**이라 무관 — 전부 그대로.
- **P7 은 관문 통과 조성으로 다시 재도 통과**합니다(72종 대 524종):
  mslm050 0.7 %p · saIm0583 0.6 %p · 통과 중앙 0.7 %p — 전부 ±5 %p 안. "72종 중" 단서 해제 유지.
- 모집단 정의(관문 통과·SI 출처·≤400원자·UFF_MOF 표현 가능 **524종**) 그대로.
- 우리 32조성 자체의 순위·1.5 규칙 판정·Q_st 창·T-C10·D8 판정 — 전부 무관.

## 5. 앞으로 지키는 것

    (a) **발표·문서에서 saIm100 / saIm075 / mslm075 / saIm0625 / saIm0667 / saIm0875 의
        값을 "우리 물질" 로 인용 금지.** 상한 참고로 쓸 때는 **"관문 탈락(LCD 감소 22.5 %)"** 병기.
    (b) 학회 초안의 대표 조성은 **saIm0583** — 관문 통과 상한이고 앙상블(e1~e10)·T-C10·
        습윤 WC·밀도맵이 전부 이 조성에 붙어 있습니다. 세 축 병기: **선택도 85.1 · K_H 66.6 · 적재 55.0 %**.
    (c) **`max()`/`min()` 으로 우리 대표를 고르는 코드는 관문 열을 먼저 잇습니다.**
        `results_v3*.json` 에는 관문 칸이 없으므로 `risk_results_v3*.json` 을 조인해야 합니다.
        등록 문서에도 "관문 통과 조성 중" 을 **등록 시점에** 적습니다.
    (d) 이 문서를 §AV·§AW 판정문에서 가리킵니다(두 문서에 정정 절 추가).

셈 `~/.claude_work/gate_recheck.py`(원 셈 `analyze_core_wc.py` 의 `load_all`·`pct` 를 그대로 import —
자료·백분위 정의가 원 판정과 같습니다). 자료: `core_wc_results*.json` 524 · `core_pop_merged.json` ·
`results_v3.json` · `results_v3grid.json` · `risk_results_v3.json` · `risk_results_v3grid.json`.
