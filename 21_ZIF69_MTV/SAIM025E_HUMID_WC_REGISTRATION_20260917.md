# §AK 등록 — **saIm025e1~e5 앞단 + 습윤 TSA 작업 용량**(298 K 표준 조건, 수정 힘장) — laptop2 배정 (2026-09-17 12:5x, 종합자; **자료 0건**; 사용자 승인 "셋 묶어 착수해")

**왜**: WC §7 의 정직한 문장은 "승자 지표에서 술폰산 단일·혼합(·술포닐 n=1)은 한 덩어리이고, **니트로·모체·저술폰산은 아래**" 입니다.
09-17 에 니트로가 n=5 로 채워졌고(§AI), 술포닐(mslm050e)은 데스크탑에서 도는 중입니다(§AJ). **남은 n=1 은 저술폰산 saIm025(0.7608) 하나**입니다(모체 base 는 자리 배치 자유도가 0 이라 앙상블이 없습니다).
MAGI-004 §6-5 는 이 구멍 때문에 허용 문장 "−SO₃H 25~58 %" 를 **050·58 % 로 축소**해야 했습니다 — `charged_v3` 에 `saIm025e*` 0건. 이 배정이 그 축소를 되돌릴 수 있는 유일한 자료입니다.
laptop2 는 §AI 완주(09-17 05:55) 이후 유휴이고 등록된 미착수 배정이 없습니다(ASSIGN 은 §AJ 까지) → CLAUDE.md §9.

## 0. 이 등록이 §AI 와 다른 점 — **앞단이 붙습니다**

`saIm025e1~e5` 는 **구조가 없습니다.** 다른 앙상블과 달리 빌드부터 시작합니다.

    ① 빌드   build_ensemble_025.py (신규, 2026-09-17)  — build_ensemble_0500.py 의 안전장치 그대로
    ② 이완   relax_series_v3.py (xtb GFN-FF)
    ③ 판정   judge_relax_v3.py — 5/5 pass 아니면 사슬 정지
    ④ 전하   charge_v3.py (PACMAN DDEC6)
    ⑤ 관문   risk_screen_v3_sub.py (Zeo++) — **RASPA 0 인 상태에서만**(CLAUDE.md §5)
    ⑥ 습윤 WC  run_humid_wc_v3w.py 15작업 — §AI 와 완전히 같은 러너·조건

사슬 스크립트 `chain_saim025e.sh` 가 이 순서를 고정하고 각 단계에서 개수를 검사합니다.
**⑤ 가 rc≠0 이면 ⑥ 을 띄우지 않고 멈춥니다** — 관문 밖 구조로 WC 를 돌리면 "관문 안 비교" 가 깨집니다.
    **[09-18 11:46 보완 — 자료 0건(WC 결과 없음) 시점]** laptop2 에 `lammps_mof`(lammps_interface·lmp_serial) 가 없어 ⑤ 가 **rc=2(실행 불가, 탈락 아님)** 로 멈췄고, ⑤ 는 데스크탑이 인계합니다(RASPA 0 창에서, `.claude_work_gate_saim025e.sh`).
    그 사이 **⑥ 을 laptop2 에서 먼저 띄울 수 있습니다**(MAGI §9-6 "먼저 띄우기" 의 조건 그대로): ⑤ 통과 전에는 ⑥ 결과를 **읽지 않고**, ⑤ 에서 어느 실현이 탈락하면 그 실현의 WC 는 **읽지 않고 폐기**하며, 이미 돈 계산은 **중단·판정에서 매몰비용으로 쓰지 않습니다.** 관문 밖 구조의 수가 판정에 섞이는 일은 이 규칙으로 막힙니다 — 낭비될 수 있는 것은 계산 시간뿐입니다.

### 빌더 검산 (자료 0건 시점에 적음)

    FRAC 6/24 · WANT 6 · 목표 5실현 · 시드 1..12
    EXPECT_EL = Zn 24 · C 240 · N 120 · O 66 · Cl 18 · S 6 · H 150  (합 624 = 600 + 4×6)
    **손으로 유도하지 않았습니다** — `structures_v2/ZIF69_saIm025.cif`(seed=0 생산 구조)를 ase 로 읽은 값입니다(2026-09-17 12:4x).
    독립 검산: Cl + S = 24 (자리 보존). saIm050 의 648 = 600 + 4×12 와 같은 꼴.
    기각 시드는 CIF·meta 를 지웁니다 — `relax_series_v3.py` 가 `structures_v2` 를 전량 스캔하므로 남기면 그대로 계산에 태워집니다(빌더 주석의 08-21 교훈).

## 1. 조건·러너 — §AI(nbIm050e)·§AC 2차 앙상블과 완전히 같음
    `run_humid_wc_v3w.py` (298 K·RH90 P_H2O 2,852.1 Pa → 373 K TSA / 298 K·0.05 bar VSA), CLAUDE.md §1 고정값.
    **파일 관문 md5 8e8ec933…** 선행(러너가 검사). 머리말 관문은 첫 Output 에서.
    HWC_V3W_TARGETS=saIm025e1,…,e5  HWC_V3W_WORKERS=12  HWC_V3W_RESULT=humid_working_capacity_w2_saIm025e_laptop2.json
    15작업(5 × ads/tsa/vsa). 소요: §AI(nbIm050e) 가 11h15m 이었고 saIm025 는 물이 더 붙는 계라 **12~20 h** 로 봅니다. 앞단 ①~⑤ 는 **4~8 h** 추정(데스크탑 mslm050e 앞단 실측 ≈ 4 h, laptop2 는 물리 6코어). 사슬 전체 **16~28 h**.

## 2. 관측량·자 (결과 전 고정) — WC §7 과 같은 자
    WC_TSA(k)·WC_VSA(k) ± (단위). 앙상블 평균 ± SD(**배치 단위, 정본 SD 형**) · SE 형 · 단위(±) **세 값 병기**(CLAUDE.md §2 — 한쪽만 인용하면 같은 자료가 문턱 양쪽에 놓입니다).
    SD 형 = d / √(SD₁² + SD₂²) · SE 형 = d / √(SE₁² + SE₂²). 문턱 **1.5**, 어느 쪽도 옮기지 않습니다.
    비교(㉨ n=5 끼리): saIm025e 대 saIm050e(0.8074 ± 0.0960) · saIm0583e(0.8247 ± 0.0566) · sa50nb50e(0.7591 ± 0.0629) · nbIm050e(0.6808 ± 0.0112).
    모체 base 0.4892(자유도 0)는 saIm025e SD 를 분모로.

## 3. 예측 (자료 0건) — 틀리면 즉시 물린다

**T-SA-2("생산 실현은 앙상블 평균 위")는 09-17 에 여섯째 점에서 반증돼 철회됐습니다. 그러므로 여기서 쓰지 않습니다.**
생산 실현 saIm025 = 0.7608 은 **예측의 근거가 아니라 대조점**으로만 적습니다.

    (가) 평균 구간        saIm025e 평균 WC_TSA ∈ **0.65 ~ 0.85**
                          (네 앙상블의 평균/생산 비 0.851·0.856·0.949·1.021 × 0.7608 = 0.647~0.777 을 양쪽으로 넓힘)
    (나) 부호             대 saIm050e · saIm0583e **아래(−)** · 대 base **위(+)** · 대 nbIm050e **위(+)**
    (다) 갈림 여부        대 saIm050e **못 가름**(예상 SD 형 −0.5~−1.3) — saIm050e 의 SD 가 0.0960 으로 커서 자가 무딥니다
                          대 saIm0583e **경계**(−1.0~−2.0) · 대 base **갈림 +**(≥1.5) · 대 nbIm050e **경계**(+0.5~+1.5)
    (라) 배치 단위 SD     0.03 ~ 0.10 (관찰용, 판정 축 아님 — nbIm050e 0.0112 과 saIm050e 0.0960 사이라는 것 말고는 근거가 없습니다)
    (마) 앞단            빌드 5/5(시드 1..12 안) · 이완 판정 5/5 pass · 관문 7/7 통과

**(다)를 이렇게 적는 이유를 미리 적습니다**: "저술폰산은 아래" 를 n=5 로 **세우지 못할 것**으로 봅니다.
니트로가 차 −0.1266 으로도 saIm050e 대비 −1.31(못 가름)이었으므로, 차가 그보다 작을 저술폰산이 문턱을 넘을 가능성은 낮습니다.
**이것은 실패가 아니라 자의 한계입니다** — 그렇게 나오면 그렇게 적습니다(SE 형·단위 병기).

    반증: 평균이 (가) 밖 / 어느 부호든 (나)와 반대 / base 대비 못 가름 / 앞단 어느 단계든 5/5 미달.

## 4. 자료 모양(⑩)
    네 쌍 각각 {갈림−, 못 가름, 갈림+} + base 쌍.
    이름: 대 saIm050e·saIm0583e 가 **둘 다 갈림−** 이라야 "저술폰산은 술폰산 단일 아래(n=5)" 라고 적습니다.
          하나라도 못 가름이면 **"저술폰산은 saIm0583e 아래이고 saIm050e 와는 못 가른다"** 처럼 **쌍별로** 적습니다(순위 없음).
          어느 하나 갈림+ 이면 예측 반증 — WC §7 의 "저술폰산은 아래" 문장을 **철회**합니다.
    결측: 상태 'timeout'/'no-output'/'물 순전하 이상'/'다른세션실행중' 은 결측 — 그 k 를 빼고 n 을 적되 **n<5 면 ㉨ 로 앙상블 비교는 병기만**.
    앞단 결측: 빌드가 5개를 못 채우면(시드 1..12 소진) **n<5 로 진행하지 않고 멈춥니다** — 종합자에게 보고, 시드 상한 확장은 별도 등록.
    기기: laptop2 산출. §AG 로 기기 항 없음이 확인돼 있습니다(0.06 단위) — 기기 열만 병기.

## 5. 이 시험이 말할 수 없는 것
    물 크기(㉦, 힘장의 유일 평형·외부 실측 대비 미검증) · 실온도(T-RT-1 별도) · saIm025 의 건조 축(n=1 그대로, `results_v3.json`).
    **MAGI-004 §6-5 의 "−SO₃H 25~58 %" 축소를 되돌릴 수 있는지**는 이 결과를 보고 종합자가 판정합니다 — 여기서 미리 약속하지 않습니다.

## 6. 착수 절차 — laptop2(Balthasar)
    1) `git pull master` → 이 문서·`build_ensemble_025.py`·`chain_saim025e.sh` 확인.
    2) `bash bgstate.sh`: **simulate 0** 확인(사슬 ⓪ 가 다시 검사합니다 — Zeo++ 와 RASPA 동시 금지).
       `structures_v2/ZIF69_saIm025e*.cif` 0건 확인(있으면 종합자에게 먼저 보고 — 이어받기 함정, CLAUDE.md §3).
    3) 경로가 데스크탑과 다르면 `CZ=` `CT=` 로 덮어서 기동:

           setsid nohup bash chain_saim025e.sh > ../.claude_work_saim025e_l2.out 2>&1 < /dev/null &

       착수 시각은 `date` 값으로 적습니다. **PID 추적은 이 러너에서 쓰지 마십시오** — §AI 에서 세 번 틀렸습니다(과도기 PID).
       감시는 **일 기반**: 단계 로그 `saim025e_chain.log` 와 결과 파일 생성.
    4) 보고는 **세 번**: 착수 · 앞단 ⑤ 관문 rc · 완주(또는 이상). 결과 JSON 을 master 에 푸시하고 해시를 우편함에.
       **판정문은 쓰지 않습니다**(§2 자 판정은 종합자 몫, 검산 Melchior·Balthasar).

## 7. 진행 기록
    [09-17 12:5x] 등록(자료 0건). 사용자 승인 "셋 묶어 착수해"(12:3x) — 30분 규칙 대기 없음.
    [09-18 00:16] laptop2 착수 → **① 빌드 2초 만에 실패**: `ModuleNotFoundError: rdkit`(laptop2 czeromof 가 `czeromof.yml:197 rdkit==2026.3.4` 명세에서 이탈). 확인 셋 통과, 부분 산출물 없음. laptop2 가 환경을 손대지 않고 보고 — 옳음.
    [09-18 00:20] **(ㄴ) 데스크탑 빌드**(nice 19, RASPA 8 옆): 시드 1~5 전부 채택, 5/5, 원자 624·원소 일치·융합 0·금지접촉 0·고아 0, `rebuild_index` 109→114. `structures_v2/ZIF69_saIm025e1~5.cif`+meta 를 master 에 푸시.
    [09-18 00:22] `chain_saim025e.sh` 수정(어디서도 돌지 않는 상태): CIF 5개 있으면 ① 건너뜀 · ②-앞에 `XTB_BIN` 존재 검사(기본 `~/miniconda3/envs/spectra/bin/xtb`). laptop2 는 pull 뒤 같은 기동 줄로 ②부터.
    [09-18 00:23] laptop2 재기동(① 건너뜀 확인, xtb spectra/bin) → ② 이완 4h57m(00:23→05:20) rc=0 · ③ 판정 e1~e5 pass · ④ 전하 5/5 → master `3c81d1a`. 앞단 견적 4~8 h, 실측 5h04m.
    [09-18 05:27] **⑤ rc=2 — 실행 불가**(laptop2 에 lammps_mof 없음; 관문 스크립트 사전검사가 스스로 멈춤, 구조 평가 0건). rdkit 과 같은 계열의 환경 이탈. 사슬은 설계대로 ⑥ 미착수.
    [09-18 11:46] 데스크탑 인계: 감시기 `.claude_work_gate_saim025e.sh`(PID 18198) — `simulate` 0 이 3분 연속이면 ⑤ 착수(RISK_SUB_TAGS base,saIm025,saIm025e1~5; lammps_mof python; 워커는 스크립트가 MemAvailable 로 계산), rc=0 이면 결과 자동 커밋·푸시. T-RT-1b 가 3작업 남아(12/15) 몇 시간 안에 창이 열림. §0 보완대로 laptop2 는 ⑥ 선착수.
