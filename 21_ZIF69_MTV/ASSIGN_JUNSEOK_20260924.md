# Junseok 복귀 배정 (2026-09-24 **07:35**, 데스크탑)

**복귀 확인**: 09-24 07:19 부팅 · 힘장 관문 자가 복구(md5 73046fd7 → **8e8ec933**) · 계산 0 · C: 93 GB.
**"은퇴" 는 감사의 추론이었습니다** — 결정한 사람이 없었고 등록부는 🟢 그대로였습니다. Junseok 정정 수용.

## 0. 제 몫으로 처리한 것

    ① postman 반입 제외에서 `junseok` **해제**(`postman.sh` (15)) — 결과가 master 로 옵니다
    ② 등록부(`COMMS.md`) Junseok 행 갱신
    ④ **postman 띄우십시오.** 가지는 등록부대로 **`junseok-20260822`** 하나입니다.
       `origin/junseok`(08-29 stray)은 그대로 둬도 됩니다 — 제 (12) 반입이 **파일별 조상 검사**를
       하므로 이미 master 에 병합된 가지에서는 아무것도 안 집습니다.
       ⚠ **재부팅 뒤 postman 을 되살리는 것이 없습니다**(09-23 laptop2 실측: 9시간 20분 배달 공백).
       러너 기동 줄에 `postman` 생존 확인을 넣으십시오. stderr 를 버리지 마시고요(`2>> .postman_junseok.err`).

## 1. ③ 배정 — **밀도맵: mslm 사다리 완성 + cf3Im 사다리 신설**

    python run_density_v3_junseok.py          # 덱은 파일 안에 있습니다

    대상   mslm025 · mslm075 · cf3Im025 · cf3Im050 · cf3Im075   (5종 × 전하 ON/OFF = **10작업**)
    규약   0.15 bar · 298 K · 초기화 2,000 + 생산 5,000 · 90³ 격자 · DDEC6 — `run_density_v3.py` 와 동일
    출력   `density_v3_junseok/` (기존 격자 폴더를 안 덮습니다)
    워커   기본 10 (`DENSITY_JUNSEOK_WORKERS`) — 등록부의 SMT 실측 6→10 워커 +55 % 를 씁니다

### 왜 이것인가

격자는 **사다리 단위**여야 "치환율을 올리면 CO₂ 자리가 어떻게 변하는가" 를 그림으로 말합니다.

    saIm   025 ✓ 050 ✓ 0583 ✓ 075 ✓ 100 ✓     완성
    nbIm   025 ✓ 050 ✓ 075 ✓ 100 ✓            완성
    mslm   025 —  050 ✓  075 —                 ← **두 칸만 채우면 완성** (선택도 58.1, saIm 다음)
    cf3Im  025 —  050 —  075 —                 ← 통째로 빔 (선택도 37.5)

이 다섯이면 **사다리 넷**이 섭니다. 09-23 에 사용자가 밀도맵을 직접 지시했고, 데스크탑이
`saIm0583`(§AU 정체점)·`mslm050` 을 06:20 에 건조 완주했습니다(습윤 RH90 은 진행 중).

### 왜 이 기기에 이것인가 — 두 제약을 둘 다 만족합니다

    ⚠ **재시작 창**  Windows 업데이트 일시중지가 09-17 에 만료돼 **02~08시** 자동 재시작 창이 있습니다.
      → 밀도맵은 작업 하나가 짧고 **조성×전하 단위로 독립**이라 재시작이 나도 **도는 한 건만** 잃고,
        `run_density_map` 이어받기가 완주분을 회수합니다. **짧은 작업 권고에 맞습니다.**
    ⚠ **OOM 이력**  08-27 에 이 기기가 `ZEO_GB_PER_JOB` 9.5→4.0 으로 3분 만에 global OOM 을 냈습니다.
      → `run_density_map.py` 는 **Zeo++ `network` 를 아예 안 부릅니다**(RASPA 만, 건당 약 471 MB).
        **그 경로가 이 배정에 없습니다.** `ZEO_GB_PER_JOB`·`RISK_WORKERS` 손대실 일이 없습니다.

## 2. 다음 대기 — `ASSIGN_20260903.md §2` (여전히 유효, 다만 **재시작 창 해소 뒤**)

세 라운드(09-03·09-04·09-05) 연속 "복귀 시 착수" 로 유지된 배정입니다. **살아 있습니다** —
09-07 의 `V3W_CHUNK_VS_SINGLE` 은 **표류(Δ40)** 시험이지 s_rep 의 **조성 축**이 아닙니다.

    무엇   `nbIm025` RH0 4회, Junseok 자기 물 러너 그대로, 동시성 1
    대조   Junseok 자신의 e1 R̂ = 0.008424 (n=8, dof 7)
    판정   F = s²(nbIm025, dof 3)/s²(e1, dof 7) · 임계 F(0.975,3,7)=5.89 · F(0.025,3,7)=0.068
    비용   약 6 h 순차 — **02~08시 창을 가로지릅니다. 그래서 지금은 안 겁니다.**

> ⚠ **착수 전 확인이 하나 필요합니다.** 대조군 R̂=0.008424 는 **09-06 힘장 수정 전** 값입니다.
> 그 수정은 `Hw none`/`Lw none` **두 항 추가**(55→57)이고 §2 는 **RH0(물 없음)** 이라
> 물리적으로는 영향이 없어야 합니다 — **그러나 확인 없이 가정하지 마십시오.**
> 권고: **e1 RH0 를 새 힘장으로 1회 더 돌려** 옛 n=8 분포 안에 드는지 보고 나서 4회를 시작하십시오.
> 이 1회는 §2 의 문턱을 **안 건드립니다**(판정 기준 불변, 전제 확인일 뿐).

## 3. 이 기기가 지금 알아야 할 변경 (26일 공백분)

    09-06  **물 힘장** `Hw none`/`Lw none` (55→57). RH>0 옛 결과는 결함판 — `v3w_*` 계열로만 인용.
           **이미 자가 복구하셨습니다**(8e8ec933).
    09-11/18 **Widom Q_st 의 RT 부호** — 러너 10곳이 `−ΔU − RT` 였음. 저장소 Q_st 전부 2RT=4.96 낮았음.
           결과 JSON 은 안 고치고 `*_rt_corrected` 사이드카. **Q_st 목표대 = 보정 척도 35~45 kJ/mol.**
    09-21  **이어받기가 중간값을 완주값으로 받던 구멍** — `finished()`('Simulation finished') + 모드별
           필수값을 요구하도록 고침. **값이 아니라 표지가 자입니다.**
    09-21~24 **postman** (12) 반입을 state 에서 분리(트리 직접 대조) · (13)(14) push 거절 시 자가 복구
           · (15) junseok 제외 해제.
    09-23  **LCD 가 두 척도**입니다 — 이완 후 LCD(정본, `results_v3*.json`)와 탐침 척도(`risk_results*`)가
           **부호 반대 상관**을 냅니다. 장표에 쓰면 어느 척도인지 **반드시 병기**.

## 4. 보고

완주하면 `COMMS/junseok.md` 에 적고 `density_v3_junseok/density_results.json` 을 밀어 주십시오.
postman 이 뜨면 자동으로 옵니다(결과 글롭 `21_ZIF69_MTV/results_*.json` 에 걸립니다 —
`density_results.json` 은 그 글롭 **밖**이니 **손 커밋이 필요합니다.** 패턴 추가가 필요하면 말씀 주십시오).
