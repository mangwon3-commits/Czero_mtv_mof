# 치환 구조가 전부 깨져 있었습니다 — 원인은 치환기가 아니라 모체입니다

2026-08-14. 수분 안정성 2단계(`HYDROLYSIS.md`)의 계를 조립하다가 걸렸습니다.
**찾으러 간 것이 아니라 걸려 넘어진 것입니다.**

> **이 문서는 하루 사이에 두 번 고쳐 썼습니다.** 처음에는 "인접 술폰산끼리 관통"이
> 원인이라고 적었고, −SO₃H 의 상한이 75%, 다시 62.5% 라고 적었습니다.
> **둘 다 틀렸습니다.** 진짜 원인은 아래 2절이고, 그것을 고치자 상한 자체가
> 사라졌습니다(100% 도 깨끗하게 만들어집니다). 틀린 중간 결론을 지우지 않고
> 남기는 이유는, 어느 단계에서 무엇을 잘못 봤는지가 다음 사람에게 필요하기
> 때문입니다.

---

## 1. 무엇을 봤나

수분 MD 용 LAMMPS 계를 만들다가 황 하나에 결합이 **5개** 붙는 것을 봤습니다.
술폰산의 황은 4결합(C 하나 + O 셋)이어야 합니다. 원 구조를 열어 보니

```
ZIF69_saIm100.cif
  S–S   2.642 Å      서로 다른 두 술폰산기
  O–O   0.924 Å      그 두 기의 산소
  S–H   0.952 Å      황이 이웃 고리의 수소를 뚫고 들어가 있음
```

산소 두 개가 0.924 Å 떨어져 있습니다. 어떤 화학에도 이런 O–O 는 없습니다.

## 2. 진짜 원인 — 모체 고리가 일그러져 있습니다

빌더는 프래그먼트의 고리 9원자를 결정구조의 고리에 Kabsch 정합시킵니다.
**그런데 치환기 방향은 정합에 참여하지 않습니다** — RDKit 이 이상적으로 임베딩한
프래그먼트가 정해 준 방향을 그대로 씁니다.

이 ZIF-69 모체는 고리가 상당히 일그러져 있습니다.

```
벤조 C–C    1.273 Å     (방향족 정상 1.39)
C–Cl        1.982 Å     (정상 1.73)
```

그래서 **이상적 프래그먼트가 가리키는 방향**과 **결정구조가 실제로 Cl 을 놓아 둔
방향**이 어긋나고, 치환기가 이웃 고리 수소 쪽으로 밀려납니다. 실측:

| 치환기 | 고리 H 까지 |
|---|---|
| −SO₃H (황) | 0.952 Å |
| −CH₃ (탄소) | 0.95 Å |
| −F | **0.561 Å** |

**치환기 종류와 무관하게 전부 나타납니다.** 원인이 치환기가 아니라 모체이기
때문입니다. −F 의 0.561 Å 은 `LINKER_DESIGN_BRIEF.md` 5-6절에 이미 기록돼 있던
값입니다 — 그때는 "−F 가 원자 하나라 회전 자유도가 없어서"로 진단했는데,
**같은 값이 −CH₃ 와 −SO₃H 에서도 나온다는 사실이 그 진단을 반증합니다.**

그리고 이것이 1절의 관통도 만들었습니다. 방향이 틀어진 채로 놓이니 인접 치환기끼리
부딪혔던 것입니다. **방향을 고치자 배타적 자리쌍이 0개가 됐습니다.**

### 부차적 원인 — 회전 최적화가 순차적이었습니다

각 치환기가 **아직 배치되지 않은 이웃을 고려하지 못한 채** 각도를 골랐습니다.
−NO₂ 는 쌍별 최적이 2.90 Å 인데 실제 구조는 1.142 Å 이었습니다. 기하학이
불가능해서가 아니라 배치 순서 때문에 도달하지 못한 것입니다.

## 3. 왜 지금까지 안 걸렸나

`audit_orphans.py` 는 네 구조 모두 "고아 0 / 충돌 0 / 분리 0" 으로 통과시킵니다.
검사기가 고장 난 게 아니라, 결함이 검사기의 예외 조항 안으로 들어가 앉았습니다.

`audit_orphans.py:57` 은 비결합 충돌을 세면서 **1,3 쌍(공통 이웃을 가진 원자쌍)을
제외**합니다. 메틸의 geminal H–H 를 충돌로 세면 순수 ZIF-8 조차 결함이 되므로
그 자체로는 옳은 처리입니다. 그런데

```
겹침이 얕으면   ->  두 원자가 그냥 가깝다             ->  충돌로 잡힌다
겹침이 깊으면   ->  둘 사이에 '결합' 이 인식된다      ->  공통 이웃이 생긴다
                ->  1,3 쌍으로 분류된다              ->  예외로 빠진다
```

**결함이 충분히 심해지면 결함 탐지기에서 스스로 사라집니다.**

> 이 구멍은 **이미 문서에 적혀 있었습니다.** `LINKER_DESIGN_BRIEF.md` 5-6절:
> "1,3-쌍 제외 로직에 원자가 개수 상한 검사를 추가하는 것이 이 감사 스크립트의
> 남은 구멍입니다." 적어 두고 막지 않은 구멍이 그대로 다시 열렸고, 이번에는
> 프로젝트의 주력 결과를 통째로 삼켰습니다.

`min_pairwise_distance` 도 못 잡습니다. `structures/*.meta.json` 에 0.9238 로
**기록되어 있는데**, 빌더 하한이 0.7 Å(구조 파손 기준)이라 통과했습니다.
0.92 Å 는 O–H 결합 길이대라 "결합 하나가 좀 짧은가 보다" 로 보입니다.

## 4. 새 검사기 — `check_substituent_clash.py`

검사 셋. 서로 다른 것을 잡습니다.

| 검사 | 내용 |
|---|---|
| **(A) 성분 수** | ZIF-69 단위셀 = Zn 24 + nIm 24 + 벤즈이미다졸레이트 24 = **72개**. 이보다 적으면 링커가 융합된 것. 거리를 세지 않으므로 3절의 함정이 **원리적으로 역전**됩니다 — 가짜 결합이 생기는 것 자체가 검출 신호가 됩니다 |
| **(B) 금지 접촉** | 이 라이브러리에서 결합할 수 없는 원소쌍(할로겐–할로겐, 할로겐–O/N/S, O–O)이 **어떤 결합보다도 짧은** 거리에 있는 경우. 공유결합 반지름 합의 0.95배 미만 |
| **(C) 수소 관통** | H 의 결합 상대가 아닌 중원자가 1.2 Å 안에 있는 경우. −F 는 링커를 융합시키지도 중원자 금지쌍을 만들지도 않으면서 H 를 0.561 Å 까지 파고들기 때문에 (A)(B) 만으로는 안 잡힙니다 |

(B) 를 고정 거리로 자르면 안 됩니다 — 니트로기의 geminal O–O 는 2.1 Å, CF₃ 의
F–F 는 2.16 Å 로 **둘 다 정상**입니다. 원소를 봐야 갈립니다.
(C) 의 결합 상대를 "가장 가까운 중원자" 로 두어도 안 됩니다 — 침입자가 진짜 결합보다
가까운 경우가 실제로 있어(−F 0.561 vs C–H 0.951) 멀쩡한 C–H 를 결함으로 신고합니다.

> **검증**: 이 검사기는 `LINKER_DESIGN_BRIEF.md` 13-1절이 수동으로 찾아 둔
> fbIm 의 F–H **0.561 Å** 을 그대로 재현합니다. 답을 아는 경우로 확인한 것입니다.

```
python check_substituent_clash.py 'charged/base_DDEC6.cif' 'structures/ZIF69_*.cif'
```

## 5. 고친 것

`mtv_cif_builder.py`:

1. **`_aligned_substituent`** — 치환기를 **결정구조가 실제로 비워 준 방향**에 맞춰
   한 번 더 돌립니다. 결합 축을 결정구조에서 가져오는 것이므로 임의의 보정이
   아닙니다. **이것이 핵심 수정입니다.**
2. **`optimize_substituent_rotations`** — 순차 배치를 좌표 상승법 동시 최적화로.
   목적함수는 거리가 아니라 **vdW 여유**이고, 자기 고리는 장애물에서 뺍니다
   (거리로 두면 S–C 결합 1.83 Å 이 최솟값을 차지해 모든 각도가 동점이 됩니다).
3. **`substituent_conflict_graph` / `forcing_pairs` / `choose_conflict_free_sites`** —
   배타쌍과 강제쌍을 함께 푸는 충돌 회피 자리 선택. 1 을 고친 뒤로는 **-SO₃H 에서
   두 종류 모두 0개**가 되어 실제로는 발동하지 않지만, 다른 치환기·다른 모체에서
   필요할 수 있으므로 남겨 둡니다. 달성 불가능한 치환율은 조용히 낮추지 않고
   **예외를 던집니다.**
4. `obabel` 을 PATH 대신 같은 환경의 bin 에서 찾도록.

## 6. 결과 — `structures_v2/`, 30종 중 26종 통과

**기존 `structures/` 는 덮지 않았습니다.** 옛 결과의 재현 수단을 남겨야 합니다.
전수 결과는 `rebuild_index.json` 에 있습니다.

| 계열 | 025 | 050 | 075 | 100 |
|---|---|---|---|---|
| **saIm** (−SO₃H) | ✅ | ✅ | ✅ | ✅ |
| mslm (−SO₂CH₃) | ✅ | ✅ | ✅ | — |
| cf3Im (−CF₃) | ✅ | ✅ | ✅ | — |
| nbIm (−NO₂) | ✅ | ✅ | ✅ | ✅ |
| brbIm (−Br) | ✅ | ✅ | ✅ | ✅ |
| cnbIm (−CN) | ✅ | ✅ | ✅ | ✅ |
| fbIm (−F) | ✅ | ✅ | ✅ | ✅ |
| **mbIm (−CH₃)** | ❌ | ❌ | ❌ | ❌ |

두 가지가 눈에 띕니다.

- **−SO₃H 는 100% 까지 깨끗합니다.** 2절을 고치자 상한이 사라졌습니다.
- **−CN 과 −F 가 전 조성 통과합니다.** 둘 다 `LINKER_DESIGN_BRIEF.md` 가
  "빌더로 만들 수 없는 작용기"로 분류했던 것입니다. 작용기의 성질이 아니라
  빌더의 버그였습니다.

### 남은 것 — −CH₃ 는 아직 안 됩니다

메틸만 전 조성 탈락입니다(금지접촉 3/9/14/18, 링커 융합은 0). 메틸 수소가 고리
탄소에서 **1.057 Å** 입니다. 자기 메틸 탄소와는 1.095 Å 이므로, 메틸이 고리 쪽으로
접혀 들어가 있습니다. 다른 일곱 계열과 달리 **치환기 자신이 수소를 갖는 유일한
경우**라 회전축 주위 수소 배치가 추가로 걸리는 것으로 보입니다.

이번 작업의 대상이 −SO₃H 였으므로 여기서 멈추고 기록해 둡니다. **−CH₃ 대조군을
쓰려면 이걸 먼저 고쳐야 합니다.**

## 7. 영향 범위

| 결과 | 상태 |
|---|---|
| saIm 전 조성 Q_st / 로딩 / 선택도 (목표권 진입의 근거) | **재계산 필요** |
| 수분 경쟁 20/20 (`water_results.json`) | **재계산 필요** |
| 건조·습윤 작업 용량, 재생 에너지 | **재계산 필요** |
| 구조 안정성 17/17 (`risk_results.json`) | **재계산 필요** — 깨진 구조를 이완시킨 값 |
| 아릴 12종 GCMC | **재계산 필요** (mbIm·fbIm 포함. 이들도 H 관통이 있었습니다) |
| gme vs sod 위상 판단, 모체 공동 크기 모형 | 무치환 모체 기반 — **영향 없음** |
| 정전기/분산 분해 (63% 정전기) | 방법은 유효, 숫자는 재계산 필요 |

무치환 모체(`base`)는 검사를 통과했습니다. **대조군과 위상 논지는 살아 있습니다.**

## 8. 다음 (지시 대기)

1. `structures_v2/` 에 PACMAN 전하 부여.
2. 재계산은 값이 싼 것부터: Widom K_H -> GCMC 로딩 -> 수분 경쟁 -> 작업 용량.
3. `check_substituent_clash.py` 를 `run_master_pipeline.py` 의 구조 생성 직후
   관문으로 넣기. `audit_orphans.py` 와 **둘 다** 돌립니다.
4. `audit_orphans.py` 의 1,3 쌍 제외에 원자가 상한 검사 추가
   (`LINKER_DESIGN_BRIEF.md` 5-6절이 요구해 둔 것).
5. 재계산이 끝나면 `DAC_PLAN.md:130` 의 "구조 안정성 17/17 — 조건 무관" 을 고칠 것.
   지금은 두 가지로 틀렸습니다 — 건조 조건 한정이고, 깨진 구조를 이완시킨 값입니다.
