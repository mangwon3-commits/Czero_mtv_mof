# 판정 문턱의 자가 둘입니다 — ~~사용자 결정 사안~~ → **결정 (다) 2026-09-06 10:4x, §7** (2026-08-28 제기)

**세 기기(데스크탑·랩탑·Junseok)가 각자 확인했고, 셋 다 바꾸지 않았습니다.**
`ERROR_BARS.md` 가 *"자를 바꾸려면 모든 과거 결과에 동시에·일괄로 적용해야
하고, 사용자가 명시적으로 결정할 사안"* 이라고 못 박은 자리입니다.

- 발견: Junseok, `mslm050` 물 비교에서 (`6dc0078` → `77397ab`)
- 확대: 랩탑이 등록식 전수 확인 (`6cffaed`)
- 정리: 데스크탑 (`dea163b`, `8ba23a3`)

---

## 1. 무엇이 문제인가

**"1.5 단위" 라는 같은 이름이 두 가지 다른 엄격도를 가리킵니다.**

| 어디 | 분모 | 문턱 | 진짜 σ 로는 |
|---|---|---|---|
| `CLAUDE.md` §2 (통계 비교) | RASPA `±` = **95%CI** | 1.5 | **4.16σ** |
| `judge_mslm050.py:14` | 실현 SD = **진짜 1σ** | 1.5 | **1.5σ** |
| `run_humid_wc_v3ens0500.py:41` | 실현 SD = **진짜 1σ** | 1.5 | **1.5σ** |
| `STAGE2_SAIM075` §3-1 | 표본 SD = **진짜 1σ** | 1.5 | **1.5σ** |

**배치 비교가 통계 비교보다 2.776배 관대합니다.** 오늘까지 아무도 안 짚었습니다.

## 2. 왜 지금까지 안 보였나 — 한 항이 지배하면 자가 안 보입니다

Junseok 관찰입니다. 배치 산포가 통계 오차를 **15배** 압도해서, 자를 섞은
식과 안 섞은 식이 **0.402 대 0.408 — 1.5% 차이**밖에 안 났습니다.

    실제 조건에서   0.402 vs 0.408   ->  아무도 못 봄
    f = 0 극한에서  6.011 vs 2.166   ->  2.776배, 즉시 보임

> **일반화**: 한 항이 지배하는 식에서는 다른 항의 자가 틀려도 안 보입니다.
> **지배하지 않는 상황을 인위적으로 만들어야** 드러납니다.
> §0(*"검사기부터 의심하라"*)의 계산판 — 검사기 대신 **식을 극한으로 밉니다.**

## 3. 무엇이 바뀌고 무엇이 안 바뀌나

### 자에 따라 **갈리는** 것

    LOWRH_18_ANALYSIS  base -> saIm050         σ자 1.80 검출  |  ±자 0.65 미검출
    LOWRH_18_ANALYSIS  ms50nb50 -> sa50nb50    σ자 1.79 검출  |  ±자 0.64 미검출
    MSLM050_CHALLENGE  경계 [0.646, 0.836]     ±자면 훨씬 좁아짐

**여섯 비교 중 둘**이고, **하필 여유가 가장 얇던 둘**입니다(랩탑 확인).
그리고 둘째 칸이 *"−OH 가 물 친화도를 올린다(12자리)"* 를 받치는 칸입니다.
그 진술은 이미 **혼합군 산포** 때문에 조건부였고, 여기에 **자 문제가 두 번째
조건**으로 붙습니다. **조건이 둘이면 사실상 못 씁니다.**

### ⚠️ [11:3x 추가] **머리기사는 살아남되 여유가 3배 → 1.08배로 줄어듭니다**

랩탑이 08-28 오전에 판정 전수를 세어 *"머리기사 세 다리가 전부 배치 자"*
임을 확인했고, **미검출 둘은 어느 자로도 안 바뀐다**고 적었습니다. 맞습니다.
**그런데 검출되는 하나를 안 봤습니다.**

    치환 대 무치환   d = 0.7306 − 0.4692 = 0.2614
    진짜σ 로 통일한 분모 0.05816  ->  **4.49**

    배치 자   4.49 대 문턱 **1.50**   ->  여유 **3.00배**
    통계 자   4.49 대 문턱 **4.16**   ->  여유 **1.08배**

**(가) 통계 자로 통일을 고르시면 머리기사가 통과는 하되 여유가 8% 입니다.**
지금 3배로 보이는 것이 실제로는 그 자에서 아슬합니다.

    미검출 둘은 반대 방향   계열 축 0.15 -> 28배 아래 · 조성 축 0.09 -> 46배 아래
                            (배치 자에서는 10배 · 17배 아래)

> **그러니 선택은 "판정이 뒤집히나" 가 아니라 "머리기사를 얼마나 두껍게
> 주장할 수 있나" 입니다.** 방향은 셋 다 어느 자로도 같습니다.

### 그리고 머리기사 분모 자체가 **두 자를 섞고 있습니다** (랩탑 발견)

    4.42 = 0.2614 / hypot(**배치SD 0.0580**, **base RASPA± 0.01186**)
                          ^진짜 1σ          ^95%CI

    진짜σ 로 통일하면 **4.49**   ·   95%CI 로 통일하면 **1.62**

**효과가 2% 라 판정은 안 바뀝니다. 결함은 실재하고 결과는 무해합니다.**
배치 성분이 분산의 **95.4%** 라 섞인 항의 영향이 작았을 뿐입니다.

> **혼합 비중이 50% 근처인 판정이 나오면** 어느 자로도 못 읽으므로 그때는
> 사용자 결정이 필요합니다. **지금은 이 하나뿐이고 95.4% 라 불필요합니다.**

### 그리고 **같은 비교가 세 값으로** 적혀 있습니다 (랩탑 `TWO_RULERS` 에서 이관)

    d = 0.7306 − 0.4692 = 0.2614

      /0.0237  ->  **11.03**   앙상블 **평균의 SE**        `VERDICT_REVISION` 63행
      /0.0580  ->   **4.51**   앙상블 **배치 SD**          `NEW_MACHINE` 52행
      /0.0591  ->   **4.42**   배치SD ⊕ base RASPA±        `VERDICT_REVISION` 219행 **머리기사**

**셋 다 옳고 셋 다 재현됩니다. 다른 물음의 답입니다:**

    11.0   "이 앙상블 **평균**이 base 와 다른가"
    4.51   "**새 배치를 뽑아도** base 를 이기는가"      <- 머리기사에 맞는 물음
    4.42   위에 base 자신의 통계오차를 얹은 것 (그래서 자가 섞임)

**라벨이 없어 읽는 사람이 못 고릅니다.** 처방은 이 문서 전체와 같습니다 —
**숫자 안 바꾸고 이름만**: `4.51 단위(배치)` · `11.0 단위(평균SE)` ·
`4.42 단위(혼합 95%)`. **혼합은 비중을 같이 적습니다.**

> **결정 문서는 이 파일 하나입니다.** 랩탑 `TWO_RULERS_20260828.md` 는
> 전수 조사 근거이고, **결정과 선택지는 여기에만 둡니다**(랩탑 제안).

### **안 바뀌는** 것

    조성 축 0.09 단위          ->  ±자면 0.03. 둘 다 미검출
    Junseok 물 비교            ->  세 관행 전부 미검출 (0.147 / 0.408 둘 다 문턱 아래)
    관문 2단계 (STAGE2)        ->  Zeo++ 는 결정론적이라 ± 가 **아예 없음**. 자가 하나뿐
    교차 검증류                ->  배치와 무관

## 4. 선택지

    (가) 통계 자로 통일 (4.16σ)   CLAUDE.md 문언에 충실.
                                  배치 비교에서 **거의 아무것도 검출 못 함**
    (나) 배치 자로 통일 (1.5σ)    배치 비교에 실용적.
                                  §2 문언보다 훨씬 느슨. **과거 통계 판정 전부 재검토**
    (다) 둘을 **다른 이름**으로   문턱은 그대로 두고 이름만 가른다.
                                  과거 판정 불변. 인용할 때 자를 병기

> **(다) 를 지지하는 관찰** — 두 자가 **재는 대상이 다릅니다**:
>
>     RASPA ±    **같은 구조를 다시 돌렸을 때**의 요동
>     실현 SD    **다른 배치를 뽑았을 때**의 요동
>
> 애초에 같은 문턱을 붙일 이유가 없었을 수 있습니다. 그렇다면 이것은
> "어느 자가 맞나" 가 아니라 **"두 자가 다른 것을 재는데 이름이 같다"**
> 는 문제이고, 처방은 문턱이 아니라 **이름**입니다.

## 5. 세 기기 공통 판단

**오늘 밤 판정은 등록된 대로 돌립니다.** `ads_mslm050` 이 한 시간 안에
나오는데, **부분 자료(tsa·vsa)를 본 뒤 문턱을 옮기는 것은 §2 위반**입니다.

    오늘 밤   judge_mslm050.py 등록식 그대로. 경계 [0.646, 0.836]
    병기      "이 1.5 는 **실현 SD 자**" 를 판정 출력에 찍음 (랩탑 `6cffaed`)
    2단계     STAGE2 §3-1 에도 자 표기 완료. 영향 없음

> ⚠️ **세 기기 의견이 일치한다는 것을 근거로 적지 않습니다.** 오늘 새벽에
> 두 기기가 **같은 실수를 각자** 했습니다(시각 과장 둘, 정도 과장 둘, 그리고
> 이 규약 건에서도 처음엔 각자 자기 규약이 맞다고 확신). **합의는 근거가
> 아닙니다.** 위 4절에 선택지와 파급 범위만 적었습니다.

> **[2026-09-06 10:4x] 사용자가 결정했다 — (다). 본문은 §7.** 이 절의 "오늘 밤 판정은 등록된 대로" 는 08-28 의 기록으로 남긴다.

## 6. 참고 — 이것을 드러낸 검사

    d 를 고정하고 배치 f 를 0 으로 보낸다.
    그러면 통계만 계산이 나와야 하는가? -> **그 요구 자체가 전제입니다.**

랩탑 정리: *"극한 검사는 **연속성**을 보이지 어느 규약이 맞는지는 안 보입니다.
통계항과 배치항이 원래 다른 자로 재는 양이면, 둘을 합성한 식이 `f=0` 에서
통계 규약으로 이어질 이유가 없습니다."*

**즉 이 검사가 드러낸 진짜 문제는 "어느 식이 틀렸다" 가 아니라 "두 항을
합성하는 것 자체가 정의되지 않았다" 입니다.** 등록 판정들이 통계항을 아예
안 쓰는 것이 그래서일 수 있습니다.

## 7. 결정 — **(다) 둘을 다른 이름으로** (사용자, 2026-09-06 10:4x, "모두 권고대로")

**문턱은 하나도 옮기지 않는다. 바뀌는 것은 이름과 "어느 비교에 어느 자가 붙는가" 뿐이다.**

    배치 비교   자 = 실현 표본 SD(진짜 1σ)          문턱 1.5   이름 **「배치 단위」**
                묻는 것: "다른 배치를 뽑아도 갈리는가" — 앙상블 대 앙상블, 앙상블 대 base(배치 자유도 0),
                관문 n실현 판정(STAGE2 §3-1 의 s), 배치 산포끼리의 비교
    통계 비교   자 = RASPA `±`(95% CI = 2.776 × SEM)   문턱 1.5   이름 **「단위」** (CLAUDE.md §2 그대로. 구별이 필요하면 「단위(±)」)
                묻는 것: "같은 구조를 다시 돌려도 갈리는가" — 단일 실현 대 단일 실현, 씨앗 반복(T_SREP 의 s_rep 포함)
    판정 자     비교의 종류가 정한다. 분모에 배치항이 있으면 **배치 단위가 판정**, 통계항만 있으면 **단위(±)가 판정**
    병기 의무   어느 비교든 **두 값을 항상 나란히** 적는다: `1.90 배치 단위 · 0.68 단위(±)`. 한쪽만 인용 금지
                (ERROR_BARS 끝 절 "당장의 규칙" 을 영구 규칙으로)
    혼합 분모   §3 의 4.42 처럼 두 자를 섞은 분모는 **새로 만들지 않는다.** 기존 값은 `단위(혼합 95%)` 이름과 비중을 붙여서만 남긴다.

**과거 판정 읽기 (수는 전부 불변)**

    LOWRH_18  base→saIm050 1.80 · ms50nb50→sa50nb50 1.79   배치 비교 → **검출(배치 단위)**, 병기 0.65·0.64 단위(±). 여유 1.2배로 얇음.
              "−OH 가 물 친화도를 올린다(12자리)" 는 자 조건은 풀렸으나 **혼합군 산포 조건은 그대로**라 여전히 못 세움.
    머리기사  치환 대 무치환 0.7306 대 0.4692 → **4.49 배치 단위, 여유 3.00배** (통계 자 1.62 단위(±) 병기). 4.42 는 `단위(혼합 95%)`, 11.0 은 `단위(평균SE)`.
    MSLM050   경계 [0.646, 0.836] 이미 σ 자 → 이름만 배치 단위, 경계 불변.
    STAGE2    saIm075 0.857 → 0.857 배치 단위, 미해결 그대로.
    T_SREP    s_rep 은 씨앗 반복(같은 구조) → **통계 비교**: 판정 6.94 단위(±), 19.94 는 σ 자 병기이되 "배치 단위" 라 부르지 않음.
    MIX10     시험① 1.90 배치 단위 **구별됨** · 0.68 단위(±) 병기 / 시험② 배치 단위 조건부 그대로(1.35) / §4 방향 1.87 배치 단위.
    안 바뀜   조성 축 0.09 · 계열 축 0.15(배치 단위로도 미검출) · Zeo++ 관문(자 하나).

**단위(±) 의 식 — 명시 (2026-09-19 15:0x, 종합자; 랩탑 Melchior 검산 지적 "어디에도 안 적혀 있다". 기존 관행의 문서화이지 자·문턱 변경이 아님)**

    평균 대 평균(앙상블끼리)   단위 = d / √( (±̄₁/√5)² + (±̄₂/√5)² ),  ±̄ = 그 앙상블 실현별 ±(95 % CI) 의 평균   ← WC §7·§8·§9·§10 이 쓴 식
    짝(같은 k 끼리)             단위 = mean(d_k) / mean( √(±_{a,k}² + ±_{b,k}²) )                                  ← REALTEMP_1B·T-RT-1 Q1·Q3 이 쓴 식
    단일 대 단일                단위 = d / √(±₁² + ±₂²)                                                            ← CLAUDE.md §2 원문
    두 식은 서로 다른 비교의 식이라 통일하지 않는다(√5 ≈ 2.24배 차이는 비교 종류의 차이). 어느 값이든 정본은 SD 형(배치 비교) 또는 단위(통계 비교) — §7 표 그대로.
