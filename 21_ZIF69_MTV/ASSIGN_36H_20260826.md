# 36시간 배정 — 두 기기 (2026-08-26 12:50 ~ 08-28 00:50)

**부팅 줄**(새 세션에 이것만 주면 됩니다):

    CLAUDE.md, SESSION_LOG.md, 21_ZIF69_MTV/ASSIGN_36H_20260826.md 읽고
    bash bgstate.sh 돌린 다음 현황 보고해

이 문서가 `ASSIGN_48H_20260824.md` 를 **대체**합니다. 그 문서는 4기기 전제로
쓰였고 그중 둘이 빠졌습니다.

---

## 0. 왜 다시 짜는가

| 기기 | 상태 | 근거 |
|---|---|---|
| 데스크탑 (HKHOME) | 🟢 가용 8코어 | — |
| 랩탑 (laptop-mof) | 🟢 가용 8코어 | — |
| Junseok (원격지) | 🔴 **다운** | 08-26 오전. 마지막 소식 08-25 17:02 |
| laptop2 (DESKTOP-JS1IB6U) | 🔴 **하드웨어 불량 확정 (08-26 14:04)** | Windows 메모리 진단 **양성**. RAM 물리 교체 전까지 복귀 불가 — 1절 참조 |

사용자 지시(08-26 12:4x): *"laptop2가 많이 이상해. 메모리 문제 크래시가 계속
발생해서 오늘까진 일단 미뤄둘거야. 36시간동안은 두 기기만 사용할 거 같으니
작업표를 재배치할 필요가 있을 거 같아."*

## 1. laptop2 반납으로 잃는 계산은 **없습니다** — 확인했습니다

laptop2 가 돌던 것은 `saIm0875` 의 RH25/50/90 이었습니다. 그런데 그 조성은
**이미 두 기기가 각각 완전한 격자를 냈고 서로 일치**합니다.

    v3_water_grid/water_results_junseok.json   saIm0875  RH 0 / 25 / 50 / 90
    v3_water_grid/water_results_laptop.json    saIm0875  RH 0 / 25 / 50 / 90
    일치도  RH50  laptop 1.0582±0.0423  junseok 1.0564±0.0373  ->  **0.03시그마**

laptop2 의 것은 **세 번째 반복**이었고, 목적은 과학이 아니라 **laptop2 자기
빌드의 검증**이었습니다. 기기가 빠지면 그 목적 자체가 사라집니다.

그리고 laptop2 의 RH0 (**1.3121 ± 0.0367**) 은 08-26 에 이미 완주해 은행에
들어와 있습니다. 그것이 분할 프로토콜 검증의 비교 대상이므로 **그 시험도
살아 있습니다.**

> **결론: laptop2 의 남은 3점은 재개하지 말고 취소합니다.** 기기가 돌아와도
> 그 3점은 값이 없습니다. 돌아오면 새 일을 주세요.

**Junseok 다운만 진짜 공백을 남깁니다** — `saIm050` 앙상블 습윤 WC 15작업.
전하 CIF 5개는 08-26 01:45 에 이미 올라가 있습니다
(`charged_v3/saIm050e{1..5}_DDEC6.cif`). 이 배정을 랩탑이 회수합니다.

## 2. 이 36시간의 축 — **오늘 21시경 빌더 판정이 나옵니다**

배치 대조군 체인(`PLACEMENT_CONTROL_20260826.md`)이 오늘 **20:00~21:00** 에
사전 등록 판정을 자동 출력합니다. 그 판정이 갈라놓는 것은 다음 하나입니다.

`mtv_cif_builder.py:800-825` 에서 **`clIm_aryl`(모체)는 `subs` 에 안 들어갑니다.**
그래서 치환기가 실제로 몇 종인지가 배치 알고리즘을 가릅니다:

| 조성 | `len(subs)` | 배치 | 판정에 영향받나 |
|---|---|---|---|
| `saIm*` · `mslm*` · `nbIm*` · `base` | **1** | 충돌 회피 | ❌ **안 받음** |
| `sa50nb50` · `ms50nb50` · `sa25nb75` | **2** | 무작위 | ✅ **받음 — non-null 이면 재빌드 대상** |

> **그래서 이 36시간의 배치 규칙은 한 줄입니다.**
> **21시 판정 전까지 두 기기 모두 단일 치환기만 새로 시작한다.**

## 3. 데스크탑 (8 물리코어)

| 시각 | 작업 | 코어 | 상태 |
|---|---|---|---|
| ~15:30 (추정) | 배치 대조군 **이완** r1·r2·r4·r5 | 3 | 🔵 진행 중 (r3 완주 116.6분) |
| **12:52 ~ 16:30** (추정) | **분할 프로토콜 검증** `saIm0875` RH0, 5조각 순차 | **1** | 🔵 **12:52 기동** |
| 15:30 ~ 16:00 | 판정 + PACMAN 전하 (5종) | 1~2 | ⬜ |
| 16:00 ~ 21:00 (추정) | 건조 GCMC **15작업** (Widom 10 + GCMC 5), 6워커 | 6 | ⬜ |
| **20:00~21:00** | **사전 등록 판정 자동 출력 → 4절 분기점** | — | ⬜ |
| 21:00 ~ 06:00 (추정) | **`mslm075` 습윤 WC 3작업** (ads/tsa/vsa) | 3 | ⬜ **신규** |
| 08-27 09:00 | Fable 전 에이전트 검증 → 빌더 확장 (non-null 인 경우) | — | ⬜ |

### 3-1. 왜 분할 검증을 지금 끼워 넣는가 — "순서는 뒤" 를 어긴 것이 아닙니다

사용자 지시는 *"짜되 순서는 뒤"* 였고, 그 이유는
`CHUNKED_PROTOCOL_20260826.md` 4절에 **"지금 병목은 배치 교란"** 이라고
적혀 있습니다. 즉 **대조군 체인을 늦추지 말라**는 뜻입니다.

    대조군 이완이 쓰는 코어   3 (xtb 워커 3 x 2스레드, 실측 부하 4.3)
    분할 검증이 쓰는 코어     1 (RASPA simulate 는 단일 스레드, nice 19)
    건조 GCMC 가 쓸 코어      6  ->  6 + 1 = 7 / 8

**한 순간도 경합하지 않습니다.** 대조군은 1분도 늦어지지 않고, 검증은
아무도 못 쓰는 코어에서 끝납니다. 큐를 새치기한 것이 아니라 **빈틈을
쓴 것**입니다.

### 3-2. 왜 밤에 `mslm075` 습윤 WC 인가 — 세 가지가 겹칩니다

**① 판정과 무관합니다.** `mslm075` 는 `len(subs)==1` → 이미 충돌 회피
배치입니다. 21시 판정이 어느 쪽이든 버릴 일이 없습니다.

**② 사용자가 오늘 지적한 공백을 정확히 메웁니다.** 건조 스크린은 9계열
31구조를 봤는데 **물 축은 `saIm` 계열에만 걸렸습니다.**

    건조 Qst 순위        물 축이 있는가
    1  saIm100  34.01        ✅
    2  saIm075  31.42        ✅
    3  saIm050  29.51        ✅ ⟨Q_st 절대값은 RT 부호 정정 전, +4.96 — QST_RT_SIGN_20260911⟩
    4  mslm075  29.01        ❌  <- 비-saIm 최고. 건조 WC 는 있는데 습윤이 없음
    5  nbIm100  28.87        ❌

`mslm075` 는 **건조 WC 가 이미 있습니다**(`v3_wc/working_capacity.json`).
습윤 3작업이면 비-saIm 계열 최상위가 처음으로 물 축에 올라갑니다.
그리고 `ms50nb50`(mslm+nbIm 반반)이 유지율 1위(91.91%)인 것이 **화학 때문인지
혼합 때문인지**를 가르는 첫 단서가 됩니다.

**③ 재생에너지 표의 빈칸이 채워집니다** — 건조·습윤이 둘 다 있어야
`regen_energy_v3.py` 가 행을 만듭니다(일일 감사 6항 지적).

**④ 크기가 맞습니다.** 3작업이라 6~9시간에 끝나고 **08-27 09:00 Fable
세션 전에 기기가 비웁니다.** 15작업짜리를 걸면 내일 아침 빌더 확장이
막힙니다.

## 4. 21시 분기점 — 미리 정해 둡니다

판정이 나오는 순간 **랩탑에 즉시 알립니다.** 두 갈래입니다.

### (가) non-null (빌더 확장 필수)

랩탑이 그때 돌고 있는 **저압 v4 혼합 3조성(9점)은 버려질 구조 위의 값**입니다.
    -> **랩탑은 진행 중인 v4 저압 작업을 중단하고 `saIm050` 15작업으로 넘어갑니다.**
       (`saIm050` 은 단일 치환기라 안전하고, 4~6시간 일찍 시작됩니다)
    -> 완주한 8점(base·saIm050·saIm0583, **전부 단일 치환기**)은 그대로 유효합니다.
    -> v4 저압은 재빌드 뒤에 다시 잽니다.

> ⚠️ **(가) 는 네 단계이고 1번을 빠뜨리면 순손실입니다.** 랩탑이 08-26 13:0x
> 에 짚었습니다 — 8-1 절의 "전량 완주 후에만 저장" 이 여기서 물립니다.
> **러너를 죽이면 완주한 8작업의 결과도 JSON 에 하나도 안 남습니다.**
>
>     1. salvage_water_data.py 로 완주분 건지기   <- **죽이기 전에**
>     2. 러너 종료
>     3. master 병합 (62커밋)
>     4. saIm050 15작업 착수
>
> `salvage_water_data.py`(랩탑, `6c9854e`)는 작업 폴더의 `.data` 에서
> `Simulation finished` 인 것만 골라 `run_water.py` 와 **같은 키**로 JSON 을
> 씁니다. 유지율·σ 는 **일부러 계산하지 않습니다**(규약 ⑥ 가) — 로딩과
> 오차만 적고 나머지는 merge 도구가 자기 자로 냅니다.
>
> 데스크탑이 인양 결과물 모양을 `merge_water_batches.py` 에 직접 통과시켜
> 확인했습니다(08-26 13:1x, 3절 참조). **터지지 않습니다.**

### (가) 실행 시 21시 현장 판단 — 랩탑 몫

**문턱: 21시에 남은 시간이 1시간 안쪽이면 기다리고, 넘으면 인양·종료합니다.**
랩탑이 착수 시각 + 단가로 실측해 판단합니다. `rh05_saIm0583` 은 단일
치환기라 어느 쪽이든 완주시킵니다.

**1시간을 문턱으로 두는 근거** — `saIm050` 15작업이 22~25시간이고 창이
08-28 00:50 에 닫힙니다.

    21:00 착수  ->  08-27 19:00 ~ 22:00 완주   창 안쪽으로 여유
    00:00 착수  ->  08-27 22:00 ~ 08-28 01:00  **창 경계에 걸림**

`saIm050` 은 **조성 축 종결이라 임계 경로 위**에 있고, 버려지는 v4 저압
9점의 용도("재빌드 후 2종 계 배치 효과 비교")는 **임계 경로 밖이며 재빌드
자체가 아직 미정**입니다. 3시간을 임계 경로에서 빼서 미정인 것에 쓰는
교환은 맞지 않습니다. 랩탑 판단이고 데스크탑이 동의합니다.

> 13:5x 랩탑 실측 기준 21시 전망: 남는 것은 `sa25nb75` 3작업 + `ms50nb50`
> rh05 정도이고 각각 **1~3시간** 남습니다. 즉 (가)면 **기다리지 않는 쪽**이
> 될 공산이 큽니다. 그래도 21시에 실측해서 문턱으로 가릅니다.

> **13:4x 재산출 — 21시에 혼합이 80~100% 이거나 이미 완주했을 수 있습니다.**
> 앞선 14.8 h 는 소요가 아니라 **런 시작 대비 경과**였습니다(아래 5-1 절).
> 실제 단가는 10 h 급이라 **저압 완주가 08-26 22:00 ~ 08-27 01:00 으로
> 당겨집니다.** (가)가 나와도 **버릴 것이 거의 없을** 수 있으므로, 21시에
> 실측해서 "몇십 분 더 기다려 전량 완주" 가 가능하면 그쪽을 택합니다.

## 5-1. 경과 시간을 소요 시간으로 읽지 마세요 — 08-26 실사고

**첫 물결 안에서는 둘이 같아서 검산이 통과합니다.** 그래서 안 드러납니다.

18작업 8워커면 **작업 1~8 만 런 시작에 착수**하고 9번부터는 워커가 빌 때까지
기다립니다. `rh10_sa50nb50` 은 **11번**이었습니다.

    런 시작 대비 경과   22:12 -> 13:01  =  14.8 h    <- 처음 보고된 값
    실제 착수(폴더 birth) 02:59:23
    **실제 소요            02:59 -> 13:01  =  10.03 h**

폴더 birth time(`stat -c %w`)으로 재니 첫 물결 8개가 전부 `08-25 22:13:29`,
9·10·11번이 `02:18:35` / `02:43:04` / `02:59:23` 로 **base 3개의 완주 시각
직후**였습니다.

> **규칙: 경과와 소요를 구별하려면 첫 물결 밖 작업이 최소 하나 필요합니다.**
> 첫 물결만 보면 둘이 같아 구별 자체가 불가능합니다. 랩탑 정리입니다.

### 진행도를 사이클로 읽지 마세요 — CLAUDE.md §4 가 이미 경고한 것

같은 사고에서 "10000c(67%)" 로 읽은 진행도가 **12분 뒤 완주**로 모순을
냈습니다. 원인은 `PrintEvery = 15000` 입니다 — **출력이 사실상 끝에만
찍힙니다.** 읽은 값은 진행도가 아니라 마지막으로 기록된 눈금입니다.

CLAUDE.md §4 에 *"`PrintEvery` 가 생산 사이클 수와 같아 처음과 끝에만
씁니다"* 라고 **이미 적혀 있는데도** 물렸습니다. 진행도는 **착수 시각 +
단가**로 추정합니다.

## 5-2. 조성별 RH10 단가 (착수 보정 후) — **아직 아무것도 확정되지 않았습니다**

    조성        치환자리  RH10물   소요(h)
    base          0     0.1255      4.49
    saIm050      12     0.1873      9.91
    saIm0583     14     0.3431     11.73
    sa50nb50     24     0.2623     10.03

    치환 자리수 vs 소요   r = +0.773   95%CI  **-0.731 ~ +0.995**
    RH10 물   vs 소요    r = +0.872   95%CI  **-0.550 ~ +0.997**
    치환 자리수 vs RH10물 r = +0.672   <- 두 예측자가 서로 얽혀 있음

> ⚠️ **둘 다 확정이 아닙니다.** n=4 는 자유도 2 라 `p<0.05` 이려면 **r 이
> 0.950 을 넘어야** 합니다(t(0.975,2)=4.303). 둘 다 못 넘고 **두 신뢰구간이
> 0 을 포함하며 서로 거의 완전히 겹칩니다.** 게다가 두 예측자끼리 r=+0.672 로
> 얽혀 있어 4점으로는 **분리 자체가 불가능**합니다.
>
> 보정 뒤 물 쪽 r 이 커진 것은 **"물 가설이 이겼다" 가 아니라 "둘 다 아직
> 아무 말도 못 한다" 입니다.** 처음 사슬(극성기 → 물 → 시간)을 제안한 것이
> 데스크탑이므로 유리한 쪽으로 읽을 위험이 데스크탑에 있습니다. 적어 둡니다.
>
> **저압 18점(6조성 × 3 RH)이 다 모인 뒤에 다시 봅니다.** 그 전에는 큐
> 정렬 규약으로 올리지 않습니다.

### (가) 라도 `water_runs_lowrh/` 는 **지우지 않습니다**

랩탑 제안이고 근거가 맞습니다. 이 문서 4절이 "대조군은 `len(subs)==1` 에서
재는 것이라 2종 계에 논리적으로 확장되지 않는다" 고 적어 뒀는데
(`PLACEMENT_CONTROL_20260826.md` 4절), **재빌드 후 같은 저압 9점을 다시
재면 2종 계에서 배치 효과를 직접 재는 유일한 자료**가 됩니다.

지금 우선순위에서 밀리는 것과 **자료를 버리는 것은 다릅니다.**

> `cleanup_for_compact.sh` 는 안전합니다 — `TARGETS` 가 글롭이 아니라 명시
> 목록이고 `water_runs_lowrh` 는 거기 없습니다(08-26 확인). 나중에 누가
> 줄을 추가하지 않는 한 지워지지 않습니다.

### (나) null 또는 작은 오프셋

    -> 랩탑은 저압 18작업을 끝까지 돕니다(08-27 01~03시 추정).
    -> 그 뒤 `saIm050` 15작업.

> 어느 쪽이든 **완주분 8점은 안전합니다.** 지금 중단할 이유가 없고,
> 판정 시각이 랩탑의 v4 완주(01~03시)보다 **4~6시간 앞서므로** 분기점이
> 실제로 작동합니다. 이것이 지금 중단하지 않는 이유입니다.

## 5. 랩탑 (8 물리코어) — 36시간 내내 만재입니다

랩탑 실측 보고(08-26 12:49)를 그대로 옮깁니다. **배율을 옮겨 쓰지 않고
랩탑 자체 실측을 씁니다**(COMMS.md 규약 ⑥).

| 시각 | 작업 | 상태 |
|---|---|---|
| ~08-26 **22:00 ~ 08-27 01:00** (랩탑 추정, 13:4x 재산출) | **저압 18작업** RH 5/10/15 × 6조성 | 🔵 완주 9 / 진행 8 / 미착수 1 |
| 완주 직후 | master 병합(62커밋) + `water_results_laptop.json` 푸시 | ⬜ |
| ~22~25시간 | **`saIm050` 앙상블 습윤 WC 15작업** (Junseok 회수분) | ⬜ |
| 08-28 00:00~04:00 (랩탑 추정) | 완주 — **창 끝과 겹침** | — |

**랩탑은 이 두 건이 한계입니다.** v4 재빌드·재계산은 이 창에 못 들어갑니다.

랩탑 자체 실측 근거(`saIm0583` 앙상블 15작업, 8워커):
`08-24 17:56 → 08-25 18:27 = 24시간 31분`. tsa 가 373 K 라 가장 빠르고
ads/vsa 가 RH90 이라 느립니다 — 데스크탑 1조성 실측(ads 8.9h / vsa 7.2h /
tsa 1.4h) 과 **순서가 같습니다.**

> ⚠️ **데스크탑 8h56m(3작업)과 랩탑 24.5h(15작업)를 직접 나누지 마세요.**
> 데스크탑은 3워커(여유), 랩탑은 8워커 만재라 메모리 대역 경합이 다릅니다.
> 08-22 비용표에 "동시 워커 수 병기" 를 넣은 이유가 이것입니다.

## 6. 취소 · 보류

| 항목 | 처분 | 이유 |
|---|---|---|
| laptop2 `saIm0875` RH25/50/90 | **취소** | 1절 — 세 번째 반복이고 앞의 둘이 0.03시그마로 일치 |
| RH1~2 12작업 | **취소** (유지) | 08-26 우선순위 ③ |
| v4 재빌드·재계산 | **보류** | 21시 판정까지 |
| `ms50nb50` 앙상블 | **보류** | 다치환기. 판정 전에 돌리면 버릴 수 있음 |
| 삼원 MTV (`build_ternary_me.py`) | **보류** (유지) | 빌더가 조성을 조용히 바꿈 — 확장이 선행 조건 |

## 7. 코어를 안 쓰는 작업 (지금 해도 되는 것)

- `regen_energy_v3.py` 의 `NAMES`·파일 목록 확장 — **승자 조성 `saIm0583` 이
  건조·습윤 WC 가 둘 다 완비인데 통째로 빠져 있습니다**(일일 감사 6항).
  `_g0583` · `_ext` · `_grid` 접미사 파일과 `v4_humid_wc/` 를 안 엽니다.
- `run_water.py:58` `[A]` / `:150` `[B]` 이어받기 주석 정정 —
  **랩탑 저압 완주 전에는 손대지 않습니다.** 그 파일로 18작업이 돌고 있고,
  파이썬 편집은 재기동할 때 터집니다(CLAUDE.md §6).

## 8. 새로 등록하는 위험

### 8-1. `run_water.py` 는 **전량 완주 후에만** JSON 을 씁니다

랩탑이 08-26 12:49 에 짚었습니다. `main()` 끝에서 한 번 쓰고 중간 저장이
없습니다. 지금 저압 18작업이 **30시간짜리**이므로 실제 위험입니다.

    죽으면          water_results_*.json 이 통째로 안 남음
    복구 가능한가    가능 — System_0/*.data 는 남음. 다만 **손으로 뽑아야** 함

08-25 `d0b6f50`(덮어쓰기 → 병합)과 같은 계열의 개선(**작업 완주마다 부분
저장**)이 다음 후보입니다. **지금은 도는 중이라 안 건드립니다.**

### 8-2. 저압 결과가 아직 파일로 없는 것은 정상입니다

랩탑이 보내온 RH5/10/15 값은 JSON 이 아니라 각 작업의 `System_0/*.data` 에서
직접 뽑은 것입니다. `v3_water_lowrh/` 는 지금 빈 폴더가 맞습니다.
**푸시 누락이 아닙니다.**

### 8-3. 저압을 지금 중단하면 진행 중 8작업이 통째로 날아갑니다

이어받기는 **작업 경계**에서만 작동하고 작업 내부 진행도는 복원하지
않습니다(08-26 확정, COMMS.md:245). 3개가 67% 지점입니다.
4절 분기점을 21시에 두는 이유가 이것입니다.

---

## 9. Junseok 복귀 시 (08-27 예상) — **판정 결과별로 미리 배정합니다**

사용자가 08-26 14:0x 에 알렸습니다 — *"내일 Junseok은 복구시킬 수 있을 거
같긴 한데."* 복귀하면 부팅 줄 하나로 바로 들어갈 수 있게 몫을 미리 정합니다.

### (가) 판정이 non-null 인 경우 — v4 재빌드 캠페인 분담

    구조 생성 + 이완    **데스크탑 전용** (구조 생성이 이 기기만 가능)
    PACMAN 전하         데스크탑 (이완 직후)
    **건조 GCMC 9작업**  **Junseok**        <- 6코어에 맞는 크기
    습윤 WC 9작업       랩탑 (saIm050 완주 후)
    저압 9작업          랩탑 또는 Junseok (먼저 비는 쪽)

전체 약 40 코어시간. **2대면 3일, 3대면 2일.** Junseok 이 하루를 법니다.

### (나) 판정이 null / 작은 오프셋인 경우 — K_H 배치 산포

    **`saIm0583` 앙상블 저압 15작업** (e1~e5 × RH 5/10/15)   **Junseok**

**오늘 확인된 가장 큰 구멍**입니다. 저압 K_H 사다리의 위 칸(12→14 자리에서
2.03배 도약)이 확립됐는데, **그 판정에 쓴 배치 산포 17.9% 가 다른 관측량
(RH90 로딩)에서 빌려온 값**입니다. K_H 자체의 산포는 아무도 안 쟀습니다.

전하 CIF 가 `charged_v3/saIm0583e{1..5}_DDEC6.cif` 로 이미 있어 **추가 빌드
없이 바로 착수**됩니다. 6코어에 맞는 크기입니다.

그 다음 순번: `saIm0625` 저압 3작업 (도약이 15자리 위로 계속되는지 포화하는지).
**"12→14 빈칸 메우기" 가 아닙니다** — 빈칸은 13/24 = 54.2% 이고 저장소에
그 구조가 없습니다.

### 공통 — 복귀 시 첫 세 가지

    1. `git fetch && git merge origin/master`  (08-25 이후 커밋 다수)
    2. `bash bgstate.sh`
    3. `COMMS/junseok.md` 에 복귀 보고 — **다운 기간에 돌던 것이 있으면
       완주분부터 확인**. `salvage_water_data.py` 로 건질 수 있습니다.

> ⚠️ **Junseok 에 배정된 `saIm050` 습윤 WC 15작업은 랩탑이 회수했습니다.**
> 복귀했다고 그것을 다시 집으면 **중복 계산**입니다. 9절의 몫만 받으세요.

---

## 10. laptop2 — 08-26 14:04 **하드웨어 불량 확정**. "오늘 하루" 가 아닙니다

사용자는 12:4x 에 *"오늘까진 일단 미뤄둘거야"* 라고 했지만, 14:04 에
laptop2 가 **Windows 메모리 진단 양성**을 보고했습니다. **내일 그냥 켠다고
돌아오지 않습니다 — 물리 작업이 필요합니다.**

    14:04:16  ID 1202  오류
    14:04:16  ID 1102  오류
    "The Windows Memory Diagnostic tested the computer's memory
     and detected hardware errors."

### 왜 이 양성이 무거운가

이 도구는 **한계선 불량을 놓치는 거짓 음성이 흔합니다.** 그래서 "이상 없음"
이었다면 아무것도 확정하지 못했을 것입니다. **실제로 틀린 값을 읽어 냈고,
양성은 거짓이 되기 어렵습니다.** laptop2 정리이고 맞습니다.

정황이 전부 한 방향입니다:

    메모리 진단                          하드웨어 오류 검출
    42시간 안 BSOD 3건 (직전 20개월 0건)  0x1A / 0xA / 0x1A
    그중 2건이 MEMORY_MANAGEMENT
    사용자 RAM 증설 시점과 일치
    연속 수명 14 h  ->  **1.7 h** 악화

### 수명 1.7 h 면 분할 러너로도 안 됩니다

    통짜 RH0            4.44 h   불가
    분할 RH25 조각 2~5  2.51 h   불가
    분할 RH0  조각 0    1.78 h   불가 (아슬아슬)
    분할 RH0  조각 1~4  0.67 h   **유일하게 가능**

> **분할 방식이 잘못된 것이 아닙니다.** 수명이 그 방식이 상정한 범위 아래로
> 내려간 것입니다. 러너는 여전히 유효하고, 정전·강제 재부팅처럼 **수명이
> 조각보다 긴** 상황에서는 그대로 값을 합니다. `CHUNKED_PROTOCOL_20260826.md`
> 3-2 절이 미리 경고한 그 혼동을 피해야 합니다 — **코드 3 을 보고 분할
> 방식을 의심하면 틀린 결론**입니다.

### 복구 경로 — 사용자 물리 작업

한 장만 꽂고 진단을 재실행하면 **어느 장이 범인인지 갈립니다.**

> **이 프로젝트에는 16 GB 한 장으로 충분합니다.** RASPA `simulate` 는
> 471 MB/건이고(CLAUDE.md §5) laptop2 는 6코어라 만재해도 약 2.8 GB 입니다.
> **불량 장을 빼고도 계산은 그대로 됩니다.**
>
> ⚠️ 다만 **Zeo++ `network` 는 v3 구조 한 건에 약 9.5 GB** 라 16 GB 한 장
> (WSL 할당 약 8 GB)에서는 못 돕니다. laptop2 에 위험도 스크리닝을 주지
> 마세요. 물·GCMC 만 줍니다.

### 살아남은 것 — 크래시와 무관하게 유효합니다

    RH0  CO2 1.3121 ± 0.0367     등록 기준 대비 0.31σ  일관 (배선 일치 확인)
    비용표  RH0 4.44 / RH25 16.74 / RH50 19.42 / RH90 22.37 h
            (워커 6, 4작업 만재. 2차 실행에서 세 조건 모두 ±5% 내 재현)

**미완인 것은 물 경로 검증 하나**이고, 하드웨어가 잡혀야 가능합니다.

### `[A]`·`[B]` 주석 정정의 근거를 바꿉니다

체크포인트 총수 40개 예측은 **완주 전에 죽어 확인 불가**입니다. 대신
laptop2 가 더 나은 근거를 냈습니다:

> **"100% 가동인데 첫 체크포인트가 사이클 500 시각에 나온다."**

세 조건의 첫 체크포인트가 1차 실측과 맞았습니다(1429/1793/1980 s vs
1506.7/1747.8/2013.3 s). 이어받은 지점에서 재개했다면 즉시 찍혔어야 합니다.

**이 근거가 낫습니다** — 코드를 안 읽고도 성립하고 **다른 기기에서도
재현됩니다.** 랩탑 저압 완주 후 주석을 고칠 때 이쪽을 쓰세요.

---

## 11. 판정 결과 (2026-08-26 19:17) — **null**. 배정이 바뀝니다

    충돌회피 n=6   평균 1.1894   표본SD 0.0963
    무작위   n=5   평균 1.2497   표본SD 0.1417
    차이 +0.0603
    (가) 검출 0.81   (나) 유의 0.35     둘 다 문턱 1.5 미만
    -> **null** — 배치 규약이 성질을 안 움직인다. 교란 해소

`PLACEMENT_CONTROL_20260826.md` 3절 결론 매핑 그대로입니다.

> ⚠️ **[08-26 20:0x 중대 정정] 이 null 은 화학 증거가 아닙니다.**
> 충돌 그래프가 **46행 전부 0** 이라 `choose_conflict_free_sites` 가 균등
> 무작위 부분집합으로 환원되고, 무작위 팔도 조성 기각표집으로 같은 분포가
> 됩니다. **두 팔이 같은 분포에서 뽑혔고 null 은 구조적으로 보장된 결과**
> 입니다. 부수 관찰 1.47배도 같은 이유로 표집 잡음입니다.
> `PLACEMENT_CONTROL_20260826.md` **8절**을 반드시 함께 읽으세요.
>
> **v4 재빌드 불필요라는 결론은 그대로입니다** — 근거가 "측정했더니 차이
> 없음" 에서 **"플래그가 무력이라 차이가 존재할 수 없음"** 으로 바뀝니다.
> 코드 검사가 실험보다 강한 답을 줬습니다.

### 11-1. null 을 "효과 0" 으로 읽지 않습니다 — 구간으로 적습니다

    차이 +0.0603   SE 0.0746
    95%CI  -0.0859 ~ +0.2065 mol/kg   =   **-7.0% ~ +16.9%**
    (참고) 같은 관측량의 배치 산포 자체가 8.1%(충돌회피) / 11.3%(무작위)

**규약 효과의 상한이 배치 산포와 같은 크기**입니다. 0 이 아니라 이 구간
안에 있습니다.

### 11-2. 사라진 일 · 남은 단서

| 항목 | 처분 |
|---|---|
| v4 재빌드 + 재계산 (약 40 코어시간) | **불필요.** 기존 측정 전부 유효 |
| 빌더 확장 (다치환기 충돌 회피) | **"있으면 좋은 것" 으로 내림** |
| 08-27 09:00 Fable 빌더 검증 세션 | **조건 미충족으로 무효** (사용자 지시가 "non-null 일 경우" 였음) |
| 7절 이완 난이도 관측 | **보고하지 않음** — 7절 등록대로 |

> ⚠️ **3절 4절 단서는 그대로입니다.** 이 시험은 **치환기 1종**에서 쟀고,
> "1종에서 효과 없음" 이 "2종에서도 없음" 을 **논리적으로 보장하지
> 않습니다.** 강한 정황이지 증명이 아닙니다. v4 비교를 인용할 때 이 단서를
> 답니다.

### 11-3. 등록된 부수 관찰 — 무작위 쪽 산포가 1.47배

    s_r / s_c = 1.47     F = 2.17 (df 4,5)     F(0.95,4,5) = 5.19  ->  미유의

3절이 *"미리 문턱을 걸지 않고 그때 별도로 판단한다"* 고 등록했습니다.
**F 검정은 사전 등록이 아니므로 참고이지 판정이 아닙니다.**

**그런데 이 "미유의" 가 "같다" 가 아니라 "모른다" 입니다.** 그리고 v4 셋은
전부 무작위 배치라 이 배수가 그 계열 판정에 직접 실립니다. 랩탑이
08-26 19:2x 에 짚었고, 실제로 뒤집힙니다:

    ms50nb50 -> sa50nb50 (−OH 대조)
      산포 같다고 볼 때 (17.9%)   1.65 단위   검출
      군 B 가 1.40배일 때          1.18 단위   **미검출**
      군 B 가 1.47배일 때          1.12 단위   **미검출**

    단일 치환기 사다리 (군 A — 배수가 안 걸림)
      base -> saIm050        1.69 단위   검출
      saIm050 -> saIm0583    2.56 단위   검출

**−OH 대조는 조건부로 내려가고, 군 A 사다리와 유지율 고원은 안 걸립니다.**

> ⚠️ **[08-26 20:0x 철회] 위 1.47배 적용은 무효입니다.** 8-2 절대로 두 팔이
> 같은 분포에서 뽑혔으므로 1.47 은 **표집 잡음**이고 "군 B 산포가 더 크다"
> 는 기전이 없습니다. **−OH 대조의 하향을 되돌립니다** — 원래 자리(살아
> 있되 아슬, `f 19.0%` 대 실측 물산포 16.7~18.3% 로 여유 1.04~1.14배)로만
> 돌립니다. 혼합군 산포가 미측정인 것은 그대로이고, **다만 그 이유가
> `conflict_aware` 플래그가 아니라 조성이 다르기 때문**입니다
> (24/24 두 종 대 14/24 한 종). **군 B 앙상블은 여전히 정확히 옳은
> 실험이고, 재는 대상의 이름만 바뀝니다.**

## 12. 되찾은 40 코어시간의 배정 — **고르지 않고 둘 다 합니다**

랩탑이 (가)/(나) 중 택일을 물었는데, **구조 생성이 데스크탑 전용**이라는
점을 쓰면 둘 다 됩니다.

| 기기 | 몫 | 왜 |
|---|---|---|
| **Junseok** (복귀 시) | `saIm0583` 앙상블 저압 15작업 — **군 A K_H 산포** | 전하 CIF 이미 있음. "로딩 산포를 기울기에 써도 되나" 는 **지금 쓰는 모든 K_H 판정의 기반** |
| **데스크탑** (08-27 06:00~) | `sa50nb50` 앙상블 **e1~e5 빌드 + 이완 + 전하** | **구조 생성이 이 기기만 가능.** 빌더 확장이 취소돼 이 시간이 통째로 비었음 |
| **랩탑** (`saIm050` 완주 후, 08-27 저녁) | `sa50nb50` 앙상블 저압 15작업 — **군 B K_H 산포** | 데스크탑이 넘긴 전하 CIF 를 받아 바로 착수 |

**랩탑 우선순위 1번(군 B)과 (나)(군 A)를 동시에 굴립니다.** 데스크탑이
빌드·이완·전하를 맡아 랩탑이 `saIm050` 을 도는 동안 준비를 끝내면, 랩탑이
비는 즉시 이어집니다. **유휴가 없습니다.**

> 랩탑이 (가)를 권한 논거는 맞습니다 — 군 A 가 기반이고 먼저입니다. 다만
> **택일해야 할 이유가 자원 부족이었는데 그 부족이 사라졌습니다.**

### 12-1. 그 다음 순번

    3. mslm 앙상블 — 건조 −OH 표의 "빌려온 1.01" 을 실측으로
    4. RH20 한 점 — 온셋 직전 곡선 보강
    5. mslm100 신규 — 실현 하나 추가라 우선순위 최하 (08-26 합의)

---

## 13. 구조 생성 — **데스크탑·랩탑은 임석 조건이 상시 충족됩니다** (2026-08-27)

사용자 확정, 08-27:

> *"구조 생성에 대해 말할게 있는데, 어짜피 laptop, HKHOME은 대부분의 시간에
> 내 옆에 있어서 구조 생성해도 아무 문제가 없어"*

### 13-1. 무엇이 바뀌나

    이전   구조 생성은 무인 창에서 금지 -> 새벽 배정에 구조 생성을 못 넣음
    지금   **데스크탑(HKHOME)·랩탑은 상시 임석으로 간주.** 시각 제약 없음

실제로 08-26 19:40 에 `sa50nb50` 앙상블 빌드를 06:00 에서 앞당긴 것이 이
규칙 때문이었는데, **그 판단이 이제 불필요합니다.**

### 13-2. 바뀌지 **않는** 것 — 자동 관문은 그대로입니다

임석은 사고를 **알아채게** 할 뿐 **막지는** 못합니다. 그래서 아래는 유지:

    구조 생성 후 검사 3종 (충돌·고아 원자·조성 개수)     유지
    build_*.py --dry-run 선행                          유지
    잘린 파일을 완성품으로 세지 않는 _validated() 가드    유지
    조성 불일치 시드는 흔적 없이 기각                    유지

### 13-3. 그리고 **08-19 함정을 이번에 막았습니다**

이 규칙을 만든 원인이 아직 살아 있었습니다. `rebuild_structures.py` 에
argparse 가 없어 `--help` 가 즉시 전체 재빌드를 시작하고 `structures_v2/` 를
덮어씁니다(08-19 실사고, 고정 시드라 피해는 없었음).

**임석 조건을 푸는 순간 이 함정이 더 중요해집니다** — 사람이 옆에 있어도
새벽에 자고 있으면 못 봅니다. 그래서 관문을 달았습니다:

    python rebuild_structures.py            -> 사용법만 출력, **아무것도 안 만듦**
    python rebuild_structures.py --help     -> 같음
    python rebuild_structures.py all        -> 같음  (예전엔 재빌드됐음)
    python rebuild_structures.py --run      -> 기본 대상 재빌드
    python rebuild_structures.py --run all  -> OTHERS 포함

**네 형태 전부 음성 시험했고 `structures_v2/` 가 무변경입니다.**

### 13-4. 다른 기기는 그대로입니다

`Junseok`·`laptop2` 는 **원격지·불안정**이라 임석 조건이 안 붙습니다.
그 기기들에는 **사람이 미리 만들어 둔 구조 위에서 계산만** 줍니다.
