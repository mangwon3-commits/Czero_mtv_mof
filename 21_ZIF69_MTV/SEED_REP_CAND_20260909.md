# 씨앗 반복 — 창 안·혼합 후보 4종 (등록, 2026-09-09 19:3x, 데스크탑; 자료 0건)

**왜**: `CAND_VERDICT §8` 습윤 표 23종은 전부 **조성당 실행 1회(n=1)** 라 유지율 오차에 **실행 간 항이 없습니다**
(`V3W_CHUNK_VS_SINGLE_20260907.md` "지금 유지율 오차에 실행 간 항이 없습니다"). 배치 단위(8.7 p)가 지배한다는 결론은 서 있지만,
"같은 구조·다른 씨앗" 의 산포를 모르면 §8 표의 `±` 가 무엇을 뜻하는지 못 적습니다. 09-09 11:5x 종합자 권고(9/12 예측 답변 첫 줄) —
30분 무응답 → CLAUDE.md §9 로 실행. **새 시험이 아닙니다** — 같은 조성·같은 설정의 반복이고 문턱을 새로 걸지 않습니다.

## 실행 (랩탑, 8코어 비어 있음 — 09-09 19:3x)

    조성    sa25nb75 · ms50nb50 · mslm025 · mslm050   (창 안·저술폰산 혼합·창 아래 혼합·단일 대조; saIm050 은 실현 5개가 있어 제외)
    씨앗    조성당 **2회 추가**(원 1회 + 2 = n=3). RASPA 씨앗은 착수 시각 → **드라이버마다 5 초 이상 어긋내 띄움**(`SEED_TWO_MEANINGS`).
    명령    `V3W_EXTRA=<t> V3W_SUFFIX=_rep2_<t> V3W_WORKERS=1 python run_water_v3w_cand_desktop.py --only <t>` 그리고 `_rep3_<t>`  → 8 드라이버, RH0→RH90
            (랩탑 경로/파이썬은 그쪽 규약대로; `charged_v3/<t>_DDEC6.cif` 는 master 에 있음)
    관문    파일 md5 8e8ec933 + 머리말 관문(`check_ff_per_run.py`) — 실행 후 조성마다.
    산출    `v3w_water_rep2_<t>/`, `v3w_water_rep3_<t>/` 의 water_results.json (+ 기기 태그 사본). 결과 표는 `CAND_VERDICT §8-1` 로.

## 쓰는 법 (등록)

    자      씨앗 반복은 **통계 비교** → 자는 **단위(±)**. "배치 단위" 라고 부르지 않는다(`RULER_DECISION §7`).
    적을 것 조성마다 유지율 3값과 그 표본 SD("실행 간 SD") 를 §8 의 `±`(블록 95 % CI) 옆에 병기. 배치 단위 8.7 p 와 나란히 셋을 적는다.
    금지    이 자료로 후보 순위·문턱 판정을 새로 만들지 않는다. n=3 의 SD 는 방향 표시용.
    예측    (미리 적음) 실행 간 SD 는 블록 CI 와 같은 자릿수(2~4 p)이고 배치 단위 8.7 p 보다 작다. 틀리면(>8.7 p) "배치 > 씨앗" 결론을 다시 본다.
