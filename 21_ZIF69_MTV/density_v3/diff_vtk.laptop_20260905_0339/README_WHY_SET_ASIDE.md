# 이 폴더는 **랩탑이 따로 뽑은 diff_vtk 한 벌**입니다 (2026-09-05 03:39)

master 가 `density_v3/diff_vtk/` 를 추적하기 시작하면서 **파일 이름이 열 개 다 겹쳤고,
내용은 열 개 다 달랐습니다.** §7(밀도 격자는 사본이 없다) 이므로 **덮지 않고 옆으로 옮겼습니다.**

**원인이 밝혀졌습니다 (04:3x).** 이 한 벌은 `COMDensityProfile_CO2.vtk`(**08-18 랩탑 실행**)
에서 뽑혔고, master 의 것은 같은 폴더의 `COMDensityProfile_CO2.vtk.gz`(**09-05 데스크탑
실행 — 현행·커밋본**)에서 뽑혔습니다. **두 벌 다 자기 원본에 r=1.000000 으로 충실합니다.**
틀린 계산이 아니라 **다른 원본**입니다.

    ⚠️ **이 한 벌은 09-05 실행의 그림이 아닙니다. 구판(08-18)의 그림입니다.**
       그림·판정에는 `../diff_vtk/`(현행)을 쓰십시오.

버리지 마십시오 — 두 벌이 있어야 **두 실행이 얼마나 다른지**를 잽니다.
전말과 측정값: `../../DENSITY_GRID_TWO_GENERATIONS_20260906.md`
