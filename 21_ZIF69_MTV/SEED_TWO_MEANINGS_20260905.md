# 이 저장소에는 "씨앗" 이 두 개 있습니다 (2026-09-05)

**서로 다른 양인데 이름이 같습니다. 09-05 에 한쪽의 크기를 다른 쪽의
근거로 쓴 사례가 나왔습니다.**

## 1. 두 씨앗

| | **빌더 씨앗** | **RASPA 난수 씨앗** |
|---|---|---|
| 무엇을 정하나 | **어느 자리를 치환하는가** (`chosen_sites`) | 몬테카를로 궤적 |
| 어디에 있나 | `build_grid_v3.py`, `build_ensemble_0583.py` (`seed`, `MAX_SEED 13`) | **없음. 러너가 안 씁니다** |
| 어떻게 정해지나 | 코드에 명시 (`[1,2,3,4,5]` 연속) | RASPA 가 **작업 폴더 생성 시각 +3초** |
| 재현되나 | **예** — 같은 씨앗은 같은 구조 | **아니오** — 매 작업마다 다름 |
| 흩어짐의 이름 | **배치 산포** (자리 배치) | **실행 간 산포** (s_rep) |
| 크기 (건조 로딩) | 통계오차의 **15배** (`RULER_DECISION_20260828.md:28`), 08-25~27 전량 **5배** (`JUNSEOK_20260827.md:80`) | **0.66 × `±`** — Junseok 4회 재실행 실측 R̂=0.008941 (`RASPA_SEED_20260828.md` 상단 배너) |
| 크기 (기타) | — | 격자 원계수 **9~16%**, 물 K_H **3.0%** (기기 간 `base` 1쌍) |
| 기록 | `COMMS/desktop.md:1762`, `rebuild_index.json` | `RASPA_SEED_20260828.md` |

## 2. 실제로 벌어진 일

laptop2 가 밀도 격자 재실행을 논하며 **"씨앗 간 최댓값 변동 28%"** 를
분모로 삼아 *"부족분 1.7% 는 28% 의 16배 작다"* 는 논거를 세웠습니다.

**28% 는 1차 출처가 없습니다.** 데스크탑이 세 브랜치 전수 감사한 결과:

- `COMMS/desktop.md` 에 **"씨앗" 이 0회**. 그 파일의 `seed` 는 전부
  **빌더 씨앗**입니다(`seed 0` 대 `1/3/5`, `chosen_sites`, `MAX_SEED 13`).
- `"씨앗 간"` 은 저장소 전 브랜치에서 **정확히 한 곳** — `COMMS/laptop.md:772`,
  순열검정 MC 씨앗 셋의 폭 **0.00027/0.02087 = 1.3%** 입니다. 28% 가 아닙니다.
- `"28%"` 검색 결과는 전부 무관합니다(75.05±3.28%, R²=0.2811,
  X≥12.28%, 자유도 28%).
- 데스크탑이 09-04~05 에 쓴 문서 넷도 28% 를 쓴 적이 없습니다.

**출처는 laptop2 자신의 인계 문서 넷이고 서로를 되풀이합니다.**

## 3. 왜 네 문서를 통과했나

**틀린 대상에 대해서는 큰 수가 그럴듯했기 때문입니다.**

두 씨앗의 흩어짐이 **한 자릿수 배 차이**입니다:

    RASPA 씨앗   실행 간 SD = 0.66 x `±`   <- `±` 보다 작다
    빌더 씨앗    배치 산포  = 5~15 x 통계오차 <- `±` 보다 크다

"씨앗 때문에 28% 흔들린다" 는 문장은 **빌더 씨앗 이야기로 읽으면 과하지
않습니다.** 그래서 검토자가 크기 감각으로 거르려 해도 안 걸립니다 —
걸러야 할 것은 크기가 아니라 **어느 씨앗인가** 였습니다.

거꾸로, 이 구분이 서면 **28% 는 즉시 이상합니다**: RASPA 씨앗의 실측 흩어짐은
`±` 보다 **작고**, `±` 자체가 로딩의 2~3% 수준이기 때문입니다.

> **규율:** "씨앗" 이라고만 쓰지 마십시오. **빌더 씨앗** / **RASPA 씨앗**
> 으로 적고, 인용하는 수는 **어느 씨앗을 잰 것인지** 함께 적으십시오.

## 4. 이 정정이 바꾸는 것 / 안 바꾸는 것

**안 바꿉니다** — laptop2 의 결론(재실행 불필요)은 9~16% 자로도 배율 9.4배라
유지됩니다. 데스크탑 `TB2_CRITERION_NOTE_20260905.md:85` 의 "씨앗 재현성 3.0%"
는 **두 기기가 각자 돈 `base` 물 K_H 1쌍**이므로 RASPA 씨앗이 다른 것이 맞고,
laptop2 의 ① 정정(모든 작업이 서로 다른 씨앗) 아래에서 **오히려 더 정확한
이름**이 됩니다.

**바꿉니다** — laptop2 의 ① 정정에 따라 `nbIm025 ↔ saIm050` 격자 비교도
`base` 와 **똑같이** 실행 간 산포를 탑니다. 세 격자 모두 **실현 1개**이므로
**T-4 판정에는 "실행 간 산포 미측정" 단서를 병기**해야 합니다. 재실행이
새로 만든 조건이 아니라 **처음부터 있던 조건**입니다.

---

## 5. 같은 이름의 다른 양 — **하루에 세 건**입니다 (09-05)

| 이름 | 갈라지는 두 양 | 어떻게 드러났나 |
|---|---|---|
| **씨앗** | 빌더 씨앗(자리 배치) ↔ RASPA 난수 씨앗 | 한쪽 크기(28%)를 다른 쪽 근거로 씀 |
| **ρ** | 실측 상관계수 ↔ **임계값** | 데스크탑이 −0.886 을 n=6 임계(0.8857)로 의심 — **철회**했으나(실측 맞음) 질문은 옳았음 |
| **K_H(물)** | 저RH 등온선 **외삽** ↔ **Widom 삽입** | `base` 에서 8~11% 차이. `SRCC_SIGN_AMENDMENT_20260905.md` 부-1 |

**셋 다 크기 감각으로는 안 걸립니다** — 두 양이 자릿수가 비슷하기 때문입니다.
정확히 그래서 이름이 같은 채로 살아남습니다.

> **규율 (laptop2 정식화, 09-05):** *이름이 같은 두 양이 있으면, 크기를 대기 전에
> 어느 쪽인지부터 확정한다. **크기 검사는 이름이 하나일 때만 작동한다.***
>
> 실무로는 — **수를 인용할 때 이름만 옮기지 말고 "무엇으로 잰 것인지" 를 함께
> 옮기십시오.** "물 K_H" 가 아니라 "물 K_H(Widom)" 입니다.

## RASPA 씨앗 규칙 — 2026-09-06 23:3x (Melchior 제안, 종합자 등록)

    평상시   씨앗을 **지정하지 않는다.** RASPA 가 정한 값(착수 시각)을 출력 머리말 `Random number seed:` 에서 **회수해 산출물 행에 `raspa_seed` 로 적는다**
             (run_water_v3w.py stamp(), T-B2w 39행 회수 완료). 결과 JSON 이 나오면 실행 폴더를 지워도 되므로 **회수는 JSON 을 쓰는 그 시점에**.
    재현     특정 실행을 재현할 때만 기록된 씨앗을 입력 `RandomSeed` 에 명시한다.
    금지     **템플릿에 고정 씨앗을 박지 않는다** — 모든 반복이 비트 단위 재생이 되어 `s_rep` 이 0 으로 무너지고, `s_rep` 은 RULER_DECISION §7 의 자다.
    반복     `s_rep` 반복을 같은 조성으로 띄울 때는 **초를 어긋내거나(2초 간격) 씨앗을 명시**한다 — 같은 초에 뜨면 같은 씨앗 = 재생(laptop2 09-05 의 2초 어긋내기가 이것).
    검사     폴더가 없어도 재생은 잡힌다: 같은 구조·같은 씨앗이면 로딩 값이 **정확히 일치**한다. 기존 s_rep 표본(saIm0583e1~5, 7·3·4·3·3판) 전부 값 상이 → 재생 0건(Melchior 09-06).
    현황     지금 결함은 "씨앗이 같다" 가 아니라 "기록이 안 남았다" 이고, 회수로 고쳐졌다. T-B2w 의 같은-초 쌍 넷은 **다른 구조**라 무해.

