# 배정 라운드 2026-09-04 — 취합을 먼저 닫고, 유휴 두 대에 비용 0 과 자 교정을 겁니다

**작성**: 데스크탑 (09-04 17:5x).
**근거**: `ASSIGN_20260903.md`(전 라운드, §6·§7 진행 중), `DESIGN_STUDY_20260903.md`
5-D 우선순위, `COMMS/audit.md` **09-04 00:10** 감사(맨 위 항목), `bash bgstate.sh` 09-04 17:28 실측.
⚠️ 초판에서 이 파일의 `tail`(08-26 항목)을 오늘 것으로 읽어 D1 을 잘못 세웠습니다 — 18:0x 정정.

> **이 문서의 판정 기준은 전부 수를 보기 전에 등록된 것입니다.** 실행 후에는
> 고치지 않습니다(CLAUDE.md §2). 등록 문턱을 바꿔야 할 이유가 생기면 **고치지
> 말고 새 항목으로 등록**하고, 바뀐 사실을 판정문에 병기하십시오.

---

## 0. 배정의 전제 — 지금 실제로 무엇이 도는가

| 기기 | 코어 | 지금 (실측) | 이 라운드 |
|---|---|---|---|
| **데스크탑** | 8 | **유휴** — `bgstate.sh` 09-04 17:28 "도는 것 0", 부하 0.15, 가용 18 GB | §2 **D2~D5·D7 완료**, D6 도착 대기 |
| **랩탑** | 8 | R 측정 **완주**(09-03 10:4x, 비 1.759 → 수렴). 이후 문서·검산만. **xtb 미설치** | §3 T-B1 → T-B2 |
| **laptop2** | 6 | **실행 중** — §6 T-C1 GCMC + §7 통짜 r3/r4 | §4 **새 일감 없음**, 완주 처리만 |
| **Junseok** | 6 | **정지** — 자격증명 만료(08-29 이후 커밋 없음) | §5 §2 유지, 복귀 시 착수 |

**살아 있는 작업은 재시작하지 않습니다.** laptop2 의 §6 체인과 §7 통짜 4회는
이 라운드에서 손대지 않습니다.

---

## 1. 취합 — 이 라운드에서 **이미 끝냈습니다** (§4 ①②)

09-03 라운드 §4 가 "그날 안에" 로 고정한 규칙이 하루 밀려 있었습니다. 09-04 17:4x 에
데스크탑이 처리했습니다.

| 가지 | 미취합이던 것 | 내용 | 병합 커밋 |
|---|---|---|---|
| `laptop-20260822` | 8커밋 (09-03) | 전부 `COMMS/laptop.md` 413행 — §7-4 구간겹침 이의, 랩탑 대조값 1.76 정정, dof 4n 근사 크기 측정, MC 잡음 판정과 그 정정 | `944df98` |
| `junseok` | 3커밋 (08-29) | 전부 `COMMS/junseok.md` 103행 | `806c115` |
| `laptop2-20260825` | 0 | 09-03 18:18 `61ee61a` 로 이미 반영됨 | — |

충돌 0(사전 `git merge-tree` 확인, 우편함은 한 파일 한 필자라 구조적으로 안 부딪힙니다).
`origin/master` 푸시 완료. **세 가지 전부 미취합 0.**

- 내용 자체는 데스크탑이 이미 `ASSIGN_20260903.md` §7-4 에 옮겨 적어 두어 **판정 공백은
  없었습니다.** 밀린 것은 장부입니다. 그래도 ② 위반이 하루 났으므로 기록합니다.
- **남은 취합 트리거**: laptop2 의 `results_tb5.json` 도착 → ①(결과 JSON)로 그날 안에.
- ⚠️ **§4 ⑤ 참조 감사는 이번 취합에도 못 돌렸습니다** — 스크립트가 아직 없습니다.
  D2 에서 만들고, 만든 뒤 이번 두 병합에 **사후 1회** 돌려 결과를 이행 기록으로 남깁니다.

---

## 2. 데스크탑 — 비용 0 묶음 · 감사 결함 2건 · 도착 처리

5-D 우선순위 1순위가 "T-1·T-2·T-3(비용 0)" 입니다. 데스크탑이 유휴이고 이 항목들은
RASPA 를 쓰지 않아 laptop2 의 체인과 코어를 다투지 않습니다.

### D1. ~~`regen_energy_v3.py` 승자 조성 누락~~ — **[09-04 18:0x 정정] 이미 고쳐져 있었습니다. 폐기**

**제 오독입니다.** 착수 직전에 `regen_energy_v3.py` 를 열어 확인한 결과:

    455e5c2 (08-27 13:06)  "재생에너지 표에서 승자 조성이 빠져 있던 것을 고쳤다 — 하드코딩이 원인이었다"
    -> NAMES 하드코딩은 그때 걷혔고 지금은 glob 으로 자료에서 유도합니다
    -> regen_energy_v3.json 실제 행 **7개**:
       base · mslm075 · saIm025 · saIm050 · **saIm0583** · saIm075 · saIm100

**제가 인용한 경고는 09-04 감사가 아니라 08-26 감사였습니다.** `COMMS/audit.md` 는
최신이 맨 위인데 제가 `tail` 로 읽어 가장 오래된 항목을 오늘 것으로 착각했습니다.
그 경고는 다음 날(08-27) 이미 처리됐고, 스크립트 주석에 그 이력이 그대로 적혀
있었습니다.

**09-04 00:10 감사의 "사람이 볼 것" 은 두 건뿐입니다:**

    ① junseok 두 가지 6.3일/5.9일 정지 + 우편함 5.9일 침묵  -> §6 사용자 몫
    ② laptop-20260822 37커밋 뒤처짐                          -> §1 에서 취합 완료
    나머지(JSON 무결성·출처 중복·사전 등록 관문·**WC 자료 공백**)는 **이상 없음**

즉 이 라운드에 남는 감사 결함은 **0건**이고, D2 부터가 실제 작업입니다.

> 이 항목을 지우지 않고 남깁니다. CLAUDE.md §0 이 경계하는 "나쁜 결과를 보면
> 검사기부터 의심" 의 거울상 — **오래된 검사 결과를 오늘 것으로 읽은** 경우이고,
> 파일이 최신 우선 정렬이라는 것만 확인했으면 안 났을 일입니다.

### D2. ✅ **완료** — `ref_audit.py` (§4 ⑤ 참조 감사 스크립트)

> **[09-04 18:1x 결과]** 취합 후 1회 실행: **끊긴 참조 0건**. 규칙 ③ 만으로는
> 못 거르는 오탐이 하나 더 있어 **③'**(플래그된 이름이 master 실파일 basename 의
> 접미사이면 자동 오탐)와 **④**(예정 산출물은 `ref_audit_ignore.json` 에 사유와
> 함께, 요약에도 찍음)를 붙였습니다. `external16.md` 는 등록표로 도달 가능하나
> **어느 가지에도 실파일이 없어** 따로 표시합니다. 클라우드 가지 전용 문서 3건은
> 파일 단위로 회수(가지 통째 병합은 .py 충돌 1건 때문에 하지 않음). 커밋 `84cfdde`.

09-03 §4 ⑤ 가 "만들면 여기 파일명을 적습니다" 로 비워 둔 칸입니다. 규칙은 그때
랩탑·laptop2 가 실제로 돌려 얻은 것 그대로이고, 새로 만들지 않습니다:

    ① 양방향 — (a) master 문서가 인용하는데 master 에 없는 .md
                (b) 가지 문서가 인용하는데 그 가지에만 있는 .md
    ② "master 에 없음" 만으로 끊긴 참조로 판정하지 않는다.
       **master 에 없음 AND `COMMS.md` 등록표로 가지·상태를 알 수 없음** 일 때만.
       (`cloud4c.md` = 14곳 인용·master 부재지만 등록표 447행이 가지와 종료 상태를
        적어 두어 도달 가능 → 오탐)
    ③ 파일명 정규식에 **선행 숫자**를 넣는다 (`48H_COMPUTE_PLAN.md` 가
       `H_COMPUTE_PLAN.md` 로 잘려 오탐 난 전력)

    산출   `ref_audit.json` + 사람이 읽을 요약. 취합 **전에** 돌리는 것이 규칙이지만
           이번 두 병합은 이미 끝났으므로 **사후 1회** 돌려 결과를 §1 밑에 붙입니다

### D3. ✅ **완료** — T-1 앙상블 재표기 → `T1_ENSEMBLE_TABLE.md` (커밋 `15313a5`)

> **[09-04 18:2x 결과]** 재표기 가능한 조성은 `saIm0583`(n=6) 하나뿐. `ens0583`
> 두 파일이 **바이트 동일**이라 md5 로 걸렀습니다(08-23 이중 계수 유형).
> `±` 는 배치 SD 0.0580 이 아니라 **0.0657**(2.776×SEM). 생산 실현 0.8328 이
> 6개 중 최댓값. 단일-단일 45쌍 중 **35쌍**이 배치 산포를 넣으면 1.5 단위
> 아래로 내려갑니다(감도, 판정 아님). 유지율 앙상블은 자료가 없어 비워 둠.

DESIGN_STUDY T-1 이 "모든 습윤 WC·유지율을 실현체 평균 ± CI 로 재표기" 인데, 데스크탑이
파일을 세어 본 결과 **재표기가 가능한 조성이 하나뿐입니다.** 수를 보기 전에 이 사실과
판정 규칙을 등록합니다.

    실현체 수 (파일 실측, 09-04)
      saIm0583   **6**  — v3_humid_wc_ens/humid_working_capacity_ens0583.json (e1~e5)
                          + v3_humid_wc/humid_working_capacity_g0583.json (1)
      그 밖 전부  **1**  — base·saIm025/050/075/100·saIm0625·mslm075·sa25nb75·
                          ms50nb50·sa50nb50 모두 단일 실현

    ⚠️ 단위 — 기존 문서의 `0.7306 ± 0.0580` 은 **배치 SD** 입니다
       (`NEW_MACHINE_20260822.md:28` "6실현 평균 ± 배치 SD"). T-1 이 요구하는 것은
       **2.776 × SEM** 이고 둘은 다른 수입니다. CLAUDE.md §2 가 경계하는 단위 혼선의
       바로 그 자리이므로, T-1 표에는 **두 값을 병기**하고 어느 것이 `±` 인지 적습니다.

    판정 (수를 보기 전에 고정)
      동급     |Δ평균| < 1.5 × 합성 ±,  합성 ± = √(±A² + ±B²)
      n=1 조성 ± 에는 RASPA 통계오차만 들어 있고 **배치 산포가 빠져 있습니다.**
               그러므로 **단일 실현 대 앙상블(n=6) 비교는 "동급" 으로도 "우세" 로도
               굳히지 않습니다** — "분모 비대칭, 판정 유보" 로 적습니다
      비교가 성립하는 것은 (a) 앙상블 대 앙상블, (b) 단일 대 단일 뿐입니다

    산출   `T1_ENSEMBLE_TABLE.md` + `t1_ensemble.json`. "1등" 표기를 쓰는 문서
           목록과 각 줄이 어느 분모 위에 있는지 함께 적습니다

### D4. ✅ **완료** — T-2 교락 표 → `LCD_LOADING_CONFOUND.md` (커밋 `908ac1b`)

> **[09-04 18:2x 결과]** 두 자 사이 **r = +0.103** — 이름만 같고 같은 양이
> 아닙니다. 같은 5구조에서 사다리 상관이 관문 자 **+0.986**, GCMC 자 **+0.213**.
> `STAGE2_SAIM075` 322행의 등록 플래그 발동: 배치 간 r 이 0583 −0.669 대
> 075 **+0.479**. 고칠 남의 문서는 없었습니다.

    할 일   §1-3 의 r 값과 쌍 비교를 저장소 문서로 고정. 관문 LCD 와 GCMC 기하 LCD 를
            나란히 놓습니다 (§0-0: 조성 간 n=31 r=0.06, saIm0583 배치 간 n=5 r=−0.76)
    문구    "좁히기" 를 **"부피 큰 극성기의 표지"** 로 정정하고, 정정 대상 문서와
            줄 번호를 목록으로 남깁니다(남의 문서는 고치지 말고 필자에게 요청 — §5)
    산출   `LCD_LOADING_CONFOUND.md`
    병기    배치 간 음의 상관(n=5, r=−0.76)은 **e6~e10(T-C10) 에서 재검** 대상이고
            지금 인과로 읽지 않습니다

### D5. ✅ **완료** — T-5 → `T5_RING_PAIRS.md` (커밋 `003a900`)

> **[09-04 18:3x 결과]** 최소상 단위 시험 통과(2×2×2 에서 정확히 8배).
> 등록 판정 **"차이 실재"**(base 0.0 대 앙상블 11.83 ± 3.16). 다만 base 의
> 마주봄 거리가 이산적(6.455 · 7.134)이라 **등록 대역이 그 사이에 떨어지고**,
> 대역을 흔들면 판정은 유지되나 **방향이 뒤집힙니다**([6.0,7.5] 에서는 base 가
> 더 많음). 그래서 등록문의 해석 조항("좁히기 서사 일부 복원")은 적용하지
> 않고, 방향을 묻는 시험은 **T-5′ 로 새로 등록**합니다.

등록문 T-5 의 판정칸이 "base 와 saIm0583 의 대역 쌍 수 차이" 로 비어 있어, 실행 전에
채웁니다. 새 자를 만드는 것이 아니라 저장소가 쓰던 2.776 규약을 그대로 씁니다.

    대상    v3 CIF (`charged_v3/*_DDEC6.cif`). base 1개 + saIm0583 계열.
            **스크립트가 먼저 대상 파일 목록을 찍고**, 앙상블 실현이 6개에 못 미치면
            그 사실을 보고하고 멈춥니다(추측으로 채우지 않습니다)
    고리    방향족 고리 = 이미다졸레이트 5원 고리와 벤젠계 6원 고리. 고리 중심은
            고리 원자 좌표의 산술 평균, 평면 법선은 고리 원자에 대한 최소제곱 평면
    마주봄  두 고리 법선 사이 각 ≤ **30°**
    대역    중심–중심 거리 **6.5–7.0 Å**, 주기 경계 최소상 적용
            ⚠️ 언랩 버그가 이 저장소를 한 번 태웠습니다(08-04). 최소상 처리를
            **단위 시험으로 먼저 확인**하고 그 결과를 보고에 적으십시오
    정규화  단위셀당 쌍 수

    판정 (수를 보기 전에 고정)
      차이 실재  base 의 값이 saIm0583 앙상블 평균 ± 2.776×SEM **밖**
      차이 없음  안에 들면 → **saIm 의 Q_st 상승은 슬릿이 아니다**(등록문 예상)
      base 는 단일 실현이라 자기 CI 가 없습니다. 이 비대칭을 판정문에 병기합니다

### D6. 도착 처리 — 대기 중인 두 건

    §7 r4 (laptop2, ~20:0x 전망)
      받을 것  ① `run_water_whole_nbim025_rep.py --summary` 출력
               ② `srep_machine_test.py` 출력 전문 (또는 v3_water_srep_whole/ 의 summary JSON)
      **둘 다 필요합니다** — `--values/--ci95` 로 넣으면 `load_inputs` 가 F_A 를 None 으로
      두고 "비교 A 의 F 를 모름" 을 찍습니다(`srep_machine_test.py:163`). §7-3 의 F 는
      `--summary` 에서만 나옵니다
      순서    §7-3(러너 축) → §7-4(기기 축, A 가 F<15.44 일 때만) → §7-5(± 동등성)
      데스크탑 재현  `--selftest` 가 09-04 17:3x 에 등록값을 자릿수까지 재현했습니다
                     (0.0634 / 0.0513 [5M 0.0528] / 0.0204). 독립 재현 가능 상태입니다

    T-C1 GCMC (`results_tb5.json`)
      도착 즉시 §4 ① — 그날 안에 master 취합
      판정문에 **T-B5 탈락 병기 필수**: azbIm050/075/100·bIm100 은 LCD +3.5~18.5% 확장으로
      관문 탈락이므로 이 넷의 Q_st 차이를 **단독 인용하지 않습니다**(laptop2 09-03 ④,
      새 문턱이 아니라 관문의 뜻 그대로)

### D7. ✅ **완료 — 등록 후 실행, 그리고 폐기** → `T3_PRIME_20260904.md` (커밋 `ec3f37f`·`9b16bf4`)

> **[09-04 19:0x 결과]** 등록문을 먼저 커밋하고 그 순서대로 돌렸습니다.
> 문턱은 시험 대상이 아닌 자료(실험 ZIF CIF 8개, 6원 고리 720개)에서만
> 유도해 **0.0556 Å**. 그런데 **판별력 시험 조건 ②에서 실패**했습니다 —
> δ=0.20 Å 로 접은 합성 대조를 24개 중 21개만 잡습니다. 등록문 결정표
> 1행대로 **T-3′ 폐기**, T-3 의 7개 인용 금지 그대로 유효, 4단계 재판정은
> 돌리지 않았습니다. 문턱을 다시 고르지 않습니다 — 갈아타려면 T-3″ 를
> 새로 등록하고 "세 번째 자" 임을 적어야 합니다.
> 부수 발견: 문턱을 `ZIF-69-crystal.cif` 한 구조가 혼자 정했고, 같은 물질의
> 두 배포본 파일이 **20배** 다릅니다(0.0027 대 0.0556).

`audit_probe_noshrink.py` (a) 가 7개를 "깨진 구조" 로 찍었는데 **전부 6원 고리 평면성
한 항목만**이고 RMSD ≤ 0.016 Å 였습니다 — 규칙의 판별력 문제(§0-0 항목 4). T-B5 5% 조항
감사(`bddafc8`)에서도 같은 형태가 나왔습니다. **재판정 기준을 결과 전에 등록**해야
하는데, 지금 그 기준이 없습니다.

D3~D5 를 끝낸 뒤 여유가 있으면 착수하고, 없으면 다음 라운드로 넘깁니다. **착수하면
기준을 먼저 문서로 올리고 그 다음 돌립니다.**

---

## 3. 랩탑 — 자 교정 (T-B1 → T-B2)

**먼저 한 줄 보고**: 지금 도는 것이 있으면 그것이 우선입니다. `bash bgstate.sh` 결과와
함께 `COMMS/laptop.md` 에 적고 시작하십시오. R 측정은 09-03 10:4x 에 완주 보고됐습니다.

**이 기기에 §6 계열 이완을 배정하지 않습니다** — xtb 미설치(랩탑 09-03 15:1x). 아래는
전부 RASPA·후처리라 그 제약과 무관합니다.

### 3-1. T-B1 — TIP5P-Ew ΔH_vap(298 K). **0단계는 덱 등록입니다**

5-D 에서 T-B1~B6 이 T-C 앞에 있고, T-B1 은 **T-B3·T-B4·T-B6 판정의 자**입니다.
T-B6 이 아자 노선의 관문이므로 이것이 임계 경로입니다.

    0단계 (실행 전)  상자 크기·앙상블·사이클 수·ΔH_vap 산출식을 `COMMS/laptop.md` 에
                     올리고 데스크탑 승인을 받은 뒤 돌립니다. **수를 보기 전에** 올려야
                     의미가 있습니다. 우리 규약 고정값(Ewald 1e-6, 컷오프 12 Å,
                     TIP5P-Ew 5자리)은 그대로 씁니다
    ⚠️ 층위        골격 없는 순수 물 상자입니다. CLAUDE.md §1 의 고정값을 바꾸는 것이
                     아니라 **같은 물 모델을 다른 계에 적용**하는 것이고, 보고문에 이
                     문장을 그대로 적어 오독을 막으십시오
    판정 (등록문 그대로)  |ΔH_vap − 44 kJ/mol| ≥ 3 이면 저장소의 문턱 문구를
                     **"TIP5P-Ew 기준 X"** 로 교체. 44 는 TIP4P 기준값입니다
    병기            이후 모든 ΔH_ads 판정이 이 값을 자로 씁니다 — 교체가 발생하면
                     영향받는 문서 목록을 같이 냅니다

### 3-2. T-B2 — 물 K_H (Widom 10⁵ 삽입), T-B1 완주 후

    대상   v3 관문 통과 32 조성 + base. **목록을 먼저 기계 추출해 등록**하십시오
           (`relax_v3_judged.json` / `rebuild_index.json` 에서). 손으로 세지 마십시오
    문턱   Moghadam **5 × 10⁻⁶ mmol/g/Pa** — ⚠️ **TIP4P 기준 문턱**입니다. TIP5P-Ew
           재보정은 T-B4 사다리의 몫이므로, 이번 판정문에는 "TIP4P 기준 문턱을
           TIP5P-Ew 값에 적용" 이라는 한정어를 반드시 답니다
    판정   ① 문턱 초과 조성은 습윤 WC 측정에서 후순위
           ② K_H 순위 대 **실측** 유지율 순위 Spearman ≥ 0.6 이면 1단 대리 지표로 채택
              (P3 ρ 0.61 과 비교). 유지율 실측이 있는 조성만 순위에 넣고, 몇 개로
              쟀는지 병기합니다 — n 이 작으면 ρ 는 쉽게 흔들립니다
    비용   GCMC 의 수백분의 1 × 33

---

## 4. laptop2 — **새 일감 없음.** 완주 처리만

6코어가 §6 체인(T-C1 GCMC)과 §7 통짜 4회로 이미 만재입니다. **여기에 아무것도 더
얹지 않습니다.** 완주 시 세 가지만:

    ① §7 r4 완주 → §7-3 → 7-4 → 7-5 순서로 판정, 출력 **전문** 두 개를 데스크탑에
       (§2 D6 의 "받을 것" 두 줄). r1~r4 네 값과 각 ±·σ_stat 을 함께
    ② T-C1 `results_tb5.json` → 가지 푸시 + `COMMS/laptop2.md` 규약 ③ 보고.
       판정문에 T-B5 탈락 병기(위 D6)
    ③ 작업 비용 실측 (RH0/25/50/90, 동시 워커 수 병기) — 08-29 에 측정 중이라고
       적고 아직 안 올라온 칸입니다. 체인이 끝나면 그 로그로 채워집니다

**T-C1 이후의 다음 단계(T-B6 / T-C2)는 이 라운드에서 배정하지 않습니다.** 조건 분기는
`DESIGN_STUDY_20260903.md` T-C1·T-C2 에 이미 등록돼 있고, 자원이 나는 시점에 배정합니다.
다만 지금 적어 둘 것 하나 — **T-B5 관문을 통과한 아자 계열은 azbIm025 (5실현) 뿐**이고
050/075/100 은 전부 탈락입니다. T-B6 이 az050·az100 을 대상으로 등록돼 있으므로,
그 배정 때 "탈락 구조에서 잰 값" 이라는 한정어를 어떻게 다룰지 **결과 전에** 정해야
합니다. 지금 정하지 않고 여는 항목으로 남깁니다.

---

## 5. Junseok — §2 그대로, 복귀 시 착수

`ASSIGN_20260903.md` §2(nbIm025 RH0 4회, 조성 축 가름) 변경 없습니다. 정지 원인은
자격증명 만료이고, 미취합 3커밋은 이 라운드에서 취합됐습니다(§1) — **계산 유실 0**.

복귀하면 §2-3 의 F 검정(임계 5.89 / 0.068)을 그대로 적용하십시오. 그 사이 §7 이
러너 축을 따로 가르고 있으므로, 두 결과가 **같은 질문의 다른 축**이라는 것을
판정문에 적습니다.

---

## 6. 사용자 몫 (세션이 하지 않는 것)

1. **`RULER_DECISION`** — 09-03 라운드에서 유일하게 남은 미결 의제. 기기 배정이 아니라
   사용자 결정입니다.
2. **Junseok 복구** — `/login` + `git push origin junseok`.
3. **C: 디스크 회수** — `bgstate.sh` 6절: WSL 안에서 1115 MB 를 비웠지만 vhdx 는 스스로
   줄지 않습니다. C: 여유가 11 GB (96% 사용) 입니다.
   `wsl --shutdown` → `wsl --manage Ubuntu --set-sparse true` → `wsl -d Ubuntu -e true`

---

## 7. 이 라운드에서 하지 말 것

| 금지 | 이유 |
|---|---|
| laptop2 체인·§7 에 무엇이든 더 얹기 | 6코어 만재. 경합하면 §6 비용 실측까지 오염됩니다 |
| 살아 있는 §6·§7 재시작 | 체크포인트는 배치만 복원하고 진행도는 복원하지 않습니다 |
| 랩탑에 GFN-FF 이완 배정 | xtb 미설치 (랩탑 09-03 15:1x) |
| azbIm050/075/100·bIm100 값 단독 인용 | T-B5 탈락 — 공극 +3.5~18.5% 확장, 화학·기하 효과 미분리 |
| 배치 SD 를 `±` 로 쓰기 | `±` 는 95% CI(2.776×SEM)입니다. D3 이 정확히 이 자리입니다 |
| 실행 후 문턱 고치기 | 고치지 말고 새 항목으로 등록하고 병기 |
| 여기서만 쓰는 용어 만들기 | 쓰기 전에 파일로 (CLAUDE.md §8) |
