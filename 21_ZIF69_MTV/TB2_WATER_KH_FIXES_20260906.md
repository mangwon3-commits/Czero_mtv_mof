# `run_tb2_water_kh.py` 고침 목록 — **여덟 건 전부 반영 완료** (랩탑, 2026-09-06)

> ✅ **11:3x 실행됨** — `WATER_FIX_20260906.md §3 ②` 로 8건 전부 승인. 아래 목록은
> **무엇을 왜 고쳤는지의 기록**입니다. 경로도 새 계열로 옮겼습니다:
> `RUNS = tb2w_runs/` · `OUT = v3w_water_kh/`.
>
> **덧붙은 것 (0절 조사에서 나온 함정 둘, 코드에 박음)**:
> ① RASPA 쌍 이름은 **오른쪽 정렬 패딩** — `'Hw - Hw' in ln` 은 절대 안 맞습니다.
> ② **`22.14170` 을 grep 하면 안 됩니다** — 고친 판 출력에도 **12,246번** 나옵니다
> (골격 수소 `H_` 의 정당한 UFF 값). **쌍 이름으로** 보십시오.

**배정**: 종합자 10:4x — *"다섯 고침 목록을 문서로 정리. 물 축 계산 정지는 `WATER_FIX_20260906.md` 도착까지 유지."*
**이 문서는 목록이고 실행이 아닙니다.** 코드도 힘장도 손대지 않았습니다.
**대상 파일** `run_tb2_water_kh.py` (148행, `4580203`).

---

## 0. 먼저 — **공용 힘장 조사 결과** (조회만, 수정 안 함)

    파일   /home/skyjun/RASPA/simulations/share/raspa/forcefield/UFF_MOF/
           force_field_mixing_rules.def   (3,384 B, 2026-08-03 15:51)

    **`Hw` 항 없음** · **`Lw` 항 없음** · `Ow` **있음**(54행, `lennard-jones 89.633 3.097`)
    -> `Ow` 는 배포본 `Dubbeldam2007FlexibleIRMOF-16` 값과 **같습니다.** 결함은 `Hw`·`Lw` 두 줄뿐입니다.

    접두 일치로 `Hw` 가 물려받는 것:  **`H_` (17행) `22.1417 / 2.57113`** — UFF 수소
    `Lw` 가 일치할 후보:               **없음** (`L` 로 시작하는 정의 0개)

**삽입 위치와 개수 (고칠 때 정확히 이 자리)**:

    6행    `55`                       <- **선언된 항 수. 57 로 바꿔야 합니다**
    8~62행 항 55개                     <- 선언과 실제가 **일치**(검산함)
    63행   `# general mixing rule for Lennard-Jones`
    64행   `Lorentz-Berthelot`

> ⚠️ **두 줄은 62행 뒤·63행 앞에 넣습니다.** 09-05 에 제가 `Lorentz-Berthelot` **뒤**에
> 붙였다가 RASPA 가 꼬리말을 항으로 읽어 `Hw-Hw` 가 22.1417 로 남았습니다.
> **개수 줄을 안 고치면 뒤쪽 두 항이 무시됩니다.**

**⭐ 그리고 어느 형태를 넣을지 — 두 형태가 서로 다른 관측량에서 검증돼 있습니다:**

    `Hw none` / `Lw none`            데스크탑 대조 `widom_control_hwnone_hkhome.json`
                                     base K_H **6.250e-05** 대 현행 **5.394e-04** -> **D = 8.63**
                                     **배포본과 같은 형태.** 골격 안 Widom 축에서 검증됨
    `Lw none` / `Hw ε=0`             제 T-B1 대조 `tb1_runs_ffctl`
                                     구조 3창 통과 · ΔH_vap 43.33 · 수소결합 95 %
                                     **순수 물 상자 축**에서 검증됨

**에너지는 같습니다**(ε=0 -> LJ 0). 다른 것은 비용뿐이고 `none` 이 싸며 배포본 형태입니다.
**둘 중 하나만 골라 넣되, 판정문에는 "이 형태는 어느 축에서 검증됐다" 를 적으십시오** —
두 축을 한 형태가 다 통과한 적은 아직 없습니다.

---

## 1. 종합자가 지정한 다섯

### (ㄱ) `import ase` 관문을 `main()` 머리에 — **인터프리터 이름을 박아서**

`unit_cells()` 가 **함수 안에서** `from ase.io import read` 합니다(45행). 이 함수는
`Pool` 워커 안에서 처음 호출되므로, **폴더를 만들고 CIF·water.def 를 복사한 뒤에야** 터집니다.

**실측**: 독스트링이 시키는 `python3 run_tb2_water_kh.py A` 의 그 `python3` 에
**ase 가 없습니다**(`ModuleNotFoundError`). 있는 곳은 conda 셋:

    czeromof 3.29.0  ·  coremof_tools 3.26.0  ·  lammps_mof 3.29.0

    고침   main() 첫 줄에서 `import ase` 를 시도하고, 실패하면 **경로를 찍고 즉시 중단**:
           "ase 없음. `~/miniconda3/envs/czeromof/bin/python run_tb2_water_kh.py A` 로 도십시오."
           독스트링의 `python3` 도 같이 고칩니다.

### (ㄴ) `PrintEvery` -> `CYCLES // 10`

70행이 `PrintEvery {CYCLES}` = **15000 = NumberOfCycles** 입니다. CLAUDE.md §4 가 경고하는
바로 그 설정 — **처음과 끝에만 씁니다.** 진행을 못 읽고, 정지 오진의 원인입니다
(`PREREG_CHECKLIST` 검사 7: `.data` 는 탈락 신호).

    고침   `PrintEvery  {CYCLES // 10}` = 1500  -> 진행점 10개

> **CLAUDE.md §3 함정에 안 걸립니다** — 사용자 결정 (라)로 **새 폴더·새 JSON 이름**에서
> 시작하므로 옛 출력을 `cached` 로 주워 오지 않습니다. **다만 그 전제가 깨지면
> 이 항목 하나로 두 규약이 섞입니다.** 새 이름을 쓰는지 착수 전에 확인하십시오.

### (ㄷ) `read_kh` 를 성분 이름으로 — `comp='water'`, 파일·성분이 둘 이상이면 거부

지금(96~103행)은 `Output/System_0` 의 **모든 파일**과 **모든 줄**을 훑어
`Average Henry coefficient:` 의 **마지막 일치**를 씁니다. **성분을 안 봅니다.**
지금은 `Component 0` 이 물 하나라 맞지만, **맞는 이유가 코드에 없습니다.**

    고침   데스크탑 `read_kh` 를 그대로 가져오고 `comp='water'` 로 호출.
           출력 파일이 둘 이상이거나 성분이 둘 이상이면 **거부하고 이름을 찍습니다.**

### (ㄹ) 파일 안 다중 일치를 **명시적으로**

같은 루프에서 일치가 여러 개면 **뒤엣것이 조용히 이깁니다**. 개수를 세고,
2개 이상이면 **행 번호와 값을 전부 찍고 거부**합니다. (ㄷ)와 같은 결함의 파일 내부 판입니다.

### (ㅁ) `ALL` 은 **한 기기 전용**임을 문서화

`main()` 이 `ALL` 을 받고 산출물 이름은 `water_kh_{half}_{tag}.json`(tag=호스트명)입니다.
두 기기가 각각 `ALL` 을 돌리면 **파일 이름은 갈리지만 32조성이 통째로 중복**되고,
같은 조성의 서로 다른 실현이 두 파일에 남습니다. A/B 분담이 있는 이유가 그것입니다.

    고침   독스트링과 실행 시 로그에 **"ALL 은 단일 기기 전용. 두 기기면 A/B"** 를 박습니다.

---

## 2. 조사 중 나온 셋 — **범위 결정은 종합자·사용자 몫**

### (ㅂ) ⭐ 산출물에 **어느 힘장으로 쟀는지**를 남깁니다

지금 JSON 에는 힘장 출처가 **한 글자도 없습니다.** `Forcefield UFF_MOF` 라고만 적히는데,
**곧 그 이름 하나가 두 세대를 가리키게 됩니다**(수정 전/후).

> **어젯밤 밀도 격자에서 정확히 그 일이 났습니다** — 같은 이름 아래 두 실행이 공존했고,
> `simulation.input` 이 한쪽 것만 남아 짝이 어긋났습니다
> (`DENSITY_GRID_TWO_GENERATIONS_20260906.md`).

    고침   JSON 에 `forcefield_file`, `forcefield_mtime`, `forcefield_md5`,
           그리고 **`HwHw_eps` 실측값**을 남깁니다. 데스크탑 대조 JSON 이 이미
           `ff_applied` · `HwHw_eps` 필드를 갖고 있습니다 — **같은 모양으로 맞춥니다.**

### (ㅅ) ⭐ 힘장이 실제로 먹었는지 **출력에서 확인**합니다

수정의 목적 자체가 "고친 힘장으로 도는 것" 인데 러너는 그걸 **아무것도 확인 안 합니다.**
확인 지점은 이미 저장소에 이름이 적혀 있습니다:

    `water_kh_verify_base.json`  ->  *"확인: 출력 머리말 **Hw-Hw p_0/k_B = 22.14170**"*

    고침   출력 머리말에서 `Hw-Hw` 를 읽어 **22.1417 이면 실패로 표시**하고 K_H 를 버립니다.
           `Ow-Ow` 도 함께 읽어 **89.633 인지** 확인합니다(안 건드린 것이 안 건드려졌는지).

### (ㅇ) 물 정의가 **5자리인지 실행 폴더에서** 확인합니다

`main()` 은 **원본** `19_WaterCompetition/water.def` 의 사이트 수만 셉니다(5 확인).
그런데 실제로 읽히는 것은 **실행 폴더에 복사된 사본**이고, `simulation.input` 에는
`MoleculeDefinition TraPPE` 라고 적혀 있습니다. **복사가 실패하면 RASPA 는 배포본
`TraPPE/water.def`(실측 3자리)로 조용히 떨어집니다.**

    실측  우리 것 **5** 사이트  ·  배포본 `molecules/TraPPE/water.def` **3** 사이트
    고침  복사 **뒤에** 실행 폴더의 `water.def` 를 다시 세어 5 가 아니면 그 조성을 버립니다.

> CLAUDE.md §1 이 *"배포본 `TraPPE/water.def` 는 3자리, 우리 것은 5자리"* 라고
> 경고하는 그 지점이고, 지금 **막는 것은 복사 순서 하나뿐**입니다.

---

## 3. 안 고칠 것 (일부러)

- **`CYCLES`·`INIT`·컷오프·Ewald·압력** — CLAUDE.md §1 및 저장소 Widom 관례. 안 건드립니다.
- **A/B 교차 분담과 "기본값 없음"** — 09-05 환경변수 감사의 결론이라 그대로 둡니다.
- **`TB2_WORKERS`** — 자원 배분이지 규약값이 아닙니다.
- **`subprocess.run(..., check=False)` 의 DEVNULL** — 실패는 `ok: False` 로 잡히므로
  당장 결함은 아닙니다. 다만 **이유가 버려집니다.** (ㅅ)를 넣으면 대개 (ㅅ)가 먼저 잡습니다.

## 4. 이 문서가 말하지 않는 것

- **어느 조성을 다시 돌릴지** — `WATER_FIX_20260906.md` 몫입니다.
- **새 폴더·JSON 이름** — 같은 문서 몫. **(ㄴ)의 안전성이 거기에 걸려 있습니다.**
- **어느 형태(`none` / `ε=0`)를 넣을지** — 0절에 두 검증 축을 적어 뒀고, **고르는 것은 등록 몫**입니다.
- **힘장 파일 수정** — **안 했습니다.** 조회만 했습니다.
