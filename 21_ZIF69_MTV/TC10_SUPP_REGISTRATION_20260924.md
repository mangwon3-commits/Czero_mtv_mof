# T-C10 §-보완 등록 — `saIm0583e6~e10` **건조 GCMC 5건**으로 P5 를 닫는다 (2026-09-24 **04:55**, 데스크탑, **자료 0건**)

## 0. 왜 — 판정문이 직접 지목한 구멍

`TC10_VERDICT_20260923.md §2` 의 P5 가 **판정 불가**로 남았습니다:

> P5 상관 — 등록 양은 r(LCD 감소, **건조** 0.15 bar 로딩)인데 **e6~e10 에는 건조 로딩이 없다**
> (T-C10 은 습윤 WC 만 — 등록 §2 의 계산 목록에 건조 GCMC 가 없었음) → **등록 양으로는 판정 불가**.

그래서 `LCD_LOADING_CONFOUND` 의 보류가 그대로 남았고, 판정문 §4 (c) 가 닫는 방법을 적어 뒀습니다 —
**"e6~e10 건조 GCMC 5건(§-보완, 자료 0건) 등록"**. 이 문서가 그 등록입니다.

## 1. ★ 먼저 「LCD 감소」의 정의를 못 박습니다 (판정문이 요구한 것)

판정문 §2:

> "LCD 감소" 가 셋 중 무엇인지 등록·확인 문서가 **못 박지 않았다** — §-보완 등록 때 **정의부터 고정**.
> 재현 점검(n=5): −0.755 는 `results_v3ens0583.json` 의 **LCD 열**로만 재현된다(r = −0.755);
> `LCD_drop_pct` 로는 −0.465, 이완 후 LCD 로는 +0.465.

**정본 정의 — 해석이 아니라 출처로 고정합니다:**

    「LCD」 = `results_v3ens0583.json` 의 `LCD` 열, 그리고 e6~e10 에 대해서는
             **같은 `run_gcmc_v3.py` 코드 경로가 낸 `LCD` 열**.

**§1-1 (2026-09-24 05:05 추가) — 물리적 정체가 확정됐습니다. 미결이 아닙니다.**

laptop2 가 좌표로 가르자고 했고, 데스크탑이 **순서에 안 기대는 두 증거**로 확인했습니다
(laptop2 와 제 첫 좌표 대조는 **원자 순서가 달라** 인덱스 변위가 무의미했습니다 — 제 값 22 Å, 그쪽 2~3 Å.
CLAUDE.md §0 의 첫 번째 근본 결함인 주기 경계·인덱스 함정을 둘 다 밟았습니다):

    ① **코드 계보**  `charge_v3.py:30` `RELAXED = relax_v3` · `relax_v3_judged.json` 의 pass 만 통과.
       → `charged_v3` 는 **구성상** 셀 고정 GFN-FF 이완 뒤입니다.
    ② **순서 무관 기하 지문**  C–H 결합 중앙값
         charged_v3   1.0773 / 1.0773 / 1.0775 Å   (e1·e5·e10, n=144)
         structures_v2 0.9493 Å                     (전부 동일)
       → 이완의 알려진 서명(0.93 → 1.08 정상화)이 **charged_v3 쪽에만** 있습니다.

**그러므로 「LCD」= 셀 고정 GFN-FF **이완 후** `charged_v3` CIF 의 Zeo++ LCD 입니다.**
`run_gcmc_v3.py:43` 이 `charged_v3` 에 Zeo++ 를 돌리므로 `results_v3ens0583.json` 의 `LCD` 열이 그것입니다.

> ⚠ **`TC10_VERDICT_20260923.md §2` 의 "이완 전 LCD 8.9~10.0 Å" 표기는 틀렸습니다.** 이완 **후**입니다.
> 판정 결과(P5 판정 불가)는 안 바뀝니다 — 그 문장은 근거 설명이었지 판정이 아니었습니다.

**§1-2 — 판정문의 "이완 후 LCD 로는 +0.465" 는 *다른 LCD* 입니다. 모순이 아닙니다.**

laptop2 가 *"같은 「이완 후」 이름의 두 LCD 가 부호 반대 상관을 낸다"* 로 연 물음인데,
**GLOSSARY 가 이미 경고한 두 종류**입니다(`DESIGN_STUDY_20260903.md §0-0`):

    (a) `results_v3*.json` 의 `LCD`  = **GFN-FF 이완본**(= `charged_v3`)의 Zeo++ 값   ← **정본**
    (b) 관문의 「LCD 감소%」           = (a) 에 **LAMMPS/UFF4MOF 12루프 안정성 탐침**을 한 번 더
                                        걸고 잰 값을, 같은 탐침을 거친 base(7.63144)에 상대화

    실측 재현 (n=5, 건조 0.15 bar 로딩 대비)
      r((a) LCD, 로딩)        = **−0.755**   ← 정본
      r(LCD_drop%, 로딩)      = −0.465       (`risk_results_v3ensA.json`)
      r(탐침 LCD, 로딩)       = **+0.465**   ← 판정문의 그 값
      r(LCD_drop%, 탐침 LCD)  = **−1.000**   ← **같은 양의 두 표기일 뿐**

**즉 서로 다른 값은 둘뿐이고**(−0.755 와 ±0.465), 둘은 **기하가 다릅니다** — (b) 는 탐침을 한 번 더
거친 구조입니다. 이름이 둘 다 "이완 후" 라 겹쳐 보였을 뿐입니다.
**이 시험의 정본은 (a) 이고, (b) 로 낸 값은 "탐침 척도" 라고 이름을 붙여 서술로만 적습니다.**

## 2. 계산 — e1~e5 와 **한 글자도 다르지 않게**

    러너   `python run_gcmc_v3.py --only saIm0583e6 saIm0583e7 saIm0583e8 saIm0583e9 saIm0583e10 \
                                  --out results_v3ens0583_e610.json`
    내용   Zeo++(LCD·PLD·AV) + Widom K_H(CO₂·N₂) + **0.15 bar 건조 GCMC** — e1~e5 와 같은 경로
    고정값 15,000 사이클 · UFF_MOF · García-Sánchez 2009 CO₂ · DDEC6 · Ewald 1e-6 · 12 Å · 298 K
    입력   `charged_v3/saIm0583e{6..10}_DDEC6.cif` — **5/5 존재 확인**
           `charged_v3.json`·`rebuild_index.json` 에 **10개 태그 전부 등재 확인**(04:5x)
    결과   `results_v3ens0583_e610.json` — **`results_v3ens0583.json` 을 안 덮습니다**

> ⚠ **Zeo++ 경고 (CLAUDE.md §5)**: `run_gcmc_v3.py` 는 **Zeo++ `network` 를 같이 돕니다**(건당 약 9.5 GB).
> **RASPA 가 도는 동안 띄우면 안 됩니다** — 08-12 에 이 조합이 OOM 으로 WSL 을 통째로 먹통냈습니다.
> `ZEO_GB_PER_JOB` 로 워커가 정해지므로 `RISK_WORKERS` 를 손으로 주지 마십시오.
> **laptop2 의 `2024_Cd__dia_3_FSR_1` 재시도가 끝난 뒤에 띄우십시오.**

## 3. 예측 — **자료 0건 시점에 고정**

e1~e5 실측(정본 정의): LCD 9.38746 / 10.04327 / 9.82669 / 8.91967 / 9.89858 ·
건조 0.15 bar 로딩 1.1750 / 1.0388 / 1.2062 / 1.2449 / 1.1466 → **r = −0.755 (n=5)**

    P1  n=10 에서 **r ∈ [−0.55, +0.25]** — 즉 −0.755 보다 **약해집니다.**
        근거: 판정문 §2 가 등록 **밖** 서술로 낸 것 — 습윤 ads 로딩으로 대체하면
        이완 전 LCD 대비 n=5 −0.369 → n=10 **−0.051**, LCD_drop 대비 −0.733 → **−0.158**.
        **어느 정의로도 n=10 에서 |r| < 0.2 였습니다.** 건조도 같은 방향으로 봅니다.
    P2  **|r| < 0.6 이면 `LCD_LOADING_CONFOUND` 의 보류를 풉니다** — "−0.755 는 n=5 요행" 으로 적습니다.
        **|r| ≥ 0.6 이면 보류를 유지**하고 교락을 정식 안건으로 올립니다.
    P3  e6~e10 의 건조 0.15 bar 로딩 평균이 e1~e5 평균(1.1623)의 **±2 SD(±0.1546) 안**.
    P4  실패 **0/5**. 실패하면 대체하지 않고 자리를 비웁니다(n 이 줄어듦).
    P5  Q_st·선택도는 **이번 판정에 안 씁니다** — 같이 나오지만 서술로만 적습니다.

**반증**: P2 의 |r| ≥ 0.6 이 나오면 P1 이 틀린 것이고, 그때는 **LCD 축을 교락으로 다시 다룹니다.**

## 4. 사용 제한

    (ㄱ) 이 5건은 **T-C10 앙상블의 연장**이지 새 조성이 아닙니다. `results_v3.json` 31조성과 섞지 않습니다.
    (ㄴ) n=10 상관은 **정본 정의로만** 인용합니다. 다른 두 정의 값은 "서술" 표시를 붙입니다.
    (ㄷ) T-C10 의 확정된 판정(P1~P4)은 **안 건드립니다.** 이 문서는 **P5 하나만** 닫습니다.

## 5. 닫힌 것 / 남은 것

**닫혔습니다**(§1-1·§1-2, 09-24 05:05). `LCD` 열은 **이완 후**이고, "부호 반대인 두 LCD" 는
**기하가 다른 두 척도**였습니다. Zeo++ 를 다시 돌릴 필요가 없었습니다 — 코드 계보와 C–H 지문으로 끝났습니다.

**남은 것 — 발표용 문구**: LCD 를 장표에 쓸 때는 **어느 척도인지 반드시 병기**하십시오.
    "이완 후 LCD"(정본, `results_v3*.json`)  ·  "탐침 척도 LCD"(관문, `risk_results*.json`)
둘은 **부호가 반대인 상관을 냅니다.** 이름만 쓰면 다음 사람이 반대로 읽습니다.

## 6. 진행 기록
