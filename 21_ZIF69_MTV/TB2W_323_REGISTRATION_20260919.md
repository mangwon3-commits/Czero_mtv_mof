# T-B2w-323 등록 — 32조성 물 K_H(Widom) 323 K (2026-09-19 23:5x, 종합자; **자료 0건** — 323 K 물 Widom 은 어느 조성도 없음)

CLAUDE.md §9: MUF-16 네 시험(T-NF-1·1w·1q·1w-2)이 23:45 에 끝나 데스크탑이 비었고 등록된 배정이 없다.
"등록 저촉이 없는 후보(관문·Widom 류)" 로 이것을 띄운다. 더 큰 후보는 `ASSIGN_20260907.md §AR` 에 사용자 결정으로 남긴다.

## 1. 왜
    323 K 절대 WC 가 네 배치(T-RT-1 셋 + 1B, 1C·1D 진행 중)로 모였고, 판정은 전부 "R = WC(323)/WC(298) ≈ 0.40, 덩어리는 한 덩어리" 다.
    R 은 CO₂ 항과 물 항의 온도 의존이 합쳐진 수다. **물 항만 따로** — 298 K 에 있는 자(T-B2w, 32조성 물 K_H)를 323 K 에서 한 번 더 재면
    물 항의 온도 의존이 조성마다 같은지(그러면 R 의 균일성은 CO₂ 항 이야기) 다른지(물 항이 조성별로 R 을 가른다)가 갈린다.
    T-NF-1w-2 가 방금 같은 도구로 MUF-16 의 온도 의존(K_H 비 3.61, van 't Hoff 34.7 ≈ Widom 33.8)을 검증했다 — 도구는 서 있다.

## 2. 계산
    대상   `tb2_targets.json` 의 32조성(T-B2w 와 동일), `charged_v3/*_DDEC6.cif`, 각 1회(n=1 — 298 K 판과 같은 n).
    자     T-B2w 와 동일: Widom 15,000 / 초기화 3,000 · UFF_MOF · 12 Å · Ewald 1e-6 · TIP5P-Ew(Hw/Lw none, md5 8e8ec933) · **323 K**. UnitCells 는 `unit_cells()` 가 셀에서(2×2×2).
    러너   `run_tb2w_323.py`(run_tb2_water_kh 래퍼, 러너 무수정). 씨앗 3 s 어긋냄·회수·중복 검사. 결과 `v3w_water_kh/water_kh_323_ALL_<host>.json`.
    비용   32 × ~25 분 / 8 워커 ≈ 1.7 h. 기기 데스크탑. Zeo++ 없음.

## 3. 판정 (결과 전 고정)
    비     r_k = K_H(298)_k / K_H(323)_k (298 K 는 `water_kh_ALLw_hkhome_seedfixed.json`). van 't Hoff 로 Q_app,k = R·ln r_k / (1/298 − 1/323).
    판정 1 (도구 일관성)  T-NF-0q 가 Widom ΔU 로 낸 Q_st(H₂O) 가 있는 여섯(base 33.70 · mbIm025 26.34 · mbIm050 34.59 · nbIm050 29.76 · saIm050 39.79 · saIm100 46.24)에서
                          |Q_app − Q_st(0q)| ≤ 3 kJ/mol 이 6/6 → 도구 일관 / 아니면 어긋난 조성을 적고 상태식·씨앗 점검 먼저(CLAUDE.md §0).
    판정 2 (조성 의존)   공동 1위 덩어리의 n=1 대표(saIm050 · saIm0583 · sa50nb50 · mslm050 · saIm025)와 base·nbIm050 의 Q_app 폭(최대−최소):
                          **≤ 5 kJ/mol → 물 항 온도 의존은 조성 무관 → 323 K R 의 균일성(0.40)은 CO₂ 항·물 경쟁의 공통 온도 효과** /
                          **≥ 10 → 조성 의존 → T-RT 판정문 §4 에 "물 항의 온도 민감도가 조성별로 다르다" 서술 추가(순위·판정은 불변)** / 사이 → 기록.
    판정 3 (크기)        32조성 Q_app 의 분포(중앙값·범위) 기록 — 실측 대조 없음(우리 조성은 실측이 없음). MUF-16 34.7 · 외부 셋(0q 13~19)과 나란히 표.
    자·인용  n=1 이라 배치 단위 없음; ± 는 RASPA 95 % CI, 비의 ± 는 합성. **순위·관문·판정을 바꾸지 않는다** — 323 K 판정문에 서술 한 줄이 늘 뿐.

## 4. 절차
    착수 → 우편함. 완주 → 판정(종합자), 검산 Melchior/Balthasar. 이의 창 8 h.
