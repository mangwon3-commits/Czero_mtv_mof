# T-4 `n_tie = 8` 독립 검산 — **통과** (2026-09-05, 데스크탑)

laptop2 가 `origin/laptop2-20260825` 를 푸시해(`8f887e8`, 30커밋) 격자가 저장소에
들어왔습니다. **laptop2 의 스크립트를 쓰지 않고** 커밋된 격자에서 직접 쟀습니다.

> 조회 범위: `21_ZIF69_MTV/density_grids_final/COMDensityProfile_water__{base,nbIm025,saIm050}.vtk.gz`

## 1. `n_tie` 재현

    구조       격자      N_max   √N/N     **n_tie**
    base      90³       3,041   0.0181   **8**
    nbIm025   90³       5,118   0.0140   **8**
    saIm050   90³      19,150   0.0072   **8**

`n_tie` = 값이 `max × (1 − √N_max/N_max)` 이상인 복셀 수(푸아송 1σ). **셋 다 8.**

## 2. **그 8이 정말 대칭 복제인지**도 확인했습니다 — 개수보다 강한 검사

세 구조 모두, 동률 복셀 8개의 **분율좌표가 각 축에서 정확히 두 값**을 갖고
**간격이 0.500** 입니다:

    base      축0 {0.333, 0.833}   축1 {0.178, 0.678}   축2 {0.044, 0.544}
    nbIm025   축0 {0.078, 0.578}   축1 {0.400, 0.900}   축2 {0.367, 0.867}
    saIm050   축0 {0.411, 0.911}   축1 {0.089, 0.589}   축2 {0.111, 0.611}

    간격 전부 **0.500**  ->  **2×2×2 반셀 병진 복제. 2³ = 8.**

**"동률이 8개" 가 아니라 "8개가 병진으로 서로 이어진다" 를 보였습니다.**
경쟁하는 서로 다른 자리가 아니므로 **거리 범위 0** 이 따라 나옵니다 —
`T4_BLOCKADE_DECISION_20260905.md` 부록 A-2 의 판정이 섭니다.

> **곁들여 육방 셀 접힘이 확인됩니다.** 간격이 세 축 모두 **정확히 0.500** 이라는
> 것은 분율좌표 환원이 옳다는 뜻입니다. 정방정계 가정이 남아 있었으면 이 값이
> 깨집니다. **판정과 무관한 축에서 나온 확인**이라 값어치가 있습니다.

## 3. 남는 것

이 검산은 **`n_tie` 와 대칭성**만 봅니다. **T-4 판정의 병기 다섯은 그대로**입니다 —
실행 간 산포 미측정 · `nbIm025` 14,500 판 · 니트로 24 대 30 · (나)는 독립 확인 아님 ·
**(마) 물 힘장 결함으로 구조의 성질과 결함이 분리되지 않음**.
