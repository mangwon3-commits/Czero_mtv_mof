# 배정 라운드 2026-09-03 — **결과가 하나도 없는 지금 등록합니다**

**작성**: 데스크탑. **근거 의제**: `SESSION_LOG.md` "다음 배정 라운드 의제"
(8fe13d5). 5일 공백(사용자 구독 시스템 문제) 뒤 첫 라운드입니다.

## 0. 이 라운드에서 닫힌 것 / 여는 것

| 의제 | 상태 |
|---|---|
| 1. frozen-framework 검정 | **여기서 배정** → laptop2 (§1) |
| 2. nbIm100 유지율 재정의 | **종결** — (B) 분기(nb3) 자료로 답 나옴, 신규 계산 0 |
| 3. saIm075 앙상블 | 데스크탑 진행 중 |
| 4. 랩탑 R 결과 처리 | **전제 충족** — (B) 분기(nb3) 완주. 랩탑 실행 중(§3) |
| 5. RULER_DECISION | 사용자 결정 대기 — 기기 배정 아님 |
| 6. Junseok 다음 일감 | **여기서 배정** → Junseok (§2, 복귀 시 착수) |
| 7. 가지 취합 주기 | **여기서 규칙 고정** (§4) |
| (추가 09-03 12:00) T-B5·T-C1 azbIm 체인 | **여기서 배정** → laptop2 (§6, 무인 장기) |
| (추가 09-03 12:00) s_rep 러너 축 통짜 4회 | **여기서 배정** → laptop2 (§7, laptop2 제안) |

---

## 1. laptop2 — frozen-framework 검정 (의제 1)

**근거**: `CALF20_ANCHOR_20260829.md` V-3b 에 등록된 리스크. CALF-20 의 고습
붕괴 기제가 **물의 Zn 배위 → 골격 상전이**(JACS 2025)라면, 강체 골격 GCMC 인
우리 유지율 예측(nbIm025 87.6%)은 그 기제를 **구조적으로 볼 수 없습니다.**
V-3b 는 착수 여부를 이 라운드로 넘겼고, 여기서 **착수**로 결정합니다.

### 1-1. 무엇을

    대상      nbIm025 (87.6% 주장이 걸린 구조)
    이완 둘   (A) 건조 대조 — 물 0
              (B) 습윤 — RH90 평형 물 배치를 얹은 채 이완
    방법      GFN-FF (`relax_fixcell.XTBGrad` + ASE FIRE), fmax 0.05

### 1-2. ⚠️ 셀 자유도 — **가변 셀입니다. 그래서 대조를 새로 돌립니다**

상전이는 셀 변화로 나타나므로 **셀 고정으로는 이 질문을 구조적으로 볼 수
없습니다**(V-3b 논지 그대로). 그러므로 이 시험은 가변 셀로 돕니다.

**따라서 기존 `relax_v3/ZIF69_nbIm025_relaxed.cif` 를 대조로 쓰지 마십시오.**
그것은 **셀 고정** 산물입니다. 셀 자유도가 다르면 재시도가 아니라 **다른
시험**입니다(CLAUDE.md §2). (A) 와 (B) 를 **같은 자유도·같은 방법으로 새로**
돌려야 비교가 성립합니다. 공통으로 생기는 수축은 그때 상쇄됩니다.

### 1-3. ⚠️ 이것은 §1 고정값을 바꾸는 것이 아닙니다

CLAUDE.md §1 의 물 모델(TIP5P-Ew)·힘장은 **GCMC 층위**의 고정값입니다.
이 시험은 **이완 층위**이고 GFN-FF 가 물을 자체 기술합니다. 층위가 다르므로
위반이 아니며, 대신 **보고문에 이 문장을 그대로 적어** 나중에 읽는 사람이
"물 모델을 바꿨다" 로 오독하지 않게 하십시오.

### 1-4. 판정 — **수를 보기 전에 고정합니다**

측정 셋 (습윤 최종 vs 건조 최종, 둘 다 가변 셀):

    ① Zn 배위 침입   min(Zn–O_water) ≤ 2.4 Å 인 Zn 자리 수
                     + Zn–N 96쌍 중 2.4 Å 초과로 끊긴 수
    ② 셀 부피        |ΔV/V|
    ③ 골격 RMSD      물 제외 원자, 건조 최종 대비

| 결과 | 판정 |
|---|---|
| ① 에서 Zn–O_water ≤2.4 Å 자리 ≥1 **그리고** Zn–N 끊김 ≥1 | **기제 재현.** nbIm 유지율은 **상한**으로 격하. V-3b 병기 관문 유지·강화 |
| ① 미충족이나 ② `\|ΔV/V\| ≥ 5%` 또는 ③ RMSD ≥ 0.5 Å | **골격 반응.** 병기 관문 유지. 정량 보정은 별도 시험 |
| 셋 다 아래 | **미검출.** 병기 관문 **해제 가능** — 단 "단일 구조·단일 조건·GFN-FF 수준" 을 한계로 명기 |

**문턱 근거**: 2.4 Å 는 이 구조의 Zn–N 실측 1.983~2.010 Å 대비 넉넉한
배위 판정선입니다. 5% 는 v3 이완이 34% 수축시킨 전력(가변 셀에서 큰 변화가
실제로 일어남)에 비해 보수적으로 잡은 선입니다. **결과를 보고 옮기지
않도록 지금 박습니다.**

### 1-4b. [09-03 결과 전 개정 — laptop2 제기, 데스크탑 승인] (C) 편향 출발 추가

**결과가 하나도 없는 상태에서** laptop2 가 출발 스냅숏만 재서 올린 것:
RH90 GCMC 마지막 조각 Restart 의 물 33분자 중 Zn 에 가장 가까운 것이
**3.97 Å** (≤3.5 Å 0/33). 이유는 그 배치를 만든 GCMC 가 **강체 골격 + Zn 을
LJ·점전하로만** 기술해 배위 자체가 힘장에 없기 때문입니다 — **검정하려는
맹점이 출발 배치를 만들었습니다.** 따라서 (B) 는 비대칭입니다: 배위를
보이면 강한 증거, 안 보이면 거의 무정보(FIRE 는 내리막만 가고 1.57 Å 를
건너야 함). 등록문 1-4 의 "셋 다 아래 → 관문 해제" 가지가 **출발 배치 탓의
null 로 풀릴 수 있는** 자리였습니다. 등록문이 예상 못 한 곳이므로 고칩니다.

**개정 내용 — 문턱 ①②③ 은 그대로, 시험 하나 추가:**

    (C) 편향 출발   물 1분자를 Zn 배위 자리(Zn–O 2.0~2.2 Å)에 **일부러**
                   놓고 (A)(B) 와 같은 방법·셀·수렴조건으로 이완.
                   묻는 것: 그 배위가 GFN-FF 에서 **안정한 극소인가**

| (C) 결과 | 판정 |
|---|---|
| 배위 유지(Zn–O ≤2.4 Å) **+** Zn–N 끊김 ≥1 | **극소 실재.** (B) 의 null 은 "GCMC 배치가 그 자리에 못 간다" 는 뜻일 뿐. 관문 **유지** |
| 배위 유지, Zn–N 끊김 0 (5배위 상태) | **약한 실재.** 관문 유지, 보고문에 "배위는 남으나 골격 개방은 미관찰" 병기 |
| 물을 밀어냄 | 이때만 (A)(B) 의 미검출이 의미 있음 → 관문 해제 가능 |

**1-4 미검출 가지의 관문 해제 조건에 "(C) 도 배위를 유지하지 못할 것" 을
추가합니다.**

**(C) 의 함정 — 이것을 안 지키면 (C) 도 무정보가 됩니다:**

    ⚠️ 밀어냄이 입체 충돌 탓이면 안 됩니다. 4배위 Zn(N)₄ 사면체에 물을
       아무 데나 박으면 GFN-FF 는 배위가 불안정해서가 아니라 원자가
       겹쳐서 밀어냅니다. 그러면 "밀어냄 → 관문 해제" 가 인공물이 됩니다.
    조치  ① 물 O 를 **사면체 면의 법선 방향**(N 셋의 반대편, 5배위 접근
             경로)에 놓을 것. ② 놓은 직후 물 원자와 골격 전 원자의 최소
             거리를 재서 **보고문에 적을 것**(1.5 Å 미만이면 재배치).
          ③ **서로 다른 Zn 자리 ≥2 곳**에서 반복 — 물 1분자라 쌉니다.
             두 곳이 갈리면 "밀어냄" 으로 판정하지 않고 자리 의존으로 적음.
    표현  (C) 가 배위를 유지해도 그것은 **"GFN-FF 에서 극소가 존재한다"**
          이지 "기제가 열역학적으로 유리하다" 가 아닙니다. 도달 가능성·
          자유에너지는 이 시험 밖입니다. 보고문에 이 한계를 그대로 적을 것.

셀: 단위셀(612원자) 승인. 2×2×2(4896)는 가변 셀 GFN-FF 로 10 h 를 크게
넘깁니다. 물은 2×2×2 스냅숏에서 단위셀 부피분만 회수. (A)(B)(C) 전부
같은 셀·같은 수렴조건. 순서 (A) → (B)+(C).

환경: xtb 는 별도 env(`spectra`)에 설치, 생산 env `czeromof` 무변경 — 옳습니다.
스크립트의 xtb 경로가 데스크탑 절대경로로 박혀 있는 것은 **데스크탑 결함**
입니다. 그쪽에서 경로만 바꿔 쓰고, 어떻게 바꿨는지 보고하십시오.

### 1-4c. [09-03 03:16 착수 직후 등록 — laptop2 사전 신고, 데스크탑 승인] 방법 이탈 1건 + 해석 규칙 2건

**결과가 하나도 없는 상태**에서 laptop2 가 신고한 것을 그대로 적습니다.

**이탈 — 이완 엔진.** 1-1 의 `relax_fixcell.XTBGrad` + ASE FIRE 는
`implemented_properties = ['energy','forces']` 뿐이라 **응력이 없어 가변
셀이 불가능**합니다(수치 응력은 612원자에서 사이클당 12회 추가 평가 —
비현실적). 대신 `relax_test.py` 선례대로 **xtb 자체 최적화
(`xtb POSCAR --gfnff --opt`, xtb 6.7.1 conda-forge, 원자+셀 동시)** 를 씁니다.
수렴 기준은 ASE fmax 0.05 가 아니라 **xtb 기본값**입니다.

    승인 근거  (A)(B)(C1)(C2) 넷이 전부 같은 엔진·같은 수렴조건 → 1-6 의
               "불일치 금지" 를 지킵니다. 문턱 ①②③ 은 그대로입니다.
    기록 의무  이것은 1-1 과 **다른 시험**입니다(CLAUDE.md §2). 보고문에
               "ASE FIRE 가 아니라 xtb 자체 옵티마이저" 와 xtb `--opt` 수준
               (기본 normal 이면 그렇게)을 적으십시오. 나중에 FIRE 로 다시
               돌린 결과가 있어도 이 결과와 합치지 않습니다.

**확정된 실행 조건(신고분 그대로):** nbIm025 단위셀 a=b=26.084, c=19.408,
γ=120°, 612원자. 출발 = `charged_v3/nbIm025_DDEC6.cif` (relax_v3 이완본과
RMSD 0.0000 — 같은 내용이지만 **대조로 쓰지 않고 출발점으로만**). (B) 627원자 =
RH90 chunk4 Restart 의 2×2×2 33분자 중 첫 단위셀 부분상자에 O 가 있는 5분자,
TIP5P 의 O,H,H 만(Lw 더미 제외). (C1)(C2) 615원자 = Zn #588, #589, Zn–O 2.10 Å.
(C) 배치는 "사면체 면 법선" 대신 **무작위 4000방향 중 골격 최소거리 최대화
방향** — 조건 ① 의 취지(겹침 인공물 방지)를 직접 보장하므로 승인. 배치 직후
최소거리 C1 2.201 / C2 2.204 Å (요구 ≥1.5 충족). 각 3스레드 × 4병렬, 작업당
6 h 상한.

[정정, laptop2 03:4x] (B) 출발 min(Zn–O_water) 는 **4.381 Å** 입니다. 1-4b 의
3.97 Å 는 2×2×2 33분자 전체의 최소값이고, 그 분자는 회수된 5분자에 들지
않았습니다. 문턱 2.4 Å 에서 1.98 Å 떨어진 출발 — (B) null 의 무정보성 논거는
약해지지 않고 강해집니다. 함께: 물/골격 원자 색인을 ASE `sort=True`(비안정
정렬) 재현으로 잡던 방식이 물 O 를 엉뚱한 자리에 두는 것을 발견, **t=0 입력과
(A) 출발 구조의 위치 일치로 골격을 확정**하는 기하 식별(`index_map.json`)로
교체하고 (C1)(C2) 가 정확히 2.100 Å 로 나오는 것으로 자기검증했습니다.

**해석 규칙 2건 — 지금 박습니다:**

    ㉠ (A) 자체에서 Zn–N 끊김 ≥1 이면  → 그것은 물이 아니라 GFN-FF 의
       다공 골격 과밀화(v3 34% 수축 전력)입니다. 이때 ① 은 판정 불가이고
       결과는 "방법 한계" 로 적습니다. **관문 해제 가지로 가지 않습니다.**
       ①②③ 은 모두 **(A) 최종 대비** 로 잽니다(출발 구조 대비가 아님).
    ㉡ 6 h 상한에 걸려 미수렴이면 → 문턱을 적용하지 않고 "미수렴" 으로
       보고합니다. 스모크에서 83사이클에 −4.2% 였고 계속 수축 중이었으므로
       ② 의 5% 는 **수렴한 (A) 와 (B) 의 차이**에만 씁니다.

### 1-5. 실행 재량과 보고 의무

물 배치의 출처(기존 RH90 GCMC 스냅숏 회수 / 짧은 재평형)와 셀 크기(단위셀
대 2×2×2)는 **그쪽 재량**입니다. 다만 **(A)와 (B)가 반드시 같아야** 하고,
실제로 무엇을 썼는지(출처·원자 수·셀·단계 수·소요)를 보고문에 적으십시오.

### 1-6. 하지 말 것

    기존 relax_v3 를 대조로 재사용      <- 셀 고정 산물. §1-2
    Zeo++ 를 RASPA 와 동시 기동         <- v3 에서 9.5 GB. 08-12 OOM 전례
    결과 본 뒤 문턱 조정                <- 등록 위반
    (A)와 (B)의 방법·셀·수렴조건 불일치  <- 그러면 다른 시험 둘입니다

비용 전망: 셀 고정 단위셀이 23분이었으므로 가변 셀 + 물이면 건당 1~3 h,
둘 합쳐 **10 h 이내** 예상. 넘어가면 중간 보고하십시오.

---

## 2. Junseok — nbIm025 RH0 반복으로 s_rep 원인 가름 (의제 6)

**복귀 시 착수.** 지금 자격증명 만료로 정지 상태이며, 사용자가 `/login` +
`git push origin junseok` 을 하면 복구됩니다(laptop2 조사: 미취합 3커밋은
전부 문서, 계산 유실 0).

### 2-1. 왜 — laptop2 의 s_rep 이 5배 작은 것의 원인을 가릅니다

    laptop2   s_rep 0.00168  (nbIm025 RH0, 분할 러너)   비 0.27
    데스크탑   s_rep 0.008301 (e5, 건조 러너)            비 1.17
    Junseok   R̂    0.008424 (e1, 물 러너, n=8)          비 1.18

세 값은 **기기·러너·조성이 전부 다릅니다.** 그래서 지금 상태로는 "기기
특성" 이라고 말할 수 없습니다(데스크탑 판단: §COMMS 회신 참조).

**이 배정이 가르는 방식**: Junseok 이 **자기 기기·자기 물 러너 그대로**
조성만 nbIm025 로 바꿔 잽니다. 그러면 기기·러너가 고정되고 **조성 축만**
움직입니다 — Junseok 자신의 e1 값(0.008424)이 곧 대조군입니다.

### 2-2. 설계

    무엇을   `nbIm025` RH0 (laptop2 와 같은 조성·조건)
    어떻게   Junseok 물 러너 **그대로**(e1 을 잰 그 러너·그 설정), 새 디렉터리,
             동시성 1. 러너·설정 변경 금지 — 변경하면 다른 시험
    몇 번    4회 (n=4)
    대조     Junseok 자신의 e1 R̂ = 0.008424 (n=8, dof 7)

### 2-3. 판정 — 수를 보기 전에 고정합니다

    F = s²(nbIm025, dof 3) / s²(e1, dof 7)
    임계   F(0.975, 3,7) = 5.89     F(0.025, 3,7) = 0.068

| 결과 | 판정 |
|---|---|
| `F ≤ 0.068` | **조성 축이 원인.** laptop2 의 작은 s_rep 은 분할 러너 탓이 아님. 기기 간 s_rep 비교는 조성 통제 없이는 무의미로 확정 |
| `0.068 < F < 5.89` | **조성으로 설명 안 됨.** 남는 후보는 러너·기기 축 → 분할 대 통짜 대조 시험을 다음 라운드에 등록 |
| `F ≥ 5.89` | 예상 밖(더 큼). 별도 조사 |

함께 적을 것: n=4 의 s_rep 95%CI 배수 [0.57, 3.73] · 이 판정은 **RH0 한
조건**이며 RH90 으로 옮기지 않음.

비용: laptop2 실측 1.47 h/작업(동시성 1) 기준 4회 순차 ≈ 6 h. 기기 속도차는
그쪽 실측으로 갱신하십시오.

---

## 3. 랩탑 — R 측정 계속 (의제 4)

**실행 중**(saIm0625, 09-03 02:29 착수, 반복 1/3). 새 지시 없습니다.

- 배정문 `LAPTOP_R_20260829.md` 의 **대상 정정 반영 완료**(09-03): 등록문이
  `saIm0583` 으로 적었던 것은 오류였습니다 — 0583 은 데스크탑 자료
  (`v3_water_grid/water_results_desktop4.json`)이고 랩탑 보유분은
  0625/0667/0875 뿐이라, 그 문서 §4 "랩탑 원본 없는 구조로는 반복 불성립"
  과 충돌했습니다. **랩탑의 0625 대체는 옳습니다. 재시작 불필요.**
- 완주 시 그 문서 §3 문턱(2×/5×)을 기계 적용해 `COMMS/laptop.md` 규약 ③
  형식으로 보고하십시오. **의제 4 의 "(B) 분기(nb3) 완주 후" 전제는
  충족됐습니다.**

---

## 4. 데스크탑 — 가지 취합 주기 규칙 (의제 7)

08-29 감사가 경고한 뒤로 더 벌어졌습니다(랩탑 가지 157커밋 뒤처짐, 45커밋
고유). **취합 트리거를 사람 판단에 맡기지 않고 규칙으로 고정합니다:**

    ① 결과 JSON 이 가지에 도착   -> 그날 안에 master 취합
    ② 판정을 낳는 문서(등록·보고) -> 그날 안에 취합
    ③ 그 밖의 문서 커밋           -> 주 1회 또는 배정 라운드마다
    ④ 가지가 master 대비 100커밋 이상 뒤처지면 -> 배정보다 먼저 취합

근거: 실측된 보고 지연 상한이 **23시간**(가지 도착 → master 반영)이었고,
이번 laptop2 건은 5일이었습니다. 지연의 상한을 규칙으로 눌러 둡니다.

이번 라운드 취합 대상: `laptop2-20260825`(0d55467, c3581e2),
`laptop-20260822`, `junseok`(복귀 후).

**(09-03 12:xx 추가) ⑤ 참조 감사 — 다음 라운드부터 취합 전에 돌린다.** 09-03 에
master 가 5~16회씩 인용하던 판정 문서 8건이 `laptop-20260822` 에만 있었고(①②
위반, c3ec837 로 해소), 등록표에 안 잡힌 1건(`STATUS_NEWMACHINE_20260822.md`,
8228e80)이 더 있었습니다. 규칙 초안 — 랩탑·laptop2 가 실제 돌려 보고 얻은 것:

    - 양방향: (a) master 문서가 인용하는데 master 에 없는 .md
              (b) 가지 문서가 인용하는데 **그 가지에만** 있는 .md  <- 8건 같은 사례를 병합 전에 잡음
    - 판정은 "master 에 없음" 만으로 하지 않는다 — "master 에 없음 AND COMMS.md 등록표로
      가지·상태를 알 수 없음" 일 때만 끊긴 참조. (cloud4c.md 는 14곳 인용·master 부재지만
      등록표 447행이 가지와 종료 상태를 적어 두어 도달 가능 = 오탐)
    - 파일명 정규식에 **선행 숫자**를 넣는다 (`48H_COMPUTE_PLAN.md` 가 `H_COMPUTE_PLAN.md`
      로 잘려 오탐 났음)

스크립트는 아직 없습니다. 만들면 여기 파일명을 적습니다.

---

## 5. 공통

- 보고는 각자 우편함(`COMMS/<기기>.md`) 규약 ③ 형식. **한 파일 한 필자.**
- 남의 문서에서 오류를 찾으면 고치지 말고 **필자에게 요청**하십시오
  (이번 라운드의 `LAPTOP_R` 정정이 그 경로로 처리된 사례입니다).
- 새 용어·분류를 만들면 **쓰기 전에 파일로** (CLAUDE.md §8).

---

## 6. laptop2 — T-B5 · T-C1 장기 체인 (azbIm/bIm 22종, 무인 실행) [09-03 12:00 추가]

**왜 이 형태인가**: laptop2 는 세션이 끊긴 전력이 있습니다. 그래서 Claude 세션
없이 **WSL 만 살아 있으면 끝까지 가는 한 덩어리**로 줍니다. 모든 단계가 이어받기이고,
죽으면 같은 명령을 다시 치면 됩니다. 예상 **35~45 h**.

**근거**: `DESIGN_STUDY_20260903.md` T-B5(관문)·T-C1(건조 사다리)·T-B6(예비).
**구조**: `build_azbim.py` 산물 22종 — `structures_v2/ZIF69_{azbIm025,050,075}{,e1..e4}`,
`azbIm100`, `bIm025{,e1..e4}`, `bIm100`. 100% 조성은 자리 무작위성이 없어 실현 1개라
등록문의 30건이 **22건**입니다(DESIGN_STUDY §0-0 5항). 같은 seed 의 azbIm/bIm/fbIm 은
**같은 자리**를 치환합니다 — 셋의 차이가 자리 배치가 아니라 치환기 차이입니다.

### 6-1. 띄우기 — 세 줄

    cd ~/mof_project && git pull
    TB5_PREFLIGHT_ONLY=1 bash 21_ZIF69_MTV/tb5_chain.sh      # 사전검사만. 빠진 것을 찍어 줍니다
    setsid nohup bash 21_ZIF69_MTV/tb5_chain.sh < /dev/null &   # 통과했으면 이것 한 번

사전검사가 잡을 것: `coremof_tools` env(PACMAN — 없으면 설치 명령을 찍어 줍니다,
데스크탑과 같은 판 `PACMAN-charge==1.4.2`), Zeo++ `network`, `simulate`, 힘장 트리의
`C_co2 0.6512`, 5자리 물, xtb(`XTB_BIN` — 그쪽 spectra env 경로로 덮으십시오),
22종 CIF. **하나라도 빠지면 아무것도 돌리지 않습니다.**

> **설치 함정 (laptop2 09-03 실측)** — 사전검사는 결과물(import)만 보므로 못 잡습니다.
> - **gcc 없는 기기**에서 지정 판 pip 설치가 `PyCifRW` 휠 빌드에서 실패합니다
>   (`command 'gcc' failed`). 전이 의존성이라 판 지정 넷과 무관합니다. 컴파일러를
>   깔지 말고 **먼저** `conda install -n coremof_tools --override-channels -c conda-forge pycifrw`
>   로 사전빌드본을 채운 뒤 지정 넷을 판 그대로 설치하십시오.
> - conda 는 `--override-channels -c conda-forge` 로 한정하십시오. 기본 채널은 Anaconda
>   약관 미동의로 실패하고, 약관 동의는 사용자 몫입니다.
> - laptop2 의 PACMAN 은 **GPU(cuda, torch 2.7.0+cu126)** 로 돕니다. 데스크탑은 CPU 입니다.
>   DDEC6 전하가 부동소수점 수준에서 갈릴 수 있으므로 **기기 간 전하값 직접 대조는
>   이 줄을 병기**하십시오. T-B5/T-C1 은 4단계가 base 를 같은 기기에서 다시 돌리므로
>   내부 정합성에는 영향 없습니다.
> - Zeo++ 워커는 가용 메모리로 자동 계산됩니다(laptop2 14 GB → 1). 4단계가 그만큼
>   깁니다. `ZEO_GB_PER_JOB` 은 실측 없이 내리지 마십시오(CLAUDE.md §5).
> - 1단계 로그의 "구조 86종" 은 색인 전체를 훑는다는 뜻입니다. `relax_v3/` 의 64종은
>   커밋돼 있어 건너뛰고 실제 이완은 22종입니다. **건너뜀은 로그에 안 찍힙니다**
>   (`relax_series_v3.py` 70행이 조용히 `skipped` 를 돌려줌 — 데스크탑이 "[건너뜀] 64줄"
>   이라고 잘못 안내했음, laptop2 정정 09-03). 진행 확인은 로그가 아니라
>   `readlink /proc/<xtb pid>/cwd` 나 `relax_v3/` 의 새 폴더 수로 하십시오.
> - **감시를 WSL 밖(Git Bash 등)에서 돌리면 `pgrep` 이 WSL 프로세스를 못 봐 멀쩡한
>   체인을 "종료" 로 오보합니다**(laptop2 09-03 실제 겪음 — 믿었으면 35시간짜리를
>   죽일 뻔). 감시는 `wsl.exe -e bash -lc '...'` 로 감싸거나 WSL 안에서 `bash bgstate.sh`.
>   **자기 기기에서 먼저 확인할 것**: Bash 도구에서 `uname` 이 `MINGW64` 면 Windows 쪽이라
>   `pgrep` 이 WSL 프로세스를 못 봅니다(laptop2 실측). `Linux … WSL2` 면 그대로
>   보입니다(랩탑·데스크탑). **Claude 의 Bash 가 WSL 안이냐 밖이냐는 기기마다 다르므로
>   다른 기기의 결론을 옮기지 마십시오** — 4축 배율 이전 금지와 같은 유형입니다.
> - **체인이 도는 중의 `git pull` 은 실행 중 스크립트를 갈아치우는 편집과 같습니다**
>   (CLAUDE.md §6 — bash 는 바이트 오프셋으로 읽어 엉뚱한 줄을 실행, 파이썬은 재기동 때
>   터짐). pull 전에 체인이 앞으로 부를 파일이 바뀌는지 먼저 봅니다(laptop2 09-03 18:2x):
>   `git fetch && git diff --stat HEAD origin/master -- tb5_chain.sh srep_whole_chain.sh
>   judge_relax_v3.py charge_v3.py run_gcmc_v3.py tb5_report.py run_water_lowrh_tb6.py
>   run_water_whole_nbim025_rep.py` 가 비어 있어야 pull. 하나라도 바뀌면 체인 완주까지
>   pull 을 미루거나 그 파일만 `git checkout HEAD --` 로 되돌려 둡니다. **다른 기기에 pull 을
>   지시할 때 이 확인을 같이 적을 것** — 이번엔 체인이 안 부르는 srep_machine_test.py 만
>   바뀌어 우연히 안전했습니다.

로그 `~/.claude_work/tb5_chain.log`. 진행은 `bash bgstate.sh` 또는 로그 꼬리.
**출력 파일이 몇 시간 그대로여도 멈춘 것이 아닙니다**(CLAUDE.md §4).

### 6-2. 단계 (전부 이어받기)

    1  이완    relax_series_v3.py — GFN-FF 셀 고정, FIRE fmax 0.05, 워커 3×스레드 2   ~15~20 h
              relax_v3/ 에 있으면 건너뜀. 실패는 CIF 를 남기지 않아 재기동 시 재시도
              [laptop2 실측 09-03 12:56] 첫 배치 azbIm025/e1/e2 동시 49분(103~104스텝,
              28.4 초/스텝, fmax 0.044~0.050) → 구조당 실효 16.4분, 22종 약 6.2 h 전망.
              단 세 종이 같은 조성이라 azbIm100·bIm100 은 더 걸릴 수 있음(배치 2~3개 후 갱신)
              [13:20 단서, laptop2·랩탑] **§7(RASPA 1코어)과 동시 실행 중 측정** — xtb 워커 3개
              117~123% + simulate 100%, 부하 5.02/물리 6코어. 6.2 h 는 경합 구간 배치 하나의
              외삽이라 등록 예상 15~20 h 와 대면 안 됨("경합 하 실측" 으로만 읽음). §7 종료 후
              구간은 더 빠를 수 있으며, 그 첫 배치 소요를 laptop2 가 따로 재어 경합/비경합 두
              값으로 비교. "등록 예상이 과대였다" 로 굳히지 않음(다음 배정 산정 근거).
              [14:10 laptop2 2배치] azbIm025e3 104스텝 48.7분 / e4 103스텝 48.3분 / **azbIm050 91스텝
              41.7분**(fmax 0.0442) — 조성 바뀜에 더 걸리지 않았고 오히려 빨랐음(azbIm100·bIm100 은
              미관측, 치환율 100% 는 자리 무작위성이 없어 성격이 다를 수 있음). 완주 6/22, 배치당
              42~49분, 1단계 완주 18:5x 전망 — **§7 종료(~22시) 전에 끝나므로 이완의 비경합 실측은
              나오지 않음.** [14:15 데스크탑 결정] 1단계 실측은 **"경합 하 42~49분/배치, 비경합 값
              없음"** 으로 명기하고 끝냄. 4단계 GCMC 는 다른 프로그램(RASPA)이라 이완 예상 15~20 h
              의 대조가 되지 못하므로 그것으로 대신 재지 않음. 등록 예상과의 비교는 미결로 둠.
              [18:05 laptop2] **1단계 22/22 완주** 12:06→17:58, **5 h 53 m**(전부 §7 경합 중, 비경합
              값 없음). relax_v3 64→86. 1단계 로그의 판정 22종 전부 `[통과]`(C-H 1.077~1.078,
              C-C폭 0.038~0.051, Zn-N 1.977~2.009). 소요 경향: 치환율 높을수록 빠름 — 025 ≈49분(103~105
              스텝) / 050 ≈42분(91~92) / 075 41.6~48.3분(91~105) / **azbIm100 38.4분(86스텝, 최속,
              C-C폭 0.038 최소)** / bIm100 45.4분(103). "100% 는 더 걸릴 수 있음" 우려는 안 나타남,
              기제 주장 없음. 2단계 judge 17:58 착수(pid 20325) — relax_v3_judged.json 갱신 전.
              [18:10 laptop2] **2단계 judge 완료 18:04(5분 32초): n=86, failed=[] — 신규 22종 전 항목
              pass.** 이완 전후: C-H 0.949→1.077~1.078(기준 1.05~1.12), 방향족 C-C폭 0.192→
              0.0385~0.0507(<0.08), Zn-N 96쌍 1.977~2.009(1.90~2.10). 즉 통과는 "원래 괜찮음" 이
              아니라 이완이 기하를 고친 것. azbIm100 은 이완 **전** C-C폭도 0.1145 로 유일하게
              작았음(다른 종 0.1922) — 관찰, 기제 주장 없음. T-B5 관문(LCD |Δ|<2%)은 4단계 Zeo++
              에서. **3단계 전하 18:04 착수** — base 는 커밋본(데스크탑 CPU, e6a8516) 건너뜀, 신규
              22종은 laptop2 GPU(PACMAN 1.4.2 · torch 2.7.0+cu126 · GTX 1660 Ti). 출처는 COMMS 판정문에.
              [18:15 laptop2] 3단계 **2분 33초** 완료(GPU), charged_v3 90개(68+22). **4단계 18:06:54
              착수 — §7 simulate 와 겹쳤고 체인이 감지해 Zeo++ 워커 5→1 자동 하향**(사전검사 예고대로).
              18:07 메모리: 총 15.9 GB, 가용 14.1 GB, simulate 453 MB. Zeo++ 9.5 GB 정점에서도 여유
              약 4 GB. 체인 무개입, 가용 2 GB 아래면 laptop2 가 즉시 보고. §7 r3 18:04 완주(118분,
              값 미열람), r4 완주 전망 약 20:0x.
              [18:20 laptop2 정정] 앞 보고의 "Zeo++ 정점 144 MB" 는 감시 오탐(종료 조건 문자열
              `results_tb5.json` 이 `--out` 인자에도 있어 착수 1분 만에 "종료" 매칭) — 폐기.
              재실측 18:08: `-res` 단계 10/23 종을 1.7분에, network RSS **907 MB**, 가용 13.2 GB.
              23종 약 4분 전망. 정점·가용 최저는 감시를 다시 걸어 완료 시 보고.
              [18:25 데스크탑 확인 — 9.5 GB 의 출처] 그 값은 `risk_screen_v3.py`(08-19 랩탑) 실측이고
              그 스크립트는 Zeo++ 를 **LAMMPS 이완 후 슈퍼셀**(`supercell_rep` 배 원자)에 돌립니다
              (`risk_screen.py` 210~213행). `run_gcmc_v3.py` 는 `charged_v3/*.cif` **단위셀**에
              같은 플래그(`-ha -res`, `-ha -vol … 20000`)로 돌리므로 입력 원자 수가 다릅니다.
              즉 9.5 GB 는 run_gcmc_v3 에 그대로 옮길 값이 아니었고(87행 주석이 옮김), 907 MB
              가 이 경로의 첫 실측입니다. **지금은 아무것도 안 바꿈** — 체인 실행 중이고 `-vol`
              단계 정점을 아직 못 봄. laptop2 의 정점 보고 후 `ZEO_GB_PER_JOB` 기본값을
              run_gcmc_v3 경로에 맞춰 낮출지 별도 결정. risk_screen_v3 의 9.5 GB 는 유효.
              [18:40 laptop2 정점 보고] `-res`(18:07:01~18:10:41)와 `-vol`(18:07:05~18:10:45)이 거의
              동시에 교대로 돌아 단계별 정점은 못 잡음(20초 표본). 확실한 상한: **가용이 전 구간
              13,944 MB 아래로 안 내려감** → 두 단계 합쳐 정점 ≈1 GB, 9.5 GB 면 가용 5 GB 대가
              있었어야 함. **run_gcmc_v3 경로(단위셀) Zeo++ 는 건당 <1 GB, 23종 워커 1 로 4분.**
              단서: res+vol 혼재 관측·20초 표본이라 정점 놓쳤을 수 있음. 단계별 정점은 다음
              기회에 1초 표본. `ZEO_GB_PER_JOB` 기본값 조정은 체인 종료 후 별도 커밋(실행 중 무변경).
    2  판정    judge_relax_v3.py -> relax_v3_judged.json (C-H·C-C폭·Zn-N·최소거리·셀)
    3  전하    charge_v3.py (PACMAN DDEC6) — 판정 통과분만, 있으면 [이미있음]
    4  Zeo++ -> 건조 GCMC  run_gcmc_v3.py --only base <통과 22종> --out results_tb5.json
              워커 5(TB5_GCMC_WORKERS). base 를 같은 기기에서 다시 돌려 대조군을 같은
              기기 값으로 둡니다. 끝나면 tb5_report.py 가 관문·사다리 표를 찍습니다
    5  T-B6 예비  run_water_lowrh_tb6.py — RH 0.15/0.10/0.05 × azbIm050·azbIm100·bIm100
              (9작업). 정식 T-B6 판정은 T-B1(ΔH_vap 자) 뒤. 여기서는 **수치만**

한 구조가 실패해도 체인은 계속 갑니다(통과분만 다음 단계로). 단계 전체가 0건일 때만
멈춥니다. Zeo++ 는 4단계에서만, RASPA 와 순차입니다.

### 6-3. 판정 — 등록문 그대로 (DESIGN_STUDY, 수를 보기 전에 고정돼 있음)

    T-B5  LCD |Δ| < 2% (base 대비, 같은 기기 Zeo++) + 기하 판정 통과
          |Δ| > 5% -> 이완 문제 또는 N 이 Zn 창 기하를 바꿈 -> T-3 와 같은 감사(audit_probe_noshrink.py)
    T-C1  Q_st(az100) − base ≥ +4   /   Q_st(az100) − Q_st(bIm100) ≥ +3   /   로딩(az100) ≥ 1.0
          차이의 오차 σ_diff = √(±₁²+±₂²), 1.5 단위 미만이면 순위 없음(CLAUDE.md §2)
          Q_st 이득이 bIm100 에도 있으면 효과는 **Cl 제거**이지 N 이 아닙니다

    [18:35 laptop2 보고 — T-B5 관문 결과, 문턱 그대로] Zeo++ 18:06:54→18:10:55(23종, 워커 1,
    RASPA 1건 동시, 정점 ≥907 MB, 가용 최저 13.9 GB). base 재계산 LCD 8.89809 / PLD 5.157 —
    results_v3.json 과 일치(열 해석 검증). **통과 9 / 탈락 13**:
        통과   azbIm025 e0/e1/e3/e4 (+1.19~1.32%) · bIm025 e0~e4 (+1.43~1.51%)
        탈락   azbIm025e2 +3.86 · azbIm050 e0~e4 +3.52~6.20 · azbIm075 e0~e4 +6.40~18.46 ·
               azbIm100 +18.46 · bIm100 +17.26      (PLD 는 최대 +44.8%)
    사실만: ① 전부 양(+) — 붕괴가 아니라 확장, |Δ| 라 잡힘. ② 치환율에 단조, 실현 간 산포도
    치환율과 함께 커짐(025 ±0.1%p, 075 12%p 폭). ③ **5% 조항 발동 9종**: azbIm050e1·e2,
    azbIm075 e0~e4, azbIm100, bIm100 → 등록대로 `audit_probe_noshrink.py`(T-3 규칙). ④ azbIm100
    +18.46 과 bIm100 +17.26 이 거의 같음(PLD 도) — 6-3 의 "bIm100 에도 있으면 Cl 제거" 패턴이
    기하에서 먼저 보임. **Q_st 결론은 GCMC 후.**
    [데스크탑 처리 18:40] (a) 감사는 데스크탑이 돌림 — laptop2 는 relax_v3 22종·charged_v3
    22종·relax_v3_judged.json·Zeo++ 산출을 가지(laptop2-20260825)에 지금 커밋·push(§6-4 의무,
    도착한 날). 감사는 CPU 스크립트라 체인과 무관. (b) 검사기부터 의심(CLAUDE.md §0):
    azbIm075e2 LCD 10.54058 과 azbIm100 10.54045 이 1.3e-4 Å 차 — 조성이 다른 두 구조가
    같은 공동을 갖는지, CIF 의 Cl/N 원자 수로 두 구조가 실제로 다른지 데스크탑이 센다.
    (c) T-C1 은 등록대로 수치로 진행하되 판정문에 T-B5 탈락을 **반드시 병기** — 관문 탈락
    구조의 Q_st 차이는 화학이 아니라 공극 확장일 수 있어 단독 인용 금지. 새 문턱 없음.
    [18:55 데스크탑 — 5% 조항 감사 결과, `audit_tb5_over5.json`] 9종 전부 T-3 규칙으로
    "깨진 구조 — 인용 금지". **걸린 항목은 6원 고리 평면성 3σ 하나뿐**(base sd 0.0007 Å →
    3σ 0.0021 Å, 9종 최대 RMSD 0.0051~0.0073 Å; 12~15개/24개). Zn–N 3σ 밖 0(max z 2.0~2.2),
    N–Zn–N 3σ 밖 0(max z 1.8~2.0), 5원 고리 0, dmin 1.073~1.075(C–H), 배위 4 전부. T-3 (a) 의
    base 계열 7종과 **같은 항목·같은 판별력 문제**(§0-0 항목 4) — 등록대로 그대로 적고 T-3′
    등록 전 재판정 없음. 감사가 실제로 물은 "N 이 Zn 창 기하를 바꿨나" 는 Zn–N·N–Zn–N 이
    base 분포 안이라 **국소 기하로는 지지되지 않음**. 검사기 의심 항목: azbIm075e2(582원자,
    Cl6) 와 azbIm100(576원자, Cl0)은 다른 구조 — laptop2 셈과 데스크탑 셈 일치.
    [18:55 등록 — 결과 보기 전, 데스크탑 유휴라 Zeo++ 가능] **T-B5-d(진단)**: 5% 조항의 두
    후보(이완 문제 / 조성이 창 기하를 바꿈)를 가르기 위해 **이완 전** 빌드 CIF
    (`structures_v2/ZIF69_<tag>.cif`, base 는 `05_MTV_Ligand_Library/ZIF69_base.cif`)에
    run_gcmc_v3 와 같은 `network -ha -res` 를 돌려 LCD Δ%(base 대비)를 구함. 읽는 규칙:
    이완 전 Δ 가 이완 후 Δ 와 **±2%p 안**이면 확장은 빌드 조성(Cl→H/N 원자 반지름) 효과이고
    이완은 무관 → "이완 문제" 후보 기각; 밖이면 이완이 기여 → 이완 경로 재검토. 22종 전부.

`tb5_report.py` 는 수와 문턱을 나란히 찍을 뿐 판정문을 쓰지 않습니다. 판정문은
그쪽이 `COMMS/laptop2.md` 에 씁니다. 앙상블(025/050/075)은 e0~e4 평균과 s_rep(n=5)을
병기하고, ± 는 실현 하나의 95% CI 임을 적으십시오.

### 6-4. 보고 의무 · 하지 말 것

- 결과 JSON(`results_tb5.json`, `relax_v3_judged.json`, `charged_v3.json`,
  `charged_v3/*_DDEC6.cif`, `relax_v3/*_relaxed.cif`, `v3_water_lowrh_tb6/`)은
  **도착한 날 가지에 커밋·push**(§4 ①). 실행 폴더(`runs_v3/`, `water_runs_*`)는 커밋하지 않음.
- 소요 시간은 그쪽 실측으로 적고 **다른 기기 배율을 옮기지 마십시오.**
- 돌고 있는 `tb5_chain.sh` 를 편집하지 마십시오. 고치려면 죽이고 고친 뒤 재기동.
- 체인이 도는 동안 다른 RASPA·Zeo++ 를 손으로 띄우지 마십시오(§7 의 통짜 체인은 예외 —
  같이 띄우도록 설계됨).
- 결과를 본 뒤 문턱을 고치지 마십시오. 문턱 결함이 보이면 새 시험(T-B5′ 등)으로 등록.

---

## 7. laptop2 — s_rep 러너 축 가름: nbIm025 RH0 **통짜 4회** [09-03 12:00 추가, laptop2 제안 채택]

### 7-1. 왜

laptop2 의 분할 러너 s_rep 0.00168(n=4)이 다른 세 기기의 1/5 입니다. laptop2 09-03
보고대로 **기기 간 비교는 자(절대 SD/CV/√N)에 따라 유의성이 갈려 자료가 자를 정하지
못하고**, §2 의 Junseok 시험은 조성 축을 지울 뿐 러너를 지목하지 못합니다(Junseok 과
laptop2 는 기기·러너가 둘 다 다름). **같은 기기·같은 조성·같은 로딩**에서 분할 대 통짜를
대는 것이 자 논쟁을 우회하는 유일한 설계이고, 그것은 laptop2 에서만 됩니다.

### 7-2. 설계

    무엇을   nbIm025 RH0, run_water.py **통짜**(초기화 5,000 + 생산 15,000 한 실행)
    어떻게   반복마다 폴더 분리(v3_water_srep_whole/r1..r4) — 같은 폴더면 이어받기가
             두 번째부터 cached 로 회수해 한 번만 돌고 4회로 보입니다
    동시성   1 (분할 4회 중 3회가 동시성 1)
    대조     laptop2 자신의 분할 4회: 0.7951 / 0.7980 / 0.7949 / 0.7978 (COMMS/laptop2.md 08-29)
    띄우기   setsid nohup bash 21_ZIF69_MTV/srep_whole_chain.sh < /dev/null &
             (§6 체인과 **동시에** 띄우십시오. 코어 1개, 순차 4회, ~6 h. 로그 ~/.claude_work/srep_whole.log)

### 7-3. 판정 — 수를 보기 전에 고정합니다

    F = s²(통짜, dof 3) / s²(분할, dof 3)
    양측 5%:  F(0.975,3,3) = 15.44     F(0.025,3,3) = 0.0648

| 결과 | 판정 |
|---|---|
| `F ≥ 15.44` | **러너 축 확인** — 분할 러너가 실행 간 산포를 줄임. **기제는 주장하지 않음**(RandomSeed 없음·마르코프 사슬 논지 미해결). 다음 라운드: 분할 러너의 ± 가 무엇을 재는지 별도 등록 |
| `0.0648 < F < 15.44` | **러너로 설명 안 됨.** 남는 후보 기기·우연. n=4 검정력 한계 병기 |
| `F ≤ 0.0648` | 예상 밖(통짜가 더 작음). 별도 조사 |

함께 적을 것: 통짜 4회 각 로딩과 ±, 평균, s_rep · 분할 원본 1회가 동시성 2 였던
비균질 · 이 판정은 RH0 한 조건. **판정 전까지 "분할 러너가 더 재현성이 높다" 는 문구를
어떤 문서에도 쓰지 않습니다**(laptop2 09-03 경고 그대로).
`run_water_whole_nbim025_rep.py --summary` 가 F 를 찍습니다.

### 7-4. 비교 B(기기 축) — r1 완주 전 등록 [09-03 12:50, 랩탑 지적·laptop2 전달, 수치 아무도 못 봄]

7-3 은 **비교 A**(laptop2 분할 대 laptop2 통짜, 같은 기기·조성·로딩 → 자 불필요)입니다.
A 가 "러너로 설명 안 됨" 으로 나오면 기기 축을 재야 하는데, 그 비교(**B**: laptop2 통짜 대
타 기기)는 구조·로딩이 달라 자 문제가 되살아나고, 그 시점엔 이미 수를 본 뒤입니다.
그래서 **지금** 무엇으로 잴지와 판정을 박습니다.

    언제   A 가 `0.0648 < F < 15.44` 또는 `F ≤ 0.0648` 일 때만. `F ≥ 15.44` 면 B 는 하지 않음
    지표   raw s_rep 이 아니라 **비 r = s_rep / σ_stat** (σ_stat 정의는 LAPTOP_R_20260829.md 3절
           그대로 — 다른 세 기기와 같은 식). 분자·분모가 같은 기기·같은 구조·같은 관측량이라
           정규화 인자가 약분돼 **자(절대 SD / CV / √N) 선택이 결과를 바꾸지 못함**
    비교   laptop2 통짜 r 의 n=4 구간 대 타 기기 r 의 **구간** 셋 — 구간 대 구간.
           배수는 LAPTOP_R_20260829.md 3절과 같은 χ² 식(s·√(dof/χ²)), 데스크탑 검산:
             dof 3 (n=4): [0.566, 3.729]     dof 7 (n=8): [0.661, 2.035]
           데스크탑  r 1.17 (n=4) -> [0.663, 4.362]
           Junseok   r 1.18 (n=8) -> [0.780, 2.402]
           랩탑      r 1.76 (n=4) -> [1.003, 6.563]   (COMMS/laptop.md 300행에 이미 등록)
    판정   laptop2 통짜 r 의 구간이 **세 구간 전부와 겹치지 않을 때만** "기기 축 실재".
           하나라도 겹치면 "기기 축 미확인 — 남는 후보 우연, n=4 검정력 한계".
           등록된 R 문턱(2배 수렴 / 5배)은 laptop2 통짜에도 **같은 식으로** 적용해 병기

    [12:55 정정, r1 완주 전] 첫 등록(0d6fbac)은 대조 셋을 **점**으로 두었습니다. laptop2 가
    지적: 랩탑 구간 [1.003, 6.563] 은 이미 저장소에 등록돼 있어 "셋은 점" 은 자기모순이고,
    점 대비는 대조군 산포를 0 으로 두어 laptop2 가 가장 달라 보이는 경우(r≈0.30)에만
    "실재" 쪽으로 기웁니다. 구간 대 구간으로 고칩니다 — 새 자 없음, 등록된 배수만 재사용.

    [13:00 재정정, r1 완주 전 — 최종] 구간 겹침은 반대편으로 지나칩니다(랩탑 지적, laptop2
    독립 모의): 점 비교는 대조군 산포를 0 으로 두어 과대 유의, 구간 겹침은 양쪽 불확실성을
    두 번 세어 과소 검출(95% 구간 둘이 겹쳐도 p<0.05 인 경우가 흔함 — r=0.30 가정 시 랩탑과
    직접검정 p=0.021 인데 구간은 겹침). **위 두 판정 줄은 폐기하고 아래로 대체합니다.**

    판정(최종)  비 r = s_rep/σ_stat 의 **직접 검정**. 대조 셋 각각에 대해 양측 5%.
                **셋 전부에서 유의(p<0.05)해야** "기기 축 실재". 하나라도 비유의면
                "기기 축 미확인 — 남는 후보 우연, n=4 검정력 한계". 셋의 p 를 전부 병기.
    귀무분포    r 의 점값은 등록된 정의 그대로(σ_stat = ±/2.776 의 **중앙값**). 검정은
                다음 **근사**를 등록해 씁니다(중앙값의 정확 분포 대신 — 셋 중 이것을 택함):
                  s_rep²  ~ σ_b² · χ²(n−1)/(n−1)        n: laptop2 4 · 데스크탑 4 · Junseok 8 · 랩탑 4
                  σ_stat² ~ σ_w² · χ²(4n)/(4n)          블록 5개(dof 4) × n 실행, 독립 가정
                귀무가설 r_A = r_B 아래 T = log(r_laptop2 / r_기기) 를 몬테카를로 **200,000회,
                seed 20260903** 으로 뽑아 양측 p = 2·min(P(T ≤ t_obs), P(T ≥ t_obs)).
                σ_stat 을 합동으로 바꾸지 않습니다 — 등록된 r 점값(1.17/1.18/1.76)과의
                정합을 위해서이고, 그 대신 dof 4n 근사임을 판정문에 적습니다.
    스크립트    `srep_machine_test.py` (데스크탑, **r1 완주 전 13:20 작성** — 위 규칙의 구현일 뿐
                규칙을 바꾸지 못함. 구현이 위와 다르면 위가 정본). §7-3 의 F 적용 조건·§7-5 도
                같이 찍고 `v3_water_srep_whole/srep_machine_test.json` 에 남김. 자체검사
                `--selftest`(r=0.30 가정): 대 1.76 p=0.0204 / 1.18(n=8) 0.0513 / 1.17 0.0634 —
                laptop2 독립 구현 0.0204 / — / 0.0634, 랩탑 0.0205 와 일치. [13:35 구현 대조 종결]
                첫판 0.0216 은 세 비교가 난수 흐름 하나를 순서대로 소비한 탓(seed 1~20 산포
                SD 0.0004, 5M회 값 0.0208) → 비교마다 seed 20260903 새 흐름으로 고정(단독
                재현 = 스크립트 값). 규칙 무관, 결과 전 수정.
    조건부 재실행 [13:45 등록, r1 완주 전 — laptop2 (나)안 채택] 등록 N=200,000 의 p 3σ 폭이
                p≈0.05 에서 ±0.0015 로 13:10 단서 폭(0.048~0.050 = 0.002)과 맞먹어, 경계대에서는
                단서가 MC 잡음에 묻힘. 등록 N 은 그대로 두고: **어느 대조든 p 가 0.045~0.055 에
                들면 같은 seed 로 N=5,000,000 을 다시 돌려 둘 다 보고**(스크립트 자동, 1초 미만).
                판정은 등록 N 의 p 로 하고 5M 값은 병기 — 둘이 0.05 를 사이에 두고 갈리면
                "경계, 판정 유보" 로 적고 어느 쪽으로도 굳히지 않음. 자체검사에서 대 1.18 이
                0.0513 → 5M 0.0528 로 작동 확인. **n=4 전에는 어느 값도
                내지 않음** — r1 은 §7-5 통짜 σ_stat 의 첫 자료점일 뿐 판정이 아님(laptop2 13:15).
    R 문턱      등록된 2배 수렴 / 5배는 laptop2 통짜에도 같은 식으로 적용해 병기

    [13:10 단서 채움 — 규칙 불변] "dof 4n 근사" 의 크기를 랩탑(8019c6c, MC 20만회)이 재고
    laptop2 가 독립 재현: σ_stat 의 log 산포 등록 0.1823 대 실제(중앙값) 0.2088, T 의 산포
    +2.3%, 대조 1.76 에 대한 p 가 r=0.30 에서 0.0205 → 0.0225 등. **반보수적** 방향("실재"
    쪽)이나 p 상대 약 5% 라, 등록 p 가 0.048~0.050 에 떨어지지 않는 한 판정 불변. 판정문에
    이 줄을 병기. 아울러 §7-4 대조 값은 랩탑 1.76(중앙값 정의), §7-5 는 합동 0.005220 을
    쓰며, 랩탑의 1.514 는 사후 탐색용이라 어느 등록 판정에도 쓰지 않음(랩탑 자체 정정).

B 의 대조 셋은 각 기기의 통짜 러너 값이라 "통짜 대 통짜" 이고, 랩탑 01cf1e0 의 p=0.033
은 **분할** laptop2 대 통짜 랩탑이었으므로 별개입니다(그건 사후, 참고로만).

### 7-5. 분할 프로토콜의 `±` 동등성 검정 — r1 완주 전 등록 [09-03 12:55, 랩탑 제안·laptop2 전달, 추가 실행 0]

`CHUNKED_PROTOCOL_20260826.md` 2절은 "같은 추정량이고 상관 구조도 같다" 를 등록했지만
관문 넷은 **평균 일치만**(0.08 단위) 봤습니다. `±` 자체가 같은 추정량인지는 검정된 적이
없고, `±` 는 실행마다 2배 이상 흔들려(REPEAT_E5 5절) 한 번의 일치로는 못 봅니다.
§7 이 같은 기기·구조·관측량·조건에서 **러너만 다른** σ_stat 4개를 만듭니다.

    자료    분할 σ_stat (laptop2 R 측정 4회)  0.00627 / 0.00598 / 0.00645 / 0.00227
            통짜 σ_stat (§7 r1~r4)             <- 4개
            [자료점만, 판정 아님] r1 14:01 완주(122분, §6 과 경합 중 — 분할 1.47 h 와 시간으로
            대지 않음): 0.8072554 ± 0.0169881 → σ_stat 0.0061196. r2 14:01 착수.
            n=4 전에는 §7-3·7-4·7-5 어느 값도 내지 않음.
            r2 16:06 완주(124분) — **laptop2 가 값을 열지 않음**(누적 인상 방지, 등록문은 보고
            금지이나 열람도 안 하는 쪽을 택함, 데스크탑 동의). r2 σ_stat 은 r4 후 네 값과 함께.
            r3 진행 중, r4 완주 전망 약 22:1x.
            σ_stat = ±/2.776 (블록 5개, 각 dof 4)
    통계량  합동 분산의 F.  F = s²(분할 합동, dof 16) / s²(통짜 합동, dof 16)
    임계    양측 5%:  F(0.975,16,16) = 2.761    F(0.025,16,16) = 0.362  (데스크탑 검산)
    판정    0.362 < F < 2.761   -> 프로토콜의 "같은 추정량" 주장 유지
            F ≥ 2.761           -> **주장 기각** (분할 ± 가 더 큼) — 분할로 낸 결과의 오차 표기 전수 재검토
            F ≤ 0.362           -> **주장 기각** (분할 ± 가 더 작음, 과소 표기) — 같은 재검토, 방향 병기
    병기    σ_stat 의 산포를 보는 것이지 평균이 아니다. 관문 넷의 평균 일치는 그대로 유효하며
            이 검정이 뒤집지 않는다. dof 16 은 블록 독립 가정 위에 있다(laptop2 단서 ②).
            분할 4회 중 0.00227 이 나머지에서 2.8배 떨어진 것은 이 검정이 볼 자리이지
            사전에 빼지 않는다.

랩탑 문안은 상단 임계만 두고 "양측" 이라 적었기에 하단 임계를 붙였습니다 — 어느 쪽으로
갈려도 "같은 추정량" 은 아니므로 둘 다 기각입니다. 이것이 B 보다 클 수 있습니다:
주장이 깨지면 §7 한 건이 아니라 **분할 프로토콜을 쓴 결과 전부**의 오차 표기가 걸립니다.
