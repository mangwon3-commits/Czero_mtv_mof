# (가) Q_st(n) 우리 덩어리 + (다) T-NF-1 RH82 씨앗 보강 — 등록 (2026-09-20 00:07, 데스크탑 세션; **자료 0건** — 283/313 K CO₂ 등온은 어느 조성도 없음; 사용자 승인 2026-09-20 "do it")

§AR(09-19 23:4x)에서 종합자가 사용자 결정으로 남긴 (가)·(다). 사용자가 2026-09-20 00:0x 에 A·B·C 를 승인.

## 1. (가) 왜
T-NF-1q 가 MUF-16 에서 "영피복 Q_st 는 Widom 과 0.2 로 맞고, 적재별 오름은 실측 +15.8 중 +3.9(부분)" 을 냈다.
우리 조성의 Q_st 창(35~45, 보정 척도, `QST_WINDOW_RT_20260918.md §6`)은 **영피복 Widom** 값이다. 작업 적재(0.15 bar, 298 K)에서 CC Q_st(n) 이 Widom 과
같은 말을 하는지 여섯 조성에서 처음 검정한다. 순위·관문·판정은 바꾸지 않는다 — 창 문장에 "적재별" 한정어가 붙느냐만 정한다.

## 2. 계산
    구조   charged_v3/{base,saIm050,saIm0583,sa50nb50,mslm050,saIm025}_DDEC6.cif (생산 실현 seed=0, 앙상블 아님 — n=1)
    자     CLAUDE.md §1 고정값 전부(5,000+15,000 · UFF_MOF · García-Sánchez CO₂(TraPPE 는 기하만) · DDEC6 · Ewald 1e-6 · 12 Å · UnitCells 는 `run_tnf.py unit_cells()` 가 셀에서 → 2×2×2 확인을 DRYRUN 이 찍음)
    러너   `run_tnf.py --rh 0`(건조 CO₂ 단일, T-NF-1q 와 같은 러너·무수정) · 씨앗 1(작업마다 시차·회수)
    점     T = 283 / 298 / 313 K × p = 0.05 / 0.15 / 0.50 / 1.00 bar × 6조성 = **72건**. 결과 `tnf_results_qn_<조성>_<T>K_<p>bar.json`.
    분석   T-NF-1q §10 과 같은 식: 온도별 등온을 ln p 대 n 으로 선형 보간(외삽 금지) → 세 온도 공통 적재 범위 안의 격자 n(0.3/0.6/1.0/1.5/2.0 mol/kg 중 드는 것) 에서
           Q_st(n) = −R·d ln p/d(1/T). 오차는 ± 를 보간에 전파(상·하한). 작업 적재 n_w = 298 K·0.15 bar 의 n(보간).
    비용   ZIF 2×2×2 건조 CO₂ 한 건 1.5~3 h(DESIGN_STUDY 비용란 "3 h/건") → 72건/8워커 **≈ 18~27 h**. §AR 의 "8~10 h" 는 MUF-16(2×2×1) 실측을 옮긴 견적이라 낮다.
           LPT: 1.00 bar·283 K 부터 큐. 09-20 01:30 착수 → 09-20 20:00 ~ 09-21 04:00 완주.
## 3. 판정 (결과 전 고정)
    판정 1 (대표성)  조성별 |Q_st(n_w) − Widom Q_st(보정, results_v3 + 4.96)| ≤ 5 kJ/mol → **Widom 이 작업 적재를 대표** / > 5 → 창 문장에 "영피복 한정" 한정어.
    판정 2 (오름)    공통 범위 최고 n 의 Q_st − 최저 격자 n 의 Q_st = ΔQ_st: ≥ +8 협동 항 있음 / ≤ +3 없음 / 사이 부분. (1q 문턱 그대로.)
    판정 3 (기록)    Q_st(n) 곡선 표. n_w 가 공통 범위 밖이면 판정 1 은 "판정 불가(범위 [a,b])" 로 적고 점을 보태는 §-보완을 새로 등록한다(1q §10-보완 전례).
    예측             여섯 조성 모두 판정 1 통과(±5), ΔQ_st ≤ +3 (GME 큰 공동에는 MUF-16 채널의 사중극자 정렬 협동 항이 없다).
    반증             어느 조성이든 |차| > 5, 또는 ΔQ_st ≥ +8.
## 4. (다) T-NF-1 RH82 293 K 씨앗 3·4·5
    러너·조건 T-NF-1 과 동일(`run_tnf.py --temp 293 --pco2 0.165 --rh 82 --psat 2339`, muf16 md5 8d3fba10). 결과 `tnf_results_muf16_rh82_s{3,4,5}.json`. 3건 × 42 분(실측) — (가) 앞에 3슬롯.
    판정   TNF_REGISTRATION §1 ㉠′ 부호 검정을 n=5 로 다시 낸다(문턱 불변). 예측: 부호 불변, ± 축소. 값·순위 무관.
## 5. 절차
    사슬 `.claude_work_qn_chain.sh`(데스크탑): T-B2w-323 드라이버(run_tb2w_323.py)·simulate·network 0 → DRYRUN(UnitCells·온도·압력 찍음) → (다) 3건 → (가) 72건 슬롯 8. 로그 `qn_chain.log`. 결과는 postman 이 올림. 판정은 종합자, 검산 Melchior/Balthasar.
## 6. 진행 기록
    [2026-09-20 00:07] 등록(자료 0건). 사슬 기동 — 대기 상태로 T-B2w-323 종료를 기다림.
    [09-20 04:30] (다) 3/3 완주 04:2x(§AS 03:42 착수, 42 분) → TNF_RESULTS T-NF-1 절에 n=5 덧붙임: RH82/RH0 0.952~1.007, 부호 통과 유지, SE 0.0104. (가) 72건 진행 중.
    [09-23 08:20] (가) 72/72(09-21 17:05 완주) 판정 → `QSTN_VERDICT_20260923.md`(이의 창 09-23 16:2x). 판정 1 통과 3/6 · 밖 ['sa50nb50', 'saIm025', 'saIm050'] · 판정 2 없음 6/부분 0/있음 0/불가 0.
    [09-23 16:30] **확정** — 이의 창 16:2x 종료, 잔여 이의 0건 (`QSTN_VERDICT_20260923.md` 머리말·§5 도장). 창 라벨 16:1x 는 초판 짐작, 정본 16:2x.
