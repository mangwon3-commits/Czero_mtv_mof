# nbIm050e1~e5 — 2×2 비가법성 시험의 조건부 칸을 실측으로 (등록, 2026-09-06 11:2x, 데스크탑, 사용자 결정 3(가))

**무엇**: `nbIm050` 실현 5개(12/24, 시드 6 이상, 기존 실현과 **독립 배열**)를 짓고 v3 규범대로 이완·판정·전하·관문·Widom/GCMC 까지.
**왜**: `MULTILIGAND §8-5-4` 의 2×2 로그 비가법성 시험에서 `nbIm050` 칸만 단일 실현(dof 0)이라 판정이 영원히 조건부였다(`MIX10_VERDICT §3`).
**판정식·문턱·척도는 이미 등록돼 있다(§8-5-4). 여기서는 표본과 씨앗과 비용만 등록한다.**

## 1. 표본
    칸        nbIm050e1~e5 **5실현 평균**, 배치 SD 는 그 5실현 자신(sa50nb50e·saIm050e 와 같은 구성). 생산 nbIm050(시드 0, 42.71±2.53)은 **병기**만.
    관문      `risk_screen_v3ens_nb050.py` (base + nbIm050 + e1~e5 = 7종). e 실현이 LCD 감소 20 % 를 넘으면 그 실현은 표본에서 빼지 않고 **표시**한다(관문 통과 집합 논의는 §8-5-2 대로).
## 2. 씨앗 — 독립 배열
    conflict_aware 경로는 충돌쌍 0 이라 `chosen_sites` 가 (시드, 자리 수)만의 함수 → 시드 1~5 는 saIm050e1~5 와 자리가 **완전히 같아진다**(짝 설계).
    등록 설계는 **독립 배열**: 시드 6 부터, {0~5, 16, 19, 23, 24, 31, 32} 건너뜀. 채택 순서가 e 번호, 실제 시드는 rebuild_index 에.
    (짝 설계를 원하면 별도 등록. 이번엔 안 한다.)
## 3. 시험 ② 재판정 (§8-5-4 그대로, 자는 결정 (다): **배치 단위(σ) 판정, 단위(±) 병기**)
    I = ln(sa50nb50) − ln(saIm050) − ln(nbIm050) + ln(base),  σ_I² = Σ_칸 (σ_stat² + σ_batch²)/n_칸,  base 배치 0, 세 칸 n=5
    예측(등록, I = −0.524 고정 가정)   r_n 11 % → **4.5 배치 단위** · 16 % → 4.1 · 22 % → 3.6 · 30 % → 3.1   (단위(±) 병기: 1.6 / 1.5 / 1.3 / 1.1)
    문턱 1.5 배치 단위. **파괴값 33 % 는 이 실측으로 대체**된다 — r_n 이 실측되므로 "조건부" 가 풀린다.
    결론 문장(미리 고정)   "혼합은 두 치환의 곱셈 가법 예측보다 **낮다(길항)** — x 배치 단위" 또는 "가법과 구별되지 않는다". 선형 척도 병기.
## 4. 절차·비용 (`.claude_work_nb050.sh`, 순차, 데스크탑 전용)
    ① build_ensemble_nb050.py           ≈ 5~10 분
    ② relax_series_v3.py (3워커, xtb)   ≈ 3.5~5.6 h   (saIm050e 3.60 h · sa50nb50e 5.30 h · saIm075e 5.64 h 앵커; 624원자)
    ③ judge_relax_v3.py 5/5 pass 확인    초
    ④ charge_v3.py (coremof_tools)       ≈ 2~5 분,  charged_v3.json 에 5건 들어갔는지 확인
    ⑤ risk_screen_v3ens_nb050.py         ≈ 70 분  (Zeo++ 9.5 GB, RASPA 0 상태에서만 — 체인이 pgrep 로 스스로 막음)
    ⑥ run_gcmc_v3.py --only nbIm050e1~5 --out results_v3ens_nb050.json  V3_WORKERS=6 nice 10   ≈ 3h10m~3h35m (saIm075e 15작업 3h24m 앵커)
    합계 ≈ 8~11 h → 11:3x 착수 기준 **20:00~22:30 완주**. 완주 후 LCD/PLD/AV null 검사, results_v3.json 31행 무손상 확인.
## 5. 함정 (scout 확인)
    --out 필수 · 관문 접미사 v3ens_nb050(없으면 0500 파일 덮음) · relax_v3_results.json 은 덮어써지므로 이완 시간은 로그에서 · PYTHONHASHSEED(관문 base 7.61978/7.63144 정상, 7.83298 이면 base 누락)
    · C: 여유 2.8 GB(vhdx) — 체인 쓰기 ≈0.5 GB, 어젯밤 같은 조건에서 완주 · 돌고 있는 .sh 편집 금지 · 죽일 땐 PID 로.
