# `density_v3/` 에 **두 세대가 같은 이름으로 공존합니다** (랩탑, 2026-09-06 04:3x)

> **04:5x 갱신**: 이름을 갈라 놨고(3절), **4-1 이 닫혔습니다 — 두 실행의 차이는 실행 잡음입니다.**

`origin/master` 를 병합하려는데 `density_v3/diff_vtk/` 열 개가 **전부 이름이 겹치고
내용이 달랐습니다.** §7(밀도 격자는 사본이 없다)이라 덮지 않고 파고들었습니다.

## 1. 결론 — 손상은 없습니다. **이름이 겹칩니다**

각 `density_v3/<조성>__q_{on,off}/VTK/System_0/` 안에 **두 실행의 산출물**이 있습니다:

    COMDensityProfile_CO2.vtk       **08-18 16:1x~17:2x**  ← 랩탑 08-18 실행 (구판)
    COMDensityProfile_CO2.vtk.gz    **09-05 13:11**        ← 데스크탑 09-05 실행 (**현행·커밋본**)
    simulation.input                **09-05 13:11**        ← **09-05 것만 있습니다**

**둘은 서로 다른 실행입니다** — 같은 격자·같은 셀·같은 머리인데 값이 다릅니다(r 0.865~0.952).
**`nbIm025` 에만 `.vtk` 가 없습니다** — 09-05 실행에서 추가된 조성이라, 이것이 `.vtk` 가
구판임을 확정합니다.

    조성        .vtk 합(08-18)   .gz 합(09-05)      r
    base           2943.48         2756.42       0.86455
    saIm025        2188.92         2454.81       0.92652
    saIm050        1263.31         1137.23       0.95212
    saIm075        1633.52         1631.73       0.94802
    saIm100        1621.75         1694.66       0.93808

**두 diff_vtk 벌은 각각 자기 원본에 충실합니다** — 서로 대조하면 r=1.000000 입니다.
`master` 의 것은 `.gz`(09-05)에서, 제 03:39 것은 `.vtk`(08-18)에서 나왔습니다.
**틀린 계산이 아니라 다른 원본입니다.**

## 2. ⚠️ 그래서 무엇이 위험한가

**`simulation.input` 은 09-05 것 하나뿐인데 그 옆의 `.vtk` 는 08-18 것입니다.**
**둘을 짝지어 읽으면 설정과 자료가 어긋납니다.** 그리고 어긋난 티가 안 납니다 —
셀·격자·머리가 전부 같습니다.

    · `COMDensityProfile_CO2.vtk` 를 **직접 읽는 스크립트는 구판을 집습니다.**
      **제 03:39 export 가 그렇게 됐습니다** — 산출물은 멀쩡해 보였고 실제로 멀쩡한데,
      **09-05 실행이 아니라 08-18 실행의 그림**이었습니다.
    · `README.md` 가 시키는 `gunzip -k ...COMDensityProfile_CO2.vtk.gz` 는 대상이
      **이미 있어 되묻습니다.** "y" 를 누르거나 `-f` 를 붙이면 **08-18 판이 사라집니다.**
      (자동 스크립트에서는 되물음 없이 실패하거나, `-f` 면 조용히 덮습니다.)

**`PREREG_CHECKLIST` 검사 5(같은 이름의 다른 양)가 파일 트리에 실물로 있는 경우입니다.**
`density_results.20260818_laptop.json` 은 **이름에 날짜를 넣어 갈라 놨는데**, 격자는 안 갈라졌습니다.

## 3. 조치 (이 기기)

    density_v3/diff_vtk.from_run_20260818_laptop/  제 03:39 export(=08-18 실행)를 **옮겨 보존**
    density_v3/diff_vtk/                        master 판(=09-05 원본)이 병합으로 들어옴

**아무것도 안 지웠습니다.** **이름만 갈라 놨습니다**(09-06 04:5x, 데스크탑 동의):

    COMDensityProfile_CO2.vtk  ->  COMDensityProfile_CO2.**20260818_laptop**.vtk   10개
    DensityProfile_CO2.vtk     ->  DensityProfile_CO2.**20260818_laptop**.vtk      10개 (전원자 판)
    diff_vtk.laptop_20260905_0339/  ->  diff_vtk.**from_run_20260818_laptop**/

> ⚠️ **`README.md` 의 *"전원자 판은 데스크탑 로컬에만 있습니다"* 는 사실과 다릅니다** —
> **이 랩탑에 10개 있습니다**(08-18 판). 그래서 그것도 같이 갈라 놨습니다.
> 데스크탑은 `.gz` 만 있어 그쪽 트리는 원래 깨끗합니다.

## 4. 덤으로 나온 수 — **격자의 실행간 차이. 저장소에 없던 수입니다**

두 실행을 그대로 비교했습니다(⚠️ **아래 4-1 을 먼저 읽으십시오**).

    **재현되는 것**
      >q99 부피 (같은 문턱)          상대차  **0.8 ~ 5.7 %**
      q99 값 자체                    상대차  **1.4 ~ 7.0 %**
      장(場) 상관 r                  RHO **0.865~0.952** · GAIN **0.508~0.788**
    **재현 안 되는 것**
      단일 복셀 최댓값 **위치**      10개 중 5개가 **2.8 ~ 23.3 Å** 이동 (셀은 52 Å)
      상위 1 % 무게중심              **0.58 ~ 7.63 Å** 이동 (다봉 분포라 애초에 잘못된 요약)
      상위 10 봉우리 일치(≤1.5 Å)   **47/100.** 조성별로 **0/10 ~ 9/10**

**장이 잘 맞는다고 봉우리가 맞지는 않습니다.** `saIm050` 은 r 이 가장 높은데(0.952)
봉우리는 2/10 이고, `base` 는 r 0.865 에 **0/10** 입니다.
**"장이 재현되니 자리도 재현된다" 는 성립하지 않습니다.**

교차 대조로 **라벨 뒤바뀜은 배제**했습니다(대각이 8·9·8, 비대각은 `saIm025`↔`saIm100`
8 뿐 — 두 조성이 실제로 자리를 공유하는 것).

### 4-1. ✅ **닫혔습니다 — 이것은 실행 잡음입니다** (09-06 04:5x)

`simulation.input` 은 09-05 것만 남았지만, **08-18 실행의 RASPA 출력 10개가 이 기기에
그대로 있고 그 안에 설정이 통째로 찍혀 있습니다.** 데스크탑 트리에도 git 이력에도
없던 것이 **출력 파일 안에** 있었습니다.

    항목            08-18 실행 (출력에서 판독)   09-05 실행 (simulation.input)
    사이클/초기화   **5000 / 2000**  (10개 전부)  5000 / 2000
    온도 / 압력     **298 K / 15000 Pa** (10개)   298.0 / 15000.0
    컷오프          **12.000000**                 12.0
    단위셀          **2 2 2**                     2 2 2
    구조 CIF        **`charged_v3/<조성>_DDEC6.cif` 와 md5 동일 (10개 전부)**
    난수 씨앗       **1787035681 · …682 · 1787037108 · …119**  (시각 기반, 실행마다 다름)

**설정도 구조도 같고, 다른 것은 난수 씨앗뿐입니다. 그러므로 4절의 수는 실행 잡음입니다.**

**병기였던 한 줄도 닫혔습니다 (05:0x, 데스크탑).** 09-05 실행의 CIF 를 직접 대조하지
못해 처음에는 *"git 추적본이니 같을 것"* 이라는 **추론**으로 뒀는데, `charged_v3/` 여섯
CIF 의 **마지막 변경이 전부 `e6a8516` (08-17 14:07)** 로 확인됐습니다 — **두 실행(08-18
15:47 · 09-05 00:58) 모두보다 앞섭니다.** 그러므로 두 실행은 **같은 파일 내용**을
읽었습니다. **08-18 쪽 md5(랩탑) + CIF 이력(데스크탑) = 구조 동일 확정.** 추론 없음.

**그래서 4절의 결론이 세집니다** — 자리를 지목하는 주장(T-4 류)에
**단일 복셀 통계를 쓰면 안 되는 이유가 "구조가 달랐을 수도" 가 아니라
"같은 계산을 두 번 돌리면 최댓값이 최대 23 Å 옮겨 간다" 입니다.**
자리를 지목하려면 **봉우리 목록 일치**를 직접 재야 하고, 그 자는 **47 %** 입니다.
