# 곡률 부등호 — `rh05_sa50nb50e3` 한 값에 대한 사전 등록 (2026-08-28)

> # 🔴 결과: **기각.** 실측 0.2776, 등록 구간 [0.2268, 0.2710] 밖.
>
> `rh05_e3` 완주 539분. 등록(11:10:32, 커밋 `d196ee3`) 이후에 나왔습니다.
> **아래 본문은 결과를 보기 전 그대로이고, 판정은 9절에 붙입니다.**

**이 문서는 `rh05_e3` 가 끝나기 전에 작성됐습니다.** 작성 시점에 그 작업은
경과 318분으로 진행 중이었고(같은 조건 완주 실적 361분·559분), 출력 파일에
`Simulation finished` 가 없음을 확인했습니다.

## 1. 무엇을 예측하나

군 B `sa50nb50` 의 저압 물 등온선이 RH 5→10→15 에서 **위로 굽는지**를
2차 차분으로 봅니다.

    c = w(RH5) − 2·w(RH10) + w(RH15)

관측된 세 실현:

| 실현 | w(5) | w(10) | w(15) | **c** |
|---|---|---|---|---|
| e1 | 0.0339 | 0.0600 | 0.0888 | **+0.0027** |
| 생산 | 0.1497 | 0.2623 | 0.3459 | **−0.0290** |
| e2 | 0.3376 | 0.4985 | 0.5862 | **−0.0732** |

**`w(10)` 이 커질수록 `c` 가 단조로 내려갑니다.** 3점 직선맞춤 R² = 0.9975,
기울기 −0.1735.

## 2. 등록된 부등호를 `e3` 로 옮기면

`e3` 는 `w(10) = 0.3875` 로 **생산(0.2623)과 e2(0.4985) 사이**입니다.
그래서 등록된 부등호는

    −0.0732  ≤  c(e3)  ≤  −0.0290

이고, `e3` 의 `w(10)`·`w(15)` 가 이미 측정됐으므로

    c(e3) = w(5) − 2(0.3875) + 0.4750 = w(5) − 0.3000

**부등호가 미측정 값 하나에 대한 예측으로 바뀝니다:**

> ## **`rh05_e3` 물 로딩 ∈ [0.2268, 0.2710] mol/kg**
>
> 점추정 **0.2476** (생산·e2 선형내삽)
> 교차 확인 **0.2471** (e1 포함 4점 맞춤) — 두 경로가 0.0005 안에서 일치

## 3. 기각 조건 — 결과를 보고 안 고칩니다

| 결과 | 판정 |
|---|---|
| `w(5) ∈ [0.2268, 0.2710]` | **부등호 성립.** 곡률이 `w(10)` 하나로 정렬된다 |
| 구간 밖 | **부등호 기각.** 단조 정렬은 3점의 우연 |

**한쪽 분기만 준비하지 않았습니다.** 기각이 나오면 `LOWRH_18_ANALYSIS.md`
재작성 때 곡률 논거를 빼고, 성립하면 4점째로 적습니다.

## 4. 검정력 — 통계오차만으로도 7% 는 벗어납니다

    σ_c = sqrt(σ05² + 4σ10² + σ15²) = 0.01225
      σ05 0.00465   σ10 0.00396   σ15 0.00811     (= RASPA± / 2.776, ERROR_BARS.md)
      rh05 의 ± 는 미측정이라 생산값 0.0129 를 가정

    구간 반폭 0.0221  =  **1.80 σ_c**
    참값이 중앙이어도 벗어날 확률  ≈ **7.1%**

> ⚠️ **그래서 한 번 벗어나는 것은 부등호를 죽이지 않습니다.** 반대로 한 번
> 맞는 것도 확정이 아닙니다. `e4`·`e5` 가 같은 시험을 두 번 더 줍니다.
> **이 한계를 결과 보기 전에 적어 둡니다** — 나중에 "노이즈였다" 고
> 말하려면 지금 적혀 있어야 합니다.

`w(10)` 계수가 2 라 `σ10` 이 분산에 4배로 들어옵니다. 곡률 시험은
**가운데 점의 오차에 가장 약합니다.**

### 4-1. 보강 — `σ05` 대입이 낙관 쪽이었습니다 (경과 322분, 여전히 미완주)

데스크탑이 *"`σ05` 를 무엇으로 채웠는지 적어 두라"* 고 물어 왔습니다.
위에 적은 대로 **`생산` 의 `rh05` 값 0.0129** 를 썼는데, `생산` 의 그
로딩은 0.1497 이고 `e3` 는 0.2476 로 예측되므로 **더 낮은 로딩의 오차를
빌려 온 것**입니다.

군 B 완주분 11점 전부에서 `±` 는 로딩에 붙어 있습니다:

    ± = 0.0403·w + 0.0055      r = 0.812   (n=11, 생산 3점 포함)

| `σ05` 를 무엇으로 | `σ_c` | 반폭 | 벗어날 확률 |
|---|---|---|---|
| **생산 rh05 (등록에 쓴 값)** | 0.01225 | 1.80σ | **7.1%** |
| 회귀 예측 (±0.0155) | 0.01263 | 1.75σ | **8.0%** |
| e2 rh05 (±0.0124, 최대 관측) | 0.01218 | 1.81σ | **7.0%** |

**7.1% 는 8.0% 여야 맞습니다** — 낙관 쪽으로 0.9%p 틀렸습니다.
**결론은 안 바뀝니다.** 구간도 판정 분기도 그대로 둡니다.

> `e1` 의 오차로 계산하면 `σ_c = 0.0067` 이 나와 확률이 0.1% 까지
> 떨어집니다. `e1` 은 로딩이 가장 낮아 오차도 가장 작습니다 — **가장
> 낙관적인 극단**이고 대표값이 아닙니다.

## 5. 이 예측이 걸고 있는 것

`c` 의 관측 범위는 +0.0027 ~ −0.0732 (폭 0.0759)이고 구간 폭은 0.0442 입니다.
**관측 범위의 58% 를 배제하지 못합니다** — 강한 시험은 아닙니다. 강한 부분은
`w(10)` 만으로 `c` 를 3점에서 R²=0.9975 로 맞춘다는 쪽이고, `e3` 는 그
직선의 **내삽점**이라 외삽 시험보다 약합니다.

**`e4`·`e5` 중 `w(10)` 이 관측 범위 밖으로 나가는 것이 있으면 그것이 진짜
시험입니다.** 그때는 이 문서를 다시 열어 외삽으로 등록하겠습니다.

---

# 9. 판정 (결과 도착 후 작성)

## 9-1. 기각

```
등록 구간   w5 ∈ [0.2268, 0.2710]
점추정      0.2476
실측        **0.2776**          상한을 0.0066 초과
```

**3절 표의 두 번째 분기입니다: 부등호 기각.**

2차 차분은 `c = −0.0224` 로 등록 구간 `[−0.0732, −0.0290]` 밖입니다.

## 9-2. 단조 정렬 자체가 깨졌습니다

| 실현 | `w(10)` | `c` |
|---|---|---|
| e1 | 0.0600 | +0.0027 |
| 생산 | 0.2623 | −0.0290 |
| **e3** | **0.3875** | **−0.0224** |
| e2 | 0.4985 | −0.0732 |

**`생산` → `e3` 에서 `w(10)` 은 커졌는데 `c` 가 올라갔습니다.** 구간을
벗어난 것만이 아니라 **정렬 방향이 뒤집힙니다.** 4점 맞춤 R² 는
0.9975 → **0.7859**.

2절의 *"`w(10)` 이 커질수록 `c` 가 단조로 내려간다"* 는 **3점의 우연**
이었습니다 — 3절이 미리 적어 둔 그대로입니다.

## 9-3. 등록문이 스스로 모순이었습니다

3절: *"구간 밖 → **부등호 기각**"*
4절: *"한 번 벗어나는 것은 **부등호를 죽이지 않습니다**"*

**이 결과에서 둘이 반대 방향을 가리킵니다.** 사전 등록의 요점은 결과를
보고 고르지 않는 것인데, **고를 여지를 제가 남겨 두었습니다.**

**엄격한 쪽을 택합니다 — 기각.** 4절은 "한 번 맞아도 확정이 아니다" 를
말하려던 것이지 기각을 무르는 장치가 아니었고, 결과를 본 뒤에 그것을
탈출구로 쓰면 등록한 의미가 없습니다.

> **다음부터: 등록문에 판정 분기를 적을 때 검정력 문단이 그 분기를
> 무를 수 있는지 확인합니다.** 4절을 쓸 때는 보강이라고 생각했는데
> 실제로는 3절과 겨루는 문장이었습니다.

## 9-4. 검정력 추정이 두 번 낙관적이었습니다

```
등록에 쓴 σ05 가정   ± 0.0129   (생산 rh05)        -> 벗어날 확률 7.1%
회귀 보정 (4-1절)     ± 0.0155                      -> 8.0%
**실측**              **± 0.0251**                  -> **12.7%**
```

**실측 오차가 회귀 예측의 1.44배입니다.** 로딩-오차 회귀(n=12, r=0.782)로도
안 잡히는 크기이고, 관측된 rh05 오차 중 최대입니다.

`σ_c = 0.01450` 으로 다시 재면 실측은 점추정에서 **2.07 σ_c**, 양측
**p = 3.9%** 입니다.

> **그래서 이 기각은 "노이즈였다" 로 넘길 수 없습니다** — 사전 확률
> 12.7% 를 감안해도 실측은 그보다 더 나갔습니다. 다만 **p=3.9% 는 한 번의
> 시험으로 확정할 크기도 아닙니다.**
>
> **판정은 기각이고, 강도는 "한 점에서 3.9%" 입니다.** 둘을 같이 적습니다.

## 9-5. 남은 시험

`e4`·`e5` 가 같은 시험을 두 번 더 줍니다. **다시 등록하지 않겠습니다** —
기각된 부등호를 재등록하는 것은 이미 본 결과에 맞춰 다시 세우는 일입니다.

`LOWRH_18_ANALYSIS.md` §2·§3 재작성 때 **곡률 논거를 뺍니다.**
5절에 적어 둔 대로, `w(10)` 만으로 `c` 를 설명한다는 주장은 4점에서
R² 0.79 이고 방향도 안 지켜집니다.
