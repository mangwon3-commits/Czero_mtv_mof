# 10건 판정 — 혼합 앙상블 (`sa50nb50e1~5` · `saIm050e1~5`) — 2026-09-06 06:2x, 데스크탑(종합자)

**등록**: `MULTILIGAND_20260905.md §8-5` (자료 0건 시점 22:47, §8-5-1 은 03:2x 보정 — 두 자 공동 주 판정). **[10:4x 재판정 — 사용자 결정 (다), `RULER_DECISION` §7: 배치 비교의 판정 자는 배치 단위(σ 자), 통계 자(±) 병기. §2·§3·§4·§6 갱신, 수는 전부 불변.]** **이 문서는 등록식을 그대로 댄다.
문턱·식·표본·자 어느 것도 바꾸지 않는다.** 결과 파일 `results_v3ens_mix10.json`(10행, `status` 전부 ok, LCD/PLD/AV null 0건), 체인 rc=0 (① 관문 22:49→23:59, ② GCMC/Widom 23:59→06:23).

## 1. 원자료 (선택도 CO₂/N₂, Widom 헨리 비; ± 는 RASPA 블록 5 의 95 % CI)

    sa50nb50   e1 52.68±2.37 · e2 76.94±4.02 · e3 83.78±5.88 · e4 87.95±3.29 · e5 56.67±3.44
               평균 **71.60**  배치 SD **16.01 (22.4 %)**  블록± 평균 3.80        정본(단일) 75.83 = 6실현 중 4위
    saIm050    e1 67.80±2.14 · e2 52.03±3.91 · e3 56.48±3.52 · e4 54.00±3.29 · e5 54.38±2.75
               평균 **56.94**  배치 SD **6.27 (11.0 %)**   블록± 평균 3.12        정본(단일) 65.21 = 6실현 중 2위

    관문(risk, `risk_results_v3ens0500.json`)  saIm050e1~5 LCD 감소 13.6 / 10.2 / 9.7 / 8.8 / 13.6 %  **5/5 통과** (전부 "덜수렴" 표시)
    sa50nb50e1~5 는 이미 있음(`risk_results_v3ens50nb50.json`) 6/6 통과 → **두 계열 다 관문 통과 집합 안.**

## 2. 등록 시험 ① — 혼합 대 saIm050 (§8-2 → §8-5-1·8-5-3)

    차            71.60 − 56.94 = **14.67**
    ± 자          조성 ±' = √(블록±² + (2.776·s)²)/√5      → **0.68 단위**
    σ 자          조성 SE = √((블록±/2.776)² + s²)/√5     → **1.90 단위**
    등록 예측     ± 0.55 · σ 1.52 (0583 11실현 빌림)          실측은 둘 다 조금 위, **문턱 기준 같은 쪽**

> **판정 [10:4x 재판정, 결정 (다)]: 구별됨(배치 단위) — 1.90 배치 단위 ≥ 1.5. 병기: 0.68 단위(±) < 1.5.**
> 앙상블 5실현 대 5실현이라 **배치 비교**이고, 결정 (다)(`RULER_DECISION_20260828.md` §7)에 따라 판정 자는 배치 단위(σ 자)다. 여유는 문턱의 1.27배로 얇다(등록 예측 1.52 와 같은 쪽).
> 쓸 수 있는 문장: *"관문 통과 집합 안에서 sa50nb50 은 saIm050 보다 높다 — 1.90 배치 단위(구별됨) · 0.68 단위(±, 병기)."* 점추정 부호는 혼합이 위.
> [06:2x 초판] 두 자가 갈려 "구별됨" 을 쓰지 않았다 — 당시 §8-5-1 의 "공동 주 판정"(결정 미루기) 규칙. 수는 그대로다.
> **MULTILIGAND §3 의 +2.92 는 이 수로 대체한다.** 단일 실현끼리의 2.92 는 앙상블 평균에서 1.90 배치 단위 / 0.68 단위(±)가 됐다.

## 3. 등록 시험 ② — 2×2 요인 비가법성, 로그 척도 (§8-5-4, 조건부)

    칸        base 20.10±0.45 (n=1, 배치 0) · saIm050 **56.94** (n=5) · nbIm050 42.71±2.53 (n=1, 배치 미정의) · sa50nb50 **71.60** (n=5)
    I = ln(71.60) − ln(56.94) − ln(42.71) + ln(20.10) = **−0.524**   (단일 실현 넷으로는 −0.603)  → 부호 음, **길항 유지**
    식        σ_I² = Σ_칸 (σ_stat² + σ_batch²)/n_칸,  r_a 실측 22.4 % · r_m 실측 11.0 % · r_base 0 · r_n(nbIm050) **미측정**

    nbIm050 배치를        σ 자         ± 자
      0 으로 두면         4.58 단위    1.65 단위
      16.2 % 빌리면       2.64         0.95
    **절대 파괴값 r_n**    **33.0 %**   **5.3 %**    (이 값을 넘으면 1.5 아래. 형제 실측 r_a 22.4 / r_m 11.0 %)

> **판정: 조건부.** σ 자에서는 nbIm050 의 배치 산포가 **33 % 를 넘어야** 무너진다(형제 조성은 11~22 %). **다만 §8-5-4 가 병기하라 한 r 의 dof-4 상대 불확실성 35 % 를 넣으면 형제 최대(22.4 %) 대비 (33.0−22.4)/(0.354×22.4) = 1.35 단위 — 문턱 아래**라 "살아남는다" 를 쓰지 못한다(Caspar 읽기 (ㄷ)). 
> 통계 자 병기: ± 자에서는 **5.3 % 만 넘어도** 무너진다 — 거의 확실히 못 선다. 무조건 판정은 `nbIm050e1~5`(15건) 뒤에만.
> **[10:4x 재판정, 결정 (다)]** 분모에 배치항이 있어 **배치 단위(σ 자)가 판정**이다: 4.58 배치 단위(r_n=0) / 2.64(16.2 % 빌림), 파괴값 33.0 % 는 형제 최대 22.4 % 위 — 그러나 **dof-4 불확실성을 넣은 1.35 배치 단위 < 1.5** 라 판정은 **조건부 그대로**, "살아남는다" 를 쓰지 못한다. 결정 (다)로도 이 시험은 안 닫힌다. 통계 자(1.65/0.95 단위, 파괴값 5.3 %)는 병기.
> 선형 척도 병기: 가법 예측 56.94 + 42.71 − 20.10 = 79.55 대 실측 71.60 (−7.95).

## 4. 등록 밖 관찰 (판정 아님, 서술)

- **혼합 조성의 배치 산포가 단일보다 크다(방향)** — 선택도 22.4 % 대 11.0 %(로딩 18.6 % 대 7.7 % 는 선택도와 corr 0.995 라 독립 증거가 아님). **선택도 축**에서 혼합 조성의 배치 산포를 잰 첫 자료(관문 LCD 축은 08-29 부터 있음).
  **이 방향 주장에도 자를 붙인다**: F(4,4) = 6.51, 양측 p = 0.097, SD 비 95 % CI [0.82, 7.91]; 로그비 **1.87 배치 단위 · 0.68 단위(±)**(Caspar 읽기 (ㄹ)). **[10:4x, 결정 (다)]** 배치 산포끼리의 비교라 자는 배치 단위이고 **방향은 배치 단위로 선다(1.87 ≥ 1.5, 여유 1.25배)** — 다만 95 % CI 가 1 을 품으므로(p 0.097) 서술은 "단일보다 크다(방향)" 까지이고 등록 밖 관찰(판정 아님)이라는 지위는 그대로다. 구조 축은 방향이 반대(LCD 상대 SD 혼합 1.8 % 대 단일 3.2 %).
  **배수는 표본 5 로는 안 정해진다**(Melchior 진단, leave-one-out 은 판정에 안 씀): sa50nb50 은 하나 빼도 18.2~25.8 % 로 강건하나 saIm050 은
  e1(67.80) 하나에 걸려 3.4~12.7 % — 비는 1.8~6.2. 방향은 서고 참값은 2배보다 클 공산이 크나 **"두 배" 라는 수는 쓰지 않는다.**
  **원인은 배치 알고리즘이 아니다** — saIm050e 는 `conflict_aware: true` 지만 충돌쌍 0 이라 회피가 균등 무작위로 축퇴하고, 두 계열 다 C(24,12) 균등 추출
  (Melchior §2). 원인 후보는 **치환 점유율**: saIm050 은 24자리 중 12자리가 모체(clIm) 그대로, sa50nb50 은 24자리 전부 치환에 화학종 둘.
  [06:4x 정정 — 초판은 "§8-3 의 알고리즘 우려가 확인됐다" 로 적었고 그 예측을 Melchior 에게 돌렸는데, 둘 다 틀렸다. Melchior 가 맞힌 것은
  "2×2 는 nbIm050 없이 조건부" 와 파괴값 약 35 %(실측 33.0 %) 이다.]
- **정본 실현의 위치**: saIm050 정본 65.21 은 6실현 중 2위(앙상블 평균 56.94 보다 8.3 위), sa50nb50 정본은 4위. §2 의 65.2 를 인용하던 문장은
  앙상블 평균 **56.9** 로 읽어야 한다. Caspar §1-1 의 "정본이 극값" 관찰과 같은 방향(사후 통계량이라 증거로 세지 않음).
- sa50nb50e5 의 AV 778.8 은 **러너 Zeo++(이완 전, 08-29 `risk_results_v3ens50nb50.json` 의 `before.AV` 와 동일값)** 이고, **이완 후 `after.AV` 는 3853.9 로 e 계열 최대**, LCD·PLD 정상 — "구조 이상치" 아님. 초판의 "구조 이상치가 혼합 계열에만" 은 **철회**(Caspar 읽기 (ㄴ)). AV 상대 SD 14 % 는 전부 이 한 점이고 빼면 3.1 % ≈ saIm050 3.0 %.
- 관문 이완 "덜수렴"(`converged: false`)은 **이번 집합의 성질이 아니라 두 관문 파일 전 14행(base·정본 포함) 공통**이다 — LCD_reference 를 만드는 base 자신이 덜수렴(Caspar 읽기 (ㄱ)). 통과/탈락에 영향이 없다는 것은 **확인된 사실이 아니라 가정**이며, LCD 소수점은 인용하지 않는다(이완 수렴 문제는 §11 열린 항목).
- `sa50nb50e3` 은 `charged_v3.json` `net_charge_warn` 에 있다(알짜 +5.6e-5, S1 관문 5e-5 초과, 크기는 무시 수준) — 10행 중 1행, 인용 시 병기.
- 이름 충돌 주의: §2 의 "σ 1.52" 는 8-5-3 의 예측이고, 8-5-1 이 인용 금지한 8-2 혼합식 값도 1.52 다 — 다른 양이다.

## 5. 비용 (데스크탑 기기 축, 검사 6-2)

    관문 7종(이완+Zeo++ 워커 1)   70분          Widom 20작업(6병렬 nice 10)   138분, 37분/작업
    GCMC 0.15 bar 10작업(6병렬)   1h38m ~ 2h11m/작업, 2파도 = 4h39m         체인 전체 22:49 → 06:23 = **7h34m**

## 6. 이 판정이 못 하는 것

- "관문 안 최고는 혼합" — **여전히 못 쓴다**(§8-5-2). 병기: **saIm075 선택도 6실현 87.64 ± 8.78(1σ), 관문 기각 미해결 0.857** — 앙상블 평균끼리도 saIm075(87.6) > sa50nb50(71.6).
- ~~어느 자가 판정인지 — 사용자 결정~~ **[10:4x 결정 (다)]** 배치 비교이므로 배치 단위가 판정: 시험① **구별됨(1.90 배치 단위)**, 시험② **조건부 그대로(1.35 배치 단위)**. 통계 자(0.68 · 0.95~1.65 단위)는 병기이지 판정이 아니다. **결정이 실제로 가른 것은 시험① 하나**이고, 첫 항목("관문 안 최고는 혼합" 못 씀)은 이 결정과 무관하게 그대로다.
- 2×2 의 무조건 판정 — nbIm050e1~5 가 없으면 영원히 조건부.
- 습윤 축 — 물 힘장 결함(D=8.63) 아래, ENSEMBLE_0500 §3 열린 채.

## 7. Caspar 단독 읽기 (§9-7, Opus 서브에이전트, 06:27~06:34) — 전문

수 재현율 100 %(§1~§3 불일치 0, RMS 합성으로도 무감). 등록 준수: 8-5-1·8-5-3·8-5-4·8-5-5 통과, **8-5-2 saIm075 병기 누락 · 8-5-4 dof-4 불확실성 누락 · §4 자 미표기 — 수정 필요 (ㄱ)~(ㄹ)**, 위에 반영(06:3x).
함정 확인: null 0 · `results_v3.json` 31행 무손상(`--out` 지켜짐) · 10 CIF 해시 상이 · 원자수 672/648 정상.
과장 넷 잡힘: "살아남을 공산이 크다"(1.35 단위) · "참값은 2배보다 클 공산"(p 0.097, 자 갈림) · "첫 자료"(선택도 축으로 한정) · "구조 이상치가 혼합에만"(반대 증거 저장소에 있음).

