# §AS (가) Q_st(n) **§-보완2 등록** — 압력을 더 내려 **오름이 이어지는지** 본다 (2026-09-24 19:37, 데스크탑 판정 세션, **자료 0건**)

## 0. 왜 — §-보완1 이 가운데서 멎었다

`QSTN_SUPP_VERDICT_20260924.md` 의 P1 이 **판정 불가**였습니다. 시험 다섯의 Δ_low 평균이 **-2.740** 로, 물리(≥ −2.5)와 도구 의심(≤ −4.0) **사이**에 떨어졌습니다. 다만 **움직였습니다** — 작업 적재에서 -4.80(≈ −2RT) 이던 것이 n=0.3 에서 -2.740 입니다.

    상수 오프셋(도구)이라면 피복을 낮춰도 **안 움직입니다.** 움직였으니 순수 도구 오차는 이 자료와 맞지 않습니다.
    피복 효과(물리)라면 **계속 움직여 0 으로 갑니다.** 아직 안 갔습니다.

그래서 **수준이 아니라 변화**를 잽니다. §-보완1 의 두 번째 한계도 같은 곳을 가리켰습니다 — n=0.3 이 공통범위 **끝**이라 등온을 +1σ 올리면 다섯 중 둘만 남았습니다. **지금 격자로는 더 못 내려갑니다.**

## 1. 계산할 것

    조성   base · mslm050 · sa50nb50 · saIm025 · saIm050 · saIm0583  (§-보완1 과 **같은 여섯**)
    온도   283 · 298 · 313 K              압력   **0.002 · 0.005 bar**
    36건. 그 밖은 §1 고정값 그대로(건조 CO₂ 단일, 2×2×2, 15,000 사이클, 씨앗은 착수 시각).
    기기 항: `base` 를 **두 기기**가 돌립니다(교차 여섯 짝) — 교차 쪽은 `--tag-suffix _l2`. 총 **42건**.
    ⚠ 유효 조건(§-보완1 §1-3 과 같음): 짝의 **씨앗이 달라야** 합니다. 같은 짝은 P6′ 에서 제외하고 제외 수를 적습니다.
      씨앗은 **결과 JSON 의 `seed` 열**에서 읽습니다(`_meta.json` 아님 — 09-24 17:4x 에 확인된 자리).

## 2. 격자 — **결과를 보기 전에 박습니다**

    주 격자   **n = 0.15 mol/kg** — n=0.3 의 **절반**. 0.3 은 §-보완1 의 주 격자였습니다.
    공통 범위 = [ max_T(min_n(T)) , min_T(max_n(T)) ] — §AS·§-보완1 과 같은 정의. **외삽 금지.**
    Δ_low(n) ≡ Q_st(n) − Widom(보정). Widom 표는 §-보완1 §3 과 **같은 값**을 씁니다(새로 안 잽니다).

## 3. 예측 (자료 0건 시점 고정)

    P1′ ★ 주 판정 — **오름** R ≡ [시험 다섯의 Δ_low(0.15) 평균] − [같은 다섯의 Δ_low(0.3) 평균 = -2.740]
          R ≥ **+0.51**        → **물리**      (오름이 이어진다)
          R ≤ **+0.15**        → **도구 의심** (멎었다 — 남은 차는 상수 오프셋)
          +0.15 < R < +0.51  → **판정 불가**

          **문턱의 근거 — 지어내지 않고 §-보완1 실측에서 끕니다.**
          작업 적재(n≈1.2) → n=0.3 에서 실측 오름은 **+2.06** 이고 ln 피복 한 단위당 **1.49** 입니다.
          같은 기울기면 0.3 → 0.15(ln 2 = 0.693)에서 **+1.03** 를 기대합니다. 물리 문턱은 그 **절반**인 +0.51 로 둡니다
          — 기울기가 0 쪽으로 휘어도(포화) 물리 가지가 살 수 있게 하는 여유입니다.
          도구 문턱 +0.15 는 **0 과 구별 안 되는 크기**입니다: §-보완1 에서 등온을 ±1σ 옮겼을 때 평균이 −2.740 → −2.956 / −3.357 로 움직였으므로, 0.15 보다 작은 오름은 잡음과 못 가릅니다.

    P2′  다섯 전부 `Δ_low(0.15) > Δ_low(0.3)` — 조성마다 개별로 올라간다.  등록 예측: **맞음**.
    P3′  대조 `base` 의 |Δ_low(0.15)| ≤ 2.5 (센 자리가 없어 안 움직임).   등록 예측: **맞음**.
    P4′  `n_lo < 0.15` 인 조성 수 **6/6**.  등록 예측: **6/6**.
         (6/6 이 아니면 그 조성은 P1′ 에서 빠지고, 빠진 수를 판정문에 적습니다. **다섯이 다 빠지면 P1′ 판정 불가**이고 그때는 압력이 아직 모자란 것입니다.)
    P6′  `base` 여섯 짝 적재 비의 **중앙값이 1 ± 0.03 안**.  등록 예측: **안**. 밖이면 **P1′ 판정 불가**.

    ⚠ **반증/이상 조건**: 다섯 중 하나라도 `Δ_low(0.15) < Δ_low(0.3) − 0.5` 면(= **내려간다**) 어느 가지도 아닙니다 —
      **판정 불가 + 도구 점검**(보간 끝점·등온 단조성·저압 수렴부터). 0.002 bar 는 적재가 작아 상대 오차가 커지는 자리입니다.

## 4. 바꾸는 것 / 안 바꾸는 것

    (a) **물리**      `QST_WINDOW_RT_20260918.md §6-한정` 에서 **"보정 재검 중" 을 뗍니다.** "영피복 한정" 한정어는 그대로입니다.
    (b) **도구 의심**  RT 보정 재검을 **사용자 안건**으로 올립니다. 결과 JSON·기존 판정문은 **안 고칩니다**(사이드카 규약).
    (c) **판정 불가**  한정어를 그대로 두고, **더 내리는 것은 여기서 멈춥니다** — 세 번째 §-보완은 열지 않습니다.
                      대신 남은 길(Widom 재측정 · 다른 코드로 교차)을 **사용자 안건**으로 올립니다.

    **어느 가지에서도 안 바뀌는 것**: Q_st 목표대 **35~45(보정 척도)** · 32조성 순위 · 유지율 · 323 K 판정 ·
    §AS 판정 1·2 의 판정값 · §-보완1 의 판정값 · CLAUDE.md §1 고정값.
    **문턱은 이 문서가 마지막입니다** — 결과를 본 뒤 P1′ 의 +0.15/+0.51 를 옮기지 않습니다.

## 5. 한계 (결과 전에 적습니다)

    ㄱ 0.002 bar 는 적재가 0.1 mol/kg 아래로 내려갈 수 있습니다. **RASPA 95 % CI 가 값의 10 % 를 넘으면** 그 점은
      등온 보간에 쓰되 판정문에 **오차를 병기**합니다. 셋 이상이 그러면 P1′ 에 "저압 잡음" 단서를 답니다.
    ㄴ 이 시험은 **CC 와 Widom 의 차**만 봅니다. 둘 중 어느 쪽이 틀렸는지는 안 가립니다 — 그건 (b) 가지의 재검 몫입니다.
    ㄷ 여섯 조성은 우리 덩어리이고 MUF-16 비교는 서술로만 씁니다(§-보완1 P5 와 같음).

## 6. 절차

    이 등록은 `QSTN_SUPP_VERDICT_20260924.md` 의 이의 창(09-25 02:5x)이 **열려 있는 동안** 냅니다 —
    문턱에 대한 이의는 **지금** 받는 것이 맞습니다. 창 안에 문턱 이의가 오면 **배정 전에** 반영합니다.
    배정은 기동 세션께 청합니다(데스크탑 18:36부터 유휴). 자료 0건 확인은 이 문서를 낸 스크립트가 직접 했습니다
    (`~/.claude_work/write_qstn_supp2_registration.py` — `tnf_results_qn_*_0.00[25]bar*.json` 0건이 아니면 멈춥니다).

