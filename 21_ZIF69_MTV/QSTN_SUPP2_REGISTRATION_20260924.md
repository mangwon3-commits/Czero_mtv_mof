# §AS (가) Q_st(n) **§-보완2 등록** — 0.005 bar 를 더해 **± 괄호가 나오게** 합니다 (2026-09-24 19:4x, 데스크탑 종합자)

> **자료 0건 확인**: `ls 21_ZIF69_MTV/tnf_results_qn_*0.005bar*.json` → **0건** (19:4x 실행).

## 0. 왜 — **§-보완 판정문이 직접 지목한 구멍**

`QSTN_SUPP_VERDICT_20260924.md §4`:

> `n = 0.3` 이 공통 범위 하한에 **붙어** 있습니다(saIm0583 여유 **+0.0011** · mslm050 +0.0034).
> 등온을 `∓±` 로 옮기면 범위 밖이 되어 **세 조성은 ± 괄호를 못 냅니다.**
> P1 의 −2.74 는 **괄호 없는 값**이라 **문턱까지의 0.24 가 ± 안인지 밖인지 못 가립니다.**

**판정 불가가 문턱 때문만이 아니라 정밀도 때문이기도** 합니다. 그 정밀도를 이 시험이 만듭니다.

⚠ 이것은 **제 §-보완 등록의 구멍**입니다 — P4 를 *"n_lo < 0.3"* 로만 적고 **±-이동판을 안 적었습니다.**
여기서는 그 실수를 반복하지 않습니다(§3 P2).

## 1. 계산

    러너   python run_qn_supp.py --only <조성들> --press 0.005

    ⚠ **러너에 결함이 있었고 고쳤습니다**(19:5x). 제가 이 등록문에 *"압력은 태그에 들어가므로 결과
      파일이 저절로 갈립니다"* 라고 쓰고 **그 줄을 시험했더니 틀렸습니다** — `tag_of` 가 `%.2f` 라
      **0.005 가 `0.01bar` 로 반올림**됩니다. 그대로 띄웠으면 **§-보완의 0.01 bar 결과 6개를 덮어썼습니다.**
      `_ptag()` 로 고쳤고, **기존 태그(0.01·0.02·0.05·0.15·0.50·1.00)가 한 글자도 안 바뀌는 것**을
      실제 파일과 대조해 확인했습니다. 0.005 → `0.005bar` 로 갈립니다.
    덱     6조성 × 3온도(283·298·313 K) × **1압력(0.005 bar)** = **18작업**
    조성   base · mslm050 · sa50nb50 · saIm025 · saIm050 · saIm0583  (관문 6/6 통과, §-보완과 동일)
    고정   CLAUDE.md §1 전부 그대로 · 2×2×2 · 건조 CO₂ 단일 · 실현 1개
    ⚠ 힘장 md5 `8e8ec933f9013c7e932da04dc256efd3` · **착수 간격 ≥ 12 s**(`flock`) · 끝나고 씨앗 감사

### 1-1. 두 기기로 — **조성 단위로**(§-보완 §1-1 과 같은 이유: Q_st 는 세 온도의 기울기)

    laptop2   base · saIm025 · saIm050          9작업   ← §-보완과 **다른 조성**(아래 이유)
    laptop    mslm050 · sa50nb50 · saIm0583     9작업

⚠ **§-보완과 기기를 바꿔 배정합니다.** 같은 조성을 같은 기기가 두 번 내면 **기기 항이 조성에 붙어** 교락합니다.
바꿔 내면 `Q_st(0.3)` 의 여섯 값이 **두 기기에 갈려** 있고, 기기 항이 있으면 §-보완 대비 **일관된 이동**으로 보입니다.
(그 비교는 **판정에 안 씁니다** — 서술입니다. 판정 양은 아래 P1 뿐입니다.)

## 2. 격자 — 결과 전에 박습니다

    주 격자   **n = 0.3 mol/kg** (§-보완과 **같은 점**) · 이제 ± 괄호까지 냅니다
    보조      n = 0.15 (새 공통 범위가 허락하면) — **서술용**, 판정에 안 씁니다

## 3. 예측 (자료 0건 시점 고정)

    P1 ★ 시험 다섯(mslm050·sa50nb50·saIm025·saIm050·saIm0583)의 Δ_low = Q_st(0.3) − Widom(보정) **평균**,
         **± 괄호 포함**. 문턱은 §-보완과 **같습니다**:
           평균 ≥ **−2.5** → **물리**   ·   ≤ **−4.0** → **도구 의심**   ·   사이 → **판정 불가**
         **등록 예측: −2.5 ~ −3.0** (§-보완 실측 −2.74 를 그대로 봅니다. 0.005 bar 는 보간의
         **하단을 넓힐 뿐** n=0.3 의 값을 크게 바꿀 이유가 없습니다.)
         ⚠ **문턱을 안 옮깁니다.** −2.74 가 −2.5 에 0.24 모자랐다고 해서 문턱을 내리면 사후 문턱입니다.
    P2 ★ **여섯 조성 전부 ± 괄호가 나온다**(`∓±` 이동판도 공통 범위 안). 등록 예측: **6/6**.
         나오면 P1 의 0.24 가 ± 폭 안인지 **처음으로** 가려집니다.
    P3   ± 폭(|Q(+±) − Q(−±)|)의 중앙값이 **≤ 2.0 kJ/mol**. (§-보완에서 괄호가 나온 셋은 0.85~2.50.)
    P4   새 n_lo < **0.2** 인 조성 6/6 — 0.005 bar 가 충분히 낮은가.
    P5   실패 0/18.

    **반증/이상**: P2 가 6/6 이 아니면(여전히 하한에 붙으면) **0.002 bar 로 한 번 더** 가지 않고
    **격자점을 n=0.4 로 올려** 다시 냅니다 — 그때는 §-보완 값과 **격자가 달라지므로** 새 등록입니다.

## 4. 바꾸는 것 / 안 바꾸는 것

    (a) P1 이 **물리**  → `QST_WINDOW_RT_20260918 §6-한정` 의 **"보정 재검 중" 을 뗍니다.**
    (b) P1 이 **도구 의심** → RT 보정 재검을 **사용자 안건**으로. 결과 JSON·기존 판정문은 안 고칩니다.
    (c) P1 이 **판정 불가** → 한정어 유지. **±가 나왔다면**(P2 적중) 그 폭을 판정문에 적어
        *"0.24 는 ± 안/밖"* 을 **명시**합니다 — 이번에는 그것만으로도 얻는 것이 있습니다.
    **안 바뀜**: Q_st 목표대 35~45(보정 척도) · 32조성 순위 · §AS·§-보완의 판정값 · CLAUDE.md §1.

## 5. 한계 (결과 전에)

    · 실현 1개 · 세 온도 30 K 폭 · 선형 보간(이제 7점).
    · 0.005 bar 는 **적재가 더 작아 통계가 더 나쁩니다** — ± 폭이 §-보완보다 넓을 수 있습니다.
      **넓으면 그 폭을 그대로 적습니다**(문턱은 안 옮김).
    · 기기 교차(§1-1)는 **서술**이고 판정 양이 아닙니다. 기기 항이 보이면 **다음 판의 등록 안건**입니다.
