# §AS (가) Q_st(n) **§-보완2 판정** — **0.24 는 ± 안입니다. 그리고 「도구」 가지는 ± 로도 안 닿습니다** (2026-09-24 21:17 종합자)

등록 `QSTN_SUPP2_REGISTRATION_20260924.md`(§3 자료 0건 09-24 19:4x) · **이의 창 09-25 05:1x 까지(8 h)** · 검산 Melchior/Balthasar/Caspar

자료 **18행** — laptop2 `base·saIm025·saIm050` 9 · laptop `mslm050·sa50nb50·saIm0583` 9.
전부 `pco2_bar 0.005` · `CO2_molkg` 빈 것 0 · 씨앗 겹침 0(양쪽 기기 전수) · **기존 0.01 bar 결과 md5 변화 0**(laptop2 확인).
식·보간은 §AS 와 같고 압력이 4 → 6 → **7점**. 셈 `~/.claude_work/qstn_supp2_verdict.py`.

## 1. ★ 결론

    시험 다섯 Δ = Q_st(0.3) − Widom(보정)
      중앙    **−2.740**          ← §-보완과 **같은 값**(0.005 는 보간의 하단만 넓힘)
      −±      **−2.956**
      +±      **−2.258**
      → 평균의 ± 띠 **[−2.956, −2.258]**, 폭 0.699

    등록 문턱 −2.5 가 띠 **안**  →  **0.24 는 ± 안입니다.**
    등록 문턱 −4.0 은 띠 **밖**  →  **「도구 의심」 가지는 ± 로도 안 닿습니다.**

**P1 은 등록 규칙대로 여전히 「판정 불가」입니다**(중앙값 −2.740 이 −4.0 과 −2.5 사이).
**문턱은 안 옮깁니다.** 그러나 §-보완이 못 하던 말을 이제 할 수 있습니다 —

> **RT 보정이 원인이라는 가지는 오차를 다 써도 안 닿습니다.** 「도구 의심」 문턱 −4.0 은
> `−±` 로 밀어도 −2.956 까지밖에 안 갑니다. **남은 차는 2RT 크기가 아닙니다.**
> 반면 「물리」 문턱 −2.5 는 **띠 안**이라, 저피복 CC 가 보정 Widom 과 **오차 안에서 만날 수 있습니다.**

## 2. 등록 예측 대조

    P1  시험 다섯 Δ 평균 **−2.740**            [≥ −2.5 물리]      **빗나감 → 판정 불가**(중앙값 규칙)
    P2 ★ **± 괄호가 6/6 전부 나옴**            [6/6]             **적중** ← 이 시험의 목적
    P3  ± 폭 중앙값 **1.26** kJ/mol            [≤ 2.0]           **적중**
    P4  새 n_lo < 0.2 인 조성 **6/6**          [6/6]             **적중**
    P5  실패 **0/18**                          [0]               **적중**
    → **4/5 적중.** P1 만 중앙값 규칙으로 판정 불가이고, **그 판정 불가의 뜻이 §-보완과 다릅니다**(§1).

    ⚠ 등록 §3 P1 의 예측대로 **−2.5 ~ −3.0** 에 들어왔습니다(−2.740). **0.005 bar 가 n=0.3 의 값을
    크게 바꾸지 않는다**는 예측이 맞았습니다 — 보간이 쓰는 두 점이 안 바뀌기 때문입니다.

## 3. 공통 범위 — 목적을 달성했습니다

    n_lo   base 0.0448 · mslm050 0.1686 · sa50nb50 0.1620 · saIm025 0.1256 · saIm050 0.1560 · saIm0583 0.1720
    여유(n_lo → 0.3)  +0.128 ~ +0.255   (§-보완은 **+0.0011 ~ +0.211** 이었고 셋이 괄호를 못 냈습니다)

**§-보완의 구멍이 닫혔습니다.** 제 §-보완 등록이 P4 를 *"n_lo < 0.3"* 으로만 적고 **±-이동판을 안 적은 것**이
그 구멍이었고, 이번 등록은 P2 로 그것을 **직접 예측**했습니다.

## 4. 바꾸는 것 / 안 바꾸는 것 (등록 §4 (c))

    (c) P1 판정 불가 → `QST_WINDOW_RT_20260918 §6-한정` 의 **"보정 재검 중" 은 그대로 둡니다.**
        **다만 한 줄을 덧붙입니다**: *"§-보완2(09-24)에서 「도구」 가지(−4.0)는 ± 로도 안 닿았다.
        RT 보정이 이 차의 원인일 가능성은 이 자료에서 낮다."*
    **안 바뀜**: Q_st 목표대 35~45(보정 척도) · 32조성 순위 · §AS·§-보완의 판정값 · CLAUDE.md §1 고정값 ·
    **P1 문턱 −2.5/−4.0**.

## 5. 남은 것 — **§-보완3 은 열지 않습니다**

압력을 더 내려도 **보간이 쓰는 두 점이 안 바뀌면 n=0.3 의 값이 안 바뀝니다**(이번에 실측으로 봤습니다).
남은 차 −2.74 를 더 좁히려면 **격자점을 더 낮은 n 으로** 옮겨야 하고, 그것은 **다른 격자 = 새 등록**입니다.
**지금 열지 않습니다** — 얻을 것이 *"더 낮은 피복에서 더 닫히는가"* 하나이고, 그 답이 발표 문구를 안 바꿉니다.
발표에는 **"보정 재검 중" 한정어와 §1 의 두 문장**을 그대로 씁니다.

## 6. 절차

    이의 창 09-25 05:1x. 검산 Melchior/Balthasar/Caspar. 셈 `~/.claude_work/qstn_supp2_verdict.py`.
    자료 `tnf_results_qn_*_0.005bar.json` 18 · 등록문 · 이 문서.
    기여: 덱 분할·기기 교차 Caspar · 기존 108 경로 고아 0 검증 Balthasar · 표시 결함 발견 Melchior.
