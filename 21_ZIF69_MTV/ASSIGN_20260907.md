# 배정 2026-09-07 오후 — 빈 코어 넷의 쓰임 (데스크탑 2 · 랩탑 2)

앞선 배정: `WATER_FIX_20260906.md §4`, `T4_PRIME_20260907.md §7`, `V3W_CHUNK_VS_SINGLE_20260907.md`.
이 문서는 그것들을 **바꾸지 않고** 09-07 13시에 생긴 빈 코어의 쓰임만 정합니다.
**어느 항목도 새 문턱·새 가설을 만들지 않습니다.** 둘 다 "같은 조성·같은 설정의 재실행" 입니다.

등록 시점: **첫 결과를 읽기 전**(MAGI_20260905 §9-6). 두 항목 다 자료 0건에서 적었습니다.

## A. 데스크탑 2코어 — T-B2w **씨앗 충돌 4건 재실행** (`run_tb2w_seedfix.py`)

    왜     39행 중 여섯 행이 씨앗 둘을 나눠 씁니다 (같은 초 착수 = 같은 씨앗, SEED_TWO_MEANINGS)
             1788683950  brbIm025 · brbIm050 · brbIm100
             1788683951  base     · brbIm075 · cf3Im025
           값이 틀린 것은 아니나 **서로 독립이 아닙니다.** 09-07 아침 SESSION_LOG 에 "분산·
           재현성 논거로 쓰기 전에 빼거나 다시 돌 것" 으로 적힌 빚입니다.
    무엇   무리마다 뒤의 둘 = brbIm050 · brbIm100 · brbIm075 · cf3Im025  (조성 구성 불변)
    어디   데스크탑 — **원본 35행과 같은 기기**여야 기기 축이 섞이지 않습니다(SREP_VERDICT §7-3)
    어떻게 새 계열 tb2w_runs_seedfix/ · v3w_water_kh_seedfix/, 워커 2, 작업마다 5초 어긋내기
    자     바뀌지 않음. 같은 힘장(md5 8e8ec93…, 파일·머리말 두 관문 통과 조건)·같은 사이클
    읽기   새 씨앗 넷이 서로·기존 35와 모두 달라야 함. 원본 JSON 은 안 건드림 —
           병합본은 별도 파일(`…_seedfixed.json`)로 내고 바뀐 행 넷을 명시
    금지   재실행 값과 원값의 차이로 무엇을 주장하지 않음 (n=1 대 n=1, 통계 비교의 자는 단위(±))

## B. 랩탑 2코어 — 씨앗 반복 **2종 추가** (`V3W_SUFFIX=_rep`, 기존 3종에 더해)

    기존   saIm025 · saIm050 · saIm0583  (V3W_CHUNK_VS_SINGLE "곁들여")
    추가   **base** · **nbIm025**
    왜     base — 오늘 세 기기가 "base 는 프로토콜이 아니라 실행 잡음" 이라 읽었습니다
                  (laptop2 +20.4 / 랩탑 사슬 −4.3 / WATER_FIX §8 0.79 단위). 그 잡음을
                  **같은 기기·같은 프로토콜·다른 씨앗**으로 직접 재는 표본이 아직 없습니다.
                  배치 자유도 0 이라 씨앗 반복이 정확히 그 항만 잽니다.
           nbIm025 — 지금 반복 3종이 전부 술폰산입니다. 나이트로 쪽 하나가 있어야
                  실행 간 항이 한 치환기의 성질이 아님을 보일 수 있습니다.
    어떻게 기존 3종과 같은 방식: RH90, 수정 힘장, 1회씩, 착수 초를 어긋냄
    자     `RULER_DECISION §7` 그대로 — 씨앗 반복은 **통계 비교, 단위(±)**. "배치 단위" 라 부르지 않음
    금지   조성 간 순위·문턱 판정에 쓰지 않음. 실행 간 항의 크기를 적는 데만 씀

## 안 바꾸는 것

§1 고정값 전부 · 유지율 분모 · 80/50 문턱 · T-4′ 등록 · 사슬 판별 시험의 예측 (가)/(나).

## C. 12:07 — **생산 연장 (가) 승인이 B 보다 앞섭니다** (`EXTEND_APPROVED_20260907.md`)

이 문서를 쓰는 사이 사용자가 랩탑 세션에서 **연장 (가)** 를 승인했습니다("연장해").
연장은 **전 조성 병렬·같은 조각 수·Δ40 정지·상한 4라운드**이고 코어를 많이 씁니다.

    B (랩탑 씨앗 반복 base·nbIm025)   **보류.** 빈 코어는 연장 라운드에 씁니다.
                                       씨앗 반복 한 건이 약 5시간이라, 지금 띄우면 연장이
                                       코어를 필요로 할 때 비켜 주지 못합니다. 등록은 남겨 두고
                                       연장이 끝난 뒤 코어가 남으면 그때 띄웁니다.
    A (데스크탑 씨앗 충돌 4건)         **계속.** 12:05 착수, 약 1시간 40분. 데스크탑 RH90 3종은
                                       아직 원 실행 중(11:33 착수)이라 연장이 코어를 요구하기 전에 끝납니다.
    데스크탑 RH90 3종(saIm0625·0667·075)  원 실행이 끝나면 **랩탑과 같은 라운드 수로 연장**합니다.
                                       이 셋은 랩탑 동일 조성과의 3 % 상한 비교용(WATER_FIX §7)이라
                                       생산 구간 정의가 같아야 비교가 섭니다.

결정 주체: 종합자 단독(MAGI §9-7, 자원 배정). 사용자 지시(§10-1, 18:00 까지 무조건 실행)에 따라 묻지 않고 적용.

## D. 12:1x — Restart 손실(`RESTART_LOSS_20260907.md`)에 대한 종합자 결정: **(ㄴ)**

랩탑 단일 9건은 완주 때 `Restart` 가 지워져 **연장 불가**입니다(옛 `run_water.py:267`). 승인된
(가)의 "전 조성 같은 조각 수" 를 줄 수 없게 됐습니다. 선택지 셋 중 **(ㄴ) 연장 가능한 것만
연장하고, 랩탑 단일 9종은 상한((나) 취급)으로 인용** — 랩탑 권고 그대로 채택합니다.

    이유   (ㄱ) 재실행은 20 h 를 쓰고도 연장을 그 뒤로 미루며, 승인문의 "재실행이 아니다" 를 뒤집습니다.
           (ㄷ) 은 반쪽이라 표가 세 갈래가 됩니다. (ㄴ) 은 잃은 것을 잃은 대로 적는 쪽입니다.
    조건   유지율 표에 **"연장됨 / 상한" 열**을 넣어 어느 조성이 어느 엄격함에서 멈췄는지 드러냅니다.
           조성 간 비교는 같은 열 안에서만. 열을 넘는 순위는 매기지 않습니다.
    근거   **[12:2x 갱신] 사용자 명시 승인입니다** — `EXTEND_SCOPE_DECISION_20260907.md`(랩탑 세션,
           사용자 원문 "권고안 실행. PM6:00까지 별도 메시지 없으면 권고안 자동 실행"). 위의 종합자
           판단(§10-1)은 그 승인이 파일로 오기 전 단계였고, 결론은 같습니다. 이제 18:00 뒤 번복
           조항은 해당 없습니다 — 사용자가 직접 정했습니다.
    표기   랩탑 진행분 5건은 **실행 중 스냅숏**에서 잇습니다(재개 시험 통과, `RESTART_LOSS §5`).
           그것은 "완주분의 연장" 이 아니라 **"중간 배치에서 갈라진 가지"** 입니다 — 물의 평탄화를
           보는 데는 유효하나 유지율의 15,000 단일 실행과는 다른 가지. 판정문·표에 그렇게 적습니다.
           표의 열은 셋: **연장됨(완주 배치) / 연장됨(중간 가지) / 상한**.

**데스크탑 RH90 3종은 12:12 에 고친 러너로 다시 띄워 연장 가능**합니다(SESSION_LOG 12:1x).
3 % 상한 비교(WATER_FIX §7)는 **양쪽 다 원래 15,000 창**으로 합니다 — 랩탑 쪽이 연장 불가이므로.

### D-1. 12:5x — 데스크탑 RH90 3종의 연장 경로 (연장 도구 `37cd76f` 수용 뒤)

`extend_chain_v3w.py` 는 **사슬(조각 폴더)** 만 봅니다. 데스크탑 RH90 3종은 **단일 실행**이라 완주해도
조각이 없습니다 — 완주 때 남는 `Restart` 를 `RestartInitial` 로 넣어 **chunk0 를 만들어야** 도구가
봅니다. 랩탑 스냅숏 5건과 **같은 변환**이고 랩탑이 그 변환기를 짭니다. 데스크탑은 그것을 받아
`CHAIN_ROOT` 를 자기 자리로 주고 돌립니다. 데스크탑 3종은 완주 배치에서 잇는 것이므로 표의 열은
**"연장됨(완주 배치)"** 입니다(랩탑 스냅숏 5건의 "중간 가지" 와 다름).

착수 순서는 그대로 — laptop2 7종 완주 뒤 세 기기가 같은 라운드로.

## E. 12:5x — Δ40 창을 **사이클 기준**으로 (종합자 결정: 랩탑이 지금 고친다)

`snapshot_to_chunk0.py`(`e18ae42`)가 만든 chunk0 는 블록이 3,000 사이클이고 사슬 조각은 600 사이클입니다.
현 구현(`d40_band`·`WINDOW_CHUNKS`)은 **블록 개수**로 40 %·창을 잡으므로 섞인 열에서는 앞·뒤가 **다른
사이클 수**가 됩니다(예: 앞 12,000 대 뒤 4,800). 오류 없이 그럴듯한 수 — 오늘 유형.

    결정   **고친다, 지금, 랩탑이.** laptop2 가 도구 주인이나 오프라인이고(§9 — 기다리지 않음), 라운드 1 이
           시작되면 그 수가 바로 판정에 쓰입니다. `base` 는 이미 "정지" 라 한 라운드만 돌아도 섞인 열이 생깁니다.
    자는   **바뀌지 않습니다.** 등록문(`MORNING §2`·`EXTEND_APPROVED §2`)의 창은 애초에 **"마지막 15,000 사이클"**
           이고, 블록 개수 구현은 블록이 균일할 때만 그것과 같았습니다. 구현을 등록문에 맞추는 것입니다.
    조건   ① 회귀: 균일 600 사슬(laptop2 7종·랩탑 base) 에서 Δ40 이 **소수점까지 동일** — 랩탑 base −4.3 % 포함.
           ② 합성 시험: chunk0(3,000×5)+chunk1(600×5) 에서 앞·뒤가 각 6,000 사이클인지 출력으로 보임.
           ③ 40 % 경계가 블록 경계에 떨어져야 함(600·3,000 다 6,000 을 나눔). 안 떨어지면 계산 거부.
           ④ 결과를 읽기 전에 등록 — 이 §E 가 그것. 첫 Δ40 을 섞인 열에서 읽기 전에 고침이 master 에 있어야 함.
    MAGI   열지 않음 — 등록문이 답을 이미 갖고 있는 구현 정합 문제.

### E-1. 13:0x — 조건 넷 충족 확인 (`25d3d50`) · 병기 규칙 하나 추가

    ① 회귀   base −4.3 % / 8.9 % · saIm050 +18.4 % / 17.3 % — 고치기 전과 소수점까지 동일
    ② 합성   창 = 블록 9개 = 정확히 15,000 (600×5 + 3000×4), 앞 6,000 · 뒤 6,000
             ⚠ 창을 **조각** 단위로 모으면 chunk0(15,000)이 통째로 들어와 18,000 이 됨 → **블록** 단위로 모음
    ③ 거부   d40_band([…],[3000,3000,600,600]) → ValueError (총 7,200 의 40 % = 2,880 이 경계에 안 떨어짐)
    ④ master 에 있음, 라운드 1 전

**병기 규칙(추가)**: 변환 계열(chunk0 가 3,000 블록)의 **첫 두어 라운드 Δ40 은 옛 3,000 블록에 희석**됩니다 —
뒤 6,000 을 새 조각(600×5 = 3,000)만으로 못 채워 옛 블록 하나가 딸려 들어오기 때문(합성 시험: 기대 +20 % 가
+10 % 로 나옴, 코드가 맞음). 정지 규칙에는 **보수적**(일찍 안 멈춤)이라 방향은 안전하나, 판정문에
*"변환 계열의 초기 라운드 Δ40 은 희석돼 있다"* 를 반드시 병기합니다. 안 적으면 "연장했는데 왜 안 움직이나" 로 오독됩니다.

### E-2. 13:1x — 변환 계열의 Δ40 은 무엇의 자인가 (랩탑 지적에 대한 종합자 판단)

랩탑 표: 창 15,000 에서 옛(3,000 블록)/새(600 조각) 구성은 라운드 1 ~4 에서 12,000/3,000 → 9,000/6,000 →
6,000/9,000 → 3,000/12,000 이고, 창이 전부 새가 되는 것은 **라운드 5** — 상한 4 안에서는 오지 않습니다.
희석은 라운드 1(뒤)과 4(앞). 랩탑은 "라운드 2·3 의 Δ40 은 옛 대 새의 **이음매 비교**이지 궤적 내 표류가 아니다" 고 봅니다.

**종합자 판단 — 변환 계열은 한 종류가 아니라 둘이고, 둘의 답이 다릅니다.**

    (a) 완주 배치 변환 (데스크탑 3종)
        chunk0 의 끝 상태 = chunk1 의 시작 상태. 궤적이 **이어져 있습니다.** 옛/새는 블록 크기의 경계일 뿐
        같은 마르코프 사슬입니다. 그러므로 Δ40(앞 6,000 대 뒤 6,000)은 **궤적 내 표류가 맞습니다** — laptop2
        사슬과 같은 양. 다른 것은 **폭**뿐입니다: 3,000 블록과 600 블록의 분산이 달라 섞인 창의 폭은 균질
        창의 폭과 다른 추정량입니다. → 판정문에 "폭은 블록 크기 혼합" 병기. 열은 그대로 "연장됨(완주 배치)".
    (b) 중간 가지 변환 (랩탑 5건, 스냅숏)
        chunk0 = 원 실행의 **전체** 15,000 출력, chunk1 = **그 중간 어느 사이클의 상태**에서 갈라진 가지.
        chunk0 의 끝과 chunk1 의 시작이 **다른 상태**입니다. 이음매가 실재하고, 그것을 가로지르는 Δ40 은
        표류도 이음매 비교도 아닌 **두 가지의 비교**입니다. 랩탑의 우려는 여기에 **전부** 해당합니다.
        → **고칠 길이 하나 있습니다**: 스냅숏의 사이클을 알면 chunk0 를 **그 사이클에서 잘라** 넣습니다. 그러면
          (a) 와 같이 이어집니다. RASPA Restart 파일·`binary_restart` 머리말·파일 mtime 대 출력 블록 시각으로
          사이클을 복원할 수 있는지 **랩탑이 확인**합니다(답 필요). 복원되면 변환기에 `--truncate-at-cycle`.
          복원이 안 되면 그 5건의 Δ40 은 상한 4 안에서 **정지 규칙의 문턱을 적용하지 않고** 창 구성만 병기해
          "가지" 로 인용합니다 — 문턱을 대면 같은 이름의 다른 양이 됩니다.

    병기(공통)   정지 판정 열에 **창(옛/새)** 을 랩탑 제안대로 넣습니다. 판정문 한 줄:
                 "변환 계열의 Δ40 은 상한 4 안에서 창이 균질해지지 않는다 — (a) 는 같은 궤적의 표류이나 폭이
                 블록 혼합, (b) 는 두 가지의 비교라 문턱을 대지 않는다."
    상한 5       변환 계열만 5라운드로 올리면 창이 균질해지나 승인된 상한 4 를 넘습니다 → **사용자 결정, 18:00 뒤.**
                 지금은 병기로 가고 결과를 본 뒤 필요하면 묻습니다(랩탑 권고와 같음).
    MAGI         열지 않음. 남은 것은 추론이 아니라 사실 하나(스냅숏 사이클 복원 가능 여부).

### E-3. 13:2x — 스냅숏 5건은 "생산 사이클 0 의 배치" → **(가) 상한 인용으로 확정** (종합자, 랩탑 권고 채택)

랩탑 실측: ASCII `Restart` 는 **초기화 끝(생산 사이클 0)에 한 번만** 쓰이고 그 뒤 mtime 이 안 변합니다
(갱신되는 것은 `CrashRestart` 뿐, 500 사이클마다). 네 실행의 시각으로 "5,000 사이클" 가정을 완주분 속도와
대 보니 92~95 % 로 일관(초기화는 물이 덜 차 사이클이 싼 구간 — 방향까지 맞음). **가설 확정.**

    귀결   스냅숏 = 원 생산이 **시작한 바로 그 배치**. 15,000 너머로 이어 붙일 지점이 아니라 **다시 도는 출발점**.
           가지를 돌리면 생산 재실행이고, 비용은 (ㄱ) 과 같음. `--truncate-at-cycle` 불필요(자를 지점 0).
    결정   **(가) 상한 인용으로 확정.** 추가 비용 0. `EXTEND_SCOPE_DECISION` 의 "연장 가능" 목록에서 5건을 빼고
           상한 인용은 **9종 → 14종**. 이것은 사용자 승인 (ㄴ) "연장 가능한 것만" 의 사실 적용이지 새 선택이 아님.
    풀     연장 풀 = laptop2 7종(사슬) + 랩탑 사슬 2종(base·saIm050) + 데스크탑 3종(완주 배치 변환) = **12종, 전부 (a) 형.**
           (b) 형이 사라지므로 §E-2 (b) 는 해당 없음. §E-2 (a) 의 "폭은 블록 혼합" 병기는 데스크탑 3종에 그대로.
    표     열은 그대로 셋으로 두되 "연장됨(중간 가지)" 열은 **비어 있음**을 명시(있었다가 없어진 열은 적어 둡니다).
    보관   스냅숏 5건은 **지우지 않습니다.** "초기화를 공유한 평행 실현" 의 출발점이라 나중에 씨앗 반복(B)을 살릴 때
           초기화 5,000 을 아끼는 길이 될 수 있음 — 다만 초기 배치를 공유하므로 독립 씨앗 반복과는 **다른 시험**.
           등록 없이는 쓰지 않음.
    MAGI   열지 않음.

### E-4. 13:3x — (a) 형 "폭" 병기 문구 확정 (랩탑 보완)

데스크탑 3종(완주 배치 변환)의 Δ40 은 궤적 내 표류가 맞으나, **폭**은 3,000 블록과 600 블록의 분산(약 5배 차)을
한 SD 로 뭉갠 **혼합 창의 추정량**이라 균질 창의 폭과 같은 양이 아닙니다. 정지 판정이 그 폭과의 비교이므로
라운드 1~3 에서 판정이 어느 쪽으로든 기울 수 있고, **방향은 조성마다 달라 단정하지 않습니다.**

    병기 문구(확정)  "폭은 혼합 창(3,000·600 블록)의 추정량이라 균질 창의 폭과 같은 양이 아니다."
    적용             데스크탑 3종의 라운드 1~4 정지 판정 행 전부. 라운드 5(창 전부 새) 는 상한 밖이라 안 옴.

`EXTEND_SCOPE_DECISION` §1-1~1-3 갱신(`3945247`) 수용: 연장 12종 / 상한 14종, "중간 가지" 열은 비어 있음 명시.
랩탑 RH90 10/11 — 상한 무리의 유지율 열(0.77~0.97, 80 % 아래는 saIm0583e1 하나)은 **연장 무리와 순위를 섞지 않음.**

### B-1. 13:5x — B 를 **데스크탑**에서 띄웁니다 (A 완료로 2코어 빔, 랩탑 코어는 연장용)

    왜 데스크탑   §C 에서 B 를 미룬 이유는 랩탑 코어였습니다. 데스크탑은 A 가 13:42 에 끝나 2코어가 비었고,
                  연장 착수(~16:40+, 데스크탑 3종)까지 6+2 = 8 로 상한 안입니다. §9 — 계산은 끊기지 않게.
    짝            같은 기기·같은 프로토콜의 원 실행 = 데스크탑 밀도 격자 계열 `water_runs_density_v3w/rh90_{base,nbIm025}`
                  (씨앗 1788705274 / …275). 밀도 러너는 `run_water` 위에 VTK 출력만 얹은 것 — 표본 추출은 같으나
                  **판정문에 "짝은 밀도 계열" 병기.** 랩탑·laptop2 의 base 는 다른 기기라 짝이 아님(참고 값으로만).
    어떻게        `run_water_v3w_rep_desktop.py`(래퍼: 등록 목록에 두 조성을 더하는 것만) · 드라이버 **둘을 따로**,
                  V3W_SUFFIX=_rep_base / _rep_nbIm025 · 착수 5초 어긋냄 · `--gate-only` 로 관문 먼저 봄
    자·금지       §B 그대로 — 통계 비교, 단위(±). 조성 순위·문턱 판정에 안 씀.

### 씨앗 메모 (13:5x)
데스크탑 RH90 3종(12:12 재착수)은 **셋이 같은 씨앗** 1788750777 — 한 드라이버의 풀이 같은 초에 띄움
(`.claude_work_rh90_desktop.sh` 에 어긋내기 없음). 조성이 달라 계는 다르지만 난수열은 같습니다.
이 셋의 용도(랩탑 동일 조성과의 3 % 상한 비교, 쌍마다 씨앗·기기 다름)에는 영향 없어 **다시 띄우지 않습니다.**
다만 **셋끼리의 분산 논거에는 쓰지 않습니다.** 밀도 덱은 런처가 2초씩 어긋내 씨앗이 다릅니다(…274/275/278).
앞으로 한 드라이버로 여러 조성을 띄울 때는 런처에서 어긋내거나 드라이버를 가릅니다.

## F. 17:0x — MAGI-002 에서 나온 배정 둘 (종합자 단독, §9-7·§10-1)

    F-1 랩탑    **T-MTV-2w** — sa50nb50e1~e5 RH90 수정 힘장(빌드 0), 워커 4, 5건 2파도 ≈ 10 h. 등록 `MAGI-002 §6-9-1`(자 재기, 가설 시험 아님).
                2코어는 남김(씨앗 반복 마무리 + 연장 라운드 1 의 랩탑 사슬 둘). 착수 초 어긋내기 · 착수 직후 check_ff_per_run.
    F-2 데스크탑 **후보 습윤**(MAGI-002 §6-5 ⑨) — sa25nb75·ms50nb50 RH90 수정 힘장 + ms50nb50 관문(after, RASPA 없는 틈에). RH90 3 완주 뒤 착수.
                자는 유지율 · 배치 단위는 F-1 의 값 · 순위·문턱 판정에 안 씀(후보 지목용). 사용자 18:00 확정 전에는 **준비만**(등록 문서·런처), 착수는 확정 뒤.

### F-3. 15:5x — 사용자 확정 "권고대로 해" → F-1·F-2 착수 승인. F-1 은 랩탑이 10건·워커 5·단계 분리로 띄움(19.3 h). F-2 는 조성당 RH90+RH0 = 4건,
데스크탑 RH90 3 완주 뒤 워커 4 로 착수(래퍼 `run_water_v3w_cand_desktop.py`, V3W_SUFFIX=_cand). ms50nb50 관문(after)은 RASPA 없는 틈에 따로.
    F-2 갱신(17:2x)  ⑩ mslm025(RH90+RH0) 를 ⑨ 묶음에 합침 — 6작업, 워커 4, 2파도. 랩탑 §6-13 등록(자료 0건). 착수 조건 그대로(데스크탑 RH90 3 완주 뒤).
