# 이 저장소에서 일하는 방법

여러 기기·여러 세션이 같은 저장소를 나눠 씁니다(데스크탑, 랩탑, 외부 지원).
**작업을 시작하기 전에 이 파일과 `SESSION_LOG.md`, `48H_COMPUTE_PLAN.md` 를 먼저 읽으세요.**

> **현재 실행 계약:** `48H_COMPUTE_PLAN.md`가 2026-08-18~20의 기기별 계산 분담,
> 실행 순서, 자원 한도, 랩탑 인계 절차를 고정합니다. 새 Claude 세션은 이를 읽고
> `bash bgstate.sh`로 실제 상태를 확인하기 전에는 새 계산을 띄우거나 살아 있는
> 작업을 재시작하지 마세요.
>
> 랩탑 dry-WC 결과의 수용·문구 불일치 판정은
> `21_ZIF69_MTV/V3_WC_LAPTOP_VALIDATION.md`, 다음 48시간의 랩탑 배정은
> `21_ZIF69_MTV/LAPTOP_DENSITY_V3.md`에 있습니다. 외부 16코어의 수분 경쟁
> 배정은 `21_ZIF69_MTV/EXTERNAL_WATER_V3.md`에 있습니다.

## 0. 이 프로젝트가 반복해서 데인 것

11일에 근본 결함 4건. 전부 같은 유형입니다 — **실패가 결과처럼 보이는 것.**

- 주기 경계 언랩 버그(08-04): 10일 뒤 발견, 계산 전량 폐기
- 부착 원자 고정 인덱스(08-14): 몇 주 묵음, v1 치환 결과 전량 폐기
- 모체 기하·조성 개수·힘장 표기(08-15 감사): v3 재구축
- 검사기 오탐 3건: 멀쩡한 구조를 결함으로 찍음

그래서 규율이 있습니다. **나쁜 결과를 보면 검사기부터 의심하고, 좋은 결과를
보면 기준을 의심하세요.**

## 1. 절대 바꾸면 안 되는 값

바꾸는 순간 기존 결과와 합칠 수 없습니다. 성능을 위해 바꾸고 싶으면
**바꾸지 말고 물어보세요.**

| 항목 | 값 |
|---|---|
| 사이클 | 초기화 5,000 + 생산 15,000 |
| 골격 힘장 | UFF_MOF |
| **CO₂ 모델** | **García-Sánchez 2009** — ε 29.933 K, σ 2.745 Å, q_C +0.6512 |
| 물 모델 | TIP5P-Ew (5자리) — **혼합 규칙에 `Hw none` / `Lw none` (2026-09-06 수정, 사용자 결정 (라)).** 그 전 물 결과(RH>0)는 수소에 UFF LJ 가 붙은 **결함판** — `WATER_FIX_20260906.md` |
| 전하 | PACMAN DDEC6 (CIF 에 포함) |
| 정전기 | Ewald 1e-6 |
| 컷오프 | 12 Å |
| 단위셀 | 2×2×2 |

> ⚠️ **CO₂ 정의 파일 경로가 `molecules/TraPPE/CO2.def` 지만 TraPPE 가 아닙니다.**
> 기하만 거기서 오고 LJ·전하는 `forcefield/UFF_MOF/` 의 두 파일에서 옵니다.
> **문서나 장표에 "TraPPE" 라고 적으면 틀립니다.** 08-15 감사에서 잡힌 지점입니다.
> 물도 같습니다 — 배포본 `TraPPE/water.def` 는 3자리, 우리 것은 5자리입니다.
>
> ⚠️ **물 힘장은 2026-09-06 에 두 줄이 바뀌었습니다** (`Hw none` / `Lw none`, 항 수 55→57). 09-05 까지의 물 계산은
> `Hw` 가 UFF `H_` 의 LJ 를 물려받아 **수소결합이 없는 물**이었습니다(물 친화도 약 8.6배 과소). **RH>0 결과는 새 계열
> (`v3w_*`)로만 인용**하고, 옛 실행 폴더는 이어받지 마십시오(§3). 기기마다 `$RASPA_DIR` 사본을 따로 고쳐야 합니다.

## 2. 결과를 읽는 규율

- **RASPA 95% 신뢰구간을 병기하고, 차이가 그 1.5배 미만이면 순위를 매기지 않습니다.**
  > ⚠️ **RASPA 의 `±` 는 1σ 가 아니라 95% 신뢰구간입니다** — 블록 5개의
  > `t(0.975,4) × SEM = 2.776 × SEM` 입니다(`21_ZIF69_MTV/ERROR_BARS.md`,
  > 08-23, 네 건에서 2.776 검산). **문턱은 그대로 1.5 이고 단위가 `±` 입니다.**
  > 진짜 σ 로 환산하지 마세요 — 과거 판정이 뒤집힙니다. 그래서 이 글에서는
  > `σ` 대신 **"단위"** 라고 부릅니다.
  >
  > **다만 분산 검정(카이제곱 등)에는 진짜 1σ = `±/2.776` 이 필요합니다.**
  > 평균 비교의 문턱(1.5 × `±`)과 다른 계산이고, 08-27 에 이 구분을 놓쳐
  > 배치 산포를 통계오차의 2.0배로 적었습니다 — 실제는 **8.7배**입니다.
  >
  > **배치 비교의 자는 다릅니다** (2026-09-06 사용자 결정 (다), `21_ZIF69_MTV/RULER_DECISION_20260828.md` §7).
  > 앙상블(여러 배치 실현)끼리, 또는 앙상블 대 모체를 견줄 때 분모는 **실현 표본 SD(진짜 1σ)** 이고
  > 문턱은 같은 1.5, 이름은 **"배치 단위"** 입니다. 위의 `±` 자 1.5 는 그대로 **"단위"** 입니다.
  > 문턱은 어느 쪽도 옮기지 않았습니다 — 이름만 갈랐습니다. **어느 비교든 두 값을 나란히 적으세요**
  > (`1.90 배치 단위 · 0.68 단위`). 한쪽만 인용하면 같은 자료가 문턱의 양쪽에 놓입니다.
- 작업 용량은 두 로딩의 차이라 오차가 더해집니다: σ_wc = √(σ_ads² + σ_des²).
- 판정 기준은 **계산을 돌리기 전에** 등록합니다. 결과를 보고 기준을 고치지 않습니다.
  > **[2026-09-06 사용자 결정, `MAGI_20260905.md §9-6`]** 후보가 **이미 등록된 배정**(ASSIGN·§7 류)이고 등록 저촉이
  > 없으면 **먼저 띄울 수 있습니다.** 그때 등록 보완의 시한은 "착수 전" 이 아니라 **"첫 결과를 읽기 전(자료 0건)"** 입니다.
  > 먼저 돈 계산은 **중단 판정 때 매몰비용을 근거로 쓰지 않습니다.** 결과를 본 뒤의 수정 금지는 그대로입니다.
- 실행 조건이 방침과 달랐다면 그것은 재시도가 아니라 **다른 시험**입니다. 그렇게 적으세요.
- v1/v2/v3 는 서로 다른 구조에서 나왔습니다. **버전을 넘나들며 인용하지 마세요.**
  특히 `run_aryl_gcmc.py` 주석에 박힌 숫자는 08-14 결함 이전 판입니다.

## 3. 이어받기 — 그리고 그 함정

`run_water.py`, `run_working_capacity.py`, `run_aryl_gcmc.py` 는 전부 **작업 단위
이어받기**가 있습니다. 죽어도 같은 명령을 다시 치면 완주분을 `cached` 로
회수합니다. 정전으로 19작업을 이렇게 살렸습니다(08-17).

> ⚠️ **그런데 이어받기는 출력 파일의 존재만 봅니다. 무슨 설정으로 만들어졌는지
> 검사하지 않습니다.** 러너나 설정을 바꾼 뒤 그대로 이어받으면 두 규약이
> 조용히 섞입니다. **설정을 바꿨으면 실행 폴더를 지우고 처음부터 도세요.**

## 4. 진행 상황을 세는 법 (전부 틀렸던 방법들)

- **실행 폴더 개수로 세지 마세요.** 결과를 뽑은 뒤 폴더를 지웁니다. 끝난 것일수록 안 보입니다.
- **출력 파일이 몇 시간째 그대로여도 멈춘 게 아닙니다.** `PrintEvery` 가 생산
  사이클 수와 같아 처음과 끝에만 씁니다.
- **`pgrep -f` 는 자기 명령줄에 매칭됩니다.** `pgrep -x` 를 쓰거나 `ps -eo args` 를 거르세요.
  > ⚠️ **같은 함정이 `pkill -f` 에서는 훨씬 비쌉니다.** 조회는 틀린 목록을 보여
  > 줄 뿐이지만 **종료는 엉뚱한 것을 죽입니다.** 2026-09-04 에 랩탑이
  > `pkill -f run_tb1_hvap.py` 로 **자기 셸 명령줄을 먼저 죽였고**, 정작 죽이려던
  > 드라이버의 `simulate` 자식이 고아로 남아 PID 로 따로 정리해야 했습니다.
  > 이 함정을 읽고도 밟은 사례입니다.
  >
  > 죽일 때는 **PID 를 먼저 확인하고 PID 로 죽이세요.** 무리로 죽여야 하면
  > 죽일 목록을 먼저 눈으로 보십시오.
  >
  >     ps -eo pid,args | grep -v grep | grep run_tb1_hvap.py   # 먼저 본다
  >     kill <PID>                                              # PID 로 죽인다
  >
  > 드라이버를 죽이면 자식이 따라 죽지 않습니다. **자식까지 정리했는지
  > `bash bgstate.sh` 로 확인**하세요.
- **CPU 0% 인 드라이버는 대개 정상**입니다(자식이 계산 중). 무리 전체가 0% 일 때만 의심하세요.
- 한 번에 보려면: `bash bgstate.sh`

## 5. 자원 한도 (실측)

- 물리 8코어. RASPA `simulate` 약 471 MB/건 — 메모리가 아니라 코어가 병목
- **Zeo++ `network` 는 v3 구조에서 한 건에 약 9.5 GB. RASPA 가 돌 때 같이 띄우지
  마세요** — 08-12 에 이 조합이 OOM 을 내고 dbus 까지 죽어 WSL 이 통째로 먹통이
  됐습니다. 오래 적혀 있던 3.2 GB 는 **v1 구조에서 잰 값**입니다. v3 는 이완으로
  셀이 34% 수축해 슈퍼셀 원자 수가 늘었고, 랩탑 실측이 9.5 GB 였습니다(08-19).
  `risk_screen_v3.py` 가 `/proc/meminfo` 에서 워커를 계산하므로 **`RISK_WORKERS`
  를 손으로 주지 마세요.** 다른 기기에서 또 다르면 `ZEO_GB_PER_JOB` 로 덮으세요
- 부수 작업은 `nice -n 19`, 기동은 `setsid nohup ... < /dev/null &`
- 작업 길이가 13배까지 차이 납니다(1.5~20.2시간). **비싼 것부터 큐에 넣으세요(LPT).**
  `pool.map` 처럼 미리 덩어리로 나누면 워커가 놉니다 — `imap_unordered(chunksize=1)`.

## 6. 셸 호출 (윈도우 + WSL 환경에서만)

`TOOLING.md` 에 상세가 있습니다. 요약:

- 복잡한 한 줄은 **Bash 도구 + `MSYS_NO_PATHCONV=1`**
- PowerShell 로 bash 한 줄을 넘기지 마세요 — `$(...)` 를 PowerShell 이 먼저 실행합니다
- **커밋 메시지는 파일에 쓰고 `git commit -F`** — 아포스트로피 하나가 `-c '...'` 를 끊습니다
- **돌고 있는 bash 스크립트를 편집하지 마세요.** 바이트 오프셋으로 읽어 엉뚱한 줄을 실행합니다.
  파이썬은 편집해도 실행 중 프로세스에는 반영되지 않습니다 — **재기동할 때 터집니다.**

## 7. 지우면 안 되는 것

- `21_ZIF69_MTV/density_v2/` — VTK 밀도 격자가 그림의 원본이고 사본이 없습니다
- 결과 JSON 전부, `structures_v2/`, `charged_v2/`, `charged_v3/`, `relax_v3/*_relaxed.cif`

실행 폴더(`*_runs*/`)는 결과 JSON 이 있으면 지워도 됩니다. `cleanup_for_compact.sh`
가 관문을 걸어 검사합니다.

## 8. 대화에서만 존재하는 용어를 만들지 마세요

세션은 서로의 대화를 모릅니다. 공유되는 것은 **이 저장소의 파일뿐**입니다.

2026-08-17 에 "티어 1~4" 라는 분류를 대화에서 만들어 포스터 배지와 여러 지시에
썼는데, **저장소 어디에도 정의가 없었습니다.** 아이패드 원격 세션이 그 말을
물었을 때 아무것도 몰랐고, 당연한 일이었습니다.

새 용어·분류·판정 기준을 만들면 **쓰기 전에 파일로 남기세요.**
지금 쓰이는 것들:

| 용어 | 정의 위치 |
|---|---|
| 티어 1~4 (제출 위험 사다리) | `21_ZIF69_MTV/TIERS.md` |
| 기기별 배정과 원격 경로 | `21_ZIF69_MTV/ASSIGN_20260819.md` |
| v2 → v3 결과 해석 | `21_ZIF69_MTV/V3_RESULT_NOTE.md` |
| 실행 계약 | `48H_COMPUTE_PLAN.md`, `AUTOMATION_60H.md` |
| azbIm · "월등" 정의 · 사전 등록 시험 T-1~T-C10 | `21_ZIF69_MTV/DESIGN_STUDY_20260903.md` |
| T-SRCC (순위 보존 검정, 부트스트랩 ρ) | `21_ZIF69_MTV/SRCC_TEST_20260904.md` |

같은 이유로, 다른 기기의 세션에 일을 시킬 때는 **대화 맥락을 옮기려 하지 말고**
읽을 파일을 지정하세요. 아이패드에서든 랩탑에서든 이 한 줄이면 부팅됩니다.

    CLAUDE.md, SESSION_LOG.md, 21_ZIF69_MTV/ASSIGN_36H_20260826.md 읽고
    bash bgstate.sh 돌린 다음 현황 보고해

**배정 문서는 날짜가 붙어 늘어납니다.** 위 줄이 낡았을 수 있으니 붙여 넣기
전에 `ls -t 21_ZIF69_MTV/ASSIGN*.md | head -1` 로 최신을 확인하세요.
08-25 에 이 줄이 08-19 판을 가리키고 있었습니다.

**처음 붙는 기기라면** 대신 `21_ZIF69_MTV/NEW_MACHINE_20260822.md` 를
읽히세요. 환경 구축·힘장 설치·검증까지 그 문서 하나로 끝나고, 맨 앞의
`0-0` 절이 본문 중 낡은 곳을 짚어 줍니다.

## 9. 대기 규칙 — 2026-09-06 사용자 지시 (모든 세션·기기 공통)

- **계산은 끊기지 않게 돌립니다.** 기기가 비면 등록된 다음 배정(`WATER_FIX_20260906.md §4`, `ASSIGN_*.md`)을 **바로** 띄웁니다.
  배정이 비어 있으면 종합자(데스크탑)에게 알리고, 그 사이 등록 저촉이 없는 후보(관문·Widom 류)를 먼저 띄웁니다.
  §5 의 자원 규칙(Zeo++ 와 RASPA 동시 금지, 8코어 상한)은 그대로입니다.
- **사용자에게 선택을 물은 뒤 30분 동안 응답이 없으면 권고안대로 실행합니다.** 질문에는 항상 권고안을 첫 줄에 적고,
  30분 뒤 실행할 때 "무응답 30분 → 권고안 실행" 을 파일에 기록합니다. 사용자가 뒤늦게 다르게 정하면 그때 바꿉니다.
  (MAGI 시간창 규칙 `MAGI_20260905.md §10` 은 그대로 — 창 밖의 MAGI 요청도 이 30분 규칙을 따릅니다.)

